// A5 / D1814 — the optional call whose receiver is a nullable FIELD of another struct, the
// receiver kind a `?.` is most often written for.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
type P = { n: i32 }
type Box = { inner: P | null }
function twice(self: P) { self.n * 2 }
const o: Box = { inner: { n: 21 } }
// @@VALUE@@
o.inner?.twice()
// @@TYPE@@
i32 | null
// @@FALLBACK@@
null
// @@PROOF@@
print(v ?? 0)
// @@WANT@@
42
