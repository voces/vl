// The SOURCE-un-annotated face of D1827's map read inside an `if` used as an EXPRESSION: the
// map is a bare `Map()` whose key and value types come from the store, not from an annotation.
// The scratch frame is reserved from a walk, not from a type, so this face is the one that says
// whether the walk and the annotation agree.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
const reg = Map()
reg["k"] = 42
// @@VALUE@@
if reg["k"] != null { reg["k"] } else { null }
// @@TYPE@@
i32 | null
// @@FALLBACK@@
null
// @@PROOF@@
print(v ?? 0)
// @@WANT@@
42
