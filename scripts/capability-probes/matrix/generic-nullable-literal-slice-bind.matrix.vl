// D2572 — a generic binds `[null, x].slice(1)` and returns the binding, at a `string` instance:
// the binding and the result are the `(string | null)[]` list.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
const src: string[] = ["a", "b"]
function g<T>(x: T) {
  const q = [null, x].slice(1)
  return q
}
const one = src[1]
// @@VALUE@@
g(one)
// @@TYPE@@
(string | null)[]
// @@PROOF@@
const e = v[0]
if v.length != 1 { print("badlen") } else if e != null { print(e) } else { print("none") }
// @@WANT@@
b
