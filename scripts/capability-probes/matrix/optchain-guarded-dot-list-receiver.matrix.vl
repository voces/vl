// A5 / D1815 — the guarded chain over a LIST ELEMENT receiver: `items[0]?.y.z`. The index read
// is the chain's base, which the `?.` lowering stages when it is not a re-readable place.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
type Leaf = { z: i32 }
type Inner = { y: Leaf }
const items: (Inner | null)[] = [{ y: { z: 42 } }, null]
// @@VALUE@@
items[0]?.y.z
// @@TYPE@@
i32 | null
// @@FALLBACK@@
null
// @@PROOF@@
print(v ?? 0)
// @@WANT@@
42
