// List-kind audit (docs/internals/list-kind-audit-2026-09.md): an if/match join of a typed `K[]` name with `null`,
// element kind `nullelem`. One row of a join-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
const src: (i32 | null)[] = [7, null]
function one() { 1 }
// @@VALUE@@
if truthy() { src } else { null }
// @@TYPE@@
(i32 | null)[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print((v[0] ?? 0) + (v[1] ?? 1))
// @@MISS@@
print("null")
// @@WANT@@
8
// @@VALUE2@@
match one() { 1 => null, _ => src }
// @@TYPE2@@
(i32 | null)[] | null
// @@PROOF2@@
if v != null { print((v[0] ?? 0) + (v[1] ?? 1)) } else { print("null") }
// @@WANT2@@
null
