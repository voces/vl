// D2238 — an `f32[] | null` from a join of a non-empty list literal with a bare `null`, the
// f32 twin of `nullable-list-join-null-i64`. The `argument` and `early_return_guard` cells
// read the element through a narrowed un-annotated parameter. VALUE is an `if`, VALUE2 a `match`.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
function one() { 1 }
// @@VALUE@@
if truthy() { [(7.5 as f32)] } else { null }
// @@TYPE@@
f32[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0])
// @@MISS@@
print("null")
// @@WANT@@
7.5
// @@VALUE2@@
match one() { 1 => [(8.5 as f32)], _ => null }
// @@TYPE2@@
f32[] | null
// @@PROOF2@@
if v != null { print(v[0]) } else { print("null") }
// @@WANT2@@
8.5
