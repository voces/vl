// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a join whose FIRST arm is `null` and whose second is a typed `K[]` name,
// element kind `i32`. One row of a join-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
const src: i32[] = [7, 1]
function one() { 1 }
function falsy() { 1 == 2 }
// @@VALUE@@
if falsy() { null } else { src }
// @@TYPE@@
i32[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0] + v[1])
// @@MISS@@
print("null")
// @@WANT@@
8
// @@VALUE2@@
match one() { 2 => null, _ => src }
// @@TYPE2@@
i32[] | null
// @@PROOF2@@
if v != null { print(v[0] + v[1]) } else { print("null") }
// @@WANT2@@
8
