// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a `K[] | null` record field read,
// element kind `u8`. One row of a nullable-field-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
type W = { f: u8[] | null }
const src: u8[] | null = [200, 1]
const none: u8[] | null = null
const rec: W = { f: src }
function mkRec0(): W { return { f: none } }
// @@VALUE@@
rec.f
// @@TYPE@@
u8[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0] + v[1])
// @@MISS@@
print("null")
// @@WANT@@
201
// @@VALUE2@@
mkRec0().f
// @@TYPE2@@
u8[] | null
// @@PROOF2@@
if v != null { print(v[0] + v[1]) } else { print("null") }
// @@WANT2@@
null
