// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a non-null `K[] | null` value,
// element kind `i64`. One row of a nullable-list-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
const src: i64[] | null = [(4000000000 as i64), (1 as i64)]
function mk(): i64[] | null { return [(4000000000 as i64), (1 as i64)] }
// @@VALUE@@
src
// @@TYPE@@
i64[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0] + v[1])
// @@MISS@@
print("null")
// @@WANT@@
4000000001
// @@VALUE2@@
mk()
// @@TYPE2@@
i64[] | null
// @@PROOF2@@
if v != null { print(v[0] + v[1]) } else { print("null") }
// @@WANT2@@
4000000001
