// D1244 at the `i32[]` rep — one of the two the row names as CHECK-CLEAN INVALID WASM, where
// the struct rep is a loud refusal. Same capability, same block scope, a different rep behind
// the capture: `-struct` is the loud cell, this and `-string` are the silent ones, and the
// three together are what makes the row's "it is every ref rep, with two of them silent"
// gradeable rather than asserted.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@VALUE@@
[7]
// @@TYPE@@
i32[]
// @@PROOF@@
print(v[0])
// @@WANT@@
7
