// D1244 at the `string` rep — the second of the two the row names as CHECK-CLEAN INVALID WASM.
// Read `-list`'s header for why the three reps are three templates: one capability, one block
// scope, and a rep axis on which the outcome splits loud/silent/silent.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@VALUE@@
"seven"
// @@TYPE@@
string
// @@PROOF@@
print(v)
// @@WANT@@
seven
