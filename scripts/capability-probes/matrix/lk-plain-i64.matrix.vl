// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a `K[]` value delivered by name,
// element kind `i64`. One row of a delivery-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
const src: i64[] = [(4000000000 as i64), (1 as i64)]
function mk(): i64[] { return [(4000000000 as i64), (1 as i64)] }
// @@VALUE@@
src
// @@TYPE@@
i64[]
// @@PROOF@@
print(v[0] + v[1])
// @@WANT@@
4000000001
// @@VALUE2@@
mk()
// @@TYPE2@@
i64[]
// @@PROOF2@@
print(v[0] + v[1])
// @@WANT2@@
4000000001
// @@SKIP@@
is_in_if: the value is a list, not a union - there is no arm to test it against
is_in_while: same
is_in_and: same
is_in_not: same
else_if: same
early_return_guard: same
