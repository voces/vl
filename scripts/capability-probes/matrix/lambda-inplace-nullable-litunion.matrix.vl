// D2567 — the literal-union twin: a lambda literal over `LN | null` called in place keys its
// result as the box, which the emitter's union classifier did not see.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
type LN = 1 | 2
// @@VALUE@@
((p: LN | null) => p)(2)
// @@TYPE@@
LN | null
// @@PROOF@@
print(v ?? 0)
// @@WANT@@
2
// @@TEST@@
v != null
// @@HIT@@
print(2)
// @@MISS@@
print(0)
// @@VALUE2@@
((p: LN | null) => p)(null)
// @@TYPE2@@
LN | null
// @@PROOF2@@
print(v ?? 0)
// @@WANT2@@
0
