// i64-keyed-map-in-container-string — an i64-keyed map (`{[i64]: string}`) delivered to every container position the
// position matrix knows, the i64 twin of `i32-keyed-map-in-container`. The PROOF writes through the
// delivered map at a key past the i32 range and reads it back, and the second instance reads the
// key whose low half is the first key's, so a map that lost the high half of its keys prints wrong.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function mkMap(): {[i64]: string} {
  const m: {[i64]: string} = Map()
  m.set(4294967296, "a")
  return m
}
// @@VALUE@@
mkMap()
// @@TYPE@@
{[i64]: string}
// @@PROOF@@
v.set(4294967297, "b")
print(v.get(4294967297) ?? "?")
// @@WANT@@
b
// @@VALUE2@@
mkMap()
// @@TYPE2@@
{[i64]: string}
// @@PROOF2@@
print(v.get(1) ?? "?")
// @@WANT2@@
?
