// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a `{[string]: K[]}` map read (`K[] | null`),
// element kind `string`. One row of a map-read-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function truthy() { 1 == 1 }
const src: string[] = ["x", "y"]
const m: {[string]: string[]} = Map()
m["k"] = src
// @@VALUE@@
m["k"]
// @@TYPE@@
string[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0] + v[1])
// @@MISS@@
print("null")
// @@WANT@@
xy
// @@VALUE2@@
m["z"] ?? src
// @@TYPE2@@
string[]
// @@PROOF2@@
print(v[0] + v[1])
// @@WANT2@@
xy
