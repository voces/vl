// A15 item 3 — `Map<K, V>()` / `Set<T>()`, the constructor with explicit type arguments,
// graded at every delivery position in both faces. The un-annotated face is the point: the
// written arguments are the only thing that types the empty collection.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@VALUE@@
Map<string, i32>()
// @@TYPE@@
Map<string, i32>
// @@PROOF@@
print(v.size + (v["k"] ?? 7))
// @@WANT@@
7
// @@VALUE2@@
Set<i32>()
// @@TYPE2@@
Set<i32>
// @@PROOF2@@
print(v.has(4))
// @@WANT2@@
false
