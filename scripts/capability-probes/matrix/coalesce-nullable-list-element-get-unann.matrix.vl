// The SOURCE-un-annotated face of `coalesce-nullable-list-element-get`: the `.get` spelling
// over a bare list literal, so nothing but inference decides the element's rep. The twin of
// the subscript template beside it; the two must grade alike.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
const elems = [42, null]
// @@VALUE@@
elems.get(0) ?? 0
// @@TYPE@@
i32
// @@FALLBACK@@
0
// @@PROOF@@
print(v)
// @@WANT@@
42
// @@VALUE2@@
elems.get(9) ?? 42
// @@TYPE2@@
i32
// @@PROOF2@@
print(v)
// @@WANT2@@
42
