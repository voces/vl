// D2606 — the value is a copy of a `i32 | string` binding. A closure capturing an
// un-annotated copy of it typed its env field from the copy's initializer in a frame whose
// locals are not registered, so a function-scope capture fell to the `i32` default.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
function __src(): i32 | string { return "s" }
// @@SETUP@@
const __x: i32 | string = __src()
// @@VALUE@@
__x
// @@TYPE@@
i32 | string
// @@PROOF@@
print(v)
// @@WANT@@
s
