// List-kind audit (docs/internals/list-kind-audit-2026-09.md): a `K[] | null` record field read,
// element kind `struct`. One row of a nullable-field-by-kind grid over nine element kinds; the proof reads
// elements back, so a value built at another list rep prints a different answer or fails to
// validate. Written by `list-kind-grid.py --write`; edit that, not this file.
//
// Format: scripts/capability-probes/matrix.py, whose docstring is the spec.
// @@PRELUDE@@
type P = { x: i32 }
function truthy() { 1 == 1 }
type W = { f: P[] | null }
const src: P[] | null = [{ x: 7 }, { x: 1 }]
const none: P[] | null = null
const rec: W = { f: src }
function mkRec0(): W { return { f: none } }
// @@VALUE@@
rec.f
// @@TYPE@@
P[] | null
// @@FALLBACK@@
null
// @@TEST@@
v != null
// @@HIT@@
print(v[0].x + v[1].x)
// @@MISS@@
print("null")
// @@WANT@@
8
// @@VALUE2@@
mkRec0().f
// @@TYPE2@@
P[] | null
// @@PROOF2@@
if v != null { print(v[0].x + v[1].x) } else { print("null") }
// @@WANT2@@
null
