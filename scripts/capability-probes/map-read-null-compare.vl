// THE SAME MAP READ COMPARED `!= null` — silent-class-inventory D1039:
//
//   emitProgram: bare null needs a struct-typed context
//
// `vl check` returns 0. Filed apart from D1038 because the message is different and no
// ablation has yet shown one mechanism: a validator sentence is not a mechanism, and
// grouping two refusals because they reject one program is that mistake in reverse.
//
// Should print 5.
const m: { [string]: i32[] } = Map()
m["a"] = [5]

const g = m["a"]
if g != null { print(g[0]) }
