// `fromCodePoints(e)` where `e` is not a bare name. `vl check` is rc 0 and the emitter
// refused every spelling but an identifier: the arm emitted its argument TWICE, once under
// `struct.get 0` for the list's backing and once under `struct.get 1` for its length, so
// only a re-readable node was safe. Binding the same literal first (`const cps = [72, 105]`)
// always ran, at both faces — the ingredient is the argument's NODE KIND alone. A field, a
// call result and a `.slice` refused identically. The argument is now spilled into one
// scratch local, evaluated exactly once. Should print Hi.
print(fromCodePoints([72, 105]))
