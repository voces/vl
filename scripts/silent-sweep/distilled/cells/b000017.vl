type Circle = { r: i32 | null }
type Dot = { r: i32 | null }
type Sq = { s: i32 }
type Shape = Circle | Sq
type Box1 = Circle
const _sp1: Box1 = { r: null }
function mksrc() {
  const cc = { r: null }
  return cc
}
function rd(c: Circle) {
  function inner() {
    if (c).r != null { print(7) } else { print(0) }
  }
  inner()
}
rd(mksrc())
