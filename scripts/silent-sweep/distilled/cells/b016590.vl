import { reverse } from "std:array"
type Circle = { r: i64 }
function rd() {
  const c = [{ r: 7 }]
  const dd = reverse([c])[0]
  let hit = 0
  for zz in dd {
    if zz.r == 7 { hit = 7 }
  }
  print(hit)
}
rd()
