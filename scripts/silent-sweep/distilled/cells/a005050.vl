type Box2 = i32[][][]
type Box1 = i32[][][]
function idg<T>(x: T): T { return x }
function sink(_x: i32[][][]) { }
const _sp1: Box1 = []
const _sp2: Box2 = []
const lv2 = [7]
const lv1 = [lv2]
const c = [lv1]
function rd() {
  sink(c)
  const dd = idg(c)
  const g0 = dd
  if g0.length > 0 {
    const g1 = g0[0]
    if g1.length > 0 {
      const g2 = g1[0]
      if g2.length > 0 {
        if (g2[0]) == 7 { print(7) } else { print(0) }
      } else { print(0) }
    } else { print(0) }
  } else { print(0) }
}
rd()
