function outer() {
  const c = []
  function inner() {
    const lamc = (x: {r: i32 | null}[]) => x
    const dd = lamc(c)
    const g0 = dd
    if g0.length > 0 {
      if (g0[0]).r != null { print(7) } else { print(0) }
    } else { print(0) }
  }
  inner()
}
outer()
