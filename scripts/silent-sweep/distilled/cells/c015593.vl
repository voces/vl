type Circle = { r: string }
type Dot = { r: string }
type Sq = { s: i32 }
type Shape = Circle | Sq
type Box1 = {[string]: Circle}[]
const _sp1: Box1 = []
function mkcall() {
  const lv1 = Map()
  lv1["k0"] = { r: "seven" }
  const cc: {[string]: Circle}[] = [lv1]
  return cc
}
function rd() {
  const g0 = mkcall()
  if g0.length > 0 {
    const g1 = (g0[0])["k0"] ?? { r: "" }
    if (g1).r == "seven" { print(7) } else { print(0) }
  } else { print(0) }
}
rd()
