import { reverse } from "std:array"
type Circle = { r: i64 }
function rd() {
  const c = [{ r: 7 }]
  const dd = reverse([c])[0]
  const g0 = dd
  if g0.length > 0 {
    if (g0[0]).r == 7 { print(7) } else { print(0) }
  } else { print(0) }
}
rd()
