import { reverse } from "std:array"
type Box2 = string[]
type Box1 = string[]
function sink(_x: string[]) { }
const _sp1: Box1 = []
const _sp2: Box2 = []
function outer() {
  function inner() {
    const c = ["seven", ""]
    sink(c)
    const dd = reverse([c])[0]
    let hit = 0
    for zz in dd {
      if zz == "seven" { hit = 7 }
    }
    print(hit)
  }
  inner()
}
outer()
