type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function f() {
  const other: Shape[] = [{ r: 1 }]
  other.push({ s: 2 })
  const a: Circle[] = [{ r: 7 }]
  const b: Shape[] = a
  print(b.length)
  print(other.length)
}
f()
