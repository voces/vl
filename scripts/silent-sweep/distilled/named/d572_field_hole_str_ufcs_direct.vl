type V = { x: i32 }
type Bx = { v: string }
function g(self: string, b: Bx, n: i32): string {
  b.v = n + 1
  return b.v
}
const bx: Bx = { v: "s" }
const p: string = "s"
const z = p.g(bx, -1)
print(z)
