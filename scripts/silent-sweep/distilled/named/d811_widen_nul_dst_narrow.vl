type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function f() {
  const xs = [{ r: 7 }]
  const a: (Circle | null)[] = xs
  const b: Shape[] = xs
  const g0 = b[0]
  if g0 is Circle { print(g0.r) }
  print(a.length)
  print(b.length)
}
f()
