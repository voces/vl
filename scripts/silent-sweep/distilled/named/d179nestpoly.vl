// FLIPPED BY D391's LANDING — this cell now RUNS and must keep running.
//
// It was filed by #1996 as a REFUTATION PIN: both owner literals have the field-name set
// `{r}`, `structIndexOfObj` matches a literal to an interned row BY FIELD-NAME SET, and
// their nested values match DIFFERENT arms of the declared union. The first cut of
// D179/D227's rung minted an anonymous row for a nested literal a union arm claimed, `q`
// then matched `p`'s owner row, and `q`'s nested — still on the union-box path — was built
// as `(struct.new $uBox …)` into a field typed `(ref null $anonCir)`: check-clean INVALID
// WASM where a loud reject stood. `anonNestFieldMono` declined the whole rung instead, and
// this program was the loud reject that said so.
//
// THE PIN'S OWN DIAGNOSIS WAS HALF WRONG, AND THE CORRECTION WAS THE MECHANISM. It read
// the collapse as a MISSING DISCRIMINATOR and parked an open question — promote
// `structIndexOfObjCtxGo`'s `strict` bookkeeping from an ambiguity tiebreak to a real one.
// `strict` was never the missing piece: both nested values are code 15 against a code-15
// or code-16 field, so `strict` stays true for both and promoting it changes nothing here.
// The target discrimination was ALREADY WRITTEN, in `anonValueFitsField`'s `vc == 15 &&
// anonRow` arm — and it was unreachable. That arm can only refute when BOTH target names
// are known; a name is `sNames[structIndexOfObj(inner)]`, which is "" for a nested literal
// with no row yet. In node order `p`'s nested got a row, `p`'s owner recorded it, and then
// `q`'s nested was SKIPPED — because `anonNestNeedsRow` asks `structIndexOfObj(ow) >= 0` of
// `q`'s owner, and that owner already matched `p`'s row leniently, precisely because `q`'s
// nested had no name yet. A CIRCULARITY, closing on itself.
//
// `anonNestPolyPremint` breaks it with ORDER, not a new rule: the nested rows of a
// polymorphic owner field set are minted BEFORE any owner row exists, so both names are
// known when the owner scan runs and the existing arm separates them. The tell was already
// on the table — delete the union declaration and this pair ALREADY worked on every
// compiler, with separate owner rows; the union case differed only in that arm-claimed
// nested literals were minted late.
//
// The 16 census cells kept beside this one in `d179-anonrow-price.json` did NOT flip. They
// are the MAP-container form, their blocker is the map-value rep layer, and D207's landing
// does not rescue them either — verified cell by cell on the combined compiler.
type Cir2 = { c2: i32 }
type Sq2 = { s2: i32 }
type Shape2 = Cir2 | Sq2
const p = { r: { c2: 1 } }
const q = { r: { s2: 1 } }
print(1)
