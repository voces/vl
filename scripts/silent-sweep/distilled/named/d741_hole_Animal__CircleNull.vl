type Circle = { r: i32 }
type Sq = { s: i32 }
type Tri = { t: i32 }
type Shape = Circle | Sq
type Animal = Circle | Sq
type Other = Circle | Tri
function f() {
  const xs = []
  const a: (Animal)[] = xs
  const b: (Circle | null)[] = xs
  print(a.length)
  print(b.length)
}
f()
