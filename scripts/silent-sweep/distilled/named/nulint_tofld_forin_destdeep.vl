type Circle = { r: i32 | null }
type Box2 = { s: i32 | null }
const v = [{ r: 7 }]
const c2: (Circle[])[] = [v]
for z in v {
const w: Box2 = { s: (z).r }
if w.s is i32 { print(w.s) } else { print(0) }
}
