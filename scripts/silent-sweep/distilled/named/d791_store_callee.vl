type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function poison(xs: Shape[]) { xs[0] = { s: 3 } }
function f() {
  const a: Circle[] = [{ r: 7 }]
  const b: Shape[] = a
  poison(b)
  print(a.length)
  print(b.length)
}
f()
