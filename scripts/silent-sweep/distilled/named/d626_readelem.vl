type P = { r: i32 }
function sink(_x: P[]) { }
function mk() {
  const cc = []
  cc.push({ r: 7 })
  return cc
}
function rd() {
  const g = mk()
  if g.length > 0 { print(g[0].r) } else { print(0) }
}
rd()
