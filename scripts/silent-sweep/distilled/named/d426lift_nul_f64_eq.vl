function opT<T>(a: T | null, b: T | null): boolean {
  const g = (x: T | null, y: T | null) => x == y
  return g(a, b)
}
function cell(): boolean {
  const a: f64 | null = 1.5
  const b: f64 | null = 1.5
  return opT(a, b)
}
print(cell())
