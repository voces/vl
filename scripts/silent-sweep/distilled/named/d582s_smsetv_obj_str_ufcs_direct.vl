type V = { x: i32 }
function g(self: string): V {
  const m: {[string]: V} = Map()
  m.set("k", self)
  const q = m.get("k")
  return q ?? { x: -9 }
}
const p: string = "s"
const z = p.g()
print(z.x)
