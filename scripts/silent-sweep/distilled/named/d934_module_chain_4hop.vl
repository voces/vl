// D934 — the chain resolves to any depth, not just the two hops d921 filed.
function src(p: {r: i32}): {r: i32} | null {
  p
}

const y0 = { r: 7 }
const y1 = y0
const y2 = y1
const y3 = y2

function rd() {
  const g1 = src(y3) ?? { r: "s" }
  print(g1.r)
}

rd()
