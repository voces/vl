type Circle = { r: i64 | null }
type Box2 = { s: i64 | null }
const v = [{ r: 7 }]
const c2: (Circle[])[] = [v]
for z in v {
const w: Box2 = { s: (z).r }
if w.s is i64 { print(w.s) } else { print(0) }
}
