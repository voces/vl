type V = { x: i32, y: i32 }

function <=(self: V, other: V): boolean { return true }

function cell(): boolean {
  const a: V = { x: 1, y: 2 }
  const b: V = { x: 3, y: 4 }
  return a <= b
}
print(cell())
