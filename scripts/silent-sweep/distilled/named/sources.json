{
 "d14-length-tyvar": "the 45 cells of `scripts/silent-sweep/d14/lengrid.py` — 15 argument reps x {`.length` through a generic `T`, `.length` on the concrete twin, `x[0]` through a generic `T`} (silent-class-inventory-2 D14). Eight `d14len_*` cells RUN after this landing and land on their `d14con_*` twin's value; seven are the measured PRICE — a rep with no length at all, where a positioned check reject becomes a positioned EMIT reject. The `d14idx_*` leg is the row's own asymmetry argument kept as evidence, and it has since MOVED: `d14idx_arr_nest`, `d14idx_arr_arr_s` and `d14idx_arr_rec` were check-clean INVALID WASM when the set was filed — the index side's permissiveness, filed as D401 (this attribution was corrected from D402 by #2003) — and D401's close makes all three LOUD CHECK REJECTS, on the direct spelling's own sentence. The leg keeps its place in the set: it is what says the asymmetry the row argued about is gone.",
 "d179-anonrow-price": "scripts/silent-sweep/census/d179-anonrow-price.json — filed by #1996 as the price of the REFUSED first cut of D179/D227's anonymous-row rung (16 census-block-D cells + the hand-written `d179nestpoly`), and REPURPOSED by D391's landing. The 16 cells are map containers (`mapval`, `nestedmap`, `map3`, `list_of_map`) at `rep=arm, declness=nodecl, annpat=none`; they are STILL `loud emit reject` on the landing and still the price of dropping `anonNestFieldMono` — what moved is only their MESSAGE ('unsupported map value type' -> 'object literal is missing a struct field'), because their remaining blocker is the map-value rep layer and not the owner row. Verified: D207's rung does NOT rescue them either, so the two landings are orthogonal and neither closes this shape. `d179nestpoly` is the one that FLIPPED: it was the refutation pin asserting the pair must keep taking the union box, and D391's RUNG N gives each owner its own row, so it now RUNS and is a tripwire that must keep running. NOTE the claim this file's own `invisible_to` still makes about `regress.py` scoring the refused candidate identically — that was true before the set was curated and is FALSE now: with these cells in `named/`, the refused candidate reports 17 classes -> silent, which is the set doing exactly its job. It remains true of the DERIVED `cells/` half alone.",
 "d188-rung-price": "the 9 programs D188's own ablation NAMED: the price of dropping rung R2 (`arrSpineIsObj` claimed by `singleAliasMemberTyIx`) from its three-rung landing (silent-class-inventory D188). All 9 RUN on the shipped tree and are the `unread` position of the nine inline-object array-leaf shapes; with R2 stripped and only the PARSER rung R1 standing they are check-clean INVALID WASM — the parser starts producing an array alias the transparency rule has no arm for, which is D181's silent shape exactly. Nothing reading current behaviour can find them: on the shipped tree they behave like every other running alias.",
 "d199-rungs": "scripts/silent-sweep/census/d199-rungs.json — the 15 cells the union/variant landing's ABLATION names (silent-class-inventory D199 / D222 / D269): nine that one named rung and only that rung buys, plus six controls that must never move. No derived population can find them — the distilled corpus moves 0 for every rung and the corpus `cmp` is byte-identical apart from the three fixtures the rows own (D201: this family has no census population).",
 "d207-floor": "the three programs D207's landing NAMES, plus `scripts/silent-sweep/census/d207-listmap-tripwire.json` (740 block-C coordinates). D207 was the LOUD half of D203's root — the `-3` map-value floor — filed 'DELIBERATELY not taken' because lifting `letMapShapeOf`'s `if ish >= 0` bound cost 620 block-C cells `loud emit reject` -> check-clean INVALID WASM (plus 85 in B and 184 in E), all at `cont=list_of_map` with `claim >= 1`. THAT PRICE WAS D181, AND D181 CLOSED 2026-08-27. Re-graded cell-matched on 053fcf64 the same lift moves 6,037 cells loud -> `runs` over blocks B/C/D/E with 0 `runs` LOST and 0 into ANY silent class, so it was taken. `d207dest` is the row's own witness and must keep RUNNING. `d207nodest` is D209's shape and must keep LOUD-REJECTING — it is the BOUND: measured with a `tErr` probe it REACHES the rung (reach=1) and gets no answer (ans=0), so the day it starts running the rung has stopped being a FIND and started MINTING, which is the discipline D-MAPNODETY costs 128 slot moves to keep. `d207listmap` is census cell c002045 verbatim, the coordinate that carried the whole old price, kept because the interaction is second-order: regress D181's alias-transparency arm and these cells do not merely break, they go SILENT, because D207's rung will still resolve the map.",
 "d207-floor-paid": "`d207nodest.vl` was filed by #2006 as the BOUND on D207's rung — a `-3` map-value floor with no destination, which had to keep loud-rejecting because D207's `letMapDestShape` is a FIND and a find cannot supply a slot nothing minted. D461 (#2008) paid it off from the other side: the mint was refused one container up, at `structIndexOfTypeName`, which refused the very row `structIndexOfObjCtx` had already adopted the literal onto. The cell stays in the set with its polarity flipped — it must now RUN and print `1`, and its header records both failure directions.",
 "d223-lift-price": "scripts/silent-sweep/census/d223-lift-price.json — the census-block-E cells the REFUSED lift of `armRecvHoldsBareArm`'s ref-list-element decline cost, kept as the tripwire that would say when the lift became free (silent-class-inventory D223). IT FIRED. Their silence was D361's, not the lift's: with D361 landed (#1995) all 24 grade `runs` under the lift, and the lift was taken. The set stays, with its baseline flipped from `loud emit reject` to `runs` — the direction it guards is now \"must keep running\", and it is the only population that separates this trade at all (the derived `cells/` half reports nothing either way).",
 "d224-cost": "scripts/silent-sweep/census/d224-cost.json — the 207 block-B cells D224's refused candidate buys its fix with (loud emit reject -> check-clean invalid wasm) — D621 (2026-08-30) PAID 130 of them for real: its one conjunct in `armLayoutAmbiguousAt` moves those 130 loud emit reject -> check-clean invalid wasm, and buys 12 of this set loud emit reject -> runs. The 130 are NOT a defect the change creates: delete the single `!= null` line that reaches the gate from each cell's own program and MASTER produces the byte-identical validator sentence at the identical offset — 130 of 130 — so the reject was a floor over the D156 nested-map family (D300's shape), not a diagnosis. Keep the set whole: it is the standing record of what that floor was hiding. — D623 (2026-08-30) PAID THE OTHER 130: one rung on `mvValsElemStructRow`, the pairwise restatement of `rlSig`'s heap signature, which mirrored only its struct leg and not the concrete-VARIANT fall-through D280 had made agree with it, so an un-annotated `Map()` and its declared destination emitted two byte-identical 7-field map structs. The set is now 207 of 207 `runs` and D224's refused price is fully repaid; this entry is the ledger of how — 77 by D621, 130 by D623, 0 outstanding. The cells STAY in `named/` with their polarity flipped to must-keep-running: they are the only population that separates either trade, and the derived half reports nothing either way.",
 "d228-is-widen": "the 72 programs rung R4 (`isWidenNotVariant`) moves (silent-class-inventory D228). 44 are the PRICE: an `is` whose check type reaches the receiver only by an implicit numeric widening RAN with an always-false test and a dead then-branch, and is now a check reject. 28 are D228 itself: the same tests with the narrowed binding READ in the then-branch, which #1972 turned from `runs` into `emitProgram: narrowed union atom has no value box`. The read axis is what separates the two halves and D228's row did not have it. No census cell contains any of them.",
 "d272": "scripts/silent-sweep/census/d291-widened-store-price.json — D291's widened-store channel: the 64 cells a PARTIAL composition of the close costs (the predicate landing without the read's narrowing and/or the coercion's guard takes them from `runs` to a loud emit reject), and the 34 the close buys (30 check-clean invalid wasm and 4 `runs but wrong value` -> `runs`)",
 "d282": "scripts/silent-sweep/d282 — the NAMED runs-lost set of D281's rung: 36 cells that RUN on the shipped tree and go check-clean invalid wasm the moment `emitUnionCoerce`'s equal-heap box arm is removed. Every derived population scored that rung at ZERO.",
 "d290": "scripts/silent-sweep/census/d290-channel-price.json — the cells D209's close costs when only SOME of its three rungs land (60), and the ones it buys (25)",
 "d300-rung-price": "scripts/silent-sweep/census/d300-rung-price.json — the two programs D300's own ablation NAMED: the price of dropping a rung from its three-rung landing (silent-class-inventory D300). Neither is derivable from current behaviour: one RUNS on every shipped compiler and is a COMPILER TRAP without the chained `mvCanonRepOf`, the other is check-clean invalid wasm on both sides of the kind-1 hoist alone.",
 "d300-twinfree": "scripts/silent-sweep/census/d300-twinfree.json — the 65 twin-FREE siblings of D224's price (silent-class-inventory D300). 27 are still check-clean invalid wasm on 21f48747 and are D300's live population; 38 were closed by #1984 and are `runs` tripwires.",
 "d301-rung-price": "scripts/silent-sweep/census/d301-rung-price.json — the two programs D301's own ablation NAMED: the price of dropping the same-WRAPPER rung `W` from the map-value twin gate (silent-class-inventory D301). Neither is derivable from current behaviour — `W` moves 0 of 1,477 derived classes and is `cmp` byte-identical on all 1,913 buildable corpus modules — because the census's `cont` axis picks ONE container per cell, so no coordinate carries a SECOND declared layout twin beside a nested container.",
 "d311-print-route": "scripts/silent-sweep/census/d311-print-route.json — the 5 cells D311's close MOVES (check-clean invalid wasm -> runs) plus the 1 that already ran and separates the axis (silent-class-inventory D311). All six RUN on the closing compiler and are indistinguishable from their neighbours there; what names them is the ABLATION — 3 need rung S, 2 more need S AND N, and the control needs neither and must never move. The census cannot carry them: block A pins the payload at `7`, so no census coordinate exercises the STRING atom of a `string | i32` code-16 field.",
 "d341-index-place-price": "scripts/silent-sweep/census/d341-index-place-price.json — the price of index-place narrowing for inventory-2 D11, FILED at #1993 as a refused candidate's and PAID at #2003. 56 cells `loud check reject` -> `check-clean invalid wasm`: the checker narrows `xs[0]` and the emitter has no index-place channel (inventory-2 D451). The three `d341_sound_*` witnesses are the price that was NOT paid — #1993 measured all three going check-clean and TRAPPING at run time, a column the census records as zero in 250,238 cells, and the landing carries the three retirement rungs instead so all three stay loud. They are kept because the ablation is what grades those rungs: strip R5, R6 or R7 one at a time and exactly one of the three traps, while the 184-cell grid does not move a single cell. Regenerate the grid with `scripts/silent-sweep/d341/mkgrid.py --verify`.",
 "d351-crossfold-price": "scripts/silent-sweep/census/d351-crossfold-price.json — the price of the fold that was REFUSED, kept whole (silent-class-inventory D351). 12 cells were `trap_loads` when the set was filed and 6 RUN. Folding a union arm's `$fnsig` slot onto the declared struct row D280 already merged its heap type with buys the 12 and costs the 6 (runs -> check-clean invalid wasm), because the token CHAR is what `sigParamKindAt` reports. #1994 closed D351 in the POOL instead — the same 12 go to `runs` and the 6 do not move — so the 12 are now tripwires and the 6 remain the refused candidate's price, which is what stops it being paid.",
 "d351-halfroute-price": "scripts/silent-sweep/census/d351-halfroute-price.json — the 12 cells a PARTIAL routing of the `$fnsig` pool costs (silent-class-inventory D351). Four sites turn a pool position into a type index and they must resolve it identically; with the DECLARED functype unrouted, or with the VALUE CALL unrouted, all 12 go from `runs` to `trap_loads`. The D-ablation is the one worth the set: the grid's histogram is IDENTICAL to the base (198 runs / 12 trap_loads) while 24 cells have moved under it. Two of the 12 were already curated as `d199-rungs` controls and stay in that block.",
 "d352-paren-place": "the 38 cells of `scripts/silent-sweep/d352/parengrid.py` that the CHECKER's paren-blind place key names (silent-class-inventory D352). Three blocks. `d352ret_*` — 16 RETIREMENT cells, of which four (`wroot_rbare`, `wroot_router`, both depths) are check-clean and TRAP on master: a paren-spelled write `(t).v = {s:3}` keyed nowhere, so it retired no narrowing and the next `t.v.r` read the stale one. Their unparenthesised control is a clean `cannot assign` reject. `d352rd_*` — 20 READ cells split so that each rung has cells only it buys (K: a paren in the READ; C: a paren in the GUARD of an ELSE-branch complement). `d352opt_*` — 2 cells that must stay REFUSED: they are rung H's whole population, the optional-chain decline, and H moves 0 cells on all three grids and 0 corpus bytes without them.",
 "d361-landed": "scripts/silent-sweep/census/d361-landed.json — the 96 census-block-E cells D361's landing moved, every one of them check-clean invalid wasm -> `runs`, and the WHOLE of that block's silent column (96 before, 0 after). They must RUN. The distilled `cells/` half is blind to all 96: regress.py on the landed compiler reports 0 moved classes, so a collapse of current behaviour cannot stand in for them.",
 "d362-alias-leaf": "the 56 cells of `scripts/silent-sweep/d188/aliasgrid.py` whose array-spine LEAF is itself a DECLARED ALIAS — four leaf kinds (string litunion `K0`, numeric litunion `Z`, declared value union `U1`, canonicalized intersection `AB`) x seven annotation positions x {alias, alias-free control} (silent-class-inventory D362). Kept as PAIRS: the readable quantity is not \"does the alias leg run\" but \"does it land on its own inline control's verdict\", and no population carrying one spelling can ask that. What names them is the ABLATION — rung B (the nominal render leg in `transparentMemberEmitName`) moves 0 of 322 aliasgrid cells, 0 of the 1,477 derived classes and 0 corpus bytes ON ITS OWN, and without it the six `isect` cells rung A buys go straight back to a loud emit reject. `d362al_isect_unread` is the set's silent cell: check-clean INVALID WASM on master `7ff0f5a8`.",
 "d401-print-tyvar": "the 23 cells of `scripts/silent-sweep/d401/printgrid.py` that decide silent-class-inventory D401 (`print` of a value derived from an unbounded type parameter). Nineteen `d401p_*` cells were CHECK-CLEAN INVALID WASM on master `8939d435` and every one of them had a CONCRETE twin that was already a clean check reject — the whole row is that one annotation separated the two. Fifteen are the BARE position (`print(x)` on `T` itself), which the row did not name at all and which is the larger half; four are the ELEMENT position the row filed. The other four cells are the one-character neighbours that RUN (`print(x[0])` at an i32, string and string-code receiver, and `print(x)` at an i32) and must not move with them: a capability check at the pin that over-refuses is a false reject nothing derived from current behaviour would find.",
 "d402-intersection-elem-gate": "nine cells around the struct-alias-union struct row (silent-class-inventory D402/D441/D442/D443), banked as a REFUSED candidate's boundary by #2001 and re-graded when #2004 landed the fix. FOUR boundary cells: `d402_reject_const_arr` was the `nameIsStructDecl` gate's bite in `emit_classify.shapeElemDeclaredStructIdx` (loud emit reject, now runs); `d402_rescue_plaintwin` shows a same-layout PLAIN struct twin rescued it even before the fix (the bridge landed on a `TypeDecl`); `d402_inert_otherlayout` shows an intersection of a DIFFERENT layout is inert; `d402_admit_field_pos` is the one position of sixteen the reject never reached (a struct FIELD of the inline shape's list). THREE were the row's silent/loud siblings, all closed by the same landing: `d402_silent_two_inters` (D441 — two intersection aliases of ONE layout, both ref lists, two struct rows but one array element type), `d402_silent_field_from_alias` (D442 — an alias ref list into a structurally-spelled field list, two ref-list wrappers, no coercion) and `d402_reject_shape_param` (D443 — an alias value into a structurally-spelled param, `ref valtype with no interned shape`). THE PRICE CELL IS `d402_permuted_arm_field_alias`, AND IT IS WHY THIS SET EARNS ITS KEEP: `type AB = {b:i32} & {a:i32}` beside a `{a:i32,b:i32}[]` field. Opening the merge (recording the SHAPE's arena type at the row mint, so `slotCanonId` stops banking -1 for a one-variant `UnionDecl`) made this pair MERGE with `a` and `b` in opposite slots — a check-clean invalid wasm became a check-clean WRONG ANSWER (`2 1` for `1 2`). It is the ONLY cell of the 151-cell D402 neighbourhood grid that any rung of the landing moved to silent, and the ONLY thing that justifies the field-order rung, which closes no row and moves no derived population on its own. `d402_uniontwin_field_alias` is the one of that grid's seven alias-naming declarations still broken at the field position: a DEGENERATE union alias (`{a,b} | {a,b}`) in D442's position compiles to a union-BOX element list and is still check-clean invalid wasm — a different root, filed as D446.",
 "d411-two-destinations": "the CONTROLS and the measured RESIDUE of `scripts/silent-sweep/d411/gen411.py` (silent-class-inventory D411), plus one witness per `d2` family from the 68 cells the landing moves. The grid is `d2` (the union-BOX destination's spelling) x `d1` (the plain-struct destination's spelling, or `none`) x `order`, 103 cells, and it had to be built: no census block carries TWO annotated destinations for one un-annotated literal, so a derived corpus cannot collapse a class its population never contained. On the base 96 of 103 are check-clean INVALID WASM and the 7 `__none__` controls run. The landing makes 68 of the 96 a LOUD emit reject, loses 0 `runs` and moves 0 cells to any silent class; `order` moves nothing, which is what says the rung is a KIND filter and not a tie-break. THE 28 KEPT WHOLE ARE THE ONES IT IS NEVER ASKED ABOUT: a `tErr` probe reads `reach=0` at `letRefListDestSlot` for exactly those 28 and `reach=1` for exactly the other 75, so the residue is bounded by REACH and not by the predicate — filed as D501. The 7 controls are also the price a CHECKER-side answer would pay: every variance or storage-class rule that reddens this class reddens them too, and they are D381's own subject. CLOSED 2026-08-29 (D501): all 28 move `check-clean invalid wasm` -> `loud emit reject`, so the grid is now 96 loud + the same 7 running controls and the class has no silent residue at all. The reach partition reproduced EXACTLY on an independently rebuilt probe (89 of 103 enter `letRefListDestSlot`, the 28 never do), and what the row INFERRED from it was wrong: probing the three CALLERS as well as the callee shows all three entered for the literal in all 28 cells, each taking its `letAnn*` early return — the ARM PIN (`dstPinAnnIn` / `synthRetPinAnn`) had already written an annotation onto the binding. The two pin sites are separate gates and the ablation is not additive: 16 cells move under the `dstPinAnnIn` gate alone, 8 under the `synthRetPinAnn` gate alone, and the four `d411_{mapstore,structfield}__ret__*` need BOTH, because each pin claims them the moment the other declines. The 7 controls keep their meaning unchanged — they are still the price a checker-side answer would pay.",
 "d421-constructor-over-t": "the 15 cells where an operand whose TYPE is a CONSTRUCTOR over the type parameter lost the refusal (12, `a: T[]`) or the dispatch (3, `a: T | null`) — silent-class-inventory D421/D422. All 15 are check-clean INVALID WASM on master and 14 loud check rejects + 1 `runs` on the closing tree. D35's and D44's own 1,712- and 741-cell grids cannot contain them: both vary the DELIVERY of the needle, the receiver and the callee and hold the operand's SHAPE at `T` in every cell. The distilled `cells/` half is blind to them too — regress.py reports 0 moved classes across this whole change and the `tests/cases` corpus is `cmp` byte-identical on all 1,930 buildable modules.",
 "d423-deferral-price": "the price of D423's `T[][]` deferral when it lands WITHOUT D421's constraint widening, kept whole (silent-class-inventory D423). 9 cells whose element has no compare core leave a loud check reject for check-clean INVALID WASM (7) or a loud emit reject (2) under the one-line deferral alone, because nothing re-asks at the pin; the other 4 are what the deferral BUYS (a false reject over `i32[][]`/`boolean[][]`/`K[][]`/`N[][]` that the direct spelling performs correctly). Two rungs, one landing, and the branch's moved set is a strict subset of the union of the singles — 25 against 34 — because of this pair. Nothing derived can find them: on the shipped tree all 13 behave exactly like their class-mates.",
 "d424-operator-local": "the 40 operator-declaration cells whose receiver is a function-body LOCAL (silent-class-inventory D424), over operator x quoted-vs-symbol-token name x annotated-vs-bare parameters. On master the four RELATIONAL operators are check-clean INVALID WASM (16 cells — a `boolean` result lets `i32.lt_s` over two struct refs through) and the six arithmetic ones are a loud `field access receiver is not a struct` (24). All 40 run on the closing tree and print the DECLARATION'S own answer. The tree's only operator fixture binds every operand as a module GLOBAL, which is one of the four deliveries that already worked, so no existing population contains this cell.",
 "d425-inert-declaration": "the price of the reject in silent-class-inventory D425, PAID on 2026-08-28: 40 cells where an operator declaration whose `self` is a BUILT-IN type parsed, type-checked and was SILENTLY IGNORED — `vl check` rc 0 and the native answer came back. This entry used to read \"they RUN today and must keep running until D425's declaration-site reject lands, at which point every one of them becomes a check reject, which is exactly the price worth having written down\". That is what happened: 20 of them are behavioural representatives and all 20 moved `runs` -> `loud check reject` under the hoist gate keyed on `binOpDeadSelf`. The set is kept whole, now as the record of a price PAID rather than one owed — a future lift of the reject moves them back and the movement is visible either way. Each cell's body disagrees with the built-in on its own pair, so a cell that starts DISPATCHING is a wrong value rather than invisible. AND, from 2026-08-29, the 20 ODD cells (`d425c001`..`d425c039`) are also D521's measured PRICE. An un-annotated `self` is typed as a fresh type VARIABLE, so it carries a hole exactly as `self: T` does and D491's first cut claimed this population too; measured, that costs these 20 `runs` cells, whose recorded expectation is `10` — the BUILT-IN's answer, i.e. the corpus records them as correct programs. D491's rung was narrowed to an ANNOTATED hole (`parType >= 0`) rather than paying that, and the population was filed as D521 with this set as its price. Do not re-derive the number: it is the odd half of this set, and it is what a wider gate blocks on. THAT PRICE WAS RE-MEASURED AND PAID on 2026-08-29 (#2014): rebuilt on `042624e1`, the wider candidate still moves exactly those 20 and nothing else, and the veto was OVERRIDDEN rather than disputed — each of the 20 printed a value its own source contradicts, so turning it loud is strictly better. The override is executable: `python3 scripts/silent-sweep/d521/unannotgrid.py --price <seed>` re-runs every lost cell beside its own do-nothing control and asserts (a) it ran, (b) the declaration contributed nothing, (c) the value differs from the declaration's answer. FOUR of the twenty are BLIND on term (c) and the check says so out loud instead of counting them as passes: `d425c033`/`c035`/`c037`/`c039` are `>` and `>=` at `1, 2`, natively `false`, with bodies that also return `false`, so their own stdout cannot separate dispatch from inert. They are settled by a distinguishing TWIN the check RUNS (the same program returning `true`, measured inert). That is this set's own axes failing the do-nothing rule `d471/opdeclgrid.py` later mechanised — the newer grid uses `7, 1` and 0 of its 160 cells are blind.",
 "d426-lambda-in-generic": "the 5 cells of silent-class-inventory D426, OPEN: a lambda declared inside a generic body whose PARAMETER TYPE mentions the enclosing `T`. Two are check-clean INVALID WASM on master and on all four rungs of D421's landing (`(x: T[], y: T[]) => x == y` and its `T | null` twin, both at a `T` binding whose direct spelling runs), one is the loud sibling that shows the shape is broken for every body (`(x: T[]) => x.length`), and two are the controls that must never move — the same generic without the lambda, and a lambda that CAPTURES the operands instead of taking them. The lifted lambda's `call_indirect` type is built at the UNSUBSTITUTED type, so `T[]` lowers as the i32 default; the fix is in monomorphization and is priced in the row.",
 "d426-lift-price": "the 33 cells `scripts/silent-sweep/d426/lamgrid.py` named as D426's PRICE — a lambda declared inside a generic body whose own parameter annotation still spells the enclosing `T`, each one check-clean invalid wasm beside a CONTROL that runs. They were kept as the tripwire that would say when someone landed per-pin lambda lifting. THAT HAS HAPPENED: `monoCloneLambdaSubst` instantiates the lambda once per pin with the pin's own parameter types and `monomorphize`'s prune stubs the template, so ALL 33 now RUN and land on their control's value. The set keeps its place and its name: it is what says the lift is still there, and a candidate that removes it moves 33 cells `runs -> not-runs`, which is the one movement the standing gate blocks on.",
 "d426-nul-coincidence-price": "the four `runs` cells D426's FLOOR cost when it landed — a `T | null` lambda parameter at `T = boolean` and at `T = K` for a string literal union, the two bindings whose rep happens to match the i32 default the unsubstituted `T | null` lowered to. The row overrode the `runs -> not-runs` veto to pay them and recorded the reversal as instrumented: 'per-pin lambda lifting restores all four, and these cells are the tripwire that says when someone has landed it'. It has been landed and all four RUN again — no longer by coincidence but because the lambda's signature is now built at the pin. They stay as a tripwire in the other direction.",
 "d444-d445-operator-declaration": "the 111 cells of `scripts/silent-sweep/d444/opgrid.py` that decide silent-class-inventory D444 and D445 — the two DECLARATION-SITE shapes of an operator that can never fire. 75 are the PRICE: 73 `runs` -> `check_reject` and 2 `emit_reject` -> `check_reject`, every one of them a program that ran ONLY because its own declaration was dead (the body says 99, or `true`, and the program printed the built-in's answer). 36 are the BOUNDARY and they are the more important half: `d444_*_a2_*` holds arity 2, which is D425's territory and must NOT move — the built-in-receiver cells keep printing the native answer (D425 still open) and `d444_lt_a2_obj` keeps printing the DECLARATION's `true`, the one comparison that separates dispatch from inertness; `d445_{i32,f64,i64,bool,new_i32,union,obj}` are the six NON-OBJECT receivers that dispatch correctly today, which is why D445's filed title (\"a non-object `self`\") was wrong and the swallow is exactly `tyBuiltinIndexable`. Nothing derived can find any of them: the distilled `cells/` half moves 0 classes across this whole change (re-measured on master 5625840c after #2004: every number identical) and the corpus `cmp` is byte-identical on all 1,954 buildable modules. Regenerate with `python3 scripts/silent-sweep/d444/opgrid.py --mkset` and check with `--verify`.",
 "d451-emitter-index-channel": "The EMITTER's index-place narrowing channel (#2007, D451). Nine cells the 184-cell d341 grid provably cannot grade, because the grid never varies the STATEMENT BETWEEN the guard and the read: three write-retirement soundness cells that RAN on the base compiler and trapped on a candidate missing R13/R14, three cells the grid's rep axis has no column for (an un-annotated binding from a narrowed cell, a boxed STRING arm, a write the narrowing admits), one both-inputs control, and two D452 tripwires that must keep taking their loud check reject. Regenerate and check with scripts/silent-sweep/d451/mkcells.py --verify.",
 "d471-d425-binary-operator-declaration": "the 328 cells of `scripts/silent-sweep/d471/opdeclgrid.py` that decide silent-class-inventory D471 and D425 — the last two DECLARATION-SITE shapes of a BINARY operator that can never fire, after #2005 closed the arity (D444) and index-receiver (D445) seams. 244 are the PRICE, every one `runs` -> `loud check reject`: 129 are D471's (the first parameter is not named `self`, so `opSelfFnTy` returns -1 before it looks at any type — inert at EVERY receiver, an object one included) and 110 are D425's (`self` names a type that is not an object, so `checkBinary` never reaches the dispatch at all), plus 5 PIN cells. Each ran ONLY because its own declaration was dead: the body says 99, or the relational opposite of the native answer, and the program printed the built-in's. 84 are the BOUNDARY and they are the half that says the gates stopped where they were told to: 60 object receivers that DISPATCH and must never stop; 19 that are D425's RESIDUE — a generic `function \"+\"<T>(self: T, other: T)` is STILL silent at an i32 site and dispatches at an object one, so the hole is decided per call site and this landing deliberately leaves it, recorded as `runs but wrong value` rather than as a quiet `runs`; and 5 PINS, of which `d471_pin_inter` is the load-bearing one. That pin declares an operator over `type AB = {a} & {b}` and never uses it, so it is the ONLY cell that sees the declaration gate by itself: it RUNS because `tyDenotesObj` looks through the opaque one-member alias box `declaredTyOfName` keeps for a canonicalized intersection, and a gate narrowed back to a bare `is TyObj` turns it runs -> not-runs, which `regress.py` BLOCKS on. Nothing site-driven can catch that — `self: AB` at a site is loud on both sides for D402's reasons. Regenerate with `python3 scripts/silent-sweep/d471/opdeclgrid.py --mkset` and check with `--verify`, which also RUNS the do-nothing rule rather than asserting it in prose (it refused 32 cells of this grid on the first pass). CLOSED 2026-08-29 (D491): the 19 RESIDUE cells move `runs but wrong value` -> `loud check reject` and the grid's `inert` column is now 0. The layer is neither of the two #2007 refuted — `checkBinary`'s per-site `if odsp is TyObj` decision is banked by operator NAME and read back once at the END of `checkProgram`, which refuses a hole-`self` declaration no site dispatched to while at least one site took the built-in. `accepted=5` and `dispatch=60` are unchanged, and `d471_pin_typaram` — the declaration with no operator site anywhere — is exactly what the second term protects. Because this set already recorded the DECLARATION's answer as the 19 cells' expectation, the standing gate never scored them as `runs` and the transition is not a `runs` -> not-runs veto: 0 `runs` lost.",
 "d492-d493-binop-pin-mirror": "the 315 cells of `scripts/silent-sweep/d492/pingrid.py` and their direct twins — 525 distinct files, one twin serving both the `typar` and the `fnval` delivery of a cell — that decide silent-class-inventory D492 and D493 — the USE-site half of the binary-operator family, after #2007 closed the declaration-site half. The TWIN is shipped because the twin IS the control: it differs from the cell in exactly one thing, the annotation, and the whole defect is that the two answers differ. 207 cells MOVED at the landing — 102 `check-clean invalid wasm` -> `loud check reject` (D492's class), 66 `loud emit reject` -> `loud check reject` (D493's), 36 `runs` -> `loud check reject` (the PRICE), 2 `loud check reject` -> `runs` (a `%` overload that the pin had been refusing) and 1 `loud check reject` -> `check-clean invalid wasm`. 107 BOUNDARY cells run on both sides and must never stop. 2 RESIDUE cells are the deliberate leftover, and they are D511: an operator overload at a pinned hole lowers only when the call's LEFT argument is spelled with the same identifier as the generic's left parameter. THE CENSUS PROVABLY CANNOT COVER ANY OF THIS: `gencensus.py` emits exactly one generic declaration in the whole population, `function idg<T>(x: T): T { return x }`, whose body holds no binary operator — so no census cell reaches `binOpDefinedFor` at all. Block D (9,000 cells) and the distilled corpus (1,477 representatives) each moved ZERO cells at this landing, and that is a structural absence, not a pass. The PRICE is 36 cells where the bitwise/shift ops ran over `boolean` and over a string literal union through a type parameter: `true ^ false` printed `true` and `\"b\" ^ \"a\"` printed `a` — an i32 opcode over the boolean rep and over two interning indices, for an operator whose direct spelling is `operator '^' is not defined for boolean and boolean`. Re-grade with `python3 scripts/silent-sweep/d492/pingrid.py --price <base-seed>`, which asserts per cell that it ran, that its direct twin is loud, and that the twin's refusal names that operator.",
 "d511-d512-pin-vs-hole": "the 316 files (158 cells and their DIRECT twins) of `scripts/silent-sweep/d511/pingrid2.py` that decide silent-class-inventory D511 and D512 — the two questions `dispatchRewrite` asks of an operand whose type is still a HOLE, because that pass runs before monomorphization. 66 are the FIX and none of them is a price: 0 `runs` cells were lost, so `--price` correctly refuses to report a green result on its empty population. 58 are D512 (`??` over a pinned non-nullable operand, every operand type but `nullable` and the three the monomorphizer's own value-union limit blocks) and 8 are D511 (an operator OVERLOAD on a pinned object; 6 invalid wasm -> runs and 2 whose invalid-wasm message moves from the missing dispatch to the return-type residue below). 12 are the BOUNDARY: the `%`/`^` cells that already ran on the base seed and must never stop, including the ones that ran only because of the NAME LEAK this set exists to record — `structIndexOfExpr`'s `declaredStructIndex` answers for a module binding merely SPELLED like a parameter, which is why the `spell` axis (match / nomatch / decoy) is in the set whole and why `d492/pingrid.py`'s grid, whose constants are all named `a`/`b`, is structurally blind to D511. 84 are the RESIDUE this landing deliberately leaves, in two families, each with a row of its own: 6 are D511's un-annotated generic RETURN through an operator overload (the instance dispatches and returns i32 while the checker typed the call as the OPERAND type — silent-class-inventory D532), and 72 are the eight operators `binOpDefinedFor` still refuses at the pin because its mirror of `checkBinary`'s dispatch-arm ORDER reaches only the integer-only arm (D531). The remaining 6 are `??` cells blocked by unrelated pre-existing limits (a value-union monomorphization argument, and an inferred-union return the emitter does not support), each loud and each naming its own cause rather than the operator. Four of the 158 are the NESTED-SHAPE witnesses: the operator inside an ARRAY or OBJECT literal rather than directly under a `const`. They exist because a copy-on-write walker fails SILENTLY on a node kind it has no arm for, and the rung's first draft had four fewer arms than `monoFoldTyParamLayout` — measured, that cost exactly these four cells.",
 "d521-unannot-self-dispatch": "the 20 BOUNDARY cells of `scripts/silent-sweep/d521/unannotgrid.py` — an UN-ANNOTATED `self` binary operator at an OBJECT receiver, all ten operator names x both name spellings (silent-class-inventory D521). They must keep DISPATCHING: this is the population D521's rung is licensed by the site bank to spare, and nothing in `named/` covered it before — `d471_pin_unannot` has no operator site at all and every `d471_*_obj_self_*` cell is ANNOTATED. Their do-nothing control is LOUD at all ten operators (`operator '+' is not defined for V and V` for the six arithmetic ones, `comparison expects numeric operands` for the four relational), so each one runs ONLY because its declaration fired — the strongest form of the distinguishing rule this family has, and the reason a lift that breaks dispatch cannot hide here. Measured on an instrumented compiler, the landing BANKS these declarations identically to the cells it refuses (`bankUn=1` on both) and spares them entirely on `siteTake`: probing only the declaration would have read the two as the same cell.",
 "d531-d532-pin-result-type": "the 184 cells of `scripts/silent-sweep/d532/opretgrid.py` that its `--verify` ledger names (92 pinned cells kept whole with their DIRECT twins) (silent-class-inventory D531 + D532). The grid exists because `d511/pingrid2.py` never PRINTS the operator's result — a deliberate confound cut there — so a module that validates and returns the WRONG VALUE grades `runs` in it. Fourteen are the price of two candidates that were built and refused: D531's hoisted dispatch gate WITHOUT D532's deferred result type sends ten cells silent (six to invalid wasm, four to a module printing `true` where its own declaration says `99`), and the deferral with no annotated-body bound sends four more to invalid wasm at a pin where the overload cannot fire. `opretgrid.py --price` re-checks all fourteen against the seed handed in and tells a wrong seed from a veto by the seed's own md5. Six more are RESIDUE the landing does NOT close and files as D551: a generic's DECLARED return type is never re-checked against the body's type at the pin, and `function g<T>(a: T): V { return a }` called at `i32` is check-clean invalid wasm with no operator in it at all.",
 "d541-nested-operator-declaration": "the 80 PRICE cells of `scripts/silent-sweep/d521/unannotgrid.py` — a binary operator declared BELOW MODULE SCOPE at an i32 receiver, ten operators x two name spellings x annotated/un-annotated `self` x two nestings (silent-class-inventory D541). Every one RAN on `042624e1` and printed the built-in's answer where its own body says 99 (or the opposite boolean); all 80 are a loud check reject on the landing. THE POSITION AXIS HAS THREE VALUES AND THE THIRD IS WHY THIS SET IS 80 AND NOT 40: a declaration inside a top-level `if true { … }` BLOCK is as far outside `gRootStmts` as one inside a function body, was equally silent, and made the rule's first message (\"nested inside a function\") false of half the population it catches. THE POINT OF KEEPING THEM is that this price costs the standing gate NOTHING and therefore cannot be seen by it: measured, no `.vl` under `tests/`, `std/`, `compiler/` or the distilled corpus declares a binary operator below module scope, so a later lift of the position rule would pay this back invisibly. The nested INDEX operator is NOT in this set and that is measured too — `function \"[]\"` inside a body is already the loud `cannot index non-array Box`, so the position gap is binary-only.",
 "d551-generic-declared-return": "the 604 cells of `scripts/silent-sweep/d551/retgrid.py` (silent-class-inventory D551) — 216 `fix` cells the landing moves, 82 `control` cells it must not, the 2-cell PRICE of the REFUSED want-side rung, and the 2 `residue` cells it leaves silent (filed as D561), each with its one-token DIRECT twin. Every cell is a generic whose DECLARED return type contradicts what its body produces at the pin; the twin is the same file with the type parameter written out, so the expected answer is always the language's own answer to the same question. The 24 `runs` cells the landing loses are in `fix` and every one has a LOUD direct twin (`function g<T>(self: T): boolean { return self }` at `g(6)` printed `true`). The price cells RUN and must keep running: `d551w_brand_typar` is `tests/cases/memory/flat-generic-rows-branded.vl` in eight lines, and it is kept because `as T` over a type parameter is not a spelling the language has — not because the pin would be wrong. Re-grade with `--verify <base>` / `--price <seed>`. UPDATED BY D561's LANDING (2026-08-29): the set is now 190 cells + twins, the grid 630. The `wanthole` block is no longer a REFUSED rung's price — it is the landing's own, minus one exemption. Seven cells move (`d551w_{bool,obj,str,xbrand}_typar` and `d551wv_{bool,obj,str}_typar`): four `check-clean invalid wasm` -> `loud check reject`, and three that RAN. `d551w_bool_typar` is the overridden one and its override is on all four terms — the `d551wv_*` block is the same programs with the VALUE printed, and it shows `print(g(true, -1))` giving `false` beside a direct twin that is a loud `return type mismatch: expected boolean, got i32` on every seed ever graded. The old block printed `1` at every cell, so the standing gate scored that loss as a regression; that blindness is why the block was added. `d551w_xbrand_typar` is a fourth cell nothing had filed: a `Meters` handed back where the call bound `A` to `Feet`, which RAN printing `10`. THREE LISTS ARE NEW AND NONE IS DERIVABLE FROM CURRENT BEHAVIOUR. `price` (2) is unchanged in kind — `d551w_{brand,forge}_typar` are what the FULL widening (`ABL_ADD_R5`) would cost, and it also costs two corpus modules. `refute` (1) is the opposite direction and `price` is structurally blind to it: `d551w_bhole_typar` is a loud reject on the base AND on the landing that a candidate INVENTED a `runs` for — the newtype exemption asked of every return constraint rather than only of a want-side-only one, which re-opens D551. `deliberate` (3) enumerates the cells that RUN on the landing while their direct twin is a loud reject — the exemption's entire surface, so a FOURTH appearing there is a leak nothing else in the file would report. D571 is the row that carries them and `tests/cases/generics/declared-return-hole-newtype-brand-keeps.vl` is their standing gate.",
 "d572-destination-seam": "the 247 cells + twins of `scripts/silent-sweep/d572/letgrid.py` (silent-class-inventory D572) — 171 `fix` cells the landing moves, 75 `control` cells it must not, and the 25-cell PRICE the landing itself paid. Every cell is a generic body in which a value lands somewhere its DESTINATION cannot check: a local declaration, a re-assignment, a struct field write or an array element write, with the hole on either side. The twin is the same file with the type parameter written out, so the expected answer is always the language's own answer to the same question. THE PRICE LIST IS THE REVERSE OF D551'S and that is the point: nothing was refused here, so `price` names what the landing TOOK — 25 cells that ran on the base and are a loud reject now — and `--price <seed>` asserts each is still a reject that AGREES with its direct twin, so a silent re-opening fires it. 24 of the 25 ran printing a value their own declaration contradicts (`g(true, -1)` -> `false` at four separate seams), which is why the grid prints the result: two earlier grids in this family scored exactly these cells as healthy `runs`. The 25th is `d572o_brandlet_typar`, which printed the RIGHT value (1032) and is overridden on a different argument — it is the laundered spelling of D561's branded accessor, the 'rewrite' D572's row warns is a relocation rather than a repair, and the accessor itself still builds byte-identically through D561's own exemption (`d572o_litbrand_*` and the `rowAt` line of `tests/cases/generics/declared-local-hole-at-pin-keeps.vl` are the executable form of that claim). `deliberate` (1) is `d572o_forge_typar`, D571's residue at the RETURN seam, which this landing must not touch. `residue` is EMPTY: the grid has no cell left silent beside a loud direct twin. Re-grade with `--verify <base>` / `--price <seed>`.",
 "d581-literal-and-builtin-seams": "the 593 cells (+ their direct twins) `scripts/silent-sweep/d581/litgrid.py` named for D581 and D582 — the two destinations D572's table could not reach. `fix` (427) is every cell the landing moved: 389 of them silent -> a loud reject that says what the one-token-different DIRECT spelling says. `price` (38) is the OVERRIDE, and it is exactly the set of cells that RAN ON THE BASE PRINTING A VALUE THEIR OWN DECLARATION CONTRADICTS — an i32 into a `boolean[]` printing `false`, a boolean into an `f64[]` printing `1` — every one of them beside a loud direct twin, so they run by coincidence of rep and the loss is loud. `control` (163) are the cells that RUN and AGREE with their twin on both seeds: the A7 boolean-into-i32 coercion through an array literal and through `.push`, the `u8` storage slot, a literal adopting a newtype brand, a hole-typed destination fed the hole itself. `deliberate` (3) are the cells that still run beside a disagreeing twin and are meant to: D571's forge, and the two `d581_arr2_i32_bool_*` whose DIRECT spelling is a pre-existing miscompile of its own (filed as D591). The set also carries the REFUSED third rung's price: `REFUSED.wasm` (md5 30f1c618b0d3ac6fd338cc10474b71b4) reconstructs the union the array literal's element join dropped and records it as the CONTAINER's type — same cells bought, and it costs those two `d581_arr2_i32_bool_*` cells, which RUN CORRECTLY. `litgrid.py --price` and `--verify` re-check all of it against any seed; `--price` returns rc 2 for the base seed by md5, rc 1 for a veto, rc 0 held, and fails on an empty population.",
 "d591-joined-element-and-inferred-return": "the 255 cells (447 with their twins) `scripts/silent-sweep/d591/joingrid.py` named for D591 and D592 — the two rows #2020's grid left in its DIRECT column, both on the FAR side of the check/emit line (`vl check` rc 0 AND correct; the emitter's rep disagrees). `fix` (90) is every cell the landing moves, all of them `check-clean invalid wasm` -> `runs` printing what their own twin prints: 74 for D591 (an array literal whose element JOIN is a union the destination has none of) and 16 for D592 (an un-annotated generic return read out of a hole-typed container). The two counts are EXACTLY ADDITIVE — 74 + 16 = 90 — which is what says they are two landings in one PR. `control` (155) is the cells it must not move. `price` (10) is what it pays: `u8[]` bindings whose literal begins with a boolean, which go from a module the engine refuses to a module that RUNS printing `true` where the declaration says a byte — every one of them inventory row D601's coordinate, which reproduces on the base with no mixed literal at all, so the landing extends an existing wrongness rather than inventing one. `residue` (6) is the cells the REFUSED struct arm would have bought; its own price is not a cell at all but FOUR corpus modules that stop building, kept executable as `REFUSED_MODULES` and scored by `joingrid.py --refused`. `deliberate` (4) are D601's own base cells. THE PRICE WAS REPAID BY #2022: D601 closed, so all ten `price` cells now RUN AND PRINT THE BYTE THEY DECLARE, and `--price` was re-derived to assert that repayment (it had been written to FAIL the day a price cell became correct, and it did). Re-grade with `--verify` (base seed), `--price` and `--refused`.",
 "d601-u8-element-print-overload": "the 18 cells of `scripts/silent-sweep/d591/joingrid.py` that D601 moved (#2022), kept whole because a derived rule cannot find them: on today's compiler they behave exactly like their class-mates, and what makes them worth keeping is that they are the entire measured population of one wrong VALUE. Every one is a `u8[]` whose initialiser literal begins with a boolean-typed element, read back and printed: `runs` printing `true` -> `runs` printing the number the declaration says (`1`, `3` or `7` — a boolean FIRST element reclassified the WHOLE list, so even an i32 element read at index 1 printed `true`). Ten of the 18 are D591's `price` list, which this landing repaid; four are its `deliberate` list; four are twins. `--price` scores the ten. NO PRICE OF ITS OWN, and that is measured rather than asserted: 0 `runs` lost, 0 -> silent, 0 new wrong values over 3,480 grid cells, and corpus `cmp` is 1,969 identical / 0 DIFFER / 0 LOST — the rung is asked 102 times across 14 corpus modules (`std/utf8.vl`, `std/fs.vl`, `std/args.vl`, `tests/cases/arrays/u8-packed-list.vl` …) and changes the answer in NONE of them.",
 "d626-inferred-return-binding": "the 19 cells of D626's own ablation grid — an INFERRED return of an empty `[]` that reaches the `return` through a BINDING (silent-class-inventory D626). THE DERIVED HALF SCORES THE WHOLE FAMILY AT ZERO and that is why the set exists: every `mkc` shape the census generates ANNOTATES its return, so no coordinate sits here at all — `regress.py` reports 0 classes moved across the entire close, `goal-scoreboard.py` on master's own 7,021-cell population is identical before and after (runs 3888, clause 1 61, total 267), and the corpus `cmp` is byte-identical on all 1,999 buildable modules. Ten cells move not-runs -> `runs` (seven check-clean INVALID WASM: the filed repro, a paren-wrapped return, a `.push`-pinned element, and the union / map / closure / nested-declared-struct elements; three LOUD: the one- and two-hop alias chains and the read-element leg, which was `field access receiver is not a struct`). TWO RUNGS AND THE ABLATION IS NOT ADDITIVE: rung 1 (`criRetHoleNode`, reading the hole off the binding rather than off the return expression) buys nine, and `d626_elem_nested` needs rung 2 as well (`emptyArrHoleElemName` resolving the ROW from the arena via `rlSlotOfTy` and answering with the row's own stored spelling), because a nested element renders `{r:i32}[]` while the row the `P[][]` annotation interned is keyed `P[]` — probed `byName=-1 byTy=1`. Rung 2 alone moves nothing. THE NINE NON-MOVERS ARE THE POINT OF KEEPING THE SET WHOLE: three element reps that already ran (`string`, `f64`, `f32` — the i32-list default happened to be wrong in a way the scalar ladders already covered), and six DECLINES that must stay declines — a returned PARAM (no binding to resolve), a returned local whose init is a CALL (its producer already committed a rep), a local carrying its OWN annotation (following past it would answer about the initializer where the declaration decided), an ANNOTATED return, the literal returned DIRECTLY (D16's own case), and an `i32` element (the one rep the default was right for, which must stay byte-identical). A rule derived from current behaviour cannot tell a decline that is correct from one that is a missing arm.",
 "d641-anon-shape-residue": "the ONE cell D592's struct arm declined, CLOSED by D681 on 2026-08-31 and kept as the family's standing witness. `function g<T>(self: T) { const xs: T[] = [self]; return xs[0] }` at `g({ x: 7 })`: the argument shape is ANONYMOUS, so the emitter interned it as `#anon0`. The filed mechanism named only half of it. The pin the monomorphizer re-checks the body under is resolved from the pin's NAME, and `nameToTy(\"#anon0\")` is -1 — `recordClonedNodeTyFb` records the coarse `objPinTyIx()` placeholder, a field-less `TyObj` — so the re-check answered `{}`, and no resolver keyed on a row could have matched it (measured: the return type is arena 57 rendering `{}` while `#anon0`'s own row is arena 52 rendering `{x: i32}`). Two rungs, either alone moving ZERO cells: hand the ANON pin its row's arena type, and let `monoRetRowName` name an ANONYMOUS row by exact arena identity. The refused spelling-match widening is pinned separately as `d681_refused_spelling_pin`.",
 "d651-lambda-tyvar-length": "the six `bare/<bind>/len` cells of `scripts/silent-sweep/d426/lamgrid.py` — `const g = (x: T) => x.length` inside `function opT<T>(a: T)`, at the six scalar bindings (silent-class-inventory D651). A LOST REFUSAL rather than a lost lowering, and the only residue of D426's family that per-pin lifting does not close: the DIRECT spelling (`(x: i32) => x.length`) is a loud CHECK reject — \"member access '.length' on non-object i32\" — while the same body behind a type parameter reaches the emitter and gets `emitProgram: '.length' on a receiver the emitter cannot classify`. Nothing derived can find them: their outcome CLASS (loud emit reject) did not move across D426's landing, only their message did, so every histogram and every grade column is flat over them.",
 "d661-destination-scope": "the 81-cell boundary of `scripts/silent-sweep/d661/gen661.py` (silent-class-inventory D661) — the SCOPE axis the D411 grid never had. D411 varies the spelling of two destinations and holds one thing fixed in all 103 of its cells: both bind the SAME `lv1`. So neither that grid nor the 33 corpus cells distilled from it can see whether the rung that refuses them also refuses programs where each binding has exactly ONE destination. It did: 164 of 182 two-binding cells, every one single-destination, refused by `letRefListDestSlotK`'s whole-arena NAME match — renaming one `lv1` was the only edit that made them run. What is kept whole here is the boundary, not the grid: the 14 single-destination CONTROLS (any candidate that reddens one has broken D381's own subject), one cell per (scope x d2) at `d1=bind` and every `d1=ret` cell (the return form is the one destination read through `fnIx` rather than the arena, so it is where the two halves of the bug can disagree), and the 15 CAPTURE cells that price the walk's nested-function rule from both sides — `d661_cap_*` must keep running (stripping the descend rung loses four of them, a `runs` LOST, while moving ZERO cells of its own) and `d661_capsh_*` / `d661_capp_bind` are the shadow tests, local and PARAMETER. THREE `d661_cap_*` cells were check-clean invalid wasm on both seeds — `assign`, `mapstore` and `ret`, the three forms that resolve their destination through `fnIx`, which a nested frame's `fnIx` is not. That residue was D661A and it is CLOSED (D681, 2026-08-31): `dsScopeWalk` now carries the frame it descended INTO and hands that to `dsDestSlotAt`. All seven forms run, and the four that already ran are byte-identical.",
 "d681-anon-pin-and-row": "the two cells D681's landing named that nothing derived could (silent-class-inventory D681). `d681_anon_two_field` is D641's family one ingredient wider than the corpus cell — a TWO-field anonymous argument shape, `check-clean invalid wasm` on master and `runs` since — kept because the derived corpus has no anonymous-shape axis to have generated it. `d681_refused_spelling_pin` is a byte copy of `tests/cases/functions/structural-generic.vl` carried as a REFUTATION PIN: it must keep RUNNING, and it flips the day someone re-tries D641's refused spelling-match widening (`structIndexOfTypeName`), which collapses `addx`'s i32 and f64 instances onto one row because both re-check to `{x: <error>}` and print the same text. Re-measured 2026-08-31 on today's master: the refusal still holds, and D622 part 1's canonical field order did not cheapen it. D681's shipped resolver is NOMINAL (`sTyIx[si] == ty`), which cannot collide that way; the pin is what keeps the two apart.",
 "d741-element-pair-line": "the 123 cells of `scripts/silent-sweep/d741/gen741.py` — the TRANSPOSE of D411's grid. D411 varies the two destinations' SPELLING and holds the element pair fixed at (Circle, Shape); this one fixes the spelling and varies the ELEMENT PAIR over seven types spanning both storage classes and both kinds of union identity, in two blocks that differ only in whether the literal is EMPTY. Filed by D741 as the price of the REFUSED checker-side hole unification D661B asked for: on the eager block the outcome is EXACTLY the K1/K2 storage partition (0 of 36 cells deviate), so `Shape[]`+`Other[]` — two unrelated declared unions, `tySame` false — RUNS, and 14 cells run with a differing element type. Any rule stated in the TYPE system's vocabulary reddens those; extending the existing empty-literal unification (`constrainEmptyD`) to a non-empty literal costs exactly 10 of them, measured cell by cell against the hole block. The set also carries the two clause-1 families neither the census nor any filed row reached: `d741_w0_base`/`d741_w6_params` are check-clean and TRAP (`wasm trap: cast failure`) in the quadrant the compiler admits, and the eight `d741_hole_{Circle,inlineobj}__{Shape,Animal,Other,CircleStr}` cells are check-clean invalid wasm where their eager twins are loud — D742's order dependence, whose two orders (`d741_o1_hole_k1_first` / `d741_o2_hole_k2_first`) are a soundness violation and a clean diagnosis for one and the same destination pair. The 46 cells that RUN are the tripwire: a future invariance, hole-unification or copy-on-assign candidate must say which of them it costs.",
 "d772-nullable-elem-hole": "the three empty-`[]`-with-a-nullable-element-destination witnesses (silent-class-inventory D772/D775). `nodeArrayElemName` renders exactly ONE nullable element — a closure — so an empty `[]` whose hole `constrainEmptyD` pinned to any other `(X | null)[]` destination synthesized no annotation and built the i32-list default into a ref/string-list cell: `vl check` rc 0 and an invalid module. `d772_hole_nul_i32` and `d772_hole_nul_struct` RUN after D772's rung and must keep running; `d775_hole_nul_string` is the residue that rung deliberately does not claim (a `(string | null)[]` rides the STRING backing, which has no ref-list row for the gate to find) and is still check-clean invalid wasm. None of the three declares a union, which is what says the family is not D771's and is not covariance.",
 "d773-readonly-covariance": "the READ-ONLY covariant container pass (silent-class-inventory D773) — the half of `DECISIONS.md`'s A9 that is blocked on REPRESENTATION rather than on the ruling. `d773_readonly_bind` and `d773_readonly_param` write nothing anywhere, are `vl check` rc 0, and produce an invalid module; `d773_readonly_same` is the control that RUNS and must keep running. Before this set the corpus had no cell for the plain covariant pass at all, so `goal-scoreboard.py` scored the language's largest single capability gap at zero. Any invariance, unification or copy-on-assign candidate has to say what it does to all three.",
 "d774-write-ablation": "the WRITE ablation over the K1/K2 two-destination quadrant (silent-class-inventory D774) — the measurement that refuses an A9-`Writable`-only reading of the covariance family. `d774_k1k2_nostore`, `d774_k1k2_store` and `d774_k1k2_param_store` are the same program with no store, with a store through a binding, and with a store through a PARAMETER; all three are the identical positioned emit reject at the identical site, so the write is not an ingredient of this quadrant's refusal and a rule conditioned on one cannot move it. D741's `w0`/`w2` pair is the same ablation in the K2/K2 quadrant, where the write DOES decide (runs vs trap); keeping both is what separates the two halves.",
 "d791-readonly-covariance-copy": "the READ-ONLY COVARIANCE landing's own witnesses (silent-class-inventory D791). `d791_narrow_widened_elem` RUNS and is what says the converted elements are genuinely READABLE through a narrow, not merely countable — a copy that boxed the wrong tag would pass a `.length` grade and fail this one. The other four must NOT run, and each is a distinct route by which the copy's private list could be observed: `d791_store_dst` (a store through the widened destination), `d791_push_alias` (a `.push` through it, whose divergence a LENGTH read makes visible), `d791_store_callee` (a store through a CALLEE's parameter, which is why the scan is whole-program rather than per-function), and `d791_unrelated_write_blocks` (a `.push` through a THIRD list of the DESTINATION's element row — the CONSERVATIVE half of the rep-scoped scan, and the honest cost of it: the program is legal, nothing aliases, and the widening declines anyway. Kept whole so the day someone narrows the scan they have to say what it buys). `d791_push_alias` IS A REFUSED CANDIDATE'S PRICE. With the write scan ablated it compiles and prints 1 then 2; VL lists ALIAS, so 2 then 2 is the answer the language requires, and that ablation therefore trades an invalid module for a check-clean SILENTLY WRONG one. The distilled corpus scores that ablation TWO CELLS HIGHER than the shipped compiler (4,315 vs 4,313), because the two cells it buys only print the lengths of lists they write. This set is what a derived rule provably cannot find.",
 "d811-nullable-niche-widening": "the NICHE-SOURCE element-converting copy's own witnesses (silent-class-inventory D811). A `(Circle | null)[]` source row is a niche — the null is the absent reference — and widening it into a union-box list has to decide what that null BECOMES. `d811_widen_nul_elem` and `d811_widen_nul_narrow` are the price of getting that wrong, and they are the ONLY thing that prices it: the loop-body rung scores ZERO on the whole 7,267-cell corpus (the eight `d741_eager_*CircleNull*` cells hold no null, so a copy that boxes the null under the ARM's tag grades identically), while ablating it makes `d811_widen_nul_elem` print `2 2 2` for `2 1 2` — a null element reading back as non-null, check-clean — and makes `d811_widen_nul_narrow` TRAP with `wasm trap: cast failure`, which is exactly the hazard D792 declined the pair over. Both are a loud emit reject on master. `d811_widen_nul_dst_narrow` RUNS and is the readability control for the non-null-destination direction, `d791_narrow_widened_elem`'s twin one element over: a copy that boxed the wrong tag passes a `.length` grade and fails this one.",
 "d812-nullable-string-list-hole": "the MODULE-SCOPE twin of `d775_hole_nul_string` (silent-class-inventory D812). `synthEmptyListAnn` has one home for both storage classes precisely so the two scopes cannot drift, and the corpus carried only the function-scope cell — so the module-scope half of every rung in it was ungraded. This is that half, kept whole.",
 "d813-three-member-nullable-hole": "the THREE-MEMBER nullable element the empty-`[]` synthesis declined on its MEMBERSHIP test rather than on its row test (silent-class-inventory D813). `(Shape | null)[]` renders `{r:i32}|{s:i32}|null`, whose ref-list row exists and answers — `nullablePartOf` is what said no, because it splits at the first depth-0 bar and demands one half be exactly `null`. No corpus cell reached it: D772's grid stopped at two-member elements, so this class had no program at all and its rung scores ZERO without these two. Both scopes, because the module one is a separate call site.",
 "d814-nulvariant-list-element-read": "the NULVARIANT LIST ELEMENT read, which is a loud emit reject on master and still is (silent-class-inventory D814). `d814_nulvariant_elem_field` is `(Circle | null)[]` where `Circle` is also a union ARM: an un-annotated local bound to `a[0]`, read through its `!= null` narrow, floors on `field access but no struct type declared`. `d814_nulvariant_elem_annot` is the identical program with the local ANNOTATED `Circle | null` and it RUNS, which is what says the gap is the local's inferred rep and not the niche. Kept as a pair because the annotated control is the whole diagnosis, and because D811's landing makes the defect REACHABLE in the two-destination spelling it used to sit behind.",
 "d821-written-through-covariance": "Four cells naming what the WRITTEN-THROUGH half of the covariance family costs, filed by D821/D822. Two must NOT run and two must. `d821_escape_push_alias` is the ABLATION's own price and the reason the claim pass exists: it is `d791_push_alias` with the widened list put inside an object literal and read back out under a different name, so the alias-set closure cannot follow it. With the claim pass (the WHITELIST of uses that create no new handle) it declines and the module is refused, as on master. WITHOUT it the closure never sees the escape, the copy is licensed, and the program compiles CHECK-CLEAN and prints 1 then 2 where VL's aliasing requires 2 then 2 — an invalid module traded for a silently wrong one, by a rung whose corpus score is ZERO. `d821_mod_store_dst` is `d791_store_dst` at MODULE scope: the checker rule is scope-independent by construction and this is what says so. `d822_global_widen` and `d822_global_unrelated_write` are the module-scope twins of `d773_readonly_bind` and `d791_unrelated_write_blocks`; both were check-clean invalid wasm on master because D791's seven delivery hooks do not include a module GLOBAL, whose init is emitted by the synthetic start function. Both must keep RUNNING.",
 "d832-match-untested-else": "the one cell that grades `match`'s UNTESTED final arm (silent-class-inventory D832). A `match` with no `_` compiles its last arm as an unconditional `else` with a bare `ref.cast`, because exhaustiveness says the value must be it — and array covariance has already broken exhaustiveness (D741). `d741_w0_base` traps on that cast because its arm reads a field; THIS cell's arms read none, so nothing casts and the compiler runs the `Sq` body for a `Tri` and prints `200`. Check-clean SILENTLY WRONG OUTPUT — the worst class in the taxonomy, and the corpus had no cell of it. `is` answers correctly on the same value (the box tags are per ARM TYPE), so the assumption is the chain's alone. The expectation `100` is D741's filed position for the same value, stated in its own row ('SHOULD PRINT 107 — `a`'s own annotation says element 0 is a Shape').",
 "d833-narrow-row-defeats-merge": "the COMPETING-ROW grid (silent-class-inventory D831/D833/D841) — what a `??`-joined pair of same-fieldset object literals does when something ELSE in the program already names that field set. D804 closed the pair by MINTING a merged row, and every rung of that mint was gated on `structIndexOfObj(ow) < 0`. `d833_none_*` (4) is D804's control and has always run. `d833_genpin_*` (4) is D831's own witness set — the competing row is the MONOMORPHIZER's pin, minted post-`monomorphize` after the literals settled theirs, and three of the four moved to `runs` at that landing; all four must keep running. The other TWELVE were D833's standing price and all twelve moved to `runs` at D841 (#PR): a `type R = { r: i32 }` the program never uses, or an inline `{r: i32}` annotation on a function never called, made both literals match THAT row so the merge never ran at all — six loud `bare null needs a struct-typed context` and six CHECK-CLEAN INVALID WASM. The two carriers were one message and two mechanisms and a probe build separated them: the generic cell reaches the leaf rung and it ANSWERS (`M L Y S`), the alias cell never calls it (`S S`), and `annShapeWiderRowOf` is not even reached for a declared `type`. Regenerate with `scripts/silent-sweep/d833/gen833.py`.",
 "d841-competing-row-controls": "the five programs D841's ablation NAMED, kept because no derived rule can find them. `d841_hazard_declared_dest` is the one that bounds the fix: `const x: R = { r: \"a\" }` shares a field-name set with an unrelated `??` over `{r: i32}` and must keep `R` — it RUNS on master, and the version of the rung that took the field SET as the `??` family took it to invalid wasm. `d841_two_joins_one_valname` is its opposite bound: two `??` families over one field set whose maps share a value NAME share one mv slot and so must land on ONE row — the version that gave each family its own set minted eight rows for five families and printed `null` for a string read back through the wrong row's tag layout. `d841_declared_box_serves` is the byte-identity control (`type R = { r: i32 | null }` beside the pair has always run, and `anonRowServesLeafSet` is what stops a second row being minted for it). `d841_competing_row_used_null` / `_used_str` are the case the D833 row said was the hard half — `R` genuinely USED elsewhere — and both close, because the fix redirects the MAP VALUE SLOT rather than `R`. `d841_merged_read_back` reads the stored i32 back out of the merged box and must print 7, not the default.",
 "d842-binding-cell": "the programs D842's ablation NAMED: closing the `??` LEFT-OPERAND READER for a plain BINDING. Two halves, and neither moves a cell alone — the bare-identifier arm of `anonLeafJoinSources` (so D804's leaf merge has two atoms) and `anonLeafSupersedeOf` at the binding's own cell resolvers. `_none_*` is the discovery that separates this from D833: the competing row is SCENERY here, because the BINDING'S OWN ANNOTATION mints the narrow row in `collectAnnShapes` before `collectAnonShapes` runs, so the defect reproduces with no `type` in the file. `_module_*` is the half rungs 3 and 4 carry alone — a module global takes its row from `globalCellStructIdx` and `globalStructIndexSid`, which resolve it separately and must both be redirected or the cell and its reader disagree. `_hazard_*` is the bound in the other direction: the redirect is PER-BINDING, and a declared destination, a parameter, a nested field, a list element, a map value and a same-named binding in another function all keep the narrow layout beside it. `_readback_*` is what a grade column cannot see — the merged box has to give the stored i32 and the default's own atom back correctly, on the hit and the miss path.",
 "d842-ident-reader-residue": "the `??` LEFT-OPERAND READER D841 measured out. `let a: {r:i32} | null = {r:7}` … `a ?? {r: null}` is the same defect one delivery form over: the literal arrives through the LOCAL'S CELL, whose struct index comes from the binding's annotation and not through the map value slot D841 supersedes, so the identifier reader closes nothing and was removed rather than shipped inert. Both cells are exactly as master has them — a loud `bare null needs a struct-typed context` and a check-clean invalid module — and they are here so the next person to widen the reader has the two programs that say whether it worked. silent-class-inventory D842.",
 "d851-nulvariant-elem-module": "the MODULE-SCOPE twin of `d814_nulvariant_elem_field` (silent-class-inventory D851). Both niche classifiers read a ref-list slot and no frame, so the two scopes should answer identically — but D822 is the day a covariance rung that looked frame-free needed an eighth delivery form to reach a module global, and this cell is the standing check that this one did not. It is also the only cell that reaches the COMPLEMENT half of the fix: instrumented, `exprNullableStruct`'s new variant decline is consulted 564 times on 198 corpus modules and changes the answer exactly ONCE, here.",
 "d852-covariant-escape": "the three shapes of the alias closure's CONTAINER-FIELD edge (silent-class-inventory D852), which closes D823's filed boundary cell `d821_escape_push_alias`. `d852_escape_field_write` is the same escape one binding shorter (`bx.xs.push(…)` instead of `const c = bx.xs; c.push(…)`) and must REFUSE. `d852_escape_field_readonly` is the unwritten twin and must RUN: putting a value into a field is still an unclaimed occurrence, so a program that escapes and is never written keeps answering `2` and keeps master's behaviour exactly. `d852_escape_unrelated_field` is the PRICE of the first key that was tried — keyed on the field NAME alone, `type Other = { xs: i32[] }` with `oz.xs.push(2)` in it reads as a write to the covariant list and the program is refused, measured `1 1 2` on master against a `WRITTEN THROUGH` check reject with the wider key. It is a running program and nothing derived from current behaviour can see why it matters.",
 "d853-two-unions": "`d853_two_unions_no_elem_read` is `d741_w5_no_narrow` kept under this row's own name, because it is what the TWO-DIFFERENT-UNIONS rule (silent-class-inventory D853) pays for its element-read gate. D824 priced the rule that reaches `d741_w0_base` and `d741_w6_params` at TWO running cells; asking whether the two unions DIFFER keeps `d741_w4_same_union` and asking whether an ELEMENT is read keeps this one, so the real price is zero. Ablating the element-read conjunct is exactly one `runs` cell lost, measured, and it is this program: the same disagreeing `Shape[]` / `Other[]` pair, the same store of a `Tri`, and then only `print(a.length)` — which is `1`, and is right.",
 "d861-call-return-reader": "OPEN. The `??` left operand is a CALL, or a binding of one (`src() ?? {r: null}` over `src(): {r:i32} | null`). The other literal lives in the CALLEE'S RETURN, and neither the map/list INDEX reader (D833) nor the bare-IDENTIFIER reader (D842) reaches it: the identifier reader finds the declaration and its initializer is a call, so the join has one atom and D804's merge never runs. The `_i32` column is the control and runs. Filed when D842 landed; its redirect would also need `nulRefStructIdxOfLet`'s and `globalCellStructIdx`'s INIT legs, which D842 measured at zero cells and left out.",
 "d862-annotation-only-atom": "OPEN. There is no SECOND LITERAL at all — `let a: {r:i32} | null = null` never binds an object, so the `??`'s family is the default alone, one atom, and the merge declines by its own `atoms.length < 2` gate. The other atom exists only in the ANNOTATION. This is not D842's reader (adding the binding to the family adds nothing) and not D804's merge (there is nothing to merge); closing it means letting an annotation contribute an atom, which is a wider family than anything measured so far.",
 "d863-two-sets-nested-row": "OPEN, and found by `tests/cases/types/coalesce-default-row-past-binding-cell.vl` rather than by any grid. TWO `??` families over one field set landing on DIFFERENT merged sets (`i32|string` and `i32|null`) while a NESTED-STRUCT row (`{inner: {r: i32}}`) also claims `{r: i32}`. Each of the three ingredients alone runs; all three together are a loud `bare null needs a struct-typed context`, identically on master and after D842. D841's \"families over one field set are grouped by the row they supersede\" is the rung the nested row's presence defeats.",
 "d872-annotation-only-atom-module": "the MODULE-SCOPE twin of D862's function-scope trio, which the filed row had no cell for. `let a: {r: i32} | null = null` at module scope with `a ?? { r: <atom> }`: the family has ONE literal, so its other atom exists only in the binding's own annotation (`anonLeafAnnAtomOf`). Three of the four were `loud emit reject` / `check-clean invalid wasm` on master and all four run here. The `_i32` column is the control that always ran — one atom mints nothing.",
 "d873-two-competing-rows": "the ablation D863's filed row got WRONG in two directions, kept whole because it is what says so. The row read 'two `??` families over different merged sets, plus a nested-struct row'; graded over carrier x default, the SECOND FAMILY IS SCENERY and what the defect needs is TWO same-fieldset rows — `type R` plus `Box`'s inline `{r: i32}` INSTANTIATED. `d873_carrier_R_only` and `d873_carrier_box_only` are the one-row controls that run on master and must keep running; only the pair reproduces. The mechanism: `structIndexOfObj` names one row and `nulRefStructIdxOfLet` the other, so the supersede recorded against the literal's row is not the one the cell reads (`anonLeafCarrierRowOf`).",
 "d874-two-families-two-rows": "the residue D871/D872/D873's own FIXTURE found, and the reason the two fixtures are split. FOUR ingredients: a CALL-carried `??` family, an ANNOTATION-ONLY `??` family, `type R`, and `Box`'s inline `{r: i32}`. Every one-ingredient-out control (`d874_ctl_*`) is check-clean invalid wasm on master and RUNS after this landing; the full four is check-clean invalid wasm on master and here alike, so it is not a regression — it is the next delivery form out, filed as D874. `_declfirst` pins that declaration ORDER does not move it.",
 "d875-reader-refusal": "the `runs`-LOSING witness this landing found and the gate that answers it. The merged row is a FIELD-SET row, so every function whose return annotation resolves to it follows the redirect — including one whose returns `anonLeafJoinCallee` could not name and therefore did not move. `srcv(): {r: i32} | null { const v = { r: 7 }; v }` beside an unrelated `??` over `{r: i32}` RUNS on master; an ungated redirect made it check-clean invalid wasm. `anonLeafRetMoved` marks the callees the reader actually read, and removing that gate alone loses exactly these two cells. `d875_read_callee_beside_fam` is the readable twin that must keep running, and `d875_assign_call` pins the assignment leg's own call hop.",
 "d876-annotation-atom-bounds": "the six programs that BOUND the annotation atom, each a decline the rung has to keep making. `d876_litunion_ann` and `d876_nested_ann` are field spellings the atom set cannot carry (a row's `{r: i32}` and `{r: boolean}` fields are both code 0, as is a literal-union field, so the atom is read off the SPELLING and only the six scalar names are admitted). `d876_two_anns_disagree` is two same-named bindings with DIFFERENT annotations — no single declared type for the operand, so the rung declines. `d876_call_ret_is_binding` and `d876_call_ret_is_call` are why the atom answers for an IDENTIFIER operand only: taking it from a CALL's return annotation mints the merged row while the value the callee actually returns stays narrow, which turned a loud reject into check-clean invalid wasm. `d876_declared_dest_beside_ann_fam` is D842's `hazard_declared_dest` for the annotation-only family — a narrow `R` destination beside it keeps its own layout.",
 "d881-two-families-two-rows": "D874's close. `anonLeafSupersede` is ONE SLOT PER NARROW ROW and the carrier supersede (D873) was recorded only where the literal's own mint APPENDED a row. A SECOND `??` family over the same field set finds the merged row already standing, takes `anonLeafMergeField`'s serving-row branch, mints nothing — and its carrier was left pointing at a row nobody merged. Two competing same-fieldset rows (`type R` beside `Box`'s inline `{r: i32}`) are what make that visible: with one row the carrier IS the literal's row and the existing line covered it. Measured at the counters: `LIT n=26 nar=3 mf=[] car=2` with `ROW 2 R sup=2` — the merged row 3 minted, row 2 never told. This cell reads back all four values plus both competing rows' own.",
 "d882-returned-binding": "The IDENT reader ONE FRAME IN, which `d876_call_ret_is_binding` was the pin for. A callee that hands back a BINDING (`{ const v = { r: 7 }; v }`) refuted the whole read, so the family was the default alone. Reading it is HALF the fix and the other half is a SIXTH owner of the merged row: the literal itself. Five owners had been named one at a time (the map's value slot D841, the binding's cell D842, three return-row resolvers D871) and the literal's own row was resolved by `structIndexOfObjCtx` — so the callee built the narrow struct and returned it as the merged one, `emitProgram: bare null needs a struct-typed context` becoming CHECK-CLEAN INVALID WASM. The redirect now sits at that resolver, keyed on the literal being a join MEMBER and armed only from `emitModule` on (`anonLeafEmitPhase`), because `collectAnonShapes`' own mint loop needs the pre-merge answer. These four cells are the delivery forms: a read-back, a REASSIGNED binding, the callee also used outside any `??`, and module scope.",
 "d883-returned-call": "One more frame of the same read: `src(): S|null { inner() }`. `anonLeafJoinCallee` was deliberately ONE hop, so `d876_call_ret_is_call` was a loud reject. The read now carries a visited set (which doubles as the list of callees whose return row must follow) and a depth cap, and publishes NOTHING until the whole read succeeds — a nested success under an outer refutation would leave a member whose row never moves and a callee MARKED with nothing behind it, which is exactly the `runs`-losing shape `anonLeafRetMoved` exists to prevent.",
 "d884-litunion-annotation-atom": "The SEVENTH atom, and the guard that came with it. `{r: K} | null` over `type K = \"a\" | \"b\"` gave the annotation-only family one atom, because `anonLeafAtomOfText` admitted six scalar spellings and a literal-union alias is not one. Admitting it spells the merged set `K|null`, which is the code-30 niche `nameFieldCode` already gives the DECLARED twin. `d884_litunion_beside_str` is the price the ungated version charged and the reason the atom is admitted only into a set of `null`s: `let a: {r: K} | null = {r: \"b\"}` beside `?? {r: null}` RUNS on master (the literal's atom is `string`, read off the VALUE, so the set is `string|null` and code 20 carries it) — and `K|string|null` is a spelling `nameFieldCode` has no code for, so the mint declined and the program went from `runs` to a loud reject. Found by writing the program; no corpus cell and no named set had it.",
 "d885-self-recursive-return": "A returned SELF-CALL is not an unnameable path. The visited set refuted a name already on the read's own path, which cost `src(n): S|null { if n <= 0 { return {r: 5} }; src(n - 1) }` its merge — check-clean invalid wasm on master and here until the visited hit ANSWERED instead of refuting. It is sound because the frame that pushed the name is being read: its return row is in the mark list, and a self-call hands back that same row.",
 "d886-returned-binding-of-call": "D882 and D883 composed: `{ const v = mid(); v }` is a binding whose value is a callee's return. Reading either alone leaves it check-clean invalid wasm. The cell reads the value back through BOTH `??`s — `src()`'s and `mid()`'s own — so the two frames' rows are asserted to agree.",
 "d887-inferred-nullable-struct-return": "The clause-2 violation D871's row named and had no id left for: `'src' infers the nullable return type {r: i32} | null — type-valid, but an inferred return of this shape is not yet supported by codegen`. It conceded the program's type-validity in its own sentence, and was reached by NO corpus cell and no capability probe. Three sites, because a floor lifted alone only MOVES the gap: the checker recorded no inferred-return row for the shape (so the A20 pass kept `fRetKind`'s `\"i32\"` default), the A20 pass had no `nulstruct` arm for the recorded name, and `emitReturnValue` had no inferred twin of the annotated `retNulRefFlag` seed — lifting only the first left `emitProgram: bare null needs a struct-typed context`. `d887_inferred_ret_beside_fam` is what makes D871's INFERRED FUNCTYPE ARM live: that arm measured ZERO cells in #2067 and its row said so, explicitly because this refusal existed. Two `tests/cases` fixtures asserted the refusal and now assert the run.",
 "d888-returned-parameter": "CLOSED 2026-08-31 by D891. `src(p: {r: i32}): {r: i32} | null { p }` hands back a PARAMETER: `anonLeafRetIdentOk` found no binding for the name in the frame and refuted, so the family was the default alone and the pair was check-clean invalid wasm — identical on master, and the LAST cell `goal-scoreboard.py` scored against the goal. The row's diagnosis held in both halves and was measured, not assumed: the literals reaching a parameter are the ARGUMENTS at every call site, and the parameter's own slot comes from its ANNOTATION, so it needed a SEVENTH owner of the merged row rather than an eighth reader — the reader with every owner stripped buys ZERO cells and pays one loud->silent. See `d891-returned-parameter`.",
 "d891-returned-parameter": "D888 CLOSED, and its four `d891_bound_*` refutation bounds CLOSED in turn by D911 — see `d911-argument-carriers`. `src(p: {r: i32}): {r: i32} | null { p }` hands back a PARAMETER: the literals that reach the `??` family are the ARGUMENTS at every call site, one frame OUT rather than one frame in. The reader alone buys ZERO — measured: with the param arm in and no owner, 16 of these cells only move their invalid-wasm offset and `d891_null_default` trades a loud emit reject for check-clean invalid wasm. The parameter's SLOT is a seventh owner of the merged row (`anonLeafParamMoved`, read at `paramStructIdxOf`, `paramStructIndex` and `captureValStructIdx`). The bound cells pin the five refutations (a generic callee, two same-named declarations, the name used as a VALUE, a named argument, an argument that is not a literal) and the four controls that run on master and must keep running. `d891_module_scope` is a COMPILER trap I caused and caught: the expression-position redirect indexed `P.nodes[-1]` at module scope until the `fnIx < 0` guard went in front of it.",
 "d892-merged-box-read": "PRE-EXISTING on master and NOT this landing's doing: reading a `??`-merged code-16 box field back at the `i32` / `f64` atom pair TRAPS (`wasm trap: cast failure`) at every carrier — `d892_box_direct_i32_f64` and `d892_box_binding_i32_f64` both trap on master's own seed. D891 adds the parameter carrier's instance of the same trap (`d892_box_param_i32_f64`), which is why the grid is kept whole. The grid also holds `d892_box_param_boolean_i32`, where MASTER runs and prints `1` for a boolean field while its own direct twin prints `true` — a runs-but-wrong-value the close fixes.",
 "d893-literal-less-family": "CLOSED by D912. A callee the reader ANSWERS for but that contributes NO literal — every argument is `null` (`d893_null_arg_only`), or the body hands back nothing at all (`d893_empty_body_callee`, `d893_empty_body_param`). The set is then the default's atom alone, `atoms.length < 2` ends the leaf merge and the pair is check-clean invalid wasm. The atom that would close it is the CALLEE'S OWN RETURN ANNOTATION — D872's annotation atom one position over, and D872 declined it for a CALL operand on a measured loud->silent.",
 "d894-returned-binding-of-parameter": "CLOSED by D913. `{ const v = p; v }` — a returned binding whose value is a PARAMETER. `anonLeafBoundValOk` splits a bound value three ways (a literal is a member, a `null` is neither, a CALL is the next frame) and refutes an identifier, so D891's param arm is never reached through a binding. D886 is the same composition one delivery form over and it closed; this one needs `body` threaded into `anonLeafBoundValOk`.",
 "d895-self-passed-parameter": "CLOSED by D914. `src(n, p): S|null { if n <= 0 { return p }; src(n - 1, p) }` — the recursive call passes the parameter to ITSELF, so the call-site scan sees an argument that is an identifier and refutes the whole callee. It is D885's argument one position over: a self-pass hands on the very value whose slot is being moved and contributes nothing new, so it is sound to SKIP rather than refute — but only once shadowing is excluded.",
 "d901-numeric-fold-mint": "D901. The `??` leaf mint built a `{tag, payload}` box over an atom pair the CHECKER's join FOLDS to one type (`joinTys` is `assignable`-driven and `numWidensName` makes i32→f64 / i32→i64 lossless), so `g1.r` type-checked as `f64` while the row said box: the read unboxed the f64 arm with an unconditional `ref.cast` and the i32 value trapped (`wasm trap: cast failure`) on a `vl check` rc 0 program. `d901_direct_i32_i64` is the i64 half of the same pair, an axis the 48-cell d892 grid never had. `d901_fold_f32_dest` is the PRICE of the first, unbounded cut and is kept for that reason: the atom column reads a bare float literal as `f64` with no destination to consult, so an f32-annotated carrier presents `{i32, f64}` while the checker's join is `f32`, and folding it lost a running program to `expected f32, found f64`. It must keep RUNNING. The two `d833_*_i32_f64` map cells are the other price and are already here: a merged row that also serves a MAP value slot has two consumers at two field types and the box is the only rep that serves both, which is why the fold stands down for a container-indexed join.",
 "d902-boolean-null-niche-mint": "D902. D804 minted the merged row for a `??`-joined literal pair but DECLINED field code 21 (`boolean | null`), so the row's field stayed a plain boolean and the `{r: null}` operand had no rep: `emitProgram: bare null needs a struct-typed context`, three cells of the d892 grid. D804's stated reason was that an `#anon` row carrying code 21 interns no map-value slot and the pair would trade one loud reject for another; `d902_map_bool_null` is that exact program and it now RUNS, because the name-side row resolver is handed the field's TEXT and can make the boolean/i32 distinction code 0 erased. `d902_decl_bool_null_join` shows the gap was never mint-only: the DECLARED `type R = { r: boolean | null }` beside the same `??` refused on master too.",
 "d903-map-carrier-box-read": "D903, OPEN. The MAP carrier of D901's read, and the one shape D901's fold deliberately does not reach: a merged row that also serves a map's value slot has two consumers at two field types, so the box stays and the box read still traps. It is PRE-EXISTING — master and the D901/D902 landing produce the identical `wasm trap: cast failure` — and no cell found it because `d833_none_i32_f64` and `d833_genpin_i32_f64` build the same family and never READ the field back. Closing it needs the read to dispatch on the box tag where the checker typed the field as a bare atom, which is a rung in `emitUnionFieldNarrowUnbox` (D209/D291's channel) and not in the mint.",
 "d911-argument-carriers": "D911 CLOSED (the four `d891_bound_*` refutation bounds). D891 read the arguments at a callee's call sites and could only name an object literal or `null` written in place; every other CARRIER refuted the whole callee. Four rungs took them: a call site's arguments are bound to parameter slots through `orderArgsByParamNames` (a NAMED argument), an argument that is a CALL opens one more frame of the callee read (which is why `fns`/`depth` came back into `anonLeafParamArgsOk`), an argument that is an IDENT is D842's reader asked of an argument, and the name-as-a-VALUE guard was narrowed to its own stated reason — a function value is only dangerous where the program can CALL one. The remaining bounds are cells here: `d911_fn_value_called` (one possibly-indirect call anywhere declines the family), `d911_arg_ident_narrow_use` (the binding the argument names also feeds a NARROW parameter, so moving its literal moves it out from under that one), `d911_arg_ident_unnameable`, `d911_generic_callee` and `d911_nested_arg_two_frames` (the depth cap is 1 frame). `d911_plain_struct_ret_unmoved` is the hazard control for the widened kind gate at the three return-row sites: a plain `{r: i32}` return with no `??` anywhere must keep its narrow row.",
 "d912-callee-return-annotation": "D912 CLOSED (D893). A `??` whose left operand is a CALL now takes the CALLEE'S DECLARED RETURN as the family's second atom, gated on `anonLeafCalleeRead` having SUCCEEDED — which is the interlock D872 measured the absence of. It buys the family with NO LITERAL AT ALL: an empty callee body, or a parameter every call site passes `null` to. The fourth owner of the merged row went in with it — `emitNullForRet`'s fall-through `ref.null`, which is the WHOLE result of an empty body. `d912_ann_named_ret` is the bound: a return annotated `R | null` through a type ALIAS is a spelling `anonLeafAnnAtomOf` declines, and it is still check-clean invalid wasm.",
 "d913-returned-binding-ident": "D913 CLOSED (D894). `anonLeafBoundValOk` split a bound value three ways and refuted an IDENTIFIER, so D882's binding arm and D891's parameter arm could not compose. The Ident arm needed `body` threaded in (a name resolves against the frame that returned it) and a NAME-visited set beside `fns` — the depth cap counts call frames and an identifier chain opens none, so `a = b` / `b = a` recursed for ever without one (`d913_ret_binding_cycle` is that program). `d913_ret_binding_of_field` is the bound: a binding written from a FIELD READ still refutes.",
 "d914-self-passed-argument": "D914 CLOSED (D895). A recursive call passing the parameter to ITSELF was an identifier argument and refuted the whole callee; it hands on the very slot the merge is moving, so it contributes nothing and is SKIPPED. It is D885's visited-set answer one position over onto the argument side. Shadowing is what makes it a question: `d914_self_pass_shadowed` passes a different local of another name, and `d914_caller_local_same_name` is a DIFFERENT function whose own local is spelled like the parameter — the skip is refused there, and the frame containment test plus a rebinding scan are what tell the two apart.",
 "d915-fnsig-result-owner": "D915 CLOSED, and PRE-EXISTING — `d915_fn_value_beside_merge` is check-clean invalid wasm on master's own seed at D871's plainest carrier, so it is not D911's doing; it was found because D911's four carriers would not COMPOSE with a function value in the same module. A module holding any function value gives every eligible function an env-leading `$fnsig`, and that pool entry's RESULT slot (`cloRetValSlot`) is both its interning key (`cloRetKeySuffix`) and its emitted valtype (`emitCloSigFunctype`) — neither of which took the `??` merge redirect, so the pool declared `(result (ref null $narrow))` beside a function functype of `(result (ref null $merged))`. The FIFTH owner of the merged row, and the one that only appears when something in the module is a function VALUE. `d915_fn_value_other_shape` is the control: a function value of a callee whose shape the merge never touched runs on master and here alike."
}
