{
 "d14-length-tyvar": "the 45 cells of `scripts/silent-sweep/d14/lengrid.py` \u2014 15 argument reps x {`.length` through a generic `T`, `.length` on the concrete twin, `x[0]` through a generic `T`} (silent-class-inventory-2 D14). Eight `d14len_*` cells RUN after this landing and land on their `d14con_*` twin's value; seven are the measured PRICE \u2014 a rep with no length at all, where a positioned check reject becomes a positioned EMIT reject. The `d14idx_*` leg is the row's own asymmetry argument kept as evidence, and it has since MOVED: `d14idx_arr_nest`, `d14idx_arr_arr_s` and `d14idx_arr_rec` were check-clean INVALID WASM when the set was filed \u2014 the index side's permissiveness, filed as D401 (this attribution was corrected from D402 by #2003) \u2014 and D401's close makes all three LOUD CHECK REJECTS, on the direct spelling's own sentence. The leg keeps its place in the set: it is what says the asymmetry the row argued about is gone.",
 "d179-anonrow-price": "scripts/silent-sweep/census/d179-anonrow-price.json \u2014 filed by #1996 as the price of the REFUSED first cut of D179/D227's anonymous-row rung (16 census-block-D cells + the hand-written `d179nestpoly`), and REPURPOSED by D391's landing. The 16 cells are map containers (`mapval`, `nestedmap`, `map3`, `list_of_map`) at `rep=arm, declness=nodecl, annpat=none`; they are STILL `loud emit reject` on the landing and still the price of dropping `anonNestFieldMono` \u2014 what moved is only their MESSAGE ('unsupported map value type' -> 'object literal is missing a struct field'), because their remaining blocker is the map-value rep layer and not the owner row. Verified: D207's rung does NOT rescue them either, so the two landings are orthogonal and neither closes this shape. `d179nestpoly` is the one that FLIPPED: it was the refutation pin asserting the pair must keep taking the union box, and D391's RUNG N gives each owner its own row, so it now RUNS and is a tripwire that must keep running. NOTE the claim this file's own `invisible_to` still makes about `regress.py` scoring the refused candidate identically \u2014 that was true before the set was curated and is FALSE now: with these cells in `named/`, the refused candidate reports 17 classes -> silent, which is the set doing exactly its job. It remains true of the DERIVED `cells/` half alone.",
 "d188-rung-price": "the 9 programs D188's own ablation NAMED: the price of dropping rung R2 (`arrSpineIsObj` claimed by `singleAliasMemberTyIx`) from its three-rung landing (silent-class-inventory D188). All 9 RUN on the shipped tree and are the `unread` position of the nine inline-object array-leaf shapes; with R2 stripped and only the PARSER rung R1 standing they are check-clean INVALID WASM \u2014 the parser starts producing an array alias the transparency rule has no arm for, which is D181's silent shape exactly. Nothing reading current behaviour can find them: on the shipped tree they behave like every other running alias.",
 "d199-rungs": "scripts/silent-sweep/census/d199-rungs.json \u2014 the 15 cells the union/variant landing's ABLATION names (silent-class-inventory D199 / D222 / D269): nine that one named rung and only that rung buys, plus six controls that must never move. No derived population can find them \u2014 the distilled corpus moves 0 for every rung and the corpus `cmp` is byte-identical apart from the three fixtures the rows own (D201: this family has no census population).",
 "d207-floor": "the three programs D207's landing NAMES, plus `scripts/silent-sweep/census/d207-listmap-tripwire.json` (740 block-C coordinates). D207 was the LOUD half of D203's root \u2014 the `-3` map-value floor \u2014 filed 'DELIBERATELY not taken' because lifting `letMapShapeOf`'s `if ish >= 0` bound cost 620 block-C cells `loud emit reject` -> check-clean INVALID WASM (plus 85 in B and 184 in E), all at `cont=list_of_map` with `claim >= 1`. THAT PRICE WAS D181, AND D181 CLOSED 2026-08-27. Re-graded cell-matched on 053fcf64 the same lift moves 6,037 cells loud -> `runs` over blocks B/C/D/E with 0 `runs` LOST and 0 into ANY silent class, so it was taken. `d207dest` is the row's own witness and must keep RUNNING. `d207nodest` is D209's shape and must keep LOUD-REJECTING \u2014 it is the BOUND: measured with a `tErr` probe it REACHES the rung (reach=1) and gets no answer (ans=0), so the day it starts running the rung has stopped being a FIND and started MINTING, which is the discipline D-MAPNODETY costs 128 slot moves to keep. `d207listmap` is census cell c002045 verbatim, the coordinate that carried the whole old price, kept because the interaction is second-order: regress D181's alias-transparency arm and these cells do not merely break, they go SILENT, because D207's rung will still resolve the map.",
 "d223-lift-price": "scripts/silent-sweep/census/d223-lift-price.json \u2014 the census-block-E cells the REFUSED lift of `armRecvHoldsBareArm`'s ref-list-element decline cost, kept as the tripwire that would say when the lift became free (silent-class-inventory D223). IT FIRED. Their silence was D361's, not the lift's: with D361 landed (#1995) all 24 grade `runs` under the lift, and the lift was taken. The set stays, with its baseline flipped from `loud emit reject` to `runs` \u2014 the direction it guards is now \"must keep running\", and it is the only population that separates this trade at all (the derived `cells/` half reports nothing either way).",
 "d224-cost": "scripts/silent-sweep/census/d224-cost.json \u2014 the 207 block-B cells D224's refused candidate buys its fix with (loud emit reject -> check-clean invalid wasm)",
 "d228-is-widen": "the 72 programs rung R4 (`isWidenNotVariant`) moves (silent-class-inventory D228). 44 are the PRICE: an `is` whose check type reaches the receiver only by an implicit numeric widening RAN with an always-false test and a dead then-branch, and is now a check reject. 28 are D228 itself: the same tests with the narrowed binding READ in the then-branch, which #1972 turned from `runs` into `emitProgram: narrowed union atom has no value box`. The read axis is what separates the two halves and D228's row did not have it. No census cell contains any of them.",
 "d272": "scripts/silent-sweep/census/d291-widened-store-price.json \u2014 D291's widened-store channel: the 64 cells a PARTIAL composition of the close costs (the predicate landing without the read's narrowing and/or the coercion's guard takes them from `runs` to a loud emit reject), and the 34 the close buys (30 check-clean invalid wasm and 4 `runs but wrong value` -> `runs`)",
 "d282": "scripts/silent-sweep/d282 \u2014 the NAMED runs-lost set of D281's rung: 36 cells that RUN on the shipped tree and go check-clean invalid wasm the moment `emitUnionCoerce`'s equal-heap box arm is removed. Every derived population scored that rung at ZERO.",
 "d290": "scripts/silent-sweep/census/d290-channel-price.json \u2014 the cells D209's close costs when only SOME of its three rungs land (60), and the ones it buys (25)",
 "d300-rung-price": "scripts/silent-sweep/census/d300-rung-price.json \u2014 the two programs D300's own ablation NAMED: the price of dropping a rung from its three-rung landing (silent-class-inventory D300). Neither is derivable from current behaviour: one RUNS on every shipped compiler and is a COMPILER TRAP without the chained `mvCanonRepOf`, the other is check-clean invalid wasm on both sides of the kind-1 hoist alone.",
 "d300-twinfree": "scripts/silent-sweep/census/d300-twinfree.json \u2014 the 65 twin-FREE siblings of D224's price (silent-class-inventory D300). 27 are still check-clean invalid wasm on 21f48747 and are D300's live population; 38 were closed by #1984 and are `runs` tripwires.",
 "d301-rung-price": "scripts/silent-sweep/census/d301-rung-price.json \u2014 the two programs D301's own ablation NAMED: the price of dropping the same-WRAPPER rung `W` from the map-value twin gate (silent-class-inventory D301). Neither is derivable from current behaviour \u2014 `W` moves 0 of 1,477 derived classes and is `cmp` byte-identical on all 1,913 buildable corpus modules \u2014 because the census's `cont` axis picks ONE container per cell, so no coordinate carries a SECOND declared layout twin beside a nested container.",
 "d311-print-route": "scripts/silent-sweep/census/d311-print-route.json \u2014 the 5 cells D311's close MOVES (check-clean invalid wasm -> runs) plus the 1 that already ran and separates the axis (silent-class-inventory D311). All six RUN on the closing compiler and are indistinguishable from their neighbours there; what names them is the ABLATION \u2014 3 need rung S, 2 more need S AND N, and the control needs neither and must never move. The census cannot carry them: block A pins the payload at `7`, so no census coordinate exercises the STRING atom of a `string | i32` code-16 field.",
 "d341-index-place-price": "scripts/silent-sweep/census/d341-index-place-price.json \u2014 the price of index-place narrowing for inventory-2 D11, FILED at #1993 as a refused candidate's and PAID at #2003. 56 cells `loud check reject` -> `check-clean invalid wasm`: the checker narrows `xs[0]` and the emitter has no index-place channel (inventory-2 D451). The three `d341_sound_*` witnesses are the price that was NOT paid \u2014 #1993 measured all three going check-clean and TRAPPING at run time, a column the census records as zero in 250,238 cells, and the landing carries the three retirement rungs instead so all three stay loud. They are kept because the ablation is what grades those rungs: strip R5, R6 or R7 one at a time and exactly one of the three traps, while the 184-cell grid does not move a single cell. Regenerate the grid with `scripts/silent-sweep/d341/mkgrid.py --verify`.",
 "d351-crossfold-price": "scripts/silent-sweep/census/d351-crossfold-price.json \u2014 the price of the fold that was REFUSED, kept whole (silent-class-inventory D351). 12 cells were `trap_loads` when the set was filed and 6 RUN. Folding a union arm's `$fnsig` slot onto the declared struct row D280 already merged its heap type with buys the 12 and costs the 6 (runs -> check-clean invalid wasm), because the token CHAR is what `sigParamKindAt` reports. #1994 closed D351 in the POOL instead \u2014 the same 12 go to `runs` and the 6 do not move \u2014 so the 12 are now tripwires and the 6 remain the refused candidate's price, which is what stops it being paid.",
 "d351-halfroute-price": "scripts/silent-sweep/census/d351-halfroute-price.json \u2014 the 12 cells a PARTIAL routing of the `$fnsig` pool costs (silent-class-inventory D351). Four sites turn a pool position into a type index and they must resolve it identically; with the DECLARED functype unrouted, or with the VALUE CALL unrouted, all 12 go from `runs` to `trap_loads`. The D-ablation is the one worth the set: the grid's histogram is IDENTICAL to the base (198 runs / 12 trap_loads) while 24 cells have moved under it. Two of the 12 were already curated as `d199-rungs` controls and stay in that block.",
 "d352-paren-place": "the 38 cells of `scripts/silent-sweep/d352/parengrid.py` that the CHECKER's paren-blind place key names (silent-class-inventory D352). Three blocks. `d352ret_*` \u2014 16 RETIREMENT cells, of which four (`wroot_rbare`, `wroot_router`, both depths) are check-clean and TRAP on master: a paren-spelled write `(t).v = {s:3}` keyed nowhere, so it retired no narrowing and the next `t.v.r` read the stale one. Their unparenthesised control is a clean `cannot assign` reject. `d352rd_*` \u2014 20 READ cells split so that each rung has cells only it buys (K: a paren in the READ; C: a paren in the GUARD of an ELSE-branch complement). `d352opt_*` \u2014 2 cells that must stay REFUSED: they are rung H's whole population, the optional-chain decline, and H moves 0 cells on all three grids and 0 corpus bytes without them.",
 "d361-landed": "scripts/silent-sweep/census/d361-landed.json \u2014 the 96 census-block-E cells D361's landing moved, every one of them check-clean invalid wasm -> `runs`, and the WHOLE of that block's silent column (96 before, 0 after). They must RUN. The distilled `cells/` half is blind to all 96: regress.py on the landed compiler reports 0 moved classes, so a collapse of current behaviour cannot stand in for them.",
 "d362-alias-leaf": "the 56 cells of `scripts/silent-sweep/d188/aliasgrid.py` whose array-spine LEAF is itself a DECLARED ALIAS \u2014 four leaf kinds (string litunion `K0`, numeric litunion `Z`, declared value union `U1`, canonicalized intersection `AB`) x seven annotation positions x {alias, alias-free control} (silent-class-inventory D362). Kept as PAIRS: the readable quantity is not \"does the alias leg run\" but \"does it land on its own inline control's verdict\", and no population carrying one spelling can ask that. What names them is the ABLATION \u2014 rung B (the nominal render leg in `transparentMemberEmitName`) moves 0 of 322 aliasgrid cells, 0 of the 1,477 derived classes and 0 corpus bytes ON ITS OWN, and without it the six `isect` cells rung A buys go straight back to a loud emit reject. `d362al_isect_unread` is the set's silent cell: check-clean INVALID WASM on master `7ff0f5a8`.",
 "d401-print-tyvar": "the 23 cells of `scripts/silent-sweep/d401/printgrid.py` that decide silent-class-inventory D401 (`print` of a value derived from an unbounded type parameter). Nineteen `d401p_*` cells were CHECK-CLEAN INVALID WASM on master `8939d435` and every one of them had a CONCRETE twin that was already a clean check reject \u2014 the whole row is that one annotation separated the two. Fifteen are the BARE position (`print(x)` on `T` itself), which the row did not name at all and which is the larger half; four are the ELEMENT position the row filed. The other four cells are the one-character neighbours that RUN (`print(x[0])` at an i32, string and string-code receiver, and `print(x)` at an i32) and must not move with them: a capability check at the pin that over-refuses is a false reject nothing derived from current behaviour would find.",
 "d402-intersection-elem-gate": "nine cells around the struct-alias-union struct row (silent-class-inventory D402/D441/D442/D443), banked as a REFUSED candidate's boundary by #2001 and re-graded when #2004 landed the fix. FOUR boundary cells: `d402_reject_const_arr` was the `nameIsStructDecl` gate's bite in `emit_classify.shapeElemDeclaredStructIdx` (loud emit reject, now runs); `d402_rescue_plaintwin` shows a same-layout PLAIN struct twin rescued it even before the fix (the bridge landed on a `TypeDecl`); `d402_inert_otherlayout` shows an intersection of a DIFFERENT layout is inert; `d402_admit_field_pos` is the one position of sixteen the reject never reached (a struct FIELD of the inline shape's list). THREE were the row's silent/loud siblings, all closed by the same landing: `d402_silent_two_inters` (D441 \u2014 two intersection aliases of ONE layout, both ref lists, two struct rows but one array element type), `d402_silent_field_from_alias` (D442 \u2014 an alias ref list into a structurally-spelled field list, two ref-list wrappers, no coercion) and `d402_reject_shape_param` (D443 \u2014 an alias value into a structurally-spelled param, `ref valtype with no interned shape`). THE PRICE CELL IS `d402_permuted_arm_field_alias`, AND IT IS WHY THIS SET EARNS ITS KEEP: `type AB = {b:i32} & {a:i32}` beside a `{a:i32,b:i32}[]` field. Opening the merge (recording the SHAPE's arena type at the row mint, so `slotCanonId` stops banking -1 for a one-variant `UnionDecl`) made this pair MERGE with `a` and `b` in opposite slots \u2014 a check-clean invalid wasm became a check-clean WRONG ANSWER (`2 1` for `1 2`). It is the ONLY cell of the 151-cell D402 neighbourhood grid that any rung of the landing moved to silent, and the ONLY thing that justifies the field-order rung, which closes no row and moves no derived population on its own. `d402_uniontwin_field_alias` is the one of that grid's seven alias-naming declarations still broken at the field position: a DEGENERATE union alias (`{a,b} | {a,b}`) in D442's position compiles to a union-BOX element list and is still check-clean invalid wasm \u2014 a different root, filed as D446.",
 "d421-constructor-over-t": "the 15 cells where an operand whose TYPE is a CONSTRUCTOR over the type parameter lost the refusal (12, `a: T[]`) or the dispatch (3, `a: T | null`) \u2014 silent-class-inventory D421/D422. All 15 are check-clean INVALID WASM on master and 14 loud check rejects + 1 `runs` on the closing tree. D35's and D44's own 1,712- and 741-cell grids cannot contain them: both vary the DELIVERY of the needle, the receiver and the callee and hold the operand's SHAPE at `T` in every cell. The distilled `cells/` half is blind to them too \u2014 regress.py reports 0 moved classes across this whole change and the `tests/cases` corpus is `cmp` byte-identical on all 1,930 buildable modules.",
 "d423-deferral-price": "the price of D423's `T[][]` deferral when it lands WITHOUT D421's constraint widening, kept whole (silent-class-inventory D423). 9 cells whose element has no compare core leave a loud check reject for check-clean INVALID WASM (7) or a loud emit reject (2) under the one-line deferral alone, because nothing re-asks at the pin; the other 4 are what the deferral BUYS (a false reject over `i32[][]`/`boolean[][]`/`K[][]`/`N[][]` that the direct spelling performs correctly). Two rungs, one landing, and the branch's moved set is a strict subset of the union of the singles \u2014 25 against 34 \u2014 because of this pair. Nothing derived can find them: on the shipped tree all 13 behave exactly like their class-mates.",
 "d424-operator-local": "the 40 operator-declaration cells whose receiver is a function-body LOCAL (silent-class-inventory D424), over operator x quoted-vs-symbol-token name x annotated-vs-bare parameters. On master the four RELATIONAL operators are check-clean INVALID WASM (16 cells \u2014 a `boolean` result lets `i32.lt_s` over two struct refs through) and the six arithmetic ones are a loud `field access receiver is not a struct` (24). All 40 run on the closing tree and print the DECLARATION'S own answer. The tree's only operator fixture binds every operand as a module GLOBAL, which is one of the four deliveries that already worked, so no existing population contains this cell.",
 "d425-inert-declaration": "the price of the UNBUILT reject in silent-class-inventory D425: 40 cells where an operator declaration whose `self` is a BUILT-IN type parses, type-checks and is SILENTLY IGNORED \u2014 `vl check` rc 0 and the native answer comes back. They RUN today and must keep running until D425's declaration-site reject lands, at which point every one of them becomes a check reject, which is exactly the price worth having written down. Each cell's body disagrees with the built-in on its own pair, so a cell that starts DISPATCHING is visible as a wrong value rather than invisible \u2014 the trap D46's row records and which cost this grid 8 cells on its first run.",
 "d426-lambda-in-generic": "the 5 cells of silent-class-inventory D426, OPEN: a lambda declared inside a generic body whose PARAMETER TYPE mentions the enclosing `T`. Two are check-clean INVALID WASM on master and on all four rungs of D421's landing (`(x: T[], y: T[]) => x == y` and its `T | null` twin, both at a `T` binding whose direct spelling runs), one is the loud sibling that shows the shape is broken for every body (`(x: T[]) => x.length`), and two are the controls that must never move \u2014 the same generic without the lambda, and a lambda that CAPTURES the operands instead of taking them. The lifted lambda's `call_indirect` type is built at the UNSUBSTITUTED type, so `T[]` lowers as the i32 default; the fix is in monomorphization and is priced in the row.",
 "d426-lift-price": "the 33 cells of `scripts/silent-sweep/d426/lamgrid.py` that were CHECK-CLEAN INVALID WASM on master `8939d435` and are a loud emit reject on this landing \u2014 and EVERY ONE OF THE 33 HAS A CONTROL THAT RUNS: the identical program with the lambda's own parameter annotation written out at the binding's concrete type. That makes them a lost LOWERING rather than a lost refusal, so the floor that turned them loud is not the close. Per-pin lambda lifting is what buys them, and this set is the tripwire that says when someone has: the day these 33 read `runs`, D426 is closed rather than floored. Three lambda-parameter shapes over ten `T` bindings and two bodies (`T[]`, `T[][]`, `T | null`).",
 "d426-nul-coincidence-price": "the PRICE of silent-class-inventory D426's parameter floor, kept whole: 4 cells of `scripts/silent-sweep/d426/lamgrid.py` that RUN on master `8939d435` and are a loud emit reject on this landing. A `T | null` lambda parameter inside a generic body is correct at exactly two bindings \u2014 `boolean | null` and a string literal union's `K | null` \u2014 because both rep as the plain i32 that an UNSUBSTITUTED `T | null` defaults to. All four inputs of the niche compare agree with the direct spelling (equal, unequal, null-vs-value, null-vs-null), so nothing about them is wrong; they are right by a coincidence nothing in the program states, and one binding away the same source is invalid wasm. The floor refuses them with the rest of the shape, and these are the cells that say so when per-pin lambda lifting buys them back.",
 "d444-d445-operator-declaration": "the 111 cells of `scripts/silent-sweep/d444/opgrid.py` that decide silent-class-inventory D444 and D445 \u2014 the two DECLARATION-SITE shapes of an operator that can never fire. 75 are the PRICE: 73 `runs` -> `check_reject` and 2 `emit_reject` -> `check_reject`, every one of them a program that ran ONLY because its own declaration was dead (the body says 99, or `true`, and the program printed the built-in's answer). 36 are the BOUNDARY and they are the more important half: `d444_*_a2_*` holds arity 2, which is D425's territory and must NOT move \u2014 the built-in-receiver cells keep printing the native answer (D425 still open) and `d444_lt_a2_obj` keeps printing the DECLARATION's `true`, the one comparison that separates dispatch from inertness; `d445_{i32,f64,i64,bool,new_i32,union,obj}` are the six NON-OBJECT receivers that dispatch correctly today, which is why D445's filed title (\"a non-object `self`\") was wrong and the swallow is exactly `tyBuiltinIndexable`. Nothing derived can find any of them: the distilled `cells/` half moves 0 classes across this whole change (re-measured on master 5625840c after #2004: every number identical) and the corpus `cmp` is byte-identical on all 1,954 buildable modules. Regenerate with `python3 scripts/silent-sweep/d444/opgrid.py --mkset` and check with `--verify`."
}
