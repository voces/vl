type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function f() {
  const a: Circle[] = [{ r: 7 }]
  const b: Shape[] = a
  b.push({ s: 3 })
  print(a.length)
  print(b.length)
}
f()
