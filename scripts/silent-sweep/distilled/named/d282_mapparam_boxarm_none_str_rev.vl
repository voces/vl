type Shape = Circle | Sq
type Sq = { z: i32 }
type Circle = { r: i32, s: string }
function fill(c, n: i32) {
  c["k1"] = { r: n, s: "a" }
}
const c = Map()
fill(c, 7)
const sh: Shape = ((c)["k1"] ?? { r: 0, s: "a" })
if sh is Circle { print(sh.r) } else { print(0) }
