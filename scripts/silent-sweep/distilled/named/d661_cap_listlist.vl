type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function outer() {
  const lv1 = [{ r: 7 }]
  function inner() {
    const cU: Shape[][] = [lv1]
    if cU.length > 0 { print((cU[0]).length) } else { print(0) }
  }
  inner()
}
outer()
