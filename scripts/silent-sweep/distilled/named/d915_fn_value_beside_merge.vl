function srcc(p: { r: i32 }): { r: i32 } | null { p }
function rc() {
  const g1 = srcc({ r: 13 }) ?? { r: "s" }
  print(g1.r)
}
rc()
function srcv(p: { r: i32 }): { r: i32 } | null { p }
const fv = srcv
function rv() {
  print(0)
}
rv()
