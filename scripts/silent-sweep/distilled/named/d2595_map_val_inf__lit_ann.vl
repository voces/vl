type P = { x: i32 }
type Dir = "n" | "s"
type Box<T> = { v: T }
const A1: Dir = "n"
const A2: Dir = "s"
function show(v: Dir) {
  print(v)
}
function f<T>(x: T): {[string]: T} {
  const m = Map()
  m["k"] = x
  m
}
const r: (Dir) | null = f(A1)["k"]
if r != null { show(r) } else { print("none") }
