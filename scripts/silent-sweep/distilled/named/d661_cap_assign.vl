type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
function outer() {
  const lv1 = [{ r: 7 }]
  function inner() {
    let aU: Shape[] = []
    aU = lv1
    if aU.length > 0 { print(aU.length) } else { print(0) }
  }
  inner()
}
outer()
