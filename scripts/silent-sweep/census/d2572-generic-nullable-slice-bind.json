{
 "note": "The 1 cell #3174's review found moving loud -> silent: a generic binds `[null, x].slice(1)` and returns the binding, read by element at a `string` instance. Master refused it loudly (`string index access but string list type not collected`); #3174 removes D2554's null-literal exclusion in `monoPinRecvLit`, and the cell reaches D2572, which is check-clean invalid wasm on master in its `r.length` spelling. Recorded as the PRICE: the expected outcome today is `check-clean invalid wasm`, and D2572's close should move it to `runs`. Grid: `d2572/gen.py`.",
 "cells": {
  "d2572_slice_bind__str_idx": {"after": "check-clean invalid wasm", "before": "loud emit reject", "block": "d2572", "coord": {"inst": "string", "op": "slice", "read": "index"}}
 }
}
