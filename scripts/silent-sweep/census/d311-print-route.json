{
 "how": "generated cells, copied whole \u2014 the 5 cells of `scripts/silent-sweep/d290/gen290.py`'s 714 that D311's close moves, plus the 1 sibling that already ran and separates the axis (silent-class-inventory D311). Materialised directly into scripts/silent-sweep/distilled/named/ rather than through d243/mkset.py: gen290.py writes no manifest.json, so there is no coordinate map for mkset.py to re-check against. Regenerate the grid with `python3 scripts/silent-sweep/d290/gen290.py /tmp/g290` and the six programs are byte-identical to the named cells below their headers.",

 "note": "WHY THESE ARE A NAMED SET AND NOT A DERIVED ONE. On the closing compiler all six RUN and print `7`, so a behavioural collapse cannot separate them from thousands of neighbours \u2014 the standing limit CLAUDE.md records. And the census does not contain them at all: its blocks pin a code-16 field's payload at `7`, which exercises the i32 atom of `string | i32` and never the STRING one, and the string atom is the only atom this family was silent at (the other four consumer classifiers `print` dispatches on each open with a typed-IR fast path off the checker's type, so they were already right; a Member arm on all four scored ans=0 at 31,062 corpus reaches). What names these six is the ABLATION, which is a fact about the candidate and not about the cells.",

 "ablation": {"N alone": "0 of 5 \u2014 and byte-IDENTICAL over all 2,347 corpus files (`cmp`), at 1,334 reaches where it never fires. Load-bearing only in composition with S.", "S alone": "3 of 5 -> runs, 0 runs lost, 0 -> silent (714-cell grid)", "S+N": "5 of 5 -> runs, 0 runs lost, 0 -> silent", "base": "both rungs stripped rebuilds the merge base byte-for-byte \u2014 md5 7847217908832c72194bb8c8ee7048b2, 1,464,975 bytes", "corpus": "S+N differs from the base on exactly ONE of 2,347 files \u2014 this row's own specimen (1,814 -> 1,905 bytes, invalid wasm -> prints 7)"},

 "cells": {

  "d311c_isnar_guard_bare_control": {"coord": "stri32_isnar_bare_none", "counters": "S reach=0 (the channel is never asked), N reach=1 ans=1", "grid": "d290", "on_2acb1772": "runs", "under_N_alone": "runs", "under_S_alone": "runs", "under_the_landing": "runs", "why": "THE CONTROL. A BARE receiver's path key is `v.r`, so the guard installs a member-path binding and `narrowedValueAtomOf` answers first. It is the axis pair that separates the five from their running sibling, and it must never move in either direction."},

  "d311g_isnar_guard_list": {"coord": "stri32_isnar_list_none", "counters": "S reach=2 ans=2 k2=2, N reach=1 ans=1", "grid": "d290", "on_2acb1772": "check-clean invalid wasm", "under_N_alone": "check-clean invalid wasm", "under_S_alone": "runs", "under_the_landing": "runs", "why": "an `is string` guard whose receiver is an Index, so `memberPathKeyOf` answers \"\" and nothing is installed \u2014 the read inside the guard is the UN-narrowed one. The row filed this as taking `emitUnionFieldNarrowUnbox`'s narrowed arm; the counters say otherwise, and that correction is what made the population one mechanism rather than two."},

  "d311l_local_rebind_bare": {"coord": "stri32_local_bare_none", "counters": "S reach=4 ans=4 k2=4, N reach=6 ans=6", "grid": "d290", "on_2acb1772": "check-clean invalid wasm", "under_N_alone": "check-clean invalid wasm", "under_S_alone": "check-clean invalid wasm", "under_the_landing": "runs", "why": "THE CELL THAT NAMES RUNG N. Under S alone the binding's slot is already a string ref while `unionNameOfExpr` still registers it as a union binding, so `unionIdentReadKind` overrides `exprString` at the print and the two ends of one binding disagree."},

  "d311l_local_rebind_list": {"coord": "stri32_local_list_none", "counters": "S reach=4 ans=4 k2=4, N reach=6 ans=6", "grid": "d290", "on_2acb1772": "check-clean invalid wasm", "under_N_alone": "check-clean invalid wasm", "under_S_alone": "check-clean invalid wasm", "under_the_landing": "runs", "why": "the local rebind through an Index receiver."},

  "d311p_print_bare_receiver": {"coord": "stri32_print_bare_none", "counters": "S reach=2 ans=2 k2=2, N reach=0", "grid": "d290", "on_2acb1772": "check-clean invalid wasm", "under_N_alone": "check-clean invalid wasm", "under_S_alone": "runs", "under_the_landing": "runs", "why": "the filed repro and the `--codegen` specimen: `print((v).r)` over an adopted `{ r: string | i32 }` row. The read unboxes to a `(ref $sTypeIdx)` and `exprString` still called it a box."},

  "d311p_print_list_receiver": {"coord": "stri32_print_list_none", "counters": "S reach=2 ans=2 k2=2, N reach=0", "grid": "d290", "on_2acb1772": "check-clean invalid wasm", "under_N_alone": "check-clean invalid wasm", "under_S_alone": "runs", "under_the_landing": "runs", "why": "the same read through an Index receiver, which no member path can key."}
 }
}
