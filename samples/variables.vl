foo // Declare a variable in the closest scope
bar = 7 // Initialize a newly created variable; a plain 7 is assumed f64
biz = 4<bigint> // We can be specific on what kind of 4 it is
i32 baz // Declare with a type
const u64 buz = 4 // Const, requires a initializer