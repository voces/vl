// `std:seed` — a smoke module that proves `std:` resolution works end to end.
// It is not part of the usable surface and can be retired once any real module
// stands in for it.
export function stdSmoke() { return 7 }
