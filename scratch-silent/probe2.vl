let nCalls = 0

function src(): string | null {
  nCalls = nCalls + 1
  return "v"
}

function body() {
  const p = src()
  if p != null { print(p) } else { print("N") }
}

body()
print(nCalls)
const redundant: i32 = 5
print(redundant)
