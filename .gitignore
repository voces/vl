node_modules
lsp/dist
dist
.claude/worktrees/
.vl-cache
build/
scripts/vl-host/target/
scripts/vl-host/target
scratch-silent/
scripts/silent-sweep/distilled/.last.json*
__pycache__/
*.pyc

# Per-worktree scratch: agent probes, gate logs, kept matrix cells. Never tracked —
# twenty of these reached master through `git add -A`, twice, including the commit that
# was meant to be the cleanup.
_scratch/

# Per-worktree scratch: agent probes, gate logs, kept matrix cells. Never tracked —
# twenty reached master through `git add -A`, including the commit meant to be the cleanup.
_scratch/
