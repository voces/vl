### D451 — the EMITTER had no index-place narrowing channel: 56 cells check-clean, INVALID WASM
**closed (#2007) — all 56 cells run, plus 8 the grid had priced as loud emit rejects and 3 it
has no column for. 0 `runs` lost, 0 → silent, no price of its own. The witness below prints
`7` and is a tripwire: it runs today and must keep running.**

    type NtI32 = new i32
    function src(): NtI32 | null { return 7 }
    function body() {
      const xs: (NtI32 | null)[] = [src()]
      if xs[0] != null { print(xs[0]) } else { print("NUL") }
    }
    body()
    // prints 7. Was `vl check` rc 0 then
    // "Invalid input WebAssembly code … type mismatch: expected i32, found (ref $type)".

**Control** (bind the element, then narrow — printed `7` before and after):

    const t = xs[0]
    if t != null { print(t) } else { print("NUL") }

* **TWO OF THE ROW'S THREE CENTRAL CLAIMS WERE FALSE, and the corrections ARE the mechanism.**
  The row said "the SIX reps whose nullable is a BOX rather than a niche", then listed **seven**
  and did `6 × 4 × 2 = 48 + 8` arithmetic for what is `7 × 4 × 2 = 56`. Re-graded per rep on
  `2f1f0621`, all seven are 8/8 silent: `nt_i32`, `nt_i64`, `nt_f32`, `nt_f64`, `nt_str`,
  `arr3_i32`, `arr3_str`.
* **And they are not one family.** Only the four scalar newtypes are BOXES. `new string` is a
  nullable-STRING niche; the two three-deep arrays are nullable-REF-ARRAY niches. The three
  families give three different wasm errors — `expected i32, found (ref $type)`,
  `expected (ref $type), found (ref null $type)`, `expected (ref null $type), found (ref
  $type)` — and needed three different rungs. So the row's stated mechanism ("the member-shaped
  emitter seam … `memberPathKeyOf` is Member-shaped") is right for the 32 box cells and wrong
  for the other 24. The arr3 cells fail in `refListElemNameOfExpr`'s Index arm, which gates on
  `nameIsArray(outer)` — false for a name ending `| null` — so it declined and
  `refListSlotOfExpr` turned its `rlSlotByName` miss into **slot 0, a real row addressed by
  mistake**. The tell is that `i32[][]` RUNS on the base compiler and `i32[][][]` does not: the
  depth is doing the work, not the nullability.
* **What landed: fourteen rungs.** The key (`indexPathKeyOf`, spelling `xs[0]` exactly as the
  checker's `placeKeyOf` does); the `narrowBareIdentOnly` gate learning `[` beside `.`; four
  push legs (`is` and null-compare, in both branch senses); the niche push; three classifier
  legs (`exprUnion`/`unionNameOfExpr` declining a narrowed cell, `indexUnionReadKind` through
  `exprString`, the narrowed inner-row lookup); the box unbox tail and the two niche
  `ref.as_non_null` recoveries; and **two write-retirement rungs the grid scores at zero**.
* **THE POPULATION IS LARGER THAN 56.** Three more cells were check-clean invalid wasm on the
  base and are in no column of the d341 grid, because that grid never varies the statement
  BETWEEN the guard and the read: an un-annotated binding from the cell (`const z = xs[0]`), a
  value union whose STRING arm is a real box arm (`(string | i32 | null)[]`), and a write the
  narrowing admits (`ns[0] = "z"` then a non-null consumer). Curated as
  `distilled/named/d451_*`, regenerated by `scripts/silent-sweep/d451/mkcells.py --verify`.
* **The regression the grid could not see, and it was real.** A candidate with the reads and
  without the retirements loses **three `runs` programs to `wasm trap: null reference`** while
  the 184-cell grid stays at 0 silent and 150 runs: `ns[0] = nul()`, `ns = [nul()]` and
  `ns[i] = nul()` inside the guard each printed `null` before and trapped after. Only a NICHE
  cell can reach it — a BOX cell's declared rep has no `print` lowering, so the checker refuses
  the read after the write and the emitter is never asked. Pinned by
  `tests/cases/soundness/index-place-narrowing-emit-retires-on-write.vl`.
