### D2659 — an ALIASED union of a map, an atom and `null` lowers as the atom's nullable niche, dropping the map member

**check-clean invalid wasm · clause 1 · found by #3189's review (s5/s6) · identical on master at the string key · filed 2026-09-26 against master `979323f83`**

Repro:

    type Rec = { x: i32 }
    type U = {[string]: Rec} | i32 | null
    const m0: {[string]: Rec} = Map()
    let gl: U = 42
    gl = m0
    print(gl is i32)
    // vl check: rc 0.  vl run: the emitted module failed to validate inside the module's top-level code

#### Mechanism

The binding `gl: U` gets an `i32` local: the alias's member set reaches the rep classifier as
the `i32 | null` sentinel niche, with the map member gone, so the map store is a box written
into an `i32` slot and `gl is i32` folds to a constant. The inline spelling
`{[string]: Rec} | i32 | null` is a box and runs, and so does the alias without `null`.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the alias (the union spelled inline) | runs, prints `false` | **yes** |
| the `null` member (`type U = {[string]: Rec} \| i32`) | runs, prints `false` | **yes** |
| the string key (`{[i32]: Rec}`) | the same invalid wasm | no |
| the `42` initialiser (`let gl: U = m0`, no reassignment) | refuses at emit on the `is` | no |

#### Grading list

The `o2_global` cells of #3189's review grid at i32 and i64 keys, carried in `distilled/named/`
as the `rv3189-int-key-price` set, and the reviewer's `s6.vl` (this witness).
