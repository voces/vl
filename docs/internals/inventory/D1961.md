### D1961 — `.length` of a struct-list field on an un-narrowed union-ARM receiver indexed the ref-list wrapper table at -1, a compiler trap

**closed 2026-09-22 — the witness prints `1`, `-1`, `no` · was compiler trap (`out of bounds array access` in `emitArrLen`, exit 70), `vl check` rc 0 · reported by plumb (PL-006) · filed against master `a8a104432` · same mechanism as [D1960](D1960.md) · fixture `tests/cases/unions/arm-struct-ref-list-field.vl`**

Repro:

    export type S = { rva: i32, name: string }
    export type P = { xs: S[] }
    export type E = { msg: string }
    export function mk(n: i32): P | E {
      const xs: S[] = []
      if n > 5 { return { msg: "no" } }
      xs.push({ name: "a", rva: n })
      { xs: xs }
    }
    export function find(self: P, r: i32): i32 {
      for i in 0 until self.xs.length {
        const s = self.xs[i]
        if s.rva == r { return 1 }
      }
      -1
    }
    const a = mk(3)
    if a is P { print(find(a, 3)); print(find(a, 4)) }
    const b = mk(9)
    if b is E { print(b.msg) }
    // vl check: rc 0.  vl build: the compiler itself trapped — out of bounds array access
    // (named seed: emitArrLen <- emitExprExpect <- emitReturnValue <- emitFuncBody)

#### Mechanism

[D1960](D1960.md)'s name miss at a second reader: `exprRefArray(self.xs)` is true (through
`memFieldTypeCode`'s variant rung) while `refListSlotOfExpr(self.xs)` is -1, and `emitArrLen`'s
ref-list arm read `rlWrapIdx[rslot]` without testing the slot. The fix is D1960's (the slot
now resolves); `emitArrLen` also refuses a -1 slot loudly, positioned at the receiver, instead
of trapping — the D1500 shape.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| `P \| E` → `P \| null` | runs | **yes** |
| the loop, keeping only `return self.xs.length` | still traps | no |
| `xs: S[]` → `xs: i32[]` | runs | **yes** |

#### Grading list

Re-grade D1960's witness in the same run (one mechanism, both directions), and the fixture's
index / push / `.length` positions. With D1960's rung reverted, this witness must be the loud
`` `.length` receiver's ref-list element does not resolve `` refusal, never a trap.
