### D2608 — a `boolean | string | null` box narrowed by `!(x is boolean)` is delivered to a `string | null` destination as the box

**closed 2026-09-26 by #3178 — the filed repro RUNS and prints `true` · was check-clean invalid
wasm, clause 1 · filed 2026-09-26 against master `524e232ed`, found by the #3178 review
(`rv3178-att/h1`), where it reads as D2605 but is not · a spelling of [D2590](D2590.md), which
#3178 closed while this row was being filed**

Repro:

    function pickS(n: i32): boolean | string | null {
      if n == 1 { return "ab" }
      if n == 2 { return null }
      true
    }
    function go(n: i32) {
      const x = pickS(n)
      if !(x is boolean) {
        const y: string | null = x
        print(y == null)
      }
    }
    go(2)
    // vl check: rc 0.  vl run on master `524e232ed`: invalid module, inside `go`:
    // "type mismatch: expected (ref null $type), found (ref $type)"

#### Mechanism

D2590's: the checker types `x` under the complement as the `string | null` niche, while the
emitter kept the two surviving members of the three-member box as the box, so the box
reference was stored where a nullable string belongs. #3178 reads a union narrowed to a niche
as that niche. The hole parameter of `rv3178-att/h1` is scenery: the direct binding fails the
same way.

#### Ablation

On master `524e232ed`.

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| the binding un-annotated (`const y = x`) | runs | **yes** |
| the value passed to `hole(v)` or `hole<T>(v: T)` and the result annotated | still invalid | no |
| a parameter annotated `v: string \| null` receiving `x` | still invalid | no |

#### Grading list

`rv3178-att/h1` runs and prints `true`; the three ablation spellings run on the merged tree.
