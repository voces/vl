### D1700 — an object literal nested 20 levels deep against a declared row of the same shape traps the COMPILER, and it is what puts the capture comparator's depth bound out of reach

**compiler trap · filed 2026-09-06 against master `d4ce2f91b` + D1696/D1674 + D1697 ·
pre-existing, both seeds · 0 corpus cells**

Repro:

    type H = { f: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { xs: i32 } } } } } } } } } } } } } } } } } } } } } }
    const b = { f: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { g: { xs: 7 } } } } } } } } } } } } } } } } } } } } } }
    print(b.f.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.g.xs)
    // vl check: rc 0.  vl run: exit 70,
    // vl: the compiler itself trapped while compiling — this is a bug in vl

#### Mechanism

The backtrace is a repeated pair of compiler frames, so this is a deep RECURSION running out of
stack rather than a table read of a sentinel — D1513's class, not the `sentinel-index-unguarded`
family, and asking which of the two a new trap is before auditing readers is the rule.

The ladder is sharp and the ablation is short. Nesting depths 10 through **19 run and print
`7`**; depth **20 and every depth above traps**. Deleting the declared `type H` makes depth 20
run, so the ingredient is a declared row of the same deeply-nested shape beside the literal, not
the literal alone. No union is involved: an `i32` leaf traps identically.

**And this is what makes the capture comparator's depth bound unreachable.**
`elemRowsCaptureCompatAt` walks one nesting level or one container per step and stops at 24,
returning its lenient default — so the wall stands at depth 25, four levels ABOVE the deepest
program the compiler can compile at all. Measured, not argued: an A/B of the bound at 8 against
24, one seed each, over container depths 2–10 and nesting depths 2–10, moves exactly the cells
past 8 —

| axis | bound 8 | bound 24 |
| --- | --- | --- |
| container depth 2, 4, 6, 7, 8 | runs `7` | runs `7` |
| container depth **9, 10** | check-clean invalid wasm | runs `7` |
| nesting depth 2, 4, 6, 7 | runs `7` | runs `7` |
| nesting depth **8, 9** | prints `true` (wrong value) | runs `7` |
| nesting depth **10** | loud `object literal is missing a struct field` | runs `7` |

— and the seed is byte-identical at both bounds (2,227,315 bytes) with a 0-of-3,053 module diff
at both, so the constant is free and only moves where the wall stands. At 8 the wall is reached
by a probe; at the shipped 24 it is not, because this row's trap fires first. The counter is
still a wall and not a proof; what removes it is a visited-pair guard, which terminates on
exactly the cyclic case and lets an acyclic walk go as deep as it likes.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the declared `type H` | runs, prints `7` | **yes** |
| one nesting level (depth 19) | runs, prints `7` | **yes** — 20 is the floor |
| the union leaf (a plain `i32`) | traps identically | no — no union is involved |
| both the declaration and the union | runs, prints `7` | no — the control |

#### Grading list

Six cells: the witness, its `type H`-deleted control, the depth-19 spelling (which must keep
running), the plain-`i32` spelling that traps identically, the both-deleted control, and a
container-depth-20 spelling, which reaches the same recursion by the other axis. The standing
controls are `tests/cases/structs/anon-struct-declared-row-capture-containers.vl` and
`anon-struct-declared-row-capture-depth-and-list.vl`, whose deepest cells are three levels, well
under the floor.
