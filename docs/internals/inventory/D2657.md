### D2657 — a generic pin that lands two union members on one map representation boxes them as one member

**check-clean invalid wasm · clause 1 · found by #3189's review (P3) · identical on master at the string key · filed 2026-09-26 against master `979323f83`**

Repro:

    function g<V>(m: V, n: {[string]: i32}, c: boolean): i32 {
      let u: V | {[string]: i32} = n
      if c { u = m }
      if u is {[string]: i32} { return 1 }
      2
    }
    const mb: {[string]: boolean} = Map()
    const mi: {[string]: i32} = Map()
    print(g(mb, mi, true))
    print(g(mb, mi, false))
    print(g("x", mi, true))
    // vl check: rc 0.  vl run: the emitted module failed to validate inside `g$1`

#### Mechanism

The direct spelling `{[string]: boolean} | {[string]: i32}` is refused at check where an `is`
has to tell the two apart (D2493): both values are one i32 cell, so the two maps share one map
struct and one box tag. Inside the generic the checker sees `V | {[string]: i32}` and has
nothing to refuse; at the pin `V := {[string]: boolean}` the instance's union collapses the
two members onto the one mono map, and the `is` and the box disagree about the member set.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the `string` instance (the last call) | runs, but prints `2`, `2` for `2`, `1` — both instances test one tag | no — the same defect, silently wrong |
| the string key (`{[i32]: i32}` and `{[i32]: boolean}`) | the same invalid wasm | no |
| the generic (the direct union) | refused at check (D2493) | **yes** |

#### Grading list

#3189's review witness `k4.vl` (the i32-keyed spelling), carried in `distilled/named/` as the
`rv3189-int-key-price` set; D2493's close (a tag per member) is predicted to close this too.
