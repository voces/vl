### D1522 — a BARE map `.get(k)` has no value-position lowering, only the `??`-fused one

**closed 2026-09-03 (`dispatchRewrite` rewrites a map `.get(k)` into `m[k]`) · was a loud emit reject, clause 2 · reported by the `~/glean` WC3 replay player as **VL-003**, VL's first external consumer, at their pin `2fb8b271` · filed against master `bd500845` · ZERO corpus cells**

Repro (their program, verbatim — prints `1` now):

    const m = Map()
    m.set("a", 1)
    print(m.get("a"))
    // was: emitProgram: callee is not a function name

#### Mechanism

`.get` on a map is a DESIGNED builtin, not UFCS: the checker's `TyMap` member arm admits
exactly `set`/`get`/`has`/`delete`/`keys`/`values` and types `m.get(k)` as `V | null`
(`docs/guide/collections-design.md` §C2.1, and `Map[k]` returning an optional at §"indexing").
Every one of those but `get` has an emit arm. `get` lowers only when it is FUSED with `??`
(`emitMapGetOr`, the same path `m[k] ?? d` takes), so a bare one falls out of the member
ladder into `emitCall`'s UFCS fallback, which looks for a free `self`-function named `get`,
finds none, and reports `callee is not a function name` — a sentence about UFCS on a call
that was never UFCS. `ROADMAP.md` B6a already names the gap; no row carried it.

The refusal is therefore not about the un-annotated `Map()` the consumer suspected: the
ANNOTATED spelling refuses identically, and it is the missing `??` that decides.

#### Ablation

Graded against the pre-fix seed; the un-annotated `Map()` the consumer suspected is
scenery, and the missing `??` is the whole trigger.

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the un-annotated map (`const m: {[string]: i32} = Map()`) | still refused | no |
| `print` as the position (`const v = m.get("a")`) | still refused | no |
| the value rep (`{[string]: string}`, `{[string]: f64}`, `{[i32]: i32}`) | still refused | no |
| the METHOD spelling (`m["a"]`) | ran | **yes** |
| the BARE read (`m.get("a") ?? -1`) | ran | **yes** |

#### Grading list

`scripts/capability-probes/matrix/map-get-method-bare.matrix.vl` is the instrument, and it
is what caught the first attempt: an emit arm ALONE made `print(m.get(k))` emit a plain i32
while `exprUnion` read the call's checker type (`i32 | null`) and claimed the value-union
BOX — clause 2 traded for clause 1 — with the binding, the f64 rep and the fused form each
still owed an arm. The rewrite grades **0 → 49 of 52 RUNS, 0 silent, 0 `runs` lost**; the
three that stay are a strict SUBSET of the six the BRACKET spelling refuses at (control
graded the same run), so no `.get`-specific gap remains and the residue is the `i32 | null`
niche family both spellings share.

The MISS is part of parity and is graded in the fixture: a mono i32 map reads `0` at both
spellings, a string map reads `null` at both. The exclusions the gate must keep are there
too — a LIST `xs.get(i)`, a closure-valued struct field named `get`, a UFCS `get(self:)`,
and `Set`, all unchanged before and after.
