### D2238 — an element read of a narrowed `i64[] | null` hole parameter prints through the i32 import

**check-clean invalid wasm 2026-09-23 — the instance calls `__print_i32__` on an `array.get` of the `i64` backing · clause 1 · found by the D2236 position matrix (`nullable-list-join-null-i64.matrix.vl`, `argument` and `early_return_guard`, un-annotated, SILENT before and after the D2236 close) · filed against master `5dd67e150`**

Repro:

    function take(a) { if a != null { print(a[0]) } }
    const q: i64[] | null = [(7 as i64)]
    take(q)
    // vl check: rc 0.
    // vl run: Error: invalid module … the emitted module failed to validate inside `take`

#### Mechanism

The instance's parameter is the right cell, `(ref null $i64wrap)`, and the narrowed read
`a[0]` lowers as `array.get` over the `i64` backing; the `print` around it dispatches to
`__print_i32__`, so the read is not classified `i64` at the call. The `f64` and `string`
twins run, and the non-null `i64[]` spelling runs, so the gap is the `i64` element of a
NARROWED nullable hole parameter. `exprIsI64`'s `Index` arm asks `exprI64Array` of the
receiver exactly as `exprIsF64`'s asks `exprF64Array`, so the disagreement is upstream of that
arm — not yet located.

#### Ablation

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| element `i64` → `f64` or `string` | runs | **yes** |
| `q: i64[]` (non-null) and no `!= null` narrow | runs, `7` | **yes** |
| parameter annotated `a: i64[] \| null` | runs, `7` | **yes** |
| argument an un-annotated `if … { [(7 as i64)] } else { null }` join | same invalid module | no |
| `print(a.length)` instead of `a[0]` | loud emit reject, `'.length' on a receiver the emitter cannot classify as a list, string, map or set` | a neighbour at the same pin |

#### Grading list

`nullable-list-join-null-i64.matrix.vl` (`argument`, `early_return_guard`, un-annotated); the
`.length` neighbour in the last ablation row.
