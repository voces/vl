### D2398 — an assignment to a wider name used as a value re-reads the name at its storage rep

**check-clean invalid wasm · clause 1 · pre-existing on master `2304ece42` · filed 2026-09-25
against `2304ece42`, found grading [D2394](D2394.md)**

Repro:

    function f(): string {
      let a: f64 = 1.5
      const r = (a = 5)
      "\{a} \{r}"
    }
    print(f())
    // vl check: rc 0.  vl run: invalid module inside `f$m0`: type mismatch: expected i32, found f64

#### Mechanism

An assignment to a name lowers as a value by storing and re-reading the name
(`assignTargetReReads` answers true for every `Ident` target), while the checker types the
assignment at its right side: `i32` for `5`. Where the name's storage is wider than that type,
the re-read pushes the storage rep (`f64`, or a nullable ref for `i32[] | null`) into a
position typed at the right side's. [D2285](D2285.md) settled the same disagreement for a
closure's boxed cell by yielding through `const t = e; v = t; t`; the plain name has no such
rewrite. [D2394](D2394.md)'s close covers only the right sides the place's type built (a
container literal, or a union in another member order), where the assignment now yields the
place's type; these right sides are values of their own type the place merely accepts.

#### Ablation

| ingredient removed | outcome on master `2304ece42` | needed? |
| --- | --- | --- |
| the value position (`a = 5` as a statement) | prints `5` | **yes** |
| the widening (`let a: i32`) | runs | **yes** |
| a nullable place (`let a: i32[] \| null = null`, `const r = (a = [1, 2])`) | still invalid wasm: expected `(ref $type)`, found `(ref null $type)` | no: a second storage, one mechanism |

#### Grading list

The value-position cells of [D2394](D2394.md)'s grid, which must keep running; `const r = (s =
x)` for `s: S | null`, `s: i32 | string` and `s: f64` with `x` a variable.
