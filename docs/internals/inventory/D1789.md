### D1789 — a nested function that calls a SIBLING nested function is refused, because the sibling's capture is resolved against the caller's frame instead of the frame that declares it

**closed 2026-09-06 — the same mechanism as [D1761](D1761.md), filed twice from two discovery
routes, and closed once · filed against master `be5a79fd2` while grading [D1780](D1780.md)'s
matrix, closed against `9f22a51d9`; no generic and no monomorphization is involved**

Repro:

    function o(n: i32) {
      function k(x: i32) { return n }
      function m(y: i32) { return k(y) }
      return m(1)
    }
    print(o(2))
    // vl check: rc 0.  vl run: rc 0, prints 2

#### Mechanism

`k` captures `o`'s parameter `n`. `m` calls `k`, so the call synthesizes `k`'s closure inside
`m`'s frame and reads `n` from there — but `m` does not capture `n`, only `k` does, so the
lookup runs off the end of `m`'s capture list and refuses. The capture that has to be
materialised belongs to the DECLARING frame (`o`), which both siblings can reach; nothing
threads it that far. `k`'s own frame is right, which is why calling `k` directly from `o`
works at every pin.

Independent of everything else on the nested-capture matrix: it refuses with `o`'s parameter
annotated, with one pin, and with no type parameter anywhere, so the monomorphizer never runs.
Respelling both siblings as lambdas runs, so the frame the lambda path builds is the one the
`function` declaration owes.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the sibling hop (`o` calls `k` directly) | runs, prints `2` | **yes** |
| the CAPTURE (`k` returns `x`, not `n`) | runs, prints `1` | **yes** |
| the annotation on `o`'s parameter | refuses, identical | no — both faces |
| both siblings respelled as lambdas | runs, prints `2` | **yes** — the declaration spelling |

#### Close

[D1761](D1761.md) carries it: at `capRecord`'s drop, a name resolving to a NESTED function
contributes that function's own free names to the caller's capture set. **This row's fourth
ablation row is what the close confirms** — the lambda spelling already ran, so the fold is
the lifting the `function` declaration owed, not a new capability.

**This row and [D1761](D1761.md) are one mechanism found twice**: this one while grading
[D1780](D1780.md)'s matrix, that one as [D1788](D1788.md)'s residue on the same matrix. Both
are kept because their ablations are complementary — this one has the lambda control, that one
has the generic row that proves the monomorphizer is not involved.

#### Grading list

* The direct-call twin and the capture-free sibling hop, which ran before and must keep running.
* The lambda spelling of the same program, which ran before and must keep running — it is the
  proof that the fold owes nothing new.
* [D1761](D1761.md)'s grading list, which carries the 264-cell matrix and the six-shape fixture.
