### D2390 — an `is` narrowing survives a call to a closure that reassigns the narrowed binding

**closed 2026-09-25 — the witness prints `true` · was loads then traps, clause 1 · owner ruling
(a): a call that may run a closure assigning a narrowed binding ends the narrowing (DECISIONS.md,
"A call that may reassign a narrowed binding ends the narrowing") · filed 2026-09-25 against
master `3ab3c402f`, found grading [D2387](D2387.md)/[D2388](D2388.md), closed against master
`2304ece42` · fixtures `tests/cases/soundness/call-ends-narrowing-diagnostic.vl`,
`tests/cases/soundness/call-ends-narrowing-recheck-runs.vl` · the other forms are
[D2400](D2400.md)–[D2405](D2405.md)**

Repro:

    function f(): string {
      let v: boolean | f64 = 1.5
      const k = () => { v = true }
      if v is f64 { k(); "\{v}" } else { "no" }
    }
    print(f())
    // vl check: rc 0.  vl run: prints true (before the close: wasm trap: cast failure)

#### Mechanism

The checker narrows `v` to `f64` for the whole then-arm and does not drop the narrowing at
`k()`, although `k` assigns `v` a different member and is called inside the arm. The emitter
reads the cell under the narrowing and casts the box's payload to the narrowed member's struct,
which now holds another member: `cast failure`. On master `3ab3c402f` that was already the
outcome for `string|i32`, `i32|string`, `i32|f64`, `boolean|string`, `string|boolean` and
`string|f64`. The D2387/D2388 close is a PRICE recorded here, not the cause: it moved
`f64|i32`, `i64|string` and `boolean|f64` from check-clean invalid wasm to this trap, and
`i32[]|string` from a loud emit reject (`'.length' on a receiver the emitter cannot
classify`) to this trap.

The close: `k()` may run a writer of `v` (`callMayWrite`), so the checker retires `v`'s
narrowing in place at the call and banks the name on the call's statement, and the emitter
retires the cell narrowing (`v[0]`) after that statement. `"\{v}"` then reads the declared
`boolean | f64` and prints `true`; `v * 2.0` there is refused, naming the call. Every member
pairs above, re-tested after the call, run and print the member `k` wrote.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the call `k()` inside the arm | prints `1.5` | **yes** |
| the closure's write (`k` only reads `v`) | runs | **yes** |
| the write's member (`v = 2.5`) | prints `2.5` | **yes** |
