### D1320 — a lambda capturing a SCALAR binding of a MODULE-SCOPE block is check-clean invalid wasm — [D1244](D1244.md)'s silent half, at the rep that row's witness never used

**runs, prints `11` — CLOSED 2026-09-03 by #2452 (a module-scope block is a FRAME), the same landing that closed [D1244](D1244.md), [D1306](D1306.md) and [D1350](D1350.md) · was check-clean invalid wasm, clause 1, `type mismatch: expected a type but nothing on stack` · ZERO corpus cells · found 2026-09-03 as the residue of [D1170](D1170.md)'s position matrix, and minimised until neither `?.` nor a nullable struct was left**

Repro:

    {
      const c = 11
      const g = () => { print(c) }
      g()
    }
    // vl check rc 0; vl run -> Invalid input WebAssembly code at offset 238:
    //   type mismatch: expected a type but nothing on stack

#### Mechanism

Not yet located. What is measured is the OUTCOME SPLIT against D1244, which files the same
scope with a REF local and gets a LOUD `emit refuses: field access receiver is not a struct`.
At an i32 binding the same shape reaches the engine instead, so a fix for D1244 has to be
graded at both reps or it converts one of these and leaves the other.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the CAPTURE (`print(c)` directly in the block) | runs | **yes** |
| the BLOCK (both bindings at module scope) | runs | **yes** |
| module scope (the same block inside `function go()`) | runs | **yes** |
| the `?.`…`??` producer the cell was found with | still invalid | no |
| the nullable struct | still invalid | no |

#### Grading list

The six `block_*_capture` cells of `matrix/optchain-coalesce-nested-struct-field.matrix.vl`
(a code-15 value — loud `field access receiver is not a struct`, D1244's message) and of its
SCALAR-field control (an i32 value — silent, this row). Both faces of both. A candidate that
moves only the loud six has closed D1244 and not this.

#### The close

**THIS ROW'S OWN WARNING WAS THE THING THAT MADE THE FIX CORRECT.** The mechanism section
above said a fix for D1244 *"has to be graded at both reps or it converts one of these and
leaves the other"*. #2452 was graded at both and converted both: the ref rep (D1244's own
witness) prints `7`, this scalar rep prints `11`. A row filed as another row's predicted
residue is worth the words precisely when it names what the other fix must not miss.

**AND THE PREDICTION HELD, WHICH IS NOT THE DEFAULT.** Residue attribution is a prediction
and has been refuted before — two of them on 2026-09-01 turned out to be separate
mechanisms. What makes this one a close rather than a coincidence is that it was written as
a prediction with a witness, so the standing gate graded it and reported the move; nobody
had to remember to check.
