### D2240 — a NON-null argument pinned into a hole whose body tests `!= null` has no null to compare against

**loud emit reject · clause 2 · every argument type tried · found while closing D2238 · filed 2026-09-23 against master `f2234c2e5`**

Repro:

    function first(a) {
      if a != null { return a[0] }
      0
    }
    const b: i32[] = [3]
    print(first(b))
    // vl check: rc 0.
    // vl run: Error: emit error …:6:16: emitProgram: bare null needs a struct-typed context

#### Mechanism

Not yet located. The instance is pinned at the argument's own non-null type, so the body's
`a != null` compares a non-nullable value with a bare `null` that no operand gives a rep. The
nullable argument (`const b: i32[] | null`) runs, and so does the same body called with one —
D1753 closed exactly that direction, the nullable READ pinned at its non-null half. Over a
non-null pin the test is statically true, which is the lowering it seems to be owed.

#### Ablation

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| `b: i32[] \| null` | runs, `3` | **yes** |
| `b` an `i64[]`, `f64[]`, `f32[]`, `string[]` or `u8[]` | same refusal | no |
| argument the scalar `3`, body `return a + 1` | same refusal | no |
| argument a struct `{ x: 3 }`, body `return a.x` | loud emit reject, `struct equality is not supported yet` | a neighbour |
| the `!= null` test removed | runs | **yes** |

#### Grading list

The struct neighbour in the ablation; `first` called with both a nullable and a non-null list
in one module, which pins two instances.
