### D2515 — the second binding of `for k, v in outer` over a map-valued map has no kind, so a check-clean walk refuses in emit

**closed 2026-09-25 — the filed repro RUNS and prints `1` · was loud emit reject, clause 2 · pre-existing on master `d29a9d220`, at string and i64 keys ·
found by #3164's review · filed 2026-09-25 against master `aed171ac5`**

Repro:

    const outer: {[string]: {[string]: i32}} = Map()
    const inner: {[string]: i32} = Map()
    inner["x"] = 1
    outer["a"] = inner
    for k, v in outer { print(v.length) }
    // vl check: rc 0.  vl build: emitProgram: unsupported for-in second binding

#### Mechanism

`mapValElemKind`'s kind-6 arm answered `mvListValKind`, which is null for a map element, so both
loop-variable kinds came back null. A nested-map value now binds `map` (or `nulmap` for a
nullable value) with the inner shape `mvInnerMapShape` as its companion, the `#l` temp is the
vals ref list, and the element read recovers non-null. The list-of-maps walk,
[D2492](D2492.md), is the ref-list twin: `forInElemKind`'s element-kind-3 arm.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the value `{[string]: i32}` replaced by `string[]` | prints `1` | **yes** — a map value |
| `for k, v in outer` replaced by `for k in outer` reading `outer[k] ?? Map()` | prints `1` | **yes** — the pair binding |
| the keys `string` replaced by `i64` (both levels) | still refuses | no |
