### D2500 — a `u8[] | null` narrowed by `!= null` and passed to a `u8[]` parameter is invalid wasm

**check-clean invalid wasm · clause 1 · filed 2026-09-25 against master `dec16071c`, found by the
review of #3162 (the `u8[]` column of its grid), which leaves it as it was**

Repro:

    function take(xs: u8[]) { print(xs.length) }
    function mk(flag: boolean): u8[] | null {
      if flag { return [3, 4] }
      null
    }
    const v = mk(true)
    if v != null { take(v) }
    // vl check: rc 0.  vl run: invalid module, "expected (ref $type), found (ref null $type)"

#### Mechanism

The argument is the nullable byte-list ref as read, with no `ref.as_non_null` where the narrowing
makes it non-null. Not yet ablated: whether the other distinct-backing scalar lists take the same
narrowed argument cleanly is the first control to run.

#### Grading list

The review grid's `u8l` column: 40 cells invalid wasm on master and on #3162.
