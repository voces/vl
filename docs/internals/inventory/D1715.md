### D1715 — a `K` holder and a `K | null` holder sharing a field name take one anonymous row, so the niche field loses the member set the atom slot needs

**loud emit reject · filed 2026-09-06 against master `4ea675797` + D1710 · residue of D1710:
its close runs the solo holder and this pair still refuses · 0 corpus cells**

Repro:

    type K = "a" | "b"
    function plain(): K { return "a" }
    function nul1(): K | null { return "b" }
    function firstHolder() {
      const first = { xs: plain() }
      print(first.xs)
    }
    const v = { xs: nul1() }
    const l = v.xs
    if l != null { print(l) } else { print("none") }
    // vl check: rc 0.  vl run: emit error,
    // emitProgram: literal-union string widening with no members

Delete `firstHolder` and the identical program prints `b`. That one ablation is the row: the
holder that collides is never called, never read, and shares nothing with the value under test
but the field NAME `xs`.

#### Mechanism

Predicted, not probed. This is D1663's shape at a different code pair. A code-0 field (the
bare atom) and a code-30 field (the atom with `-1` for null) are two different slots, but
`structIndexOfObjCtxGo` matches a literal to a row by field NAMES and storage CODES, and
whatever tells 0 and 30 apart is not being asked — so the second holder twins onto the first's
row and the member set recorded beside it is the wrong one. The sentence names exactly that:
the widening finds no members. D1663 built `fieldCodeRefsElemHeapC` for the ref codes; the atom
codes have no equivalent. Probe `anonValueFitsField` against this failing witness before
fixing.

The two LIST spellings do NOT collide — `anon-nullable-list-field-litunion-element.matrix.vl`
carries a non-null twin holder in its prelude and is 46 of 46 — so the pair is specifically
codes 0 and 30, not the litunion family at large.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the second holder (`firstHolder` deleted) | runs, prints `b` | **yes** — the collision is the whole row |
| the shared field NAME (the twin holder's field renamed `ys`) | runs, prints `b` | **yes** |
| the `\| null` (both holders `K`) | runs | **yes** — the code pair is the ingredient |
| the literal union (`A` and `A \| null` over a struct) | runs | **yes** |

#### Grading list

The witness and its four ablation rows, plus the two list spellings that must keep running
with a twin holder in scope (`K[]` beside `K[] | null`, and the same for the nullable-element
form), which is what scopes this row to the code-0/code-30 pair rather than to litunions.
