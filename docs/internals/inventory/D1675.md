### D1675 — an un-annotated `Map()` carrier resolves its value row by rendered shape name, and that reader has no member-set test, so a union-box field takes the wrong box

**closed 2026-09-06 — the rendered-name reader keys a union-box field on its member
SEQUENCE, so a carrier lands on its own row · filed against master `bce33eae0` · D1689's
residue at the second reader · residue D1690 · 0 corpus cells**

Repro:

    type U1 = i32 | boolean
    type U2 = string | boolean
    function u1(): U1 { return 7 }
    function u2(): U2 { return "hi" }
    function _firstHolder() {
      const first = { xs: u1() }
      print(first.xs)
    }
    const m = Map()
    m["k"] = { xs: u2() }
    const got = m["k"]
    if got != null { print(got.xs) }
    // vl check: rc 0.  vl run: prints hi

#### Mechanism

The row identity is already right here, which is what separates this row from D1689. The
probe, on the witness above:

    ROW 0 #anon0 [xs:16:<i32|boolean> ]
    ROW 1 #anon1 [xs:16:<string|boolean> ]
    LIT node15 -> row0 #anon0 lit-fields[xs:16:<i32|boolean> ]
    LIT node32 -> row1 #anon1 lit-fields[xs:16:<string|boolean> ]

Two rows, each literal on its own. What picks the wrong one is the OTHER reader: an
un-annotated map's value row is resolved from a rendered shape NAME through
`structIndexOfTypeName` and `shapeFieldTypeCompatK`, which carries per-code tightenings for
codes 0, 4, 15, 19, 5 and 28 and none for 16. `{ xs: string | boolean }` therefore matches the
first `{ xs: <box> }` row in the table — `#anon0`, over the other member set — and the box
written under one set is narrowed against the other's tags.

That the map value goes through this reader is measured, not read off the code path: adding
the codes 5 and 28 arm to `shapeFieldTypeCompatK` is what moved the same `map_value` cell of
the REF-LIST matrix template from refusing to `runs`, while this cell, whose code is 16, did
not move.

The comparator code 16 needs is a different shape from the one that arm takes.
`nestedStructNamesCompat` asks whether two type SPELLINGS denote one structure; a member set
is a set, and `i32|boolean` against `string | boolean` differs in membership and in spacing at
once — so this wants a set-aware test beside `mvValNamesCompat`, not another canon-key call.

Pre-existing and unchanged by D1689's close: both seeds trap identically. `_firstHolder` is
never called on purpose — its row is minted by the arena walk, so the collision needs no
second execution, and calling it would only add a second failure.

#### The close

`shapeFieldTypeCompatK` gets a code-16 arm beside its code-19 one, and the comparator behind
it is `unionMemberSetsCompat`: both keys resolve through the union registry
(`unionBoxMemberSeqOf` — a registered alias answers with its own member set), then the
DESPACED sequences are compared with no dedupe and no reorder, because a box's tags are
positional (DECISIONS.md, the union member-set ABI). Refutation is one-sided: a key that
resolves to "" is unknown, not a mismatch.

`anonValueFitsField`'s own box arm, which had compared the two keys as raw strings since
D1689, now asks the same comparator — so an alias and its expansion stop being two keys at
the node reader as well as at the name one.

The readers that map a literal or a rendered shape to a struct row, and where the code-16
companion column stands in each:

| reader | input | code-16 test |
| --- | --- | --- |
| `anonValueFitsField` (via `structIndexOfObjCtxGo`) | a literal NODE | `unionMemberSetsCompat`, anon rows only |
| `shapeFieldTypeCompatK` (via `shapeRowScanK` ← `structIndexOfTypeName`) | a rendered SHAPE NAME | `unionMemberSetsCompat` |
| `shapeFieldTypeCompat` (via `variantIndexOfTypeName`) | a rendered SHAPE NAME, variant table | the same arm, leniency 0 |
| `structRowOfObjFieldSetK` | an arena `TyObj` | none — codes only; `rowElemDeclConflicts` covers 15/5/28 |
| `repRowOfTyStruct` | a canon key | by construction: two member sets are two keys |

The fourth row is the residue, D1690.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the first holder | runs, prints `hi` | **yes** |
| the map carrier (a plain binding instead) | runs, prints `hi` | **yes** |
| the annotation on the map (`{ [string]: { xs: U2 } }`) | runs, prints `hi` | **yes** — the rescue |
| the differing member set (both `U2`) | runs, prints `hi` | **yes** |

#### Grading list

Four cells: the witness above, its annotated twin, the same pair delivered through a plain
binding, and the map with a single holder — the last three must all keep running. The standing
control is the `map_value` row of
`scripts/capability-probes/matrix/anon-struct-twin-holders-union-box-field.matrix.vl`, the one
cell of forty-six that template does not reach, and its ref-list sibling's `map_value` row,
which must stay green. The standing fixture is
`tests/cases/structs/anon-struct-map-carried-box-field-member-set.vl`, whose last function is
the leniency's own control: an alias and its expansion must stay ONE row.
