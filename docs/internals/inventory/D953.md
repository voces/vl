### D953 — [CLOSED 2026-09-01] the merged row NESTED one layer down: four rungs, and D923 named three of them

**closed as `runs` · was check-clean invalid wasm · `type mismatch: expected i32, found (ref $type)` · 1 cell (`distilled/named/d913_ret_binding_of_field`), the LAST clause-1 cell outside D950's family · distilled corpus: `runs -> not-runs` ZERO, `-> silent` ZERO, 1 class forward · fixture `tests/cases/types/coalesce-nested-carrier-field.vl`**

Every close in the eleven-deep `??` family moved a TOP-LEVEL row — a param slot, a return row,
a literal. This one is the merged row sitting INSIDE another row's field, and it needed four
rungs that only work together. D923 built rung 1, measured that it bought nothing, deleted it,
and wrote down where the owner had to be. It was right about all of that.

* **RUNG 1, THE READER (D923's, restored).** A `Member` arm on `anonLeafBoundValOk`: a field
  read of a PARAMETER is nameable in two rungs this file already had — D891 reads a param's
  sources as the arguments at every call site, and an argument that is an object literal names
  its own fields. Composing them projects `p.r` to the inner literal.

* **THE OWNERS GO TO A SCRATCH AND THE PARAMS DO NOT, which is the correction to D923's own
  note.** That row says the base is read into a scratch because "the base's own row does NOT
  move and publishing its owners would move a slot with no member behind it" — true of `out`,
  and NOT of `pars`. The carrier's param slot is exactly what has to follow (rung 4), and
  discarding the param scratch is what left the fix one rung short.

* **RUNG 2, THE OWNER — the nested field's COMPANION NAME, at the mint.** `sFieldTgtStructIdx`
  resolves a nested-struct field's target row from the name the mint recorded, so naming the
  narrow inner is what made the call site build `struct.new $outer (struct.new $inner_i32 …)`
  against a callee whose result had already moved. D923 refused a redirect AT
  `sFieldTgtStructIdx` because that is the one home for every nested field's target, and
  superseding there moves EVERY `{r: i32}`-typed nested field in the program. Correct — so the
  answer is the same one scoped to the literal, written where that literal's own row is minted.

* **RUNG 3, THE MINT GATE.** `anonLeafMergeField` asks whether a literal's row is narrower than
  the join THE LITERAL BELONGS TO, and answers "" for a carrier, which belongs to none.
  `anonLeafNestedMergedField` asks it one layer down, so the carrier re-mints and supersedes.

* **RUNG 4, THE PARAM SLOT.** `paramStructIdxFinal` takes the supersede only for a MARKED
  param — gated on the mark and never on the row, because a merge's narrow row is a FIELD-SET
  row and an ungated redirect would move every same-shaped parameter in the program.

* **D923's HAZARD WAS TESTED AND DOES NOT MATERIALISE, which is rung 4's doing.** That row
  warns that "a second `{r: {r: 3}}` literal whose inner is not a family member would then mint
  narrow into a merged field". Run: the second carrier prints `3` and the family prints `7`.
  The mark-gate is why — `keep`'s param is never marked, so it keeps the narrow row, and the
  second literal builds against it.

* **THE ERROR MOVED ONCE PER RUNG, which is how the four were found.** Reader alone:
  `rd` -> `src` (D923's measurement, silent to silent). Owner added: the box mints and the
  merged inner and new outer rows appear, `src`'s RESULT moves, its PARAM does not. Mint gate:
  the carrier gets its row. Param mark: prints 7. No rung was guessed.

Repro (now RUNS, printing `7`):

    function src(p: {r: {r: i32}}): {r: i32} | null {
      const v = p.r
      v
    }
    function rd() {
      const g1 = src({ r: { r: 7 } }) ?? { r: "s" }
      print(g1.r)
    }
    rd()
    // vl check rc 0; runs, prints 7.
