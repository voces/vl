### D1660 — twelve memo staleness keys read only epochs and table lengths, so an in-place fill of an input is invisible to them, and only a measurement says none is stale today

**runs today and must keep running · filed 2026-09-05 against master `55f25c3e7` · D1655's family · 0 corpus cells**

Repro:

    type Circle = { r: i32 }
    type Sq = { s: i32 }
    type Shape = Circle | Sq
    function deliver() {
      const b: Shape[] = [{ r: 7 }]
      return b
    }
    const v = deliver()
    const e0 = v[0]
    if e0 is Circle { print(e0.r) } else { print(0) }
    // vl check: rc 0.  vl run: prints 7.

#### Mechanism

D1655's witness, kept as the family's REFUTATION PIN: it is the one program that proves the
probe below can fire. `globalCellKind` stamped its answers on the arena epoch and on four
collect-table LENGTHS, and `fRetKind` is pushed by `buildFnMap` and refined IN PLACE by
`computeRetInference`, so a query from an earlier pass froze the seeded `"i32"`. #2690's
`emitPassGen` fixed that one; twelve sibling memos still carry keys of the same shape.

`scripts/memo-generation-probe.py --list` derives the population (121 stamp variables, 34 read
by a guard, 27 rows) and fails on an unclassified stamp. Twelve rows are graded `probe`, which
means their safety is a measurement rather than an argument: `--run --only=<id>` disables one
memo — strictly stronger than stamping it on `emitPassGen` — and grades it. **Seventeen of
nineteen disable edits leave the compiler's own codegen of itself byte-identical to master's
fixpoint, and the eleven graded against the corpus leave every cell in its class.** The
control is this row's own witness: with #2690's two lines reverted it grades SILENT, and the
probe moves it to RUNS.

Two rows are unmeasured: disabling `repKeyMemo` or `repTree` exhausts the GC heap on the
compiler's own source, which is what the memo exists for (D1513's class). So the family is
measured-clean, not proven clean, and the bound is only as wide as those populations.
`docs/internals/memo-generation-census-2026-09.md` holds the per-row table.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| `if e0 is Circle` deleted | runs | **yes — it is what asks before inference** |
| the inferred return annotated `: Shape[]` | runs | **yes** |
| #2690's `gckGenP` compare reverted | rc 0, invalid wasm | **yes — the control** |
| any single other memo disabled by the probe | runs | no (self-compile byte-identical, corpus unmoved) |

#### Grading list

`python3 scripts/memo-generation-probe.py --list` must exit 0 — a new memo joins the table or
the row fails, and `tests/vl_memo_generation_test.ts` is that check on every PR. When a
`probe` row's memo changes, re-run `--run --only=<id> --before <master seed>` and re-grade its
self-compile and the corpus; this row flips the day one of them moves.
