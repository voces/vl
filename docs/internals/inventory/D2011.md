### D2011 — a closure in a module-scope `for` body that captures a body-local COPIED from the loop variable gets an `i32` env field

**check-clean invalid wasm · `vl check` rc 0, then the engine refuses the module (`expected (ref $type), found i32` inside `f`) · clause 1 · the residue of D1480 after #3018 · the same loop inside a function runs · filed 2026-09-22 against master `5c4fc52ba`**

Repro:

    for name in ["red"] {
      const g = name
      const f = () => g
      print(f())
    }

#### Mechanism

Not traced. A prediction for the fix: `captureValKind` types the capture `g` through
`letInitCellKind(g, frame)`, and at module scope the frame is -1, where the initialiser's
`Ident` read of the loop variable `name` has no local to classify. D2010 fixed the direct
capture of the loop variable (`captureLoopVarIterOf`); the binding one step removed from it goes
through the let ladder instead, which has no module-scope loop-variable rung.

#### Ablation

| ingredient changed | outcome |
| --- | --- |
| capture `name` directly (`() => name`) | runs (D1480, closed) |
| `const g = "blue"` (not derived from the loop variable) | runs |
| the loop inside `function go()` | runs |
| as filed | check-clean invalid wasm |
