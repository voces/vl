### D2165 — [CLOSED 2026-09-23] a method-call receiver skipped the argument seam, so a widened container passed as `self` was check-clean invalid wasm

**closed as `now a loud check reject` · was `check-clean invalid wasm` · found 2026-09-23 by the no-implicit-container-widening inventory · filed against master `9fd219de7`**

Repro:

    type K = "a" | "b"
    function jn(self: string[]): i32 { return self.length }
    const ks: K[] = ["a", "b"]
    print(ks.jn())
    // was: vl check rc 0, vl run invalid module. Now: K[] is not string[]: a container never widens implicitly

#### Mechanism

`ufcsCallTy` gated the receiver with bare `assignable`, the dispatch question ("is this
`self`-function a candidate at all"), and never asked `assignableExpr`, the seam every
argument, binding and return delivery goes through and where the element-storage refusal and
the covariant-write refusals live. So `jn(ks)` refused and `ks.jn()` compiled a `K[]` into a
`string[]` slot. A second half hid behind the first: the write-state closure
(`covarValueWriteState`) followed an argument into its parameter but not a receiver into
`self`, so a write through `self` inside a method did not count as a write through the
receiver. Both are closed. The receiver takes the seam's verdict after dispatch, for a plain
and an optional-chain (`xs?.f()`) receiver, with a generic `self` substituted under the
receiver's binding; a receiver that is still a hole (inside an un-annotated body) is re-asked
at the call that pins it (`ufcsRecvPinSeam`). The closure has a receiver edge (`cwGrowRecv`),
and a nullable list handle (`Circle[] | null`) now fixes its list's element storage, which the
split-storage test had read as "no list".

#### Ablation

| spelling | before | now |
| --- | --- | --- |
| `ks.jn()` with `jn(self: string[])` (the witness) | invalid module | check reject, the argument's message |
| `ks.join(",")` via `std:str` | invalid module | check reject |
| `xs.sm()` with `sm(self: f64[])` writing `self[0]`, `xs: i32[]` | invalid module | check reject |
| `const b: f64[] = a; b.wr()` with `wr(self: f64[])` writing `self` | invalid module | check reject at the binding |
| `xs.rd()` with `rd(self: f64[])` only reading | runs, prints 1 | runs, prints 1 (the converting copy) |
| `roView().concat([3])`, generic `concat(self: readonly T[], …)` | runs | runs |
| the direct spellings `jn(ks)`, `sm(xs)` | check reject | check reject, unchanged |
| `ks?.jn()` over a `K[] \| null` | invalid module | check reject |
| `xs?.sm()` over an `i32[] \| null`, `sm` writing `self` | runs, prints the stale `1` | check reject |
| `cs?.put()` over a `Circle[] \| null`, `put(self: Shape[])` writing `self` | invalid module | check reject |
| `if cs != null { put(cs) }`, the direct twin of the line above | invalid module | check reject |
| `xs.nl()` with `nl<T>(self: (T \| null)[])` writing `null` | loud emit reject | check reject, as `nl(xs)` |
| `function g(x) { x.sm() }` then `g(zs)`, `zs: i32[]` | invalid module | check reject at `g(zs)` |
| `function g(x) { return x.rd() }` then `g(zs)`, read-only | runs | runs |

#### Grading list

The receiver-vs-argument grid: every array pair of the widening inventory, spelled `a.f()`
and `f(a)` against the same `function f(self: D)`, read-only, written through `self` and
written through the original. The two spellings agree on every array pair. A map receiver
never dispatches (`unknown property`), which is a separate question.
