### D1920 — `structIndexOfExpr`'s nominal rung is a PRECEDENCE too: its bridge is already rung 3, and promoting it turns four modules into loud refusals

**runs today and must keep running · REFUTATION PIN · filed 2026-09-07 against master `a0838627d`**

Repro (`tests/cases/structs/nested-null-hole-list-return-twin.vl`, verbatim):

    type T0 = {f: {f: i64 | null}}
    function useTwin(p: T0[]) {
      const t0 = p[0].f.f
      if (t0 != null) { print(t0) } else { print("NULL") }
    }
    function mk(): {f: {f: i64 | null}}[] {
      return [{ f: { f: null } }]
    }
    function go() { useTwin(mk()) }
    go()
    // prints NULL

#### Mechanism

The fourth of ROADMAP row 25's held-back sites, and **the label the earlier sweep gave it no
longer points at it**. `structIndexOfExpr#2` is positional; tranche 1 converted the first
occurrence and renumbered the rest, so the four loud refusals belong to today's
`structIndexOfExpr` at `emit_classify.vl:26231`, not to today's `#2` at 26325 (which is inert
on all six modules — that one is §11.8's).

The mechanism is D1858's: the nominal call is a rung in a ladder whose structural bridge is
already downstream. The ladder reads nominal → arena (`repRowOfTyStruct`) → a GATED
`structIndexOfTypeName`, and converting rung 1 promotes the fieldset scan above the arena rung.

**The order test proves it without converting anything.** Leaving the nominal call as it is and
asking the function's own existing rung-3 bridge one place earlier reproduces all four
refusals, message for message:

| module | base | rung REORDERED | rung 1 converted |
| --- | --- | --- | --- |
| `declared-twin-inline-elem-array-param` | builds | `bare null needs a struct-typed context` | same |
| `declared-twin-inline-elem-map-field-param` | builds | `index receiver is not an array or string` | same |
| `nested-null-hole-list-return-twin` | builds | `field access receiver is not a struct` | same |
| `nested-union-softening-list-return-twin` | builds | `field access receiver is not a struct` | same |

Unlike D1858's site, these land LOUD rather than as invalid wasm — the promoted row is wrong
in a way a later rung notices. That is a better failure and still a lost build.

#### Ablation

The rung fires on **1,368 queries in 20 `tests/cases` modules and 2,295 in 55 corpus modules**,
on names that are structural renderings: `{y:i32,x:i32}` ×424, `{f:{f:f32|string|{q:i64}}}`
×198, `{r:i32|null}` ×972, `{r:{c2:i32}|{s2:i32}}` ×620. The arena rung is what tells a nested
twin from its inner row, and the promoted fieldset scan cannot.

#### Grading list

A pin: these four modules must keep building. They flip the day someone converts rung 1 or
moves the bridge up. `rep-descriptor-campaign.md` §11.8 carries the precedence family — this
site and `rlElemStructRow` (D1858) are both of it — and the flip-count bar that the sibling
site at 26325 forced.
