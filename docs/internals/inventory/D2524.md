### D2524 — an un-annotated list parameter pinned only by `push(null)` checks against a `(string | null)[]` argument and then has no rep for the `null`

**loud emit reject · clause 2 · pre-existing on master `ca544493e` · found by [D2506](D2506.md)'s ruling question · filed 2026-09-25 against master `ca544493e`**

Repro:

    function use(v) {
      v.push(null)
      print(v.length)
    }
    const xs: (string | null)[] = []
    use(xs)
    // vl check: rc 0.  vl build: emitProgram: bare null needs a struct-typed context

#### Mechanism

Not traced. The checker accepts the call, so the list hole was not pinned to `null` the way
[D2506](D2506.md)'s map hole is; the emitter then lowers the `push(null)` in the instance with
no element rep to build the `null` at. Which of the two agrees with the language is D2506's
ruling; this row is the emit half, a refusal after a clean check either way.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| `v` annotated `(string \| null)[]` | prints `1` | **yes** — the inferred parameter |

#### Grading list

* `i32 | null` and a struct `| null` element.
