### D1707 — an inline nested shape whose field is typed as a union ARM has no rep, though the same field one level out codes 15 through the arm's cross-table twin

**loud emit reject · filed 2026-09-06 against master `1efd26c50` + D1692 · residue of D1692 ·
0 corpus cells**

Repro:

    type C = { r: i32 }
    type S = { s: i32 }
    type Shape = C | S
    type W = { f: { xs: C } }
    const w: W = { f: { xs: { r: 4 } } }
    print(w.f.xs.r)
    // vl check: rc 0.  vl run: emit error,
    // emitProgram: nested-struct field element type is not interned

Three neighbouring spellings print `4`: the field one level out (`type W = { f: C }`), the
named inner shape (`type Inner = { xs: C }`, `type W = { f: Inner }`), and the wholly inferred
literal (`const w = { f: { xs: mk() } }` over `function mk(): C`).

#### Mechanism

Predicted, not yet probed: D761 gives an arm-typed field the struct row the arm shares, and
`internInlineShapeTy` applies that rung — `if fc == 15 { armFieldStructRow(...) }` — to
rename the field's recorded element. `armFieldStructRow` reads `variantStructHeapTwinAt`,
which resolves through the struct table, and `collectNestedFieldShapes` runs BEFORE `collectS`
builds it. So the same pass-order shape D1692 closed for codes 5 and 28, one code over: the
recorded name stays the arm's, which `structIndexByName` cannot resolve at emit. The
prediction is on D1692's grading list; confirm it with a probe before fixing.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the nesting (`type W = { f: C }`) | runs, prints `4` | **yes** |
| the inline inner shape (`type Inner = { xs: C }`) | runs, prints `4` | **yes** — the rescue |
| the `type W` declaration (wholly inferred) | runs, prints `4` | **yes** |
| the union (`type Shape = C \| S` deleted, `C` a plain struct) | runs, prints `4` | **yes** |
| the construction (`type W` alone) | still refuses, same sentence | no |

#### Grading list

The four spellings above, the `C | null` niche face of the same field, and the 40-type inner
field sweep in D1692's PR body, whose only two remaining refusals are this row's `C` and
`C | null`.
