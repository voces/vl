### D2578 — `__array_new_default__` over a generic `T[]` at a `string` instance builds, where the direct spelling is refused

**check-clean invalid wasm · clause 1 · found closing D1472 (its `string` instance) · filed 2026-09-26 against master `216770428`**

Repro:

    function mk<T>(n: i32, _seed: T): T[] {
      const xs: T[] = __array_new_default__(n)
      return xs
    }
    print(mk(2, "x").length)
    // vl check: rc 0.  vl run: invalid module inside `mk`, "type mismatch: expected (ref $type),
    // found (ref $type)"

#### Mechanism

Not located. The direct spelling, `const xs: string[] = __array_new_default__(n)`, is a check
error: `string has none — use __array_new__(n, fill)`. The checker asks that rule of the call's
element type, and in the generic body the element is `T`, so the rule is not asked at the pin.
The numeric instances run since D1472's close; this is the instance whose element has no default,
and the owed outcome is the direct spelling's refusal, not a build.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the generic (`xs: string[]`) | loud check reject | **yes** |
| the `string` instance (`mk(2, 1.5)`) | runs, prints `2` | **yes** |

#### Grading list

* A record instance and a nullable-string instance, which have no default either.
