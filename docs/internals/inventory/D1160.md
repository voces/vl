### D1160 — [CLOSED 2026-09-02] DUPLICATE of [D1140](#d1140), filed 40 minutes apart by two workers on the same defect

**runs, prints `2` — CLOSED 2026-09-02 by [D1174](#d1174)'s fix, together with [D1140](#d1140) ·
was a loud emit reject, clause 2 · **the independent grading did exactly what it was filed to
do**: this row reported MOVED in the same run as D1140, so the duplicate could not be left
reading as live · `emitProgram: narrowed union atom has no value box` fired because nothing
else in the program named `i32 | null`, so its box types were never interned — `collectU` now
registers the union from the `.pop` node's own recorded type**

**THE TWO FACES REALLY DO DIFFER, and that is the one thing the duplication bought.** This
row's `print(a.pop())` is a LOUD refusal; D1140's unused `const v = xs.pop()` is check-clean
invalid wasm. So the gap spans clause 2 and clause 1, and a fix graded on either witness
alone would report the other as untouched.

Repro (kept so this row stays gradeable; it is D1140's witness):

    const a: i32[] = [1, 2]
    print(a.pop())

* **WHY THIS ROW SURVIVES AS A POINTER RATHER THAN A DELETION.** Two workers filed one
  defect 40 minutes apart, which is the exact shape that produced D1009 / D1009-N and left a
  stale row reading as live for a day. Deleting the second copy hides that it happened;
  pointing it at the first makes the duplication legible and keeps every citation of D1160
  resolving.

* **AND THIS COPY WAS THE WORSE ONE, on a point that matters.** D1160 listed
  `a.pop() ?? 0` as a "control that runs" and used it to argue a consumer already exists,
  bounding the fix. **That is true only at `i32`.** `emitPopOr` is an i32-family-only fused
  lowering, so `??` is invalid wasm at `i64[]`, and a loud emit reject at `f64[]` and
  `f32[]` — a FIFTH outcome for one method, which D1140 measured and this row asserted away.
  A control that was not swept across the reps it claims to cover is not a control.

* **What D1140 has that this did not**: the four delivery spellings graded separately
  (unused binding / bound-then-narrowed / annotated `T | null` / `?? d`), and the reason the
  two working reps work — `string[]` rides a nullable ref and `boolean[]` rides the i32
  niche, so every rep with no null representation in its own valtype is broken.

* **What this row contributed and D1140 keeps**: that the literal is D964's own cited
  example of a refusal reading as an internal invariant while being a clause-2 violation,
  and that the statement/result split also qualifies [D1131](#d1131)'s control column.

* **AND THE DUPLICATION ITSELF MEASURED SOMETHING NEITHER ROW WOULD HAVE.** Grading the two
  witnesses side by side shows the gap spans BOTH clauses: consuming the pop result through
  `print` is a loud clause-2 refusal, while binding it and not using it is clause-1 invalid
  wasm. A fix graded on either witness alone would report the other as untouched.
