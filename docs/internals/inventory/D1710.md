### D1710 — the anonymous mint's `| null` niche has no LITERAL-union family, so every nullable litunion field spelling is refused while its non-null twin runs

**loud emit reject · filed 2026-09-06 against master `fde7f41de` + D1691 · residue of D1691:
its subtraction made this family loud instead of silent, and nothing built its codes ·
0 corpus cells**

Repro:

    type K = "a" | "b"
    function mk(): K[] | null { return ["a"] }
    const w = { xs: mk() }
    print(1)
    // vl check: rc 0.  vl run: emit error,
    // emitProgram: object literal matches no union variant

Three spellings share it, all on the UN-ANNOTATED face and all with a running non-null twin:
`K | null` (the atom niche, code 30), `K[] | null` (above), and `(K | null)[] | null`. Every
annotated face runs — `const w: { xs: K[] | null } = { xs: mk() }` prints `1`.

#### Mechanism

Predicted, not probed. `anonNulFieldCodeOfTy` is the only path a nullable anonymous field has,
since `anonTySpellable` declines a `TyNullable` and the render below it never runs. Its ladder
codes a niche from the inner arena kind, and the literal-union family reaches it as a `TyUnion`
of `TyLit`s — which `tyElemIsRefListElem` now subtracts, correctly, because the atom backing is
not a ref. Nothing then claims it: there is no code-30 arm for the alias and no atom-list arm
for `K[] | null`, so the field takes no code and the holder no row. The declared twin gets all
three from the SPELLING ladder (`nameIsNulLitUnion`, `nameIsNulI32List`), which is why only
this face refuses. Probe the ladder against this failing witness before fixing.

**D1691's close is what made this loud.** Before it, `K[] | null` took the ref wrapper on
trust and the module failed validation check-clean; the subtraction that fixed that
miscompile left the family with no code at all. That is the trade recorded, not hidden: a
clause-1 silent became a clause-2 refusal, and this row is the rest of the debt.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the `\| null` on the field (`K[]`, `K`, `(K \| null)[]`) | runs | **yes** |
| the annotation added (`const w: { xs: K[] \| null } = …`) | runs | **yes** — the un-annotated face is the row |
| the literal union (`A[] \| null` over a struct) | runs | **yes** |
| the read of the field (the mint alone) | still refuses | no |

#### Grading list

The three spellings above at both faces, their non-null twins as pins, and the litunion rows
of the 192-cell element-kind grid in D1691's PR — `litunion/code28/*` and
`litunion_nul/code28/*`, which must not return to `SILENT`. A close here should re-run
`anon-nullable-list-field-nullable-ref-element.matrix.vl`, whose element is the ref twin of
this row's.
