### D2249 — a `null` stored into a `u8[] | null` record field traps on the store

**closed 2026-09-24 by D2295 — the witness prints `true` · was loads-then-traps, `wasm trap: null reference` at the record literal, clause 1 · the bare-`null` initialiser and the `{[K]: u8[] | null}` map read, both loud emit rejects when filed, run · list-kind grid `nullable-field` u8 56 → 64 of 64 · fixture `tests/cases/lists/u8-nullable-list-element-and-field.vl` · found by the D2247 value-type grid · filed 2026-09-23 against master `91d4210a0`**

Repro:

    type W = { f: u8[] | null }
    const x: u8[] | null = null
    const w: W = { f: x }
    print(w.f == null)
    // vl check: rc 0.  vl run (as filed): wasm trap, null reference. Now prints true.

#### Mechanism

Confirmed by the close: `u8[] | null` and `u8[]` shared field code 35, whose storage is a
non-null `(ref …)` and whose store owes `ref.as_non_null` (`fieldCodeVKind(35)` is `u8list`),
which is the trap. D2295 gives the niche code 36. The filing's prediction follows. The `f64[] | null` spelling of the same
program runs, and the nullable distinct-backing list field codes cover string, f64, i64 and
f32 leaves but not the packed byte list, so the field is most likely stored at the non-null
`u8[]` code and the literal recovers the value with `ref.as_non_null`. [D2239](D2239.md) is the
same leaf missing from the `(u8[] | null)[]` element path. A prediction, to be graded by the
close.

#### Grading list

`type W = { f: u8[] | null }` with `{ f: null }` (loud emit reject), `{[string]: u8[] | null}`
with `print(m["k"] != null)` (loud emit reject), and the `u8l` rows of the D2247 grid
(`field` over `{[K]: u8[]}` traps; bind, compare, field and if-arm over `{[K]: u8[] | null}`
refuse or are invalid wasm).
