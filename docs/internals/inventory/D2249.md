### D2249 — a `null` stored into a `u8[] | null` record field traps on the store

**loads then traps · `vl check` rc 0, then `wasm trap: null reference` at the record literal, clause 1 · the same field initialised with a bare `null` is a loud emit reject (`bare null needs a struct-typed context`), and a `{[K]: u8[] | null}` map read's `!= null` refuses the same way · found by the D2247 value-type grid · filed 2026-09-23 against master `91d4210a0`**

Repro:

    type W = { f: u8[] | null }
    const x: u8[] | null = null
    const w: W = { f: x }
    print(w.f == null)
    // vl check: rc 0.  vl run: wasm trap, null reference

#### Mechanism

Not yet ablated past the sibling comparison below. The `f64[] | null` spelling of the same
program runs, and the nullable distinct-backing list field codes cover string, f64, i64 and
f32 leaves but not the packed byte list, so the field is most likely stored at the non-null
`u8[]` code and the literal recovers the value with `ref.as_non_null`. [D2239](D2239.md) is the
same leaf missing from the `(u8[] | null)[]` element path. A prediction, to be graded by the
close.

#### Grading list

`type W = { f: u8[] | null }` with `{ f: null }` (loud emit reject), `{[string]: u8[] | null}`
with `print(m["k"] != null)` (loud emit reject), and the `u8l` rows of the D2247 grid
(`field` over `{[K]: u8[]}` traps; bind, compare, field and if-arm over `{[K]: u8[] | null}`
refuse or are invalid wasm).
