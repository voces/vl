### D1625 — a closure array from an un-annotated inferred return, bound at MODULE scope, mis-reps its element

**check-clean invalid wasm · 1 cell (`return_inferred` × un-annotated) of the `container-read-callee` matrix · filed 2026-09-05 against master `8a7b2872`**

Repro:

    function deliver() {
      return [(n: i32) => n + 1]
    }
    const v = deliver()
    const g = (m: i32) => v[0](m)
    print(g(54))
    // vl check: rc 0.  vl run: the emitted module failed to validate inside `g`

#### Mechanism

The same program written INSIDE a function runs, and that is the whole ablation: wrap the
last three lines in `function go() { … }` and it prints 55. So the ingredient is the binding
being a module GLOBAL rather than a frame local, over an element whose rep came from an
UN-ANNOTATED inferred return.

What it is NOT: neither the resolved `$fnsig` key nor the ref-list element slot differs from
the working spellings. Probing both at the tail-value decision reads `key=[i>i] rlSlot=0` for
this cell, for the annotated-return control that runs, and for the plain-local witness that
runs — all three identical. So the disagreement is downstream of the call's own resolution,
in how the module-global cell for an inferred-return ref list is built.

**This row records a price D1623 PAID.** Before D1623 this spelling was a loud
`emitProgram: bare call statement is not supported`; D1623 routes the tail through
`emitReturnExit` and the mis-rep becomes reachable. Every other cell of both position
templates is loud or running, and `return_inferred` is skipped by name in
`container-read-callee.matrix.vl` until this closes.

#### Ablation

Every row is `vl check` rc 0.

| spelling | outcome |
| --- | --- |
| the witness (module scope, inferred return, direct element call) | **check-clean invalid wasm** |
| the identical program wrapped in `function go() { … }` | runs, 55 |
| `function deliver(): ((i32) => i32)[]` (annotated return) | runs, 55 |
| `const e = v[0]` bound first, then `e(m)` | runs, 55 |
| `print(v[0](54))` — expression position, not a tail | runs, 55 |
| `print(v.length + 54)` — the list read without a call | runs, 55 |

Needed: module scope AND the un-annotated inferred return AND the call being the lambda's
tail. Any one of the three removed and it runs.

#### Grading list

* The five running spellings above, each of which must keep running.
* Deleting the `@@SKIP@@` in `scripts/capability-probes/matrix/container-read-callee.matrix.vl`
  is part of the close, and the matrix must then grade 54 of 54 with no silent cell.
* The frame-scoped twin, since a fix reaching only module scope would leave the two reps
  disagreeing in the other direction.
