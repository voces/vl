### D973 — [CLOSED 2026-08-31 in #2181] `==` over a STRUCT UNION (`A | B` of declared shapes): compared through the VARIANT table, because the struct table is empty

**closed as `runs` · was a loud emit reject conceding capability (``emitProgram: `==` over a struct union is not supported yet``) after `vl check` rc 0 — a clause-2 violation by construction · CITED 15x AND NEVER FILED (citations outside the refs test's own list, counted on `2fb8b271`): `compiler/wasmEmit.vl`, `compiler/emit_bytes.vl`, `compiler/emit_classify.vl`, two capability probes, `docs/identity-design.md`, the two identity critiques and `D972.md` all name this id; the row is written 2026-09-03 against master `2fb8b271` from those citations and #2181, and the witness re-run · fixture `tests/cases/unions/struct-union-equality.vl`**

Repro (runs, prints `true`, `false`, `false`):

    type A = { a: i32 }
    type B = { b: i32 }

    function mk(n: i32): A | B {
      if n > 0 { return { a: n } }
      { b: 2 }
    }

    print(mk(1) == mk(1))
    print(mk(1) == mk(0))
    print(mk(1) == mk(3))

#### Mechanism

`A | B` of declared shapes rides the SAME `{tag, payload}` box a value union does
(disassembled in #2181), so the dispatch is the value union's: tags unequal means different
arms and therefore unequal; tags equal selects ONE arm and compares its fields.

Three things had to be found, each costing a build. The refusal that fires for `u == v` is
**`emitUnionUnionEq`'s**, not `emitUnionConcreteEq`'s. `structIdxOfElemName` answers -1 for an
arm, because arms register as VARIANTS. And the obvious repair — recover the struct row by
matching `sHeapIdx` against `uVarHeap[vi]` — cannot work, because **`sHeapIdx.length` is ZERO**:
`emit_state`'s own header says the struct table stays empty when a union is declared, since the
union path owns the types. So `emitStructEqRec` was the wrong tool and the compare reads the
arms' fields from `uFieldNames` / `uFieldTypes` instead.

It is tried BEFORE the re-readability gate, because it does not need one: it evaluates each
operand once into its own slot. That gate exists for the value-union lowering, which re-reads
both sides per arm — so `mk3(3) == mk3(3)` works here while the same spelling needed D972's tee
on the value-union path.

#### Ablation

| ingredient | outcome | needed? |
| --- | --- | --- |
| the UNION (one declared shape: `mk(1) == mk(2)` over `A` alone) | runs; #2181 records it ran BEFORE this row too | **yes** |
| both operands non-binding calls (`mk(1) == mk(1)`) | runs — no tee needed on this path | no |
| a three-arm union (`A \| B \| C`) | runs, so the tag chain is not a two-arm special case | no |
| a STRING arm field | runs — codes 0 and 3 were what this row built | no |

#### Grading list

* `tests/cases/unions/struct-union-equality.vl` — eight `@log` lines, the fixture this row
  shipped with.
* D974 built the NESTED-shape field on top of this; grade both together.
* **The ref/list/map DECLINE this row left is GONE, measured 2026-09-03.** #2181's own
  sentence — "a ref / list / map field DECLINES and keeps the loud refusal" — no longer
  describes the tree: `type A = { a: i32[] }` beside `type B = { b: i32 }` runs and
  discriminates (`true`, `false`, `false`), and `scripts/capability-probes/struct-union-eq-ref-field.vl`
  runs. What still refuses at `emitProgram: unsupported struct field type in equality` is
  narrower — `nullable-ref-list-struct-field-equality.vl` and
  `value-union-box-struct-field-equality.vl` in the probe suite.
