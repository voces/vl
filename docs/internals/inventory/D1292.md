### D1292 — a `fn | null` FIELD is the second ref niche held out of the guard table: its closure-eq frame is reserved per NON-NULL code

**closed — `shapeHasCloField` resolves a niche field through `nulFieldInnerCode` before asking for code 14, the way the LIST frame pre-pass already did, and `22 -> 14` enters the niche table · was a loud emit reject at check rc 0, clause 2 · ZERO corpus cells · found 2026-09-03 the same way as [D1291](#d1291)**

Repro (runs, prints `true`):

    type Fn1 = (i32) => i32
    type A = { d: Fn1 | null }
    const f: Fn1 = (n: i32) => n
    const a: A = { d: f }
    const b: A = { d: f }
    print(a == b)

* **THE RECORDED PRICE STILL HOLDS; THE SECOND CAUSE IT NAMED DOES NOT.** Re-measured
  2026-09-03: `22 -> 14` alone still grades `unknown local 12: local index out of bounds` at
  the plain struct spelling and `expected (ref null $type), found (ref $type)` at the arm one,
  both verbatim. The row read those as TWO rungs — the frame scan, and `emitCloEqCore`'s stash
  pair being typed non-null. Only the FIRST is real: with the frame scan fixed, both spellings
  run and the stash pair was never touched. The arm-spelling message was the same missing
  frame presenting differently, not a second mechanism.

* **A RESERVATION SCAN MAY OVER-RESERVE AND MAY NOT UNDER-RESERVE**, so it has to see every
  code that reaches the core. `shapeHasCloField` tested `code == 14` and nothing else, while
  the guard opens the identity core for code 22 as well — the same one-home pairing
  `leqNoteBin` already had for the list frames.

* **Graded.** `f == g` at the direct spelling still runs; both spellings of the field compare
  equal, unequal, null-vs-value, value-vs-null and null-vs-null correctly.
