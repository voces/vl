### D2298 — `==` over two `u8[][]` found no compare core, because the ref-list element table had no u8 row for equality

**closed 2026-09-24 — the witness prints `true` · was a loud emit reject (`the two operands of this list == do not resolve to one comparable list rep`), clause 2 · fixture `tests/cases/closures/list-literal-into-function-value-param.vl` · found while grading D2295 · filed against master `a295cf301`**

Repro:

    const a: u8[][] = [[1, 2], [3]]
    const c: u8[][] = [[1, 2], [3]]
    print(a == c)
    // vl check: rc 0. vl run (as filed): emit error. Now prints true.

#### Mechanism

`eqgElemClass` and `eqgInnerKind` translated a ref-list element code to a generic-compare kind by
hand and listed codes 4, 6, 7, 8, 9 and 10, but not 11, the packed byte list. The compare core
for `u8[]` (kind 10) already existed for a `u8[]` record field. Both now decode through
`rlElemListRep` and `eqListKindOfListRep`, D2295's encoders.

#### Ablation

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| `f32[][]` | runs | **yes** |
| `u8[] == u8[]` | runs | **yes** (the nesting) |

#### Grading list

The fixture, and a mutated element (`c[1][0] = 300` computed) reading `false` and then `44`.
