### D1291 — an `S[] | null` FIELD is the one ref niche whose guarded compare is check-clean invalid wasm, so it is held OUT of the niche table and refuses loudly

**closed — `eqChainRecover` is scoped to the chain that opened it: a list core starts a FRESH chain and clears it, so `28 -> 5` enters the niche table · was a loud emit reject at check rc 0, clause 2 · ZERO corpus cells · found 2026-09-03 by MEASURING every entry of [D1180](#d1180)'s `nulFieldInnerCode` under the guard**

Repro (runs, prints `true`):

    type Inner = { p: i32 }
    type A = { d: Inner[] | null }
    const a: A = { d: [{ p: 5 }] }
    const b: A = { d: [{ p: 5 }] }
    print(a == b)

* **THE FILED MECHANISM WAS THE WRONG RUNG, and the recorded PRICE was right.** Re-measured
  2026-09-03: `28 -> 5` alone still grades `type mismatch: expected ref but found i32`,
  verbatim. But the code-5 arm's two field-path re-reads are correct and correctly recovered
  — the disassembly puts the bad `ref.as_non_null` on `(struct.get $0 0 (local.get $16))`,
  i.e. on the `p: i32` leaf INSIDE the nested element compare. `eqChainRecover` is a module
  global that stayed true across the whole recursive subtree, and `emitChainRead` appends the
  recover to whatever it read.

* **THE FLAG NAMES THE CURRENT CHAIN.** `emitListEqAnyRep` saves, clears and restores it: both
  wrappers are already on the stack and were recovered by the arm that read them, and every
  read the core makes below is its own — `backing[i]`, then a struct element's fields off the
  stash pair. One save/clear/restore serves every list rep the niche table maps to.

* **Graded.** All five sibling niches still run (`string | null`, `i32[] | null`,
  `string[] | null`, `f64[] | null`, `i64[] | null`, `f32[] | null`), null case included.
  Position matrix `matrix/nul-ref-list-field-eq.matrix.vl`: 44/50, 0 silent, 0 runs-lost.
