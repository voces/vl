### D2431 — a module-level `const` bound to a map read is initialised ahead of the statements above it when a `?.` call statement precedes them

**check-clean silently wrong · found while grading A15's `Map<K, V>` position matrix (the nested-map block) · filed 2026-09-25 against master `a277bf7b0`**

Repro:

    let xs: i32[] | null = [1]
    xs?.push(4)
    const n: {[i32]: string} = Map()
    n[1] = "v"
    const q = n[1]
    print(q ?? "null!")
    // PRINTS null!
    // vl check: rc 0.  vl run prints `null!`; the program means `v`.

#### Mechanism

The start function evaluates `const q = n[1]` straight after `n`'s own initialiser and
before `xs?.push(4)` and `n[1] = "v"` run: the disassembly shows `global.set` of `q` (the
map probe on a still-empty `n`) ahead of the optional push and the index store. So the
module-level statement order the program wrote is not the order its globals are
initialised in once a `?.` call statement stands at top level. Not traced further; the
ordering decision is the place to look (`topStmtOrderKey` and the global-init pass).

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| `xs?.push(4)` replaced by `if xs != null { xs.push(4) }` | prints `v` | **yes** |
| the map declared ABOVE the `?.push` line | prints `v` | **yes** |
| the whole program inside a `function main()` | prints `v` | **yes** — module level only |
| `const q = n[1]` replaced by a direct `print(n[1] ?? "null!")` | prints `v` | **yes** — a module-level binding |
| `const q = n[1]` replaced by `let q = null` then `q = n[1]` | prints `v` | **yes** — an initialiser, not an assignment |
| `q`'s annotation added (`const q: string \| null = n[1]`) | still `null!` | no |
| the map read replaced by a list `.get(0)` | prints `v` | **yes** |
| `xs` initialised `null` instead of `[1]` | still wrong | no |

#### Grading list

* The nested-map block of `tests/cases/maps/map-set-type-names-positions.vl`, which ran
  into this when it carried a `lists["xs"]?.push(4)` line; the fixture now avoids it.
* The same shape with a string-keyed map (`{[string]: i32[]}` receiver for the `?.push`,
  `{[string]: string}` for the read) — reproduces.
