### D2648 — an un-annotated function whose returns join a map with another member floors at check, at every key rep

**loud check reject · clause 2 · the `return_inferred` cells of 51 of the D2519 lane's 56 templates (102 cells, string keys included; the five string-keyed `| null` templates run, D937) · identical on master · filed 2026-09-26 against master `19dfb80af`**

Repro:

    function mk(): {[i32]: i32} {
      const m: {[i32]: i32} = Map()
      m.set(1, 10)
      m
    }
    function pick(n: i32) {
      if n > 0 { return mk() }
      return 7
    }
    const v = pick(1)
    if v is {[i32]: i32} { print(v.get(1) ?? -1) } else { print(v) }
    // vl check: rc 1 — 'pick' infers the union return type {[i32]: i32} | i32 — type-valid, but an inferred return of this shape is not yet supported by codegen; annotate the return type

#### Mechanism

The robustness floor in `checkFnDecl` accepts an inferred union return only when a recorder
names it for the emitter: `valueUnionRetName` (value atoms), `variantBoxUnionRetName` and
`structUnionRetName` (records). None of the three admits a map member (`tyIsValueUnion` declines
it), so the join floors even though the annotated spelling `(): {[i32]: i32} | i32` lowers at
every position. The nullable twin (`{[i32]: i32} | null`) floors through the `TyNullable` arm:
D937 built the inferred nullable-map return for a string key only.

The join of TWO maps is worse, and the same missing lowering: `{[i32]: i32} | {[i64]: i32}`
(or `| {[string]: i32}`) passes the floor, and an inferred return or an if-expression join of
the two lowers as whichever map struct the first branch has, so the module is check-clean
invalid wasm (#3189's review P1, identical on master). The floor is the smaller half of this
row; the build closes both.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the i32 key (`{[string]: i32}`) | the same floor | no |
| the `i32` partner (`return "s"`) | the same floor | no |
| the `i32` partner (`return null`) | floors as a nullable return | no |
| the map (`return { n: 10 }` over `type R`) | runs | **yes** |
| the inferred return (`: {[i32]: i32} \| i32`) | runs | **yes** |

#### Grading list

The `return_inferred` cells of the lane's `b6-*` templates, both faces.
The reviewer's `j1.vl` (an inferred return joining an i32- and an i64-keyed map) and `j4.vl`
(an if-expression join of an i32- and a string-keyed map), both check-clean invalid wasm; and the
`o6_join` cells of `rv3189/gen.py`, carried in `distilled/named/` as `rv3189-int-key-price`.
