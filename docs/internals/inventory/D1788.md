### D1788 — a STRUCT at the template's pin and a scalar at the instance's leaves the instance's inferred return at the template's struct kind, because no ref classifier resolves the callee through the frame

**check-clean invalid wasm — `the emitted module failed to validate inside \`o$1\`: type
mismatch: expected (ref $type), found i32` · clause 1 · 3 cells of the 264-cell nested-capture
matrix, all in the un-annotated outer-return face · filed 2026-09-06 against master
`be5a79fd2` as the residue [D1781](D1781.md)'s close does not move**

Repro:

    function o(n) {
      function k(x: i32) { return n }
      return k(1)
    }
    print(o({ a: 9 }).a)
    print(o(2))
    // vl check: rc 0, "Checked 1 file, no errors."
    // vl run:   the emitted module failed to validate inside `o$1`:
    //           type mismatch: expected (ref $type), found i32

#### Mechanism

The mirror of [D1781](D1781.md) one index space over. That row converted the scalar and
list readers to `fnIndexOfInScopeSid`; the STRUCT readers (`fnRetStructSid`,
`fnRetStructIndexSid` and their nullable and variant siblings) take a bare sid with no frame
argument, so the instance keeps reading the template's `"struct"` kind and its struct-table
row. It shows only when the struct is the TEMPLATE's pin: with the struct at the instance's
pin the instance is the one that needs the ref kind, and its own return expression supplies
it. Annotating the outer return (`o<T>(n: T): T`) fixes it, because the annotation names the
substituted type instead of the inferred one.

One of the three cells is a price this PR paid rather than found: the un-annotated nested
parameter spelling (`function k(x) { … }`) was an emit refusal behind B15's generic-value
floor and is check-clean invalid wasm now that the floor no longer fires on a direct call.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the struct at the FIRST pin (i32 first, struct second) | runs, prints `2` / `9` | **yes** |
| the SECOND pin | runs | **yes** |
| the CAPTURE (`k` closed over nothing) | runs | **yes** |
| un-annotated `o(n)` → explicit `o<T>(n: T): T` | runs, prints `9` / `2` | **yes** |
| `function k(x: i32)` → `function k(x)` | invalid wasm, identical | no — both spellings |

#### Grading list

* The reversed-pin twin (`o(2)` then `o({ a: 9 })`), which RUNS today and must keep running.
* The annotated face of this witness, which RUNS today and must keep running.
* [D1782](D1782.md), the other standing residue of the same matrix.
