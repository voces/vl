### D2211 — an un-annotated alias of a narrowed `(string | null)[] | null` is classified as no list

**loud emit reject · found by D2209's position grid · filed 2026-09-23 against master `99d1f4fdd`**

Repro:

    function f(): (string | null)[] | null { ["a", null] }
    const mv = f()
    if mv != null {
      const v = mv
      v.push("x")
      print(v.length)
    }
    // vl check: rc 0.  vl run: emitProgram: .push receiver is not a list

#### Mechanism

Not yet ablated. The narrowed read itself works (`mv.push("x")` inside the guard runs). So do
the annotated alias (`const v: (string | null)[] = mv`) and the `string[] | null` spelling.
Only the un-annotated binding of the narrowed nullable-element list loses its list rep.

#### Grading list

The two `map_value` cells of D2209's grid (`(string | null)[]` element, empty arm first,
un-annotated face), which reach this row through a narrowed map read.
