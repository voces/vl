### D1800 — a bare `null` bound under a negation refuses for want of a struct-typed context, at both scopes; the un-annotated `const x = null` beside it runs

**loud emit reject — `emitProgram: bare null needs a struct-typed context`, `vl check` rc 0 ·
clause 2 · filed 2026-09-06 against master `57c8ae423` as the measured residue of
[D1784](D1784.md), the one rep of fifteen its close did not reach · both spellings, both
scopes**

Repro:

    const x: !string = null
    print(x == null)
    // vl check rc 0; vl run -> emitProgram: bare null needs a struct-typed context

#### Mechanism

Not probed beyond the ablation. [D1784](D1784.md) routed `letIsNulNone` through
`letRepAnnOf`, so the binding's CELL is now the `nulnone` one an un-annotated `const x = null`
gets — and the refusal moved not at all, which is what says the deciding site is elsewhere.
The message is emitted where a bare `NullLit` is LOWERED, not where the binding is classified:
that site asks for a struct-typed context and a negation supplies none, exactly as it supplies
no rep. `const x = null` runs because its context is the `nulnone` cell the classifier gives
it, so the two ends already agree there.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the negation, for `const x = null` (un-annotated) | runs, prints `true` | **yes** |
| the `null` value, for any other rep | runs after [D1784](D1784.md) | **yes** |
| the module scope, at a LOCAL | the same refusal, same message | no |
| the inline spelling, for a `type N = !string` alias | the same refusal | no |

#### Grading list

* the four ablation cells, and the `null` row of [D1784](D1784.md)'s rep grid in all three
  negation faces — the last three `emit-refuses` cells that grid reports.
* the other fourteen reps of that grid, which must keep running.
* [D1784](D1784.md), whose `letIsNulNone` rung is in place and waiting for this site.
