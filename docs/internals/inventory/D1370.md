### D1370 — an ANNOTATED rebind of a narrowed nullable, captured in a module-scope block, is check-clean invalid wasm: the price D1244's frame bought

**check-clean invalid wasm · clause 1 · `Invalid input WebAssembly code: type mismatch` inside the closure body · four matrix cells, ANNOTATED face only · filed 2026-09-03 as [D1244](#d1244)'s measured price, with the before/after grid in this row**

Repro (`vl check` rc 0; deleting the `: Item` annotation makes it print `a`):

    type Item = { name: string }
    function mk(i: i32): Item | null {
      if i > 0 { return { name: "a" } }
      null
    }
    const e = mk(1)
    if e != null {
      const __c: Item = e
      const __f = () => {
        const v = __c
        print(v.name)
      }
      __f()
    }

* **IT IS D1244'S PRICE, NOT AN INDEPENDENT DEFECT, AND THE A/B SAYS SO.** On master this
  program is the LOUD `emitProgram: field access receiver is not a struct` at the `v.name`
  read — D1244's own message, from the very gate D1244's close removes. Making a module-scope
  block a real frame lets these cells reach the emitter, and the annotated face arrives with a
  rep nothing converts. Graded with the seed and `VL_STD` pinned per arm, in separate
  subshells.

* **THE ANNOTATION IS THE WHOLE INGREDIENT.** The generated pair differs in exactly one
  character sequence — `const __c: Item = e` against `const __c = e` — and the un-annotated
  face RUNS at all four positions. Inference gives the rebind the niche rep the captured value
  actually carries; the annotation pins plain `Item` instead.

* **THE FAILING READ IS INSIDE THE CLOSURE, SO THE RUNG IS THE CAPTURE KIND, NOT THE BINDING.**
  `emitLet`'s binding boundary already asks `nulNicheRecoverOwed(localIsRef[slot], s.letInit,
  fnIx)` (D1115's site, `wasmEmit.vl:20037`). What has no answer for this shape is the kind the
  ENV FIELD gets for a module-BLOCK local: a module-scope block is a frame kind that did not
  exist before D1244, so `capturedKindOf` resolves names that are function locals or module
  globals and a block local is a third thing.

* **THE PRICE, MEASURED** (`scripts/capability-probes/matrix/narrowed-nullable-ref-push.matrix.vl`,
  36 cells):

  | | master `281a50ba` | with D1244's fix |
  | --- | --- | --- |
  | runs | 27 / 36 | 31 / 36 |
  | silent | 0 | 4 |
  | other (loud) | 9 | 1 |
  | **runs → not-runs** | **0** | **0** |

  The four: `closure_capture/ann`, `block_if_capture/ann`, `block_while_capture/ann`,
  `block_bare_capture/ann`. Eight cells left the loud column — four to `runs`, four to here.

* **WHY IT SHIPPED ANYWAY.** The standing bar refuses a candidate on `runs → not-runs`, which
  is zero, and CLAUDE.md records a 72-cell win declined once on a loud→silent bar the gate does
  not apply. This row is the other half of that discipline: the price is NAMED, its cells are
  pinned by name in `tests/vl_capability_matrix_test.ts` so the number cannot move again
  unexplained, and the witness above is the thing to make print `a`.
