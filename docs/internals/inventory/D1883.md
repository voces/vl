### D1883 — an UN-ANNOTATED binding of a value-union list's checked read has no interned arm rep once a lambda captures it

**loud emit reject · 1 of the 64 cells of the `pop-union-box-element` position matrix
(`fn_body_capture`, un-annotated face) · filed 2026-09-07 against master `8bdd886af`, found
grading [D1881](D1881.md)'s close**

Repro:

    type V = i32 | string
    function vs(): V[] { [1, 2] }

    function frame() {
      const src = vs()
      const c = src.get(1)
      const f = () => {
        if c is i32 { print(c) } else { print(0) }
      }
      f()
    }

    frame()
    // vl check: `Checked 1 file, no errors.` rc 0
    // vl run: emitProgram: `is` names a declared union member with no interned arm
    //         representation (deferred value-union composition)

#### Mechanism

**Not a price of [D1881](D1881.md)'s close — the same refusal stands on master.** It is the
one cell of that matrix the close leaves where it found it, which is why it is filed rather
than left to the "already loud" pile: the two neighbouring cells it looks like both RUN now.

The ingredient is the ANNOTATION being absent on the captured binding. Annotate it
(`const c: V | null = src.get(1)`) and the cell runs — the annotation is what interns the
arm's rep before the capture list is built, and the inferred spelling reaches the capture
after the composition it would have been deferred by. Remove the CAPTURE instead and the
un-annotated binding runs too, so neither ingredient alone is enough.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the missing annotation — spell the binding `V`-or-null | runs, prints `2` | **yes** |
| the capture — narrow `c` in the enclosing body | runs, prints `2` | **yes** |
| the checked read — `mkv(): V` in place of `src.get(1)` | runs, prints `2` | **yes** |

#### Grading list

* The `fn_body_capture` un-annotated cell of
  `scripts/capability-probes/matrix/pop-union-box-element.matrix.vl`, which a close moves to
  RUNS, and its five capture siblings (`closure_capture`, `block_if_capture`,
  `block_while_capture`, `block_bare_capture`, `fn_body_capture` annotated) which RUN today.
* The three ablation programs above, all of which RUN today and must keep running.
