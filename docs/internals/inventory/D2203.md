### D2203 — an empty `[]` delivered to an `S[] | S` union emits a module that fails to validate

**closed 2026-09-23 — the witness prints `0` · was `check-clean invalid wasm`, `expected (ref $type), found (ref $type)` · found beside D2201 · filed 2026-09-23 against master `66e74235a` · closed with [D2202](D2202.md), one mechanism · fixture `tests/cases/arrays/empty-list-join-arm.vl` (`union-ref`, `union-call`)**

Repro (runs, prints `0`):

    type B = { base: i32, length: i32 }
    function sink(data: B[] | B): i32 {
      if data is B { return 1000 }
      return data.length
    }
    print(sink([]))
    // before the close: vl check rc 0, then an invalid module

#### Mechanism

D2201's shape with a struct element: the empty literal is built as the i32 list where the
`B[]` arm's ref list is expected. Fails at all five delivery positions (argument, annotated
binding, return, assignment, struct field).
