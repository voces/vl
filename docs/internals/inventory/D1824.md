### D1824 — `.pop()` off a union arm narrowed to a `Json[]` refuses `bare null needs a struct-typed context` at every narrowing depth, while the same shape over `Circle[]` runs

**closed 2026-09-07 — the witness prints `1` · was a loud emit reject, `emitProgram: bare null
needs a struct-typed context` · clause 2 · filed 2026-09-07 against master `a47af0893`, found
grading [D1823](D1823.md)'s position grid; resolved against master `dc2023aab` by
[D1825](D1825.md)'s rung · fixture `tests/cases/unions/pop-union-box-element.vl`**

Repro:

    import { Json, JsonError, parseJson } from "std:json"

    const zp = parseJson("[1, 2]")
    if zp is Json[] {
      const t = zp.pop()
      if t != null { print(zp.length) } else { print("no") }
    } else { print("not an array") }
    // vl check: rc 0.  vl run: emitProgram: bare null needs a struct-typed context

#### Mechanism

**One mechanism with [D1825](D1825.md), and the same rung closes both — which is what the
brief asked to be settled either way.** `.pop()` over a ref-element list is `T | null` to the checker, and the miss arm
synthesises a bare `null`. Over `Circle[]` that null has a struct-typed context to take —
`Circle` — and the same program **runs and prints `1`** at both narrowing depths. Over `Json[]`
the element is itself a UNION, so the null has no single struct type to be, and the emitter
floors.

**The narrowing depth is not the ingredient, and that is the measurement that matters here.**
[D1823](D1823.md) closed a depth-2-only gap in the ref-list frame collector, which moved these
four cells from that row's message to this one. The DEPTH-1 spelling above hits this same
message on master, before and after that close — so what D1823 did was uncover this refusal,
not cause it.

The prediction filed here — "the ref-list pop's miss arm wants `emitMapGetUnionBox`'s
null-tagged box" — is the one that held. [D1825](D1825.md) carries the probe and the three
rungs; this row's four cells move with its four, from one change.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the `Json` element — the same shape over `Circle[]` | runs, prints `1` | **yes** |
| the second narrowing (depth 1 as filed above) | refuses identically | no |
| `.pop` for `.push`, `.clear`, `.length` or an index read | runs since [D1823](D1823.md) | **yes** |
| the `!= null` guard — discard the popped value instead | was **check-clean invalid wasm** ([D1825](D1825.md)), runs now | no |

#### Grading list

* [D1823](D1823.md)'s fixture, whose `Circle[]` `.pop`, `.push` and `.clear` cells and whose
  `Json[]` `.push` and `.clear` cells RUN today and must keep running.
* The `Circle[]` `.pop` at both depths, which RUNS today and prints `1`.
* The four `json2_*_pop` cells of [D1823](D1823.md)'s grid, which are this row's own witnesses
  at depth 2.
* [D1825](D1825.md), the SILENT face of the same ingredient — discard the popped value and the
  module validates nowhere. A close here that leaves that one silent has resolved the lesser
  half.
