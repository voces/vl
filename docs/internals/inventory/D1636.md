### D1636 — a struct field FED BY a nullable map read reads back as the niche, and an annotated rebind of that read, captured, is invalid wasm

**check-clean invalid wasm · 1 cell, `frame-scoped-narrowed-map-read`'s `struct_field_assign` annotated row · filed 2026-09-05 against master `abecf8da`**

Repro:

    type W = { f: { name: string, n: i32 } }
    const m: { [string]: { name: string, n: i32 } } = Map()
    m["k"] = { name: "ada", n: 3 }
    const mv = m["k"]
    if mv != null {
      const w: W = { f: mv }
      w.f = mv
      const v: { name: string, n: i32 } = w.f
      const g = () => v
      print(g().n)
    }
    // vl check: rc 0.  vl run: the emitted module failed to validate inside the module's
    // top-level code: type mismatch: expected (ref $type), found (ref null $type)

#### Mechanism

Not ablated to a mechanism, and the ablation is what makes it worth its own row rather than
a second witness on D1635: the ingredient is WHERE THE FIELD'S VALUE CAME FROM. Assign the
same field a struct LITERAL and the identical rebind-and-capture runs; assign it the
narrowed map read and the capture's env field is declared non-null over a nullable value.
The field's declared type is the same in both, so what differs is which struct row the
field's storage was interned as, not what the annotation says.

This is at MODULE scope, so it is not D1633's axis, and its neighbour D1635 needs a frame
and no map — the two ablations name different ingredients.

#### Ablation

Every row is `vl check` rc 0.

| spelling | outcome |
| --- | --- |
| the witness (field fed by the map read, annotated rebind, captured) | **invalid wasm** |
| the field fed by a struct LITERAL instead (no map at all) | runs, 3 |
| the rebind UN-annotated (`const v = w.f`) | runs, 3 |
| the capture hop removed (`print(v.n)`) | runs, 3 |

Needed: the map-read source for the field AND the annotated rebind AND the capture.

#### Grading list

* The three running spellings above.
* D1635, the frame-scoped nested-field row: a fix for either should say what it does to the
  other's witness, since both go through `captureValKind`'s annotated arm.
* `scripts/capability-probes/matrix/frame-scoped-narrowed-map-read.matrix.vl` skips
  `struct_field_assign` by name until this closes; deleting that `@@SKIP@@` is part of the
  close.
