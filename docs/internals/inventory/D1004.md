### D1004 — the EMPTY member-access diagnostic survives on an UNBOUNDED `<T>` (D952's remaining half)

**runs today and must keep running · CLOSED 2026-09-03 by the owner ruling that an unbounded `<T>` is an entirely open hole ([D1431](D1431.md)) — the witness prints `1`; the refusal the morning's message fix worded no longer exists · ZERO corpus cells**

D952's bounded half closed with constraints phase 1: a bounded parameter names its bound and
what the bound grants. The unbounded half reached `memberFloorMsg`'s `TyVar` rung, which
answered `""`. That rung no longer decides this program — an unbounded parameter is routed
onto the hole before it, so the read has a type and the CALL has the question.

* **THE WORDING WAS NOT THE WHOLE FIX, and believing it was cost a day.** A sentence was
  written for the refusal on the morning of 2026-09-03 (`… a parameter's members come from
  its bound. Declare one: …`); the owner read it, called it passive aggressive, and ruled the
  program legal the same day. The refusal it worded is gone. What a message fix cannot do is
  ask whether the refusal should exist.

* **IT DID NOT CLOSE BY MAKING UNBOUNDED `<T>` STRICT — it closed the other way.** The owner
  ruled the parameter an entirely OPEN hole, so `x.n` derives its type as an inference hole's
  does and each call decides. `tests/cases/constraints/unbounded-type-param-unchanged.vl` — the
  pin against the strict reading — is still green, and the operator half it also holds never
  moved.

* **THE `""` WAS AN IN-BAND SENTINEL WITH NO READER, AND ONE CALLER FORMATTED IT.**
  `memberFloorMsg` has three callers and `""` means "this read is fine, or is deferred" to
  two of them — `validateMemCstrs` (through `memCstrMsg`) tests `mMsg != ""`, and
  `validateUfcsCstrs` tests `uMsg == ""` and substitutes its own words. Neither can ever
  receive a `TyVar`: both guard on `!tyHasHole(…)` first, and `tyIsHole` answers TRUE for
  every `TyVar`, a declared parameter included. So the rung's `""` reached exactly one
  caller, `memberFloorErr`, which raised it unconditionally — and that is the whole
  diagnostic a user got. 1 of 3.

* **THE FIX IS TWO LAYERS, on purpose.** The rung answers (`tpMemberFloorMsg`), and
  `memberFloorErr` now refuses to raise an empty string at all — so a future rung that
  forgets costs a vague sentence, not a wordless one. The class is closed, not the instance.

* **THE SENTENCE'S SUGGESTION IS MEASURED.** `<T: { n: i32 }>` makes this witness run, at
  two instantiation shapes (`{n: i32}` and `{n: i32, m: i32}`), so "declare a bound" is an
  edit that actually works rather than a hedge.

Repro (prints `1`):

    function getN<T>(x: T): i32 {
      x.n
    }
    print(getN({ n: 1 }))
    // An argument that does NOT carry `n` is still refused, at the call, in the
    // inference hole's own words: `argument 1: expected {n: _}, got {r: f64}`.
