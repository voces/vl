### D2182 — a function of 1,000 nested bare blocks exhausts the compiler's call stack

**compiler trap · filed 2026-09-23 against master `a453c09c5` · also traps on 4789d71f2, so it is older than PL-019 · found while writing D2181's witness; not investigated · 300 levels run**

Repro:

    function f(a: i32): i32 {
      let acc = 0
      @@repeat(1000, "{ const m = acc + 1; acc = m; ")@@ @@repeat(1000, "} ")@@
      acc
    }
    print(f(1))
    // vl run: "the call stack was exhausted — most often unbounded recursion", exit 70.
    // At 300 levels it prints 300.

#### Mechanism

Not yet measured. The likely cause is that some pass recurses once per nesting level. D1990
walked an `else if` chain along its spine for this reason, and nested bare blocks have no spine
to walk. Build a `--names` seed and read the trapping frames before choosing a pass.
