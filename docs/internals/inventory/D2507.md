### D2507 — a value whose type is a recursive-alias map, passed to a generic parameter, is check-clean invalid wasm at the call site

**check-clean invalid wasm · 8 cells of `recursive-alias-map-into-union.matrix.vl` (every un-annotated face that reaches a hole) · filed 2026-09-25 against master `d29a9d220`**

Repro:

    type V = f64 | string | { [string]: V }
    type VMap = { [string]: V }
    function t<T>(a: T) { print(1) }
    const m: VMap = Map()
    t(m)
    // vl check: rc 0.  vl run: invalid module, type mismatch: expected (ref $type), found (ref $type)

#### Mechanism

Not yet located. The body of the generic is scenery (it need not read `a` at all), so the
defect is at the call's argument delivery: the instance pinned for `T = VMap` and the value
the caller holds disagree about which struct the map is. D1865 closed the same alias spelling
delivered into the union `V`; the generic pin is a second delivery that was not graded then.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the body's use of `a` (`print(1)` only) | still invalid | no |
| the `is { [string]: V }` test of the matrix cell | still invalid | no |
| `string` from `V` | still invalid | no |
| the alias (`{ [string]: V }` written inline) | runs | **yes** |
| the recursion (`type VMap = { [string]: f64 }`) | runs | **yes** |
| the generic (`a: VMap` annotated) | runs | **yes** |
| explicit `<T>` vs an un-annotated `a` | both invalid | no |

#### Grading list

`python3 scripts/capability-probes/matrix.py scripts/capability-probes/matrix/recursive-alias-map-into-union.matrix.vl`
must read `8 silent` → `0 silent`: argument, array_element, array_element_place,
early_return_guard, map_value, map_value_place, struct_field_init and struct_field_place, all
in the un-annotated face.
