### D1544 — the NULLABLE half of D1543: an anonymous literal's `Rec | null` field still gets no row, and the field CODE is not what is missing

**loud emit reject: `emitProgram: ref valtype with no interned shape` · `vl check` rc 0 · clause 2 · filed 2026-09-03 as the measured residue of [D1543](D1543.md), whose close takes the NON-nullable face of the same three-line shape to `runs`**

Repro:

    type Rec = { name: string, n: i32 }
    function mkMaybe(): Rec | null { return { name: "ada", n: 3 } }
    const box = { item: mkMaybe() }
    const v = box.item
    if v != null { print(v.n) } else { print(0) }
    // vl check rc 0; vl run -> emitProgram: ref valtype with no interned shape
    // want: 3

#### Mechanism — what is NOT missing

D1543's close gives `anonFieldCode` a rung that admits code 15 when `anonFieldElemName` can
name the field's target row, and that rung was written to cover this case too:
`fieldCodeForMatch` already folds a `TyNullable`-over-struct onto code 15 (its own
`t is TyNullable` arm), and `anonFieldElemName`'s code-15 arm was given a nullable peel
(`structRowOfObjFieldSet` of `nInner`) in the same change. **Neither closed this cell**, so
the remaining rung is downstream of the mint — the nullable STRUCT-field slot needs more than
the code and the element name that the non-null one needs (the declared twin
`const box: { item: Rec | null } = …` runs, so the slot exists; something the ANON mint fills
differently is what the emitter then cannot resolve).

Stated as a decline rather than a diagnosis: the two obvious rungs were built and measured,
and the cell did not move. The next reader should probe the mint's OUTPUT (the row's field
columns for this field) against the declared twin's, not the code path.

#### Ablation

| variant | outcome |
| --- | --- |
| the non-nullable value (`mkr(): Rec`) | **runs** `3` (D1543) |
| the outer destination annotated (`const box: { item: Rec \| null } = …`) | **runs** `3` |
| no outer literal (`const v = mkMaybe()`, then the null test) | **runs** `3` |
| a nullable SCALAR field (`{ item: passh(mkNulI32()) }`) | **runs** `4` |
| through `passh` / through `pass<T>` / direct | all three refuse identically |
| inside a `function` | `object literal field count does not match struct` — the same shape, D1543's other sentence |

#### Grading list

* [D1543](D1543.md)'s own fixture — the five non-nullable sources, which must keep running.
* The annotated outer spelling of this row's witness, which runs today.
