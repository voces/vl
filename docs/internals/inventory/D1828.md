### D1828 — `??` over a nullable LIST ELEMENT refuses at emit, while the same element read through a guard runs

**loud emit reject · clause 2 · filed 2026-09-06 against master `7b51ce6e7` · found while
writing D1827's fixture, whose array-element cell had to spell the guard by hand**

Repro:

    const xs: (i32 | null)[] = [7]
    print(xs[0] ?? 0)
    // vl check: rc 0.  vl run: emitProgram: `??` over this nullable value is not
    //   supported yet — narrow it first, e.g. `if v != null { … }`
    // Should print 7.

#### Mechanism

Not yet ablated to a rung. The refusal's own sentence prescribes the edit, and the edit
runs — `const v = xs[0]` then `if v != null { v } else { 0 }` prints `7` — so the operand's
rep is buildable and it is the `??` arm that has no case for a nullable element read. The
map twin (`m["k"] ?? 0`) has its own fused lowering (`emitMapGetOr`) and runs, which is why
the gap shows only on a list.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the `??` (a `!= null` guard over a bound copy) | prints `7` | **yes** |
| the list (a map read `m["k"] ?? 0`) | prints `7` — the fused map arm owns it | **yes** |
| the annotation on `xs` | same refusal | no |

#### Grading list

The map twin `m["k"] ?? 0`, which must keep running; the array-element cell of
`tests/cases/maps/map-read-in-if-expression.vl`, which spells the guard by hand only because
of this row and can take the `??` back the day it closes.
