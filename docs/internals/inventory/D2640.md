### D2640 — an inferred record's empty `[]` field that a later push pins to a ref element has no ref-list row

**loud emit reject · clause 2 · pre-existing on master `a2d2ca88f` · found by the D2506 grid's
`list × record field` cells · filed 2026-09-26 against master `a2d2ca88f`**

Repro:

    function main() {
      const r = { c: [] }
      r.c.push({ a: 3 })
      print(r.c[0].a)
    }
    main()
    // vl check: rc 0.  vl build: emitProgram: ref-list field element type is not interned

#### Mechanism

Not traced to the line. The push pins the element through `r.c`, the record's field type, and
the checker is satisfied. The emitter's struct row for the literal's shape records the field as
a ref list (code 5) whose element row `sFieldRefSlot` cannot find, so `pushFieldStorage` refuses.
Every ref element does it (a record, a union, `i32 | null`, and D2506's `_ | null` filled to any
`T`), and no scalar one does (`i32`, `f64`, `string` run), which points at the element row being
interned under a key the field's lookup does not ask. A non-empty literal (`{ c: [{ a: 1 }] }`)
and an annotated binding (`const r: { c: C[] } = { c: [] }`) both run.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the element `{ a: 3 }` replaced by `3` | prints `3` | **yes** — a ref element |
| `r` annotated `{ c: C[] }` | prints `3` | **yes** — the inferred record |
| the field's `[]` given an element (`[{ a: 1 }]`) | prints `3` | **yes** — the empty literal |
| the element replaced by an `i32 \| null` value | still refuses | no |
| moved from `main` to module scope | still refuses | no |

#### Grading list

* The D2506 grid's `list × field × {later, callsite, retann}` inferred cells: 12 refuse with this
  sentence (the element `i32`, `f64`, record and union, each `| null`).
* A union element: `const u: i32 | string = 3` then `r.c.push(u)`.
