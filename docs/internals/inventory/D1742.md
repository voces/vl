### D1742 — a DOUBLY-nested array element narrowed in place and read directly was check-clean invalid wasm; one level of nesting ran, and so did one hop through an intermediate binding

**closed 2026-09-06 by #2756 — the witness prints `8` · was check-clean invalid wasm · filed
2026-09-06 against master `40334c8cf` · found by the 2026-09-06 discovery pass, as the residue
the D1741 close left standing · re-graded an hour later on the merged tree and already fixed**

Repro:

    type A = { r: i32 }
    type T = i32 | A
    function st(): A { return { r: 8 } }
    function mk(b: boolean): T {
      if b { return st() }
      7
    }
    function go() {
      const ys = [[mk(true)]]
      if ys[0][0] is A { print(ys[0][0].r) } else { print(0) }
    }
    go()
    // vl check: rc 0.  vl run: prints 8.

#### Mechanism

The narrowed place had to be a doubly-nested element read TWICE — once by the `is` test and
once by the consumer — with the consumer a direct read or an annotated binding. The flat
`xs[0]` spelling ran, and so did `const xs = ys[0]` followed by `xs[0]`, so the second index was
what the place machinery lost: `narrowedStructArmVi` answered -1 for `ys[0][0]`, which D1741
measured from the other side, its binding boundary having to take the CHECKER's answer because
the emitter's narrowing stack had nothing for this place.

**#2756 gave a nested index place its key**, which is exactly that gap, and both validator
sentences this row recorded — `expected (ref null $type), found (ref $type)` at the direct read
and `expected (ref $type), found (ref $type)` at the annotated binding — went with it. So they
were one family, which the row could not say when it was filed.

#### Ablation

The table as filed, against master `40334c8cf`; every row of it now prints `8` or `1`.

| ingredient removed | outcome then | needed? |
| --- | --- | --- |
| the second index (`xs[0]` over a flat list) | ran, printed `8` | **yes** |
| the nesting, via an intermediate binding (`const xs = ys[0]`) | ran, printed `8` | **yes** |
| the direct read, delivered as an ARGUMENT instead | ran, printed `8` | **yes** |
| the read entirely (`is` test then `print(1)`) | ran, printed `1` | **yes** |
| the annotated binding instead of the direct read | still invalid, other message | no |
| the declared union (`type T = i32 \| A`) for an inferred one | still invalid | no |

#### Grading list

The place grid's `ys[0][0]` row at both union kinds, both faces and every consumer. On the
merged tree the whole 168-cell grid reads **148 runs, 0 silent, 0 emit reject, 20 check
reject** — the 20 are `xs[i]` at a variable index, which is a check-side refusal and not this
row.
