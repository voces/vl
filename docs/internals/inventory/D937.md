### D937 — [CLOSED 2026-09-01] the inferred NULLABLE-MAP return: five sites, and a PRIORITY LADDER that let the non-null fact overwrite the nullable one

**closed as `runs` · was a CAPABILITY REFUSAL conceding type-validity (`'pick' infers the nullable return type {[string]: i32} | null — type-valid, but an inferred return of this shape is not yet supported by codegen; annotate the return type`) · 0 corpus cells — this gap is reached by NO cell, and `scripts/goal-scoreboard.py --sites` is the only thing that counts it · capability probes 1/6 → 2/6 run · distilled corpus 0 movement, and a stdout-level dual-run over all 4,614 running cells shows 0 DIFFER · fixture `tests/cases/functions/infer-nullable-map-return.vl`**

The map's rep is a ref (the kind-3 map struct), so `| null` is the same niche the nullable
STRUCT return takes — D887's shape, one inner over. The ANNOTATED spelling lowered and printed
`7` throughout; what was missing is that the un-annotated function never asked.

* **FIVE SITES, EACH MEASURED TO STILL FAIL BEFORE THE NEXT WAS ADDED.** This is the value of
  the row: D887's own note called this "the same three-site shape", and it is five.

  (1) `nullableRetName` — no map arm, so the renderer returned "" and the floor read that as
  "no rep exists" rather than "nobody wrote the name down". (2) `buildFnMap` — classify the
  recorded name as kind `nulmap`. (3) **`retKindPri` — rank `nulmap` ABOVE `map`**, the one
  that does not follow from D887. (4) `emitReturnValue` — seed `pendingNulRefHeap` so a bare
  `return null` types. (5) `exprNullableMap` — the CALLER's `const r = pick(…)`, so
  `if r != null` has a typed null to compare against.

* **SITE 3 IS THE INTERESTING ONE, AND NOTHING IN D887 PREDICTS IT.** `criSetRetKind` lands a
  write only when it outranks the current kind on a historical priority ladder. `map` sits at
  11; `nulmap` was absent from the ladder entirely, so it ranked 0. `buildFnMap` recorded the
  nullable fact off the checker's name, and then `criClassify` saw the returned map LOCAL and
  wrote the non-null `map` fact about the same return — and won. The result valtype came out
  `(ref $mapStruct)` and the bare `return null` reached `emitNullLitNode` with no context.
  Diagnosed by ISOLATION, not by reading: forcing the seed unconditionally moved the failure
  from the function to the call site, which proved site 4 correct and the kind test wrong.
  `nulmap` now shares `nulstr`'s tier, on the `variant` / `u8list` precedent — a return is a
  nullable map or a nullable string, never both, so neither needs to outrank the other.

* **RESTRICTED TO THE i32-VALUED MONO SHAPE, and the restriction is load-bearing.** An
  unrestricted renderer arm lifts the CHECKER's floor for every nullable map while the four
  consumers still handle only one — `{[string]: string} | null` passed `check` and then failed
  in the emitter, trading a clean refusal for a worse one. Caught before shipping by running
  the string-valued twin. The gate is asked of the ARENA (`mSet == 0`, key `string`, value the
  `i32` prim), not of the rendering, because re-reading `tyToEmitName`'s characters is the
  spelling test this file keeps replacing.

* **TWO TESTS USED THIS REFUSAL AS THEIR WITNESS, and both said so in advance.**
  `lsp_wasm_checker_test.ts` and `selfhost_native_diag_code_test.ts` pin the CHANNEL (a
  capability admission carries the stable `unsupported-lowering` code), not the gap, and each
  carried a note that its witness had already moved twice and would move again. It has now
  moved a third time, by one word — to a `string`-valued map, which still floors identically.

Repro (now RUNS, printing `7` then `0`):

    function pick(c: boolean) {
      if c { return null }
      const m: {[string]: i32} = Map()
      m["k"] = 7
      m
    }
    function show(c: boolean) {
      const r = pick(c)
      if r != null { print((r)["k"]) } else { print(0) }
    }
    show(false)
    show(true)
    // vl check rc 0; runs, prints 7 then 0.
