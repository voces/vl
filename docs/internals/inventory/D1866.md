### D1866 — a recursive-aliased map READ BACK OUT of a container is check-clean invalid wasm at nine delivery positions

**check-clean invalid wasm · filed 2026-09-07 against master `e32d6ebe4`**

Repro:

    type V = f64 | string | { [string]: V }
    type VMap = { [string]: V }

    function mkVMap(): VMap {
      const m: VMap = Map()
      m["a"] = 1.0
      m["b"] = "x"
      m
    }
    const __m: { [string]: V } = Map()
    __m["k"] = mkVMap()
    if __m["k"] != null {
      if __m["k"] is { [string]: V } {
        print(__m["k"].size)
      } else {
        print(-1)
      }
    }
    // vl check: rc 0.  vl run: the emitted module failed to validate — type mismatch: expected (ref null $type), found (ref $type)

#### Mechanism

D1865's residue, and the same capability. That row closed the two places a recursive map alias
loses its identity — the union ARM classifier, which read the alias NAME, and the map-value
slot MINT, which sliced the value out of a rendered spelling and interned an unrolled
anonymous twin. With both closed, the capability's position matrix moves from **62 of 62
SILENT** to **53 RUNS and 9 SILENT**. This row is those nine.

They are not one mechanism yet, and the row says so rather than guessing: eight are the
UN-ANNOTATED face, where inference rather than an annotation pins the value's rep, and the
ninth (`map_value_place`, annotated) is the read-back above. What they share is that the map
is recovered from a CONTAINER — a map value, a list element, a struct field — through a table
the emitter keys on a stored SPELLING (`rlElemName`, and `rlElemMapShape`'s
`mvShapeOfMapName` off it) rather than on the arena row the checker banked. D1865's fix reached
the mint sites that had a row in hand; these read from a spelling column with no row beside it,
so closing them means banking the row in that column, not another peel.

Two hand-written shapes refuse LOUDLY rather than silently at the same positions — a local
`const one: VMap` delivered through `VMap[]` and through a struct field — so the family spans
both outcomes and a fix owes the loud pair as well as the silent nine.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the recursive alias — the same read-back with the map spelled inline everywhere | runs | **yes** |
| the container — the same value delivered straight as an argument | runs (D1865's close) | **yes** |
| the read-back — storing into the map value without reading it out | runs | **yes** |
| the annotation, at the eight other positions | silent the same way | no |

#### Grading list

`scripts/capability-probes/matrix/recursive-alias-map-into-union.matrix.vl` is the instrument
and the population: a close takes it to 62 RUNS with `runs -> not-runs` 0, and any cell that
leaves SILENT is this row still open. `tests/cases/maps/recursive-alias-map-into-union.vl` and
`tests/cases/maps/alias-map-into-union-controls.vl` are the 53 that already run and must not
move. D1865 is the closed half and its mechanism section is where the two fixed sites are
described; D1864 is the read-side row over the same two types.
