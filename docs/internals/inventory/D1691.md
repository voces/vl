### D1691 — the `| null` niche's element ladder has NAMED empty arms for a nested-list and a map element, so an inferred `i32[][] | null` field gets no code while its non-null twin gets one

**closed 2026-09-06 — the ref-element set moves to one home both codes ask, and the niche's own
ladder stops carrying a narrower copy · filed against master `ddc4b852a` · D1663's residue ·
residue D1710 · 0 corpus cells**

Repro:

    function nul(): i32[][] | null { return [[7]] }
    function two() {
      const box = { xs: nul() }
      const l = box.xs
      if l != null { print(l[0][0]) }
    }
    two()
    // vl check: rc 0.  vl run: rc 0, prints 7

Its non-null twin — `function plain(): i32[][]` and `print(box.xs[0][0])` — ran at both seeds,
with nothing else changed, which is what named the ingredient.

#### Mechanism

`anonNulFieldCodeOfTy` coded a `TyArray` inner through `anonNulListFieldCodeOfElem`, a SECOND
ladder over the element kind whose `TyArray`, `TyMap` and `TyNullable` arms were named empty
ones. Probed with an `emitFail` in each of the three, validated against the three programs that
already failed: every one fired, and each printed the element key its code-5 twin would have
used — `i32[]`, `{[string]:i32}`, `A|null`. So the arm never reached the shared key ladder at
all; it decided the CODE off the element kind and stopped there.

**The decline was terminal, which is why the code-5 twin ran and this did not.** For a plain
`i32[][]` field the arena rung declines and the RENDER answers 5; for the niche there is no
render, because `anonTySpellable` refuses a `TyNullable` and `nodeTyName` spells no `| null`.
The niche ladder is the only path, so a gap in it costs the whole literal its row and the
sentence a user reads is two readers away.

`tyElemIsRefListElem` is now that set's one home — struct, union, closure, nested list, map —
asked by `nulRefListFieldCodeOfTy` and by this ladder, with `tyElemIsNulRefListElem` for the
nullable-REF element the third arm named.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the `\| null` (a plain `i32[][]` field) | ran at both seeds | **yes** — the code was the ingredient |
| the element's own container (`A[] \| null`, a plain struct element) | ran at both seeds | **yes** |
| the annotation added (`const box: { xs: i32[][] \| null } = …`) | ran at both seeds | **yes** — the un-annotated face was the row |
| the read (`const box = { xs: nul() }` alone) | still refused | no — the mint was the whole ingredient |

#### Grading list

The three element kinds one per row (nested list, map, nullable ref), each at both faces and
both codes; the LITERAL-union controls that must NOT take the ref wrapper (`K[] | null`,
`(K | null)[] | null`), which a first candidate moved loud→SILENT before the subtraction was
added; and the three matrices this close owed —
`anon-nullable-list-field-nested-list-element`, `-map-element` and `-nullable-ref-element`,
27 of 46 to 46 of 46 each.
