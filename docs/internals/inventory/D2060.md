### D2060 — [CLOSED 2026-09-22] a MUTABLE record field is accepted covariantly in depth, so a callee writes a wider member into a narrower caller's struct

**now a loud check reject (that is the close) · was loads then traps · clause 1 (soundness) · filed 2026-09-22 against master `31ea77721`, found by the getters/effects persona review (type-theory lens, probes `p2`/`p3`/`p6`/`p7`), closed against master `4b7460028` · fixtures `tests/cases/soundness/error-covariant-record-field-written.vl`, `tests/cases/soundness/covariant-record-field-read-only-runs.vl`**

Repro:

    function f(p: { x: i32 | string }) { p.x = "s" }
    const q = { x: 1 }
    f(q)
    print(q.x + 1)
    // was: vl check rc 0, vl run: wasm trap: cast failure
    // now: vl check rc 1 at `f(q)` — a {x: i32} cannot be delivered as {x: i32 | string} here:
    //   the assignment on line 1 writes a widened field

#### Mechanism

`assignable` admits `{ x: i32 }` where `{ x: i32 | string }` is declared, and the emitter
adopts the destination's rep for the source, so both handles are one struct. A store of a
`string` through the wider handle then sits in the field the caller reads as `i32`. The close
applies the owner's reads-only rule for covariant lists (DECISIONS.md A9) field by field: at
every delivery the checker computes the widened slots (`.x`, `.inner.x`, `.xs[]`, and the slots of
a union member the value may be narrowed to), drops the ones a fresh literal owns, and follows
the destination through the list rule's occurrence index — bindings, parameters, relays,
closures, stores, loop variables, `map`/`filter` callbacks, returns (every call, method-spelled
ones included), function-typed parameters (every argument their callers pass), `??`, `if`
arms; a discarded expression statement ends a path. A JOIN is a delivery too: a list
literal's element, an `if` value's arm, a `??` operand or a `return` of a function whose result
type is inferred, narrower than the joined type, is checked as delivered into it. Every delivery
is decided after all bodies are checked, so the order of declarations does not matter. A write
to a widened slot of a value the source's own slot type does not accept (or a type variable,
unless parametricity proves it is one of the slot's own values) refuses the delivery, naming
the write and the imprecise hop it came through (`p.x = 5` into an `i32` source runs); a handle
reaching a form the index does not model refuses too, since a record has no converting copy to
fall back on (DECISIONS.md "Record depth covariance"). The residue is [D2100](D2100.md), [D2101](D2101.md) and [D2102](D2102.md) (a join of lists whose element changes storage, the container rule's to refuse). A list or map whose element changes storage is the container rule's throughout; one that keeps it (a list of records whose fields widen, [D2161](D2161.md)'s `C[]` into `(C | null)[]`) is shared, and this rule refuses its unsafe writes.

#### Ablation

| spelling | before | after |
| --- | --- | --- |
| annotated parameter `p: { x: i32 \| string }` (the witness) | rc 0, then `cast failure` | check reject at `f(q)` |
| bound `setx<T: { x: i32 \| string }>(t: T)` | rc 0, then `cast failure` | check reject at `setx(q)` |
| field `{ x: i32 \| null }`, written `null` | rc 0, then `cast failure` | check reject |
| field `{ sh: Circle \| Sq }` given `{ sh: Circle }`, written `{ s: 5 }` | emit floor `field access receiver is not a struct` | check reject |
| un-annotated `function setx(t) { t.x = "s" }` | loud check reject at the pin | unchanged |
| the same callee only READING `p.x` | runs | runs |

#### Grading list

The 840-cell grid (7 widening kinds × 12 positions × 5 callee behaviours × annotated/bare
source): 278 traps and 280 check-clean invalid modules → check rejects, 19 emit floors → check
rejects, 81 `runs` → 81 `runs`; the 64 read-only silent cells left are D2100. 77 hand probes,
the first review's 98 and the second review's 130 (`self`, std relays and `sort`/`reduce`
callbacks, map values, narrower-value stores, generic writes, parametric swaps and their
laundering attempts, function-typed parameters, joins by list literal, `if`, `??` and inferred
returns, declaration order, cross-module writes, alias chains to 40,000 and call chains to 300,
10,000-link `else if` chains): every trap among them is a check reject, the invalid modules left
are read-only (D2100), and 13 `runs` → check reject, each refused on purpose. Seven store a value
the source's field cannot hold: a string into a popped element; `1.5`, `3.5` or `7.5` into an
`i32` field, which base printed as wrong values (`2`, `3`, `8`); a `Node` into a field the
un-annotated source types `null`, twice; a string record pushed into a widened list field. One
writes in an `if` arm this run did not take (flow-insensitive). One stores a parameter typed
`i32 | string` that held an `i32` at run time. Three write through a result or a callback
parameter another call shares (context-insensitive, and the message names that hop). One writes
`"s"` at the end of a 300-deep chain the source never read back. The sibling
[D2036](D2036.md) (`i32` field into `f64`) is the same admission's read-only face and still
emits an invalid module for its own witness, which does not write.
