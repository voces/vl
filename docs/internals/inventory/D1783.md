### D1783 — an ANONYMOUS shape whose field takes an INLINE-litunion array element gets no interned shape; the alias spelling, the annotated holder and a member literal all run

**loud emit reject — `emitProgram: ref valtype with no interned shape`, `vl check` rc 0 ·
clause 2 · filed 2026-09-06 against master `99a240700` · found while closing
[D1768](D1768.md), as `inline-litunion-from-list-element.matrix.vl`'s
`struct_field_init/un-annotated` cell — red before and after that close, so it is nobody's
regression · [D1541](D1541.md)'s inline/alias seam at an anonymous holder**

Repro:

    const srcArr: ("red" | "green")[] = ["green", "red"]
    const w = { f: srcArr[0] }
    const v = w.f
    print(v)
    // vl check rc 0; vl run -> emitProgram: ref valtype with no interned shape

#### Mechanism

Not probed — the ablation below is the whole of what is measured. The seam is
[D1541](D1541.md)'s: a litunion ARRAY is the interned-i32 atom list while a scalar litunion
destination's rep follows the SPELLING, and an anonymous holder's field row is minted from
the initialiser rather than from an annotation, so the field has no spelling to soften and no
declared row to intern against. An ALIAS element gives the field a registered name and the
row is minted; an annotated holder gives it one too.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the INLINE spelling (`type Color` at the array, un-annotated holder) | runs `green` | **yes** |
| the ANONYMOUS holder (`const w: { f: "red" \| "green" } = …`) | runs `green` | **yes** |
| the LIST ELEMENT source (`const w = { f: "green" }`) | runs `green` | **yes** |
| the literal union (`srcArr: string[]`) | runs `green` | **yes** |
| the binding hop (`print(w.f)` directly) | same refusal | no |

#### Grading list

* the five ablation cells above, and the `struct_field_init` row of
  `scripts/capability-probes/matrix/inline-litunion-from-list-element.matrix.vl` in both
  faces — the annotated one RUNS today and must keep running.
* [D1541](D1541.md) and [D1768](D1768.md), the two closed ends of the same seam, and the
  anonymous-row family [D1674](D1674.md)/[D1690](D1690.md), whose keying this may share.
