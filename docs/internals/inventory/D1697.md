### D1697 — a LIST-OF-LIST element names no struct row, so the capture comparator has nothing to walk and a declared row takes the literal anyway

**check-clean invalid wasm · filed 2026-09-06 against master `ad287fae7` + D1690 + D1694 +
D1696/D1674 · the capture preference's one remaining blind spot · 0 corpus cells**

Repro:

    type U1 = i32 | boolean
    type U2 = string | boolean
    type A = { xs: U1 }
    type B = { xs: U2 }
    type H = { ys: B[][] }
    function as1(): A[][] { return [[{ xs: 7 }]] }
    const b = { ys: as1() }
    print(b.ys[0][0].xs)
    // vl check: rc 0.  vl run: invalid module — type mismatch:
    // expected (ref null $type), found (ref $type)

Take one container off — `B[]` against `A[]` — and the identical program prints `7`.

#### Mechanism

The capture preference refuses a declared row whose element ROW disagrees with the value's, and
it reaches that row by NAME: `structIndexByName(vn)` and `structIndexByName(dn)`. A ref-list
field whose element is itself a list records the key `A[]`, which names no struct row, so both
lookups decline and the comparator answers its lenient default — the same answer it gives a row
it cannot resolve, which is the contract that keeps it from refusing on ignorance.

So the blind spot is one container deep, not one nesting level: the walk into a nested-struct
field is depth-general (D1696), and the walk THROUGH a second list is not there at all.

**Two outcomes, one ingredient**, which is worth naming because the second is the loud one and
would otherwise look like a different row: with the two elements' member SETS differing the
module fails to validate, and with their FIELD SETS differing (`{p:i32}[][]` against
`{q:i32,tag:string}[][]`) it is a loud `emitProgram: unknown struct field in field access`.
Both need the same walk.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| one container (`B[]` against `A[]`) | runs, prints `7` | **yes** — the second list is the ingredient |
| the declared `type H` | runs, prints `7` | **yes** |
| the differing member set (both `B[][]`) | runs, prints `hi` | **yes** |
| the annotation on the binding | runs, prints `7` | no — the rescue |
| the union (disjoint FIELD sets instead) | loud emit reject, not invalid wasm | no — the same ingredient, the other outcome |

#### Grading list

Five cells: the witness, its one-container control (which must keep running), the
`type H`-deleted control, the same-set control and the annotated rescue, plus the
field-set-differing spelling that is loud today and must move with it. The standing controls
are `tests/cases/structs/anon-struct-declared-row-capture-depth-and-list.vl` — whose
`refListElement`, `nullableRefListElement`, `reInternDepthOne` and `reInternDepthTwo` cells
are the ones a close must not move — and the eight `anon-struct-*.matrix.vl` templates, at
43/46, 42/46 and 46 of 46 for the other six.
