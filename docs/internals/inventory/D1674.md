### D1674 — an anonymous literal is keyed against a DECLARED inline-shape row without its element, so an annotated holder and an inferred one still share one row

**closed 2026-09-06 — the declared-row capture preference reaches the ref-list arm, asking the
element rows' field-name SUBSET and their boxes · filed against master `bce33eae0` · D1689's
residue: the same under-determined row identity, one row KIND over · residue D1697 · 0 corpus
cells**

Repro:

    type A = { r: i32 }
    type B = { q: i32, tag: string }
    function as1(): A[] { return [{ r: 7 }] }
    function bs1(): B[] { return [{ q: 4, tag: "a" }] }
    function one() {
      const box: { xs: A[] } = { xs: as1() }
      print(box.xs[0].r)
    }
    function two() {
      const box = { xs: bs1() }
      print(box.xs[0].q)
    }
    one()
    two()
    // vl check: rc 0.  vl run: prints 7 then 4

#### Mechanism

`structIndexOfObjCtxGo` scans EVERY struct row for a field-name and field-code match, declared
rows included. D1689 gave the match an element test, but only against an `#anon` row —
`anonValueFitsField`'s composite arms have always been anon-only, because a declared field's
code can differ from the value's through adoption and boxing. So the annotation on `one`'s
holder mints a declared `{xs:A[]}` row that `two`'s inferred literal still lands on, and the
element column is read off the wrong holder exactly as before.

The element comparison the declared side needs is the one `structRowOfObjFieldSet` already
runs on the type side (`fieldElemDeclConflict`): nominal, refutation-only, and a preference
rather than a veto. Dropping the `anonRow` guard and comparing the two names textually is not
it — measured 2026-09-06 on D1689's ref-list arm with the guard removed, that moves 19 of
3,046 `tests/cases` modules, four of them from building to a compiler trap (exit 70), because
a declared row spells its element while a minted one names a row, and the two spellings need
not agree.

**The close is D1690's destination gate plus a comparator narrow enough to keep that price at
zero.** The refutation is asked only for a row the literal's OWN destination does not name
(`repRowOfTyStruct(nodeRepTyIxOf(objIx))`), and it compares the two element ROWS on the two
things a re-intern cannot do: invent a field the source lacks, and renumber a box's positional
tags. Dropping and widening stay lenient, which is what the four modules above were about. With
that comparator the arm reaches codes 15, 5 and 28 and `compile(candidate, tests/cases)`
differs in **0 of 3,052** modules; the ref-list position matrix goes 19 of 46 to **43 of 46**,
its three remaining reds being a declaration-time rep gap of their own (D1692). What still
collapses is a LIST-OF-LIST element, which names no row for the comparator to walk: D1697.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the annotation on `one`'s holder | runs, prints `7` then `4` | **yes** — it is what makes the row declared |
| the differing element (both `A[]`) | runs, prints `7` then `4` | **yes** |
| annotating `two`'s holder instead of `one`'s | refuses identically | no (the same pair, other face) |
| annotating BOTH holders | runs, prints `7` then `4` | no — the rescue |

#### Grading list

Four cells: the witness above, its mirror with the annotation on `two` instead (the other
face), the both-annotated twin and the neither-annotated twin, which must both keep running.
The standing controls a close must not move are the two `tests/cases/structs/anon-struct-twin-holders-*.vl`
fixtures and `tests/cases/structs/anon-struct-list-field-union-and-nullable-element.vl`. The
matrix template `scripts/capability-probes/matrix/anon-struct-twin-holders-ref-list-field.matrix.vl`
grades this row at 23 delivery positions: its ANNOTATED face is this row, red at both seeds,
and its un-annotated face is D1689.

**And the code-15 arm of this same gate has a WRONG-VALUE spelling, filed as D1694** — a
declared `type H = { f: Inner }` captures the outer literal and the inner box is rebuilt at
`Inner`'s tags, printing `true` for the i32 `7`. Written as a prediction rather than a claim: a
close here that refutes on `fieldCodeRefsElemHeapC` (15, 5, 28) moves that row too, and one
that handles the ref-list arm alone does not — so re-grade D1694's witness either way. D1690's
close is the shape the declared side needs: condition the refutation on
`repRowOfTyStruct(nodeRepTyIxOf(objIx))`, the row the literal's own destination names, so a
delivered literal keeps its declared row while an uninvolved one cannot capture it.
