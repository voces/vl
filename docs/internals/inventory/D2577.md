### D2577 — an un-annotated parameter handed a `(boolean | null)[]` cannot lower the call

**loud emit reject · clause 2 · found by lane Y's D2572 position matrix (`argument`, un-annotated, at `boolean`) · filed 2026-09-26 against master `3e43afa90`**

Repro:

    function take(a) {
      const e = a[0]
      if e != null { print(e) } else { print("none") }
    }
    const xs: (boolean | null)[] = [true]
    take(xs)
    // vl check: rc 0.  vl build: emitProgram: bare null needs a struct-typed context

#### Mechanism

Not located. The refusal is anchored at the call `take(xs)`: the hole parameter is pinned from
the argument, and a nullable boolean element, which rides the i32 niche, reaches a lowering that
seeds its `null` as a struct. The `(i32 | null)[]` twin, a value box, runs.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the un-annotated parameter (`a: (boolean \| null)[]`) | runs, prints `true` | **yes** |
| the boolean element (`(i32 \| null)[]`) | runs, prints `3` | **yes** |

#### Grading list

* Lane Y's D2572 matrix cell `argument` un-annotated at `boolean`, which reaches this through
  a generic's `(boolean | null)[]` result.
