### D1340 — the ONE element rep [D1270](#d1270)'s rung declines: an un-annotated array literal whose element is a nullable STRUCT still builds the mono i32 list

**closed 2026-09-03 by building BOTH halves the row named — the niche re-grouping and the
NOMINAL render · was check-clean invalid wasm, clause 1,
`type mismatch: expected i32, found (ref null $type)` · 4 cells of the 40-cell D1270 grid ·
ZERO corpus cells · filed 2026-09-03 as the residue [D1270](#d1270)'s close named**

**THE CLOSE, AS WHAT THE FAILING RUNG RECEIVED.** An `emitFail` probe inside
`arrLitNulRefElemName` printed `nen=[{a:i32}|null] an=[{a:i32}|null[]] shapeK=-1
nom=[S|null] nomGrpK=1`. Both halves are in that one line: the render is STRUCTURAL, and the
re-formed array name has no grouping parens, so `refArrShapeKind` answers -1 where the same
element under its DECLARED name answers 1. The ANNOTATED control printed the identical probe
line — it reaches this rung and declines here too, and runs by the annotation walk instead,
which is why one half alone could not have closed the row.

`refElemArrName` now re-groups the niche `refArrShapeKind` has a kind-1 rung for (a declared
struct, or a union VARIANT — `nulStructElemNiche` reads that function's own two rungs, in its
order, so the grouping and the kind cannot disagree), and `nulRefElemEmitName` renders a
nullable-struct element through `nominalStructNameOfTy`, the reverse map an inferred
struct-union return is already spelled with. The VALUE-UNION neighbour the exclusion protected
is untouched: it has no niche, `nullablePartOf("i32|null")` is `i32`, neither rung claims it,
and it is still the `parenUnionArrElemName` arm below that groups it as kind 2.

**THE 20-CELL A/B, two seeds, `--before` at `origin/integrate-clause1`.** SEVEN cells moved,
every one to `runs`, and ZERO were lost: the four cells this row filed, plus an ANONYMOUS
nullable-struct element (no `type` declaration — the inline shape has its own registered row,
so it takes the niche too), a nullable VARIANT element, and a two-element literal whose
`bare null needs a struct-typed context` emit reject is gone. Unchanged: every annotated
control, all eight of D1270's sibling element reps, and the value-union neighbour at both
faces. Distilled corpus: 0 cells changed class.

Repro (prints `1`, as the annotated `const xs: (S | null)[] = [f(0)]` control always did;
it was check rc 0 and then the engine refusing the module):

    type S = {a: i32}
    function f(n: i32): S | null {
      if n == 0 { return {a: 5} }
      return null
    }
    const xs = [f(0)]
    print(xs.length)

#### Mechanism

`arrLitNulRefElemName` (D1270) claims a literal whose recorded element is a `TyNullable`
only when the ref-list layer can NAME the re-formed array — `nameIsRefArray(refElemArrName(nen))`.
`refElemArrName` re-groups the element in parens for exactly three niches, and its own header
says so: a nullable LIST, a nullable MAP and a nullable CLOSURE. A nullable STRUCT is
deliberately excluded there ("value-union / nullable-struct elements keep their existing
(munged-name) kind"), so `S | null` munges to `S | null[]`, `nameIsRefArray` reads a
multi-atom element and answers false, and the literal falls through to the i32 default exactly
as before.

So this is TWO missing halves, not one, and that is why it was not closed with its eight
siblings. `refElemArrName` needs a nullable-STRUCT arm, and the exclusion is load-bearing for
its NEIGHBOUR: re-grouping a value-union element (`i32 | null`) turns it from "not a ref array"
into kind 2, a real behaviour change on the annotated path. And the name the inferred literal
would key must be the NOMINAL `S|null` that `refArrShapeKind`'s `structIndexByName` rung
resolves — `tyToEmitName` renders a `TyObj` structurally (`{a:i32}|null`), which resolves no
struct row, so the two producers would name different rows for one element.

#### Ablation

Each line is one ingredient removed from the repro, as the row was FILED; the outcome column
is what it was then. The first row has since moved with the close — the un-annotated spelling
now runs too, which is the whole of it — and the rest still describe the mechanism.

| ingredient removed | outcome when filed | needed? |
| --- | --- | --- |
| the annotation (`const xs: (S \| null)[] = [f(0)]`) | runs, prints 5 | **yes** (its absence is) |
| the `\| null` on the element (`f(): S`) | runs | **yes** |
| the struct-ness (`f(): i32[] \| null`, `{[string]: i32} \| null`) | runs (D1270) | **yes** |
| the module scope (the same lines inside a function) | still fails | no |
| the silent spelling (`const r = xs[0]` + `if r != null`) | loud `emitProgram: bare null needs a struct-typed context` | no — the clause-2 face |

#### Grading list

All four cells (module / function scope × silent / read spelling); the annotated
`(S | null)[]` control; D1270's eight other element reps (`i32[]`, `i64[]`, `f64[]`,
`string[]`, `P[]`, `i32[][]`, `{[string]: i32}`, and the `string | null` control), every one
of which runs and must keep running; the ANONYMOUS `{a: i32} | null` element and the nullable
VARIANT element, which this close also buys; and the value-union neighbour `i32 | null` at
BOTH faces — the annotated one runs, the inferred one is still the kind-2 decline this rung
does not claim, and a candidate that moves the annotated one has broken what the exclusion
protects.
