### D957 — [CLOSED 2026-09-02] the inferred nullable SCALAR LIST: four leaves measured WORKING and backed out, because the return-site seed leaks into two other null contexts

**CLOSED 2026-09-02 by #2381 (D1062) — the filed witness runs and prints 2, un-annotated, at every element type this row named and at the i32-backed family it did not. Was: a loud check reject conceding type-validity (`… is not yet supported by codegen; annotate the return type`) for `i64[] | null`, `f64[] | null`, `f32[] | null` and `string[] | null`, with the i32-backed family refusing one stage later at emit · ZERO corpus cells · A FIX WAS BUILT HERE, ALL FOUR LEAVES MEASURED RUNNING, AND REVERTED; what shipped is a different design, and this row's own diagnosis of why the first one failed is the part worth keeping**

D956's map widening and D937 before it are the same four-site shape, and it applies here
almost cleanly: the four `nul*list` VKinds already exist, `nulScalarListWrapHeap` already maps
each to its wrapper heap, and **every annotated twin lowers and runs today**. Four sites —
renderer arm, `buildFnMap` classifier, `retKindPri` tier, return-site seed, plus
`exprNulScalarListKind`'s caller fallback — made all four leaves print correctly, both
branches.

* **IT WAS REVERTED FOR TWO MEASURED REASONS, and both are about the SEED rather than the
  capability.**

  1. **The i32-BACKED family needs a fifth site and is worse without it.** `i32[]`,
     `boolean[]` and litunion arrays share one `nullist` kind, and
     `exprNulScalarListKind`'s caller fallback answers only for the DISTINCT-backing kinds.
     Classify a `nullist` return without extending that arm and the caller's binding stays
     non-null: the program becomes check-clean INVALID WASM, strictly worse than the loud
     emit reject it replaced. Backed out on its own before the rest.

  2. **The seed leaks into an assignment-as-implicit-return.**
     `tests/cases/functions/tail-assign-coercing-global.vl` broke with
     `global.set expected (ref null 0), found ref.null of type (ref null 10)`. For a function
     whose last statement is an ASSIGNMENT, `criClassify` reads `fRetKind` from the TARGET
     CELL (`globalCellKind`) — so a nullable-scalar-list target makes the return-site seed
     type a null that belongs to the global, not to the return. The nulmap seed D937 shipped
     has the same shape and does not hit this, which is why it was invisible until a list
     kind reached the same column.

* **SO THE MISSING PIECE IS NAMED, and it is not the renderer.** The seed has to know it is
  typing the RETURN's own null rather than whatever null comes next, and `fRetKind` alone does
  not carry that — for an assignment tail it is describing a cell. Either the seed is scoped to
  a genuine `return null` / tail-`null`, or the assignment-tail case is excluded where
  `fRetKind` is read.

* **THE CORPUS DID NOT CATCH EITHER ONE** — 0 movement, 0 runs lost, on both cuts. Both were
  caught by `deno task test`'s fixtures. A capability with no corpus cell has no corpus
  protection either, which is the same asymmetry `--sites` exists to report.

Repro (runs, prints 2):

    function pick(c: boolean) {
      if c { return null }
      const m: i64[] = [1, 2]
      m
    }
    const r = pick(false)
    if r != null { print((r).length) } else { print(0) }
    // NOW: vl check rc 0, vl run prints 2 — the inferred `i64[] | null` return lowers.
    // WAS: `'pick' infers the nullable return type i64[] | null — type-valid, but an
    // inferred return of this shape is not yet supported by codegen; annotate the return
    // type`. The annotated ": i64[] | null" spelling ran throughout.

#### The close — a DIFFERENT design shipped, and it covered the leaf this row could not

**#2381 CLOSED IT AS D1062, not by finishing the seed this row was left holding.** D1062 read
the same floor as ONE shape split across TWO channels — `nullableRetName` named
`i32[]|null` / `boolean[]|null` and nothing else, so those two refused at the EMITTER while
every other element type refused at the CHECKER — and answered with `nulListRetName` naming
the whole family plus `synthNulListRetAnns` pinning each function's own return annotation to
it. The floor then accepts exactly what the pin can serve. That is why it reached the
i32-backed leaf whose absent fifth site sank the attempt filed here: the pin is the annotated
spelling, so no new un-annotated seed had to be typed at all.

**MEASURED ON THE SEED AT `4990e0c2`, 2026-09-03**, twelve programs — the witness above at
`i64`, `f64`, `f32`, `string`, `i32` and `boolean`, each in BOTH faces (un-annotated, and
`: T[] | null` declared). **12 of 12 print `2` at rc 0.** The two-face grid is the point: this
row's whole subject was a gap that existed only at the un-annotated face, so a fixture that
annotated could not have seen it either way.

**THIS ROW IS ALSO WHY THE GRADER NO LONGER TAKES PROSE FOR A WITNESS.** Its numbered list
above writes continuation lines five spaces in — correct Markdown, and indistinguishable from
a program to a checker whose fallback rule was "the first indented block after the status
line". `check-filed-witnesses.py` graded THAT English, `vl check` called it a parse error, the
row's then-declared `check reject` matched, and `--strict` passed on a program nobody wrote —
for the whole time this row was already fixed. The `Repro:` block now wins over an earlier
indented one, and a witness that does not PARSE is its own outcome (`witness_unparsed`).
