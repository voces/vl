### D1666 — the emitter's capability refusals are counted by their WORDING, and 97% of them do not confess

**loud emit reject · 21 witnessed refusal literals, of which 18 are named by this row · filed 2026-09-05 against master `213504611`**

Repro:

    type Circle = { r: i32 }
    type Sq = { s: i32 }
    type K = Circle | Sq
    function f(h: () => ({[string]: i32} | K)): i32 { 1 }
    print(f(() => Map()))
    // vl check: rc 0.  vl run: emitProgram: a map union member is not supported — use a struct

#### Mechanism

`scripts/goal-scoreboard.py` finds capability refusals by matching eight CONCEDING PHRASES
against the compiler's message literals. Re-derived 2026-09-05, `emitFail` / `emitFailAt`
have **533** call sites and the predicate matches **16**; the other **517 (97%)** say nothing
about whether the refused program was legal, so no wording-keyed count can see them. A seeded
sample of 48 grades **21 LIVE / 20 UNREACHABLE / 7 UNDECIDED**, putting ≈226–302 of the 517
inside clause 2 — and the sixteen the predicate does match were witnessed as floors in the
2026-09-02 audit, so the count is not merely low, it is aimed at the wrong sites.

This is an umbrella row. The population it stands for is
`scripts/capability-probes/live-sites.json`, one refusal literal per row with a probe that
reaches it; the measurement is `docs/internals/emit-refusal-wording-2026-09.md`. The witness
above is the plainest of the 21: the 2026-09-02 ruling that an else-less `if` used as a VALUE
is `T | null` reached the binding position and not the argument position.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the argument position (`const x = if true { 1 }`) | runs | **yes** |
| the missing `else` (`if true { 1 } else { 2 }`) | runs | **yes** |
| the numeric arms (`print(if true { true })`) | still refuses | no |

#### Grading list

The row closes when `python3 scripts/capability-probes/run.py --live-sites` reports every
listed literal FELL, not when this witness runs — so the witness is any one still-live literal
and is re-pointed whenever the one it names falls. It named `union atom has no value box`
(`if-expr-no-else-argument-position.vl`) until [D1767](D1767.md) closed that gap and took the
entry off the ratchet, which is why the list reads 16 rather than 17. Four of the 21 (the `.map` result classified
only when its callback resolves to a lifted function) are one mechanism and should be re-graded
together. Three clause-1 findings from the same measurement are filed as D1667, D1668 and
D1669, each carrying its probe as its witness.
