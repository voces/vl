### D2444 — an unused `boolean | null` record field breaks another function through an inferred boolean-or-record return

**check-clean invalid wasm · clause 1 · found in review of #3148, the same on master `a20d69104` · filed 2026-09-25 against master `a20d69104`**

Repro:

    type A = { r: i32 }
    function mkA(): A | null { return { r: 1 } }
    type R = { f: boolean | null }
    function rr(c: boolean) { if c { return true } return { r: 7 } }
    print(rr(false) is A)
    // vl check: rc 0.  vl run: invalid module, inside `mkA`:
    // "type mismatch: expected (ref null $type), found (ref $type)".  Expected: true

#### Mechanism

Not yet located. Three unrelated declarations have to meet: the `boolean | null` field type
registers something that the inferred `boolean | A` return of `rr` then keys on, and `mkA`'s
`A | null` return is emitted at the wrong heap type. Each ingredient alone is inert.

#### Ablation

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| no `type R` | runs, `true` | **yes** |
| no `mkA` | runs, `true` | **yes** |
