### D2258 — a closure capturing a `for-in` variable over an annotated nullable-scalar list types the variable as the bare scalar

**check-clean invalid wasm · `vl check` rc 0, then the emitted module fails validation inside
the enclosing function, clause 1 · 18 cells of the D2252 closure grid, SILENT with or without
the shadowing that grid was built for · filed 2026-09-23 against master `b2b56b450`**

Repro:

    function mk() {
      const xs: (i32 | null)[] = [41]
      for r in xs {
        const g = () => r
        print(g() == null)
      }
    }
    mk()
    // vl check: rc 0.  vl run: invalid module,
    // "type mismatch: expected i32, found (ref $type)"

#### Mechanism

The disassembly shows the loop variable's local declared `i32` while the element read leaves
the `(ref $uBox)` the list holds. A captured loop variable is typed through
`captureLoopVarIterOf` / `forInElemKind` in the type section, where no frame's locals are
bound; for a bare local iterable that ladder cannot see `xs`'s annotation (`exprRefArray`'s
`Ident` arm has no `frameLetOf` rung) and re-asks the initializer instead, and `[41]` alone
classifies as an `i32` list. An initializer holding a `null` (`[41, null]`) classifies as the
box list and runs. A prediction until a close confirms which rung.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the closure (read `r` directly in the loop) | runs | **yes** |
| the element's `null` (`i32[]`, `() => i32`) | runs | **yes** |
| the enclosing function (the loop at module scope) | runs | **yes** |
| a `null` in the initializer (`[41, null]`) | runs | **yes** |
| the closure's annotation (`const g: () => i32 \| null = () => r`) | still invalid wasm | no |
| `i32` for `f64` | still invalid wasm | no |
| `i32` for `string` (`(string \| null)[]`) | runs | **yes** |
| the loop variable itself (`const r = x` from `for x in xs`, captured) | still invalid wasm | no |

#### Grading list

The D2252 closure grid's `nint_forin_*` cells (a `for r in xs` over `(i32 | null)[]` assigned
into an outer closure variable, depth 1-3), and the derived-`const` spelling in the ablation.
