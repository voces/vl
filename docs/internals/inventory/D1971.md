### D1971 — an `if` whose ELSE arm always exits does not narrow the code after it to the then-arm's facts

**loud check reject, clause 2 · residue of D1970's grid (the else-exits column, all 15 shapes, both return annotations) · filed 2026-09-22 against master `a8a104432`**

Repro:

    type IoError = { msg: string }
    function f(x: i64 | IoError): i64 | null {
      if x is i64 {
        print("ok")
      } else {
        return null
      }
      x
    }
    print(f(3 as i64))
    // vl check: rc 1, `expected i64 | null, got i64 | IoError` at the trailing `x`

#### Mechanism

`checkIfStmtNode`'s post-guard leg applies the ELSE narrowings when the then-arm exits; there is
no mirror that applies the THEN narrowings when the else arm exits and the then-arm does not,
and `pushPostGuardNarrow` in the emitter has none either. Only the code after the `if` is
affected — inside each arm the narrowing is already right. Both halves must land together
(checker leg, emitter `setNarrowFromCond` leg), gated on the same `stmtAlwaysExits(ifElse)`.

#### Grading list

Every shape of D1970's grid in the else position, all three directions, both return spellings;
an `else if` chain whose final `else` exits (only that chain's LAST condition may narrow).
