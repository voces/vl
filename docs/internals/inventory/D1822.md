### D1822 — the twice-narrowed ALIAS spelling loses the LIST arm the same way the map arm lost it, and the list channel has no arena rung to fall back on

**loud emit reject — `emitProgram: '.length' on a receiver the emitter cannot classify as a
list, string, map or set` · clause 2 · filed 2026-09-07 against master `678f578e3`, found
grading [D1864](D1864.md)'s array-arm control at the ALIAS spelling its witness does not use**

Repro:

    import { Json, JsonError, parseJson } from "std:json"

    type JsonArr = Json[]

    const r4 = parseJson("[1, 2]")
    if r4 is JsonError {
      print("err")
    } else {
      if r4 is JsonArr { print(r4.length) } else { print("not an array") }
    }
    // vl check: rc 0.  vl run: emitProgram: '.length' on a receiver the emitter
    //           cannot classify as a list, string, map or set

#### Mechanism

**Not instrumented; predicted from the sibling that WAS.** [D1864](D1864.md) measured the map
arm at this exact position: a second narrowing over one binding banks the ALIAS the program
wrote (`JsonArr`) where the first banked the rendered spelling, and the classifier's last rung
is a name test that cannot see through it. The map channel had an arena rung one line above the
name test — `unMemIsMap(narrowSlotTy(i))` — so its close renders that row rather than letting
the name veto it.

The list channel is the same shape without that neighbour, so the prediction is that
`narrowedRefArrayOf` (and the scalar-list twin) needs the same arena render. **Probe the decline
before believing it**: the map row's own first two attributions in this family died to
measurement, and one arm's mechanism is not evidence for another's.

The INLINE spelling runs and is [D1864](D1864.md)'s control — `r4 is Json[]` prints `2` at the
same twice-narrowed position, on master and after that close. So this is the alias, not the
narrowing depth.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the alias (`r4 is Json[]` inline) | runs, prints `2` | **yes** |
| the outer `is JsonError` guard | runs, prints `2` | **yes** |
| the list arm — the same alias over the MAP arm | runs since [D1864](D1864.md) | **yes** |

#### Grading list

* [D1864](D1864.md)'s fixture, whose array and string arms RUN today and must keep running.
* The inline spelling `r4 is Json[]`, which RUNS today at both narrowing depths.
* The map arm at the same alias spelling, which [D1864](D1864.md) closed — a list-side close
  must not disturb it.
