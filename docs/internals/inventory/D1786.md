### D1786 — a nested-map ref-list element resolves its value slot TWICE in one compile, `-3` before the inner map's mv row exists and the slot after

**loud emit reject · clause 2 · 8 cells of the nested-map capture grid · filed 2026-09-06 against master `73ca4dfc1`, mechanism probed and two candidates refused against `be5a79fd2` · found while closing [D1772](D1772.md) · 0 corpus cells**

Repro:

    type Rec = { n: i32 }
    function mki(): { [string]: Rec } {
      const m: { [string]: Rec } = Map()
      m["k"] = { n: 4 }
      return m
    }
    function mko(): { [string]: { [string]: Rec } } {
      const o: { [string]: { [string]: Rec } } = Map()
      o["o"] = mki()
      return o
    }
    const xs = [mko()]
    const held = xs[0]
    print(((held["o"] ?? Map())["k"] ?? { n: 0 }).n)
    // vl check: rc 0.  vl run: emitProgram: the locals vector desyncs from the frame layout at the map-values output

#### Mechanism

**The candidate rung is the right one and the defect is not a missing arm — the answer is
UNSTABLE.** Instrumented at `compositionMapReadSlot`'s list-of-maps arm, beside the three
controls that run:

    witness  elemName=[{[string]:{[string]:{n:i32}}}]  LISTans=-3  atFalse=-1  atTrue=-1
    witness  elemName=[{[string]:{[string]:{n:i32}}}]  LISTans=3   atFalse=3   atTrue=3
    string   elemName=[{[string]:{[string]:string}}]   LISTans=1   atFalse=1   atTrue=1
    one-deep elemName=[{[string]:{n:i32}}]             LISTans=1   atFalse=1   atTrue=1
    annot'd  elemName=[{[string]:{[string]:Rec}}]      LISTans=2   atFalse=2   atTrue=2

The witness answers twice in one compile; every control answers once. `rlInternNameTy` pushed
`rlElemTyIx = -1` for this element, so the arena leg is uncovered and the resolver falls to its
element-NAME leg — which answers `-3` (unsupported) until the inner map's mv row is interned,
then the real slot. `-3` is what the frame-layout pass reserves on and the slot is what the
emit pass declares, which is exactly what `fbBeginFunc reserved a different slot count than
fbEmitLocalsVec declared` reports. The annotated ARRAY runs because its element name keeps the
declared `Rec` and resolves on the first ask.

**The three messages are one coordinate.** A 32-cell grid over inner value × projection × face
× scope × capture leaves exactly eight loud cells, all `struct × index`; every `field` cell and
every non-struct leaf runs. `locals vector desyncs` on six, `arity has no interned signature`
on one, `` `??` … not supported yet `` on one — three consumers of one unresolved slot.

#### Ablation

Every row is `vl check` rc 0. Two candidates were built and BOTH REFUSED, each with its price.

| candidate / ingredient | measured |
| --- | --- |
| **the mint fall-back** — give `rlElemMapValMvSlot` the `mvShapeOfMapName` its sibling `rlElemMapShape` already takes | **REFUSED**: 0 cells gained, **3 loud → check-clean invalid wasm**. D5-final's find-only discipline is right; the row must exist EARLIER, not be minted at the read. |
| **the map hint arm** — `arrLitElemHintTy` hands `arrLitArenaElemRow` for a map element, so the mint records the arena type | **REFUSED**: grid 23 → 32 stacked on [D1772](D1772.md), but a **`runs → not-runs`** — a plain shallow-map list (`const ds = [mki()]`) runs on master and refuses `ref valtype with no interned shape` once a nested-map function is merely DECLARED in the module, and a second spelling moves loud → SILENT. |
| the ARRAY annotated | runs, 4 — **needed** |
| the intermediate array removed | runs, 4 — **needed** |
| the FIELD projection at the same rep | runs, 4 — **needed** |
| one container deep, struct leaf, index | runs, 4 — **needed** |
| a STRING-valued inner map (a ref, not a struct) | runs, 4 — **needed** |
| an f64-valued inner map | runs, 4 — **needed** |
| the binding, the capture, or reading the inner map | loud, unchanged — not needed |

Needed: a nested map AND a struct leaf AND an un-annotated ref-list element.

**Neither the 32-cell grid nor `tests/cases` byte-identity could see the second candidate's
regression** — the grid carries one list shape per program and no fixture holds the pair, so
the loss surfaced only when a fixture put a shallow-map list beside a nested-map one. A close
here must grade that pair explicitly.

#### Grading list

* the 32-cell grid — a close reaches **32 of 32** only stacked on [D1772](D1772.md); alone it
  moves `struct_index_unann_function_cap` loud → SILENT, that cell being D1772's struct twin.
* **the shallow-map list beside a nested-map one, both directions**, and a shallow-map list in
  a module that merely DECLARES a nested-map function — the shapes the second candidate lost.
* the three controls that bound the ingredient (string leaf, f64 leaf, one container deep) and
  the annotated ARRAY, all of which run today.
* [D1772](D1772.md)'s witness and fixture, the same grid's i32 column.
