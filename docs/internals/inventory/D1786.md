### D1786 — an un-annotated ref-list ELEMENT that is a nested map with a STRUCT-valued inner map resolves no inner slot, and three refusal sites report it in three sentences

**loud emit reject · clause 2 · 8 cells of the nested-map capture grid · filed 2026-09-06 against master `73ca4dfc1` · found while closing [D1772](D1772.md) · 0 corpus cells**

Repro:

    type Rec = { n: i32 }
    function mki(): { [string]: Rec } {
      const m: { [string]: Rec } = Map()
      m["k"] = { n: 4 }
      return m
    }
    function mko(): { [string]: { [string]: Rec } } {
      const o: { [string]: { [string]: Rec } } = Map()
      o["o"] = mki()
      return o
    }
    const xs = [mko()]
    const held = xs[0]
    print(((held["o"] ?? Map())["k"] ?? { n: 0 }).n)
    // vl check: rc 0.  vl run: emitProgram: the locals vector desyncs from the frame layout at the map-values output

#### Mechanism

**The three messages are one coordinate, and the grid is what says so.** A 32-cell grid over
inner value × projection × face × scope × capture leaves exactly eight loud cells after
[D1772](D1772.md)'s close, and all eight are `struct_*_index_*` — a struct-valued inner map
delivered through an INDEX of a ref list. Every `field` cell and every non-struct inner value
runs. The three sentences are three consumers downstream of one unresolved slot:

| cell | message |
| --- | --- |
| `struct_index_ann_{function,module}_{cap,nocap}` (4) | `the locals vector desyncs from the frame layout at the map-values output` |
| `struct_index_unann_module_{cap,nocap}` (2) | the same |
| `struct_index_unann_function_cap` | `function-value call arity has no interned signature` |
| `struct_index_unann_function_nocap` | `` `??` over this nullable value is not supported yet `` |

Grouping by message would file three rows; grouping by where the cells came from files one.
The mechanism is not yet probed to a rung — what is measured is the coordinate, and the
candidate rung is the ref-list element's inner-map value slot (`rlElemMapValMvSlot` off
`compositionMapReadSlot`'s list-of-maps arm), since annotating the ARRAY is what clears it.

#### Ablation

Every row is `vl check` rc 0.

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| — the witness | loud, `locals vector desyncs` | — |
| the ARRAY annotated (`xs: { [string]: { [string]: Rec } }[]`) | runs, 4 | **yes** |
| the intermediate array (`const held = mko()`) | runs, 4 | **yes** |
| the FIELD projection at the same rep | runs, 4 | **yes** |
| one container deep, struct-valued, index | runs, 4 | **yes** |
| a STRING-valued inner map (a ref, not a struct) | runs, 4 | **yes** |
| an f64-valued inner map (a scalar, not i32) | runs, 4 | **yes** |
| the binding (`xs[0]["o"]` read straight) | loud, the same | no |
| the capture | loud, the same | no |
| indexing the inner map (bind it, then read) | loud, the same | no |

Needed: a nested map AND a STRUCT-valued inner map AND an un-annotated ref-list element.
Not needed: the binding, the capture, the scope, the face, or reading the inner map at all —
which is why this is not [D1772](D1772.md)'s cell at another rep.

#### Grading list

* the eight cells above, which a close must move together — a close that moves only the
  `no-fnsig` one has grouped by message again.
* the string-valued and f64-valued inner maps, and the one-container-deep struct map, all of
  which run today and must keep running: they are what bounds the ingredient to the struct leaf.
* [D1772](D1772.md)'s witness and its fixture
  `tests/cases/closures/capture-inferred-projection-nested-map.vl`, the same grid's i32 column.
