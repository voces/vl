### D1306 — a BLOCK-BODIED lambda declared inside a module-scope block is check-clean invalid wasm; the expression-bodied twin runs

**runs, prints `7` — CLOSED 2026-09-03 with [D1244](#d1244): a module-scope block is a FRAME, and both rungs of the family were the same missing premise · was check-clean invalid wasm (`type mismatch: expected a type but nothing on stack`), reproducing identically on master `e6823dc8` and after #2447 · found 2026-09-03 grading `block_if_capture` in `matrix/global-shadowed-by-local.matrix.vl`, which is what that landing's position matrix was for**

Repro:

    if true {
      const f = () => {
        const q = 7
        print(q)
      }
      f()
    }
    // vl check rc 0. vl run: Invalid input WebAssembly code at offset 224:
    // type mismatch: expected a type but nothing on stack

#### Mechanism

Not yet named. What is measured: the ingredient is the BLOCK BODY of the lambda, not the
capture and not any global. `() => 7` in the same module-scope `if` runs and prints; giving
the lambda a `{ … }` body with its own `const` makes the module invalid. Wrapping the whole
block in `function g() { … }` runs, so it is the MODULE-scope block specifically — the same
scope D1244's row isolates. The stack-shape message points at the lifted body's result
seam rather than at any type, so the row records the ablation and stops there.

#### Ablation

| ingredient removed | outcome | needed? |
| --- | --- | --- |
| the lambda's block body (`() => 7`) | runs, prints `7` | **yes** |
| the module-scope block (the lambda at top level) | runs | **yes** |
| the block moved inside a function (`function g() { if true { … } }`) | runs | **yes** |
| a captured variable in the lambda | still invalid wasm | no |
| a module global shadowed by the lambda's `const` | still invalid wasm | no |

#### Grading list

* D1244 — the same family's other rung. A fix for either must re-grade the other's witness.
* `matrix/global-shadowed-by-local.matrix.vl`'s three `block_*_capture` positions, which
  SKIP on this row and must be un-skipped when it closes.

* **THE CLOSE PROVES IT WAS ONE PREMISE, NOT TWO RUNGS.** This row was filed as D1244's
  sibling *at a different rung* — a lifted body's result seam here, a captured ref field
  there. D1244's fix closes both without touching either seam, so the shared cause is
  simply that a module-scope block was never a frame: anything needing one failed there,
  whatever the local reason looked like. Graded on this row's own witness, verbatim, on
  the merged tree — `7`, where master fails to compile.

* **THE ABLATION HELD AND IS WHY THE RECONSTRUCTION FAILED.** A capture is scenery for
  this row and the whole ingredient for D1244. A program written from this row's one-line
  description WITH a capture added prints `7` on master too, so it grades nothing — the
  filed witness is the only thing that separates the two rows.
