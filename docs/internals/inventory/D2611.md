### D2611 — a value of a union of numeric types is accepted where one of its wider members is expected, and the box is passed as that number

**loud check reject · clause 2 · open (was invalid wasm past the checker, clause 1, on master
`e289f84d7`; #3184 refuses it) · filed 2026-09-26, found by the narrowed-write grid of D2609**

Repro:

    function useF(v: f64) { print(v * 2.0) }
    function src(): i32 | f64 { return 7 }
    useF(src())
    // vl check: argument 1: expected f64, got i32 | f64.  Wanted: prints `14`

#### Mechanism

`assignable` accepted a union when every member is assignable, and `i32` widens to `f64`, so
`i32 | f64` was assignable to `f64`. No delivery position converts the value box: the argument,
the binding and the return passed the box where an `f64` is expected, and the module failed to
validate. #3184 made the arm join keep a numeric member as its own (D2627), which put many more
programs into that shape, so `assignable` now refuses a numeric union into a numeric primitive
it only widens to. The open part is the lowering: unbox by tag and convert each payload
(`f64.convert_i32_s`, `f64.promote_f32`), as a plain `i32` widening does, at every delivery
position; then the refusal goes. `emitExprAsF64` is the seam for most of them.

#### Ablation

| ingredient changed | outcome | needed? |
| --- | --- | --- |
| `const y: f64 = src()`, a binding | the same refusal | no |
| `src` returns `1.5`, the `f64` member | the same refusal | no |
| `i32 \| i64` into `i64` | the same refusal | no |
| `f32 \| f64` into `f64` | the same refusal | no |

#### Grading list

Every delivery position of `scripts/capability-probes/matrix.py` with a numeric union value and
its widest member as the destination type; the `S4c0` rows of #3184's review grid.
