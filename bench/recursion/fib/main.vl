// Naive binary recursion: fib(42) is ~866M calls, ~2 per returned unit.
// The axis is raw non-tail call overhead — the body is one compare and one add,
// so nearly all the time is call/return.
function fib(n: i32): i32 {
  if n < 2 { return n }
  return fib(n - 1) + fib(n - 2)
}

print(fib(42))
