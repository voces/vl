{
  "name": "convert",
  "category": "arith",
  "axis": "i32 <-> f64 conversion round trip",
  "n": 400000000,
  "nPython": 4000000,
  "expect": "19659595480064\n",
  "expectPython": "196494735104\n",
  "notes": "Widen an i32 to f64, scale, truncate back to i32, accumulate in i64 — two conversions per iteration, both explicit `as` casts in VL. All values stay well inside i32, so wasm's trapping i32.trunc_f64_s, Rust's saturating `as i32`, JS `|0` and Python `int()` all agree."
}
