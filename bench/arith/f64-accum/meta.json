{
  "name": "f64-accum",
  "category": "arith",
  "axis": "f64 add/mul — accumulate + globals-cliff probe",
  "n": 300000000,
  "nPython": 3000000,
  "expect": "177773602.85764426\n",
  "expectPython": "1777477.142379654\n",
  "notes": "Sum of the logistic map x <- x*3.9*(1-x), r=3.9 so the orbit is chaotic and bounded in (0,1). Chaos makes the loop impossible to close in any compiler, and float addition is non-associative so no runtime may reassociate or vectorize it; it is also a very strong equivalence check, since a single divergent ULP would amplify into a visibly different sum. All four agree bit-for-bit. Scale-verified by an independent 4-point sweep (75M/150M/300M/600M -> 138/273/550/1090 ms, ratios 1.98/2.01/1.98); the in-harness 2N/2 reading of 1.81 was box contention, not sub-linearity. main_global.vl is the top-level-binding spelling for the globals cliff.",
  "audit": {
    "pythonStartupFloor": "Python column is 72ms = 17% interpreter startup; under the 200ms floor. nPython should be raised ~3x before normalising. measured empty-program startup on this box (min-of-5, taskset -c 2): rustc-O binary 6.8ms, deno 19.3ms, python3 12.5ms, `vl run <prebuilt.wasm>` 10.6ms.",
    "protocol": "ADVERSARIAL AUDIT, measurement protocol: (1) VL must be measured from a PREBUILT module — `vl run <src>` carries compile time, measured at 10-390ms (collections/map-string 1.47s prebuilt vs 1.86s from source = 27% compile; arrays/struct-aos 1.63s vs 1.92s). (2) This box swings up to 2.5x under contention even with taskset: recursion/flatcall's Rust binary read 2.53s in a sequential sweep and 0.996s as min-of-5 alone; recursion/fib's Rust read 2.02s while a second timing job ran and 0.53s alone. Every number must be min-of-N with EXCLUSIVE use of the box. (3) The known top-level-binding globals cliff is largely GONE at HEAD (#1321): measured locals-vs-globals 1.00-1.63x (f64-accum 1.63x and i64-accum 1.43x are the only real ones; fill-sum 1.05x, reverse-inplace 1.00x, map-i32 1.17x, struct-field 0.99x, char-scan 1.08x, token-count 0.95x). flatcall/flatcall-inlined/treewalk/ackermann/deeprec/mutual/tailcall spell their hot loop at TOP LEVEL while arith/* and collections/* wrap it in `function main()`; verified this no longer biases them (flatcall top-level 1.477s vs in-a-function 1.470s)."
  }
}
