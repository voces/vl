# Written by build.ts (and by verify.ts before it bundles): the content-hashed
# filename of the compiler seed, pinned into the bundle as a module so a missing
# name is a build error rather than a runtime 404. Regenerated every build, so it
# must never be committed.
src/generated/
