# VL — Changelog

Shipped work, in rough area order. Each entry is a terse one-liner; detail lives in git
history, linked PRs, and `docs/`. For the forward plan see **`ROADMAP.md`**; for rationale
see **`DECISIONS.md`**.

---

## Type system (Track A)

- **A0 Type-algebra inventory** — `Alias`, `Function`, `Object` (structural + index sigs), literal types, `Union`, `Nullable`, `Intersection`, `Negation`, `Unknown`/`Infer`, `Never`, `Type`, `Custom`.
- **A3 Intersection surface syntax** — `A & B` parses, binds tighter than `|`, folds through `intersectType` at parse time; narrowing algebra (`intersectType`) done. Object-type structural intersection: `{a} & {b}` now merges field-wise to `{a, b}` (shared fields refine to their per-field meet; an impossible field → `Never`).
- **A3 fix: `never`-typed value rejected cleanly (no codegen crash)** — forming a VALUE for a binding/annotation whose type folds to `never` (`type C = A & B` with `A` an object and `B` a disjoint string literal) is now a clear, source-located type error — `Type \`C\` is \`never\` (an empty type — \`A & B\`, an object and a string literal, have no common values), so it has no values` — instead of slipping through to codegen and dying as the opaque `Unhandled AST -> WASM "Never" type`. The diagnostic RANGE points at the `A & B` intersection EXPRESSION (a source span the parser captures when it folds the `&` and threads alongside the empty-intersection tag), not the alias name or the value site. `ensureType`'s never branch reports when the TARGET is a (tagged) empty intersection, a real value flows into it, and a real `ctx` is present; legitimate `never` (a narrowed unreachable branch, a membership/narrowing query) is untouched. A defensive backstop in `toWasm.ts`'s `toWasmType` throws a clear compiler diagnostic if a `Never` ever reaches codegen (unreachable in practice). (`tests/cases/types/never-value-*.vl`, `never-narrowing-legit.vl`)
- **A14 fix: honest messages for degenerate self-referential aliases** — a pure self-cycle `type D = D` no longer claims `opaque/recursive type aliases are not yet supported` (which implied a future feature); it reports `Type \`D\` is defined in terms of itself with no base type, so it can never have a value` — it has no structural base, so nothing can inhabit it. A self-referential intersection `type E = A & E` (same class: a definition in terms of itself that collapses to `never`) reports `Type \`E\` refers to itself inside an intersection (\`A & E\`), so it reduces to \`never\``, ranged on the `A & E` expression. LEGITIMATE recursive aliases (`type List = { rest: List[] }`, mutual recursion) are unaffected — they resolve to a real structural body and never hit the degenerate-cycle path. (`tests/cases/types/self-alias-still-clean.vl`, `never-value-self-intersection.vl`, `recursive-array-element.vl`, `mutual-recursive-type.vl`)
- **B17 lint: empty-intersection warning** — a `type X = A & B` (or a binding so annotated) whose two CONCRETE operands share no values folds to `never`; the declaration is flagged with a `warning` (`code: empty-intersection`, `source: vital`) — `Intersection \`A & B\` is empty (\`never\`) — an object and a string literal, have no common values` — ranged on the `A & B` intersection expression (not the alias name), matching the never-value error. A self-referential `type E = A & E` instead warns `Type \`E\` refers to itself inside an intersection (\`A & E\`), so it reduces to \`never\``. Scoped to concrete operands: a generic `T & U` (empty only for some instantiation) is NOT flagged. (`tests/cases/lint/empty-intersection.vl`, `generic-intersection-no-warn.vl`)
- **A4 Negation surface syntax** — `!A` and `x !is T` (Kotlin-style negated guard); `subtractType` narrowing algebra. → `DECISIONS.md`
- **A5 Flow narrowing (broad)** — nullness, union-member, post-guard clauses, `&&`/`||` chains, De Morgan, else-of-else-if, literal discrimination, `?.`/`??`. (→ `docs/narrowing.md`)
- **A6 `is` operator + tagged unions** — `x is T` discriminates a value union at runtime (niche/value-kind/boxed encodings, global tag registry, `coerceUnion`). (→ `docs/unions.md`)
- **A6b Proof-carrying narrowing (degenerate)** — a body that is exactly `return <predicate-on-a-param>` is an inferred guard; guard-clause `if absent(v) { return }` supported.
- **A7 Real `string` type (core)** — proper Object (`{[i32]: i32}` index sig → i32-array of char codes), `.length`/`s[i]`/`+`/`==`/`slice`/`indexOf`/`includes`/`charCodeAt`.
- **A10 Parametric types / generics** — Stage 1 (function type params), Stage 2 (array element inference), Stage 3 (generic `type` aliases incl. nested/array); `map`/`filter` build-side generics over growable `T[]`. (`tests/cases/generics/`)
- **A-infer-null `let x = null` as a nullable hole** — an un-annotated `let x = null` is no longer pinned to the exact `null` type (which made `let x = null; x = 5` error `expected null, got 5`); `null` is now hole-bearing like `[]`. The binding is `T | null` where `T` is a fresh shared inference hole (`Nullable<Infer<Unknown>>`), and a later constraint — a reassignment `x = <expr>`, a `T | null` param, an annotated assignment — FILLS the hole's `T` in place via the same `constrainElementHole` machinery the empty array uses (`x = 5` ⇒ `x: i32 | null`). This is NOT a pin violation: VL still pins COMPLETE types (`let x = 7; x = "foo"` errors — `7` is complete, `null` is not), and once the hole is filled the cell behaves as that complete type (`let x = null; x = 5; x = "foo"` errors). The initializer contributes the `| null`, which is sticky (it reflects that `x` really was null) EXCEPT flow-narrowing strips it on a path where `x` is provably non-null: a straight-line `x = <non-null>` narrows `x` to its non-null base for the rest of the block (no null tax — `let x = null; x = 5; x + 1` runs), threaded through the shared `postGuardNarrowings` entry point so both the type checker's scope overlay and codegen's narrowed overlay get it; an assignment inside an `if` branch stays in that branch, so the conditional-assign idiom (`let x = null; if c { x = f() }`) still requires a null check across the branch. An UNCONSTRAINED `let x = null` (never reassigned/used to pin `T`) resolves to the plain `null` type — a lone `null` IS fully determined, so the inference floor (`reportUninferredBinding`) does NOT raise "cannot infer" for it (distinct from an empty `[]`, whose element is genuinely unknown). A `const x = null` (immutable) keeps the exact `null` type, and an explicit `let x: T | null = null` is unchanged. Also fixes a latent bug exposed here: typing an assignment EXPRESSION (a block-tail `{ x = v }`) whose target is any `T | null` cell (`let n: i32 | null` too) no longer spuriously fails the scalar `=` operator-method lookup. (`tests/cases/types/infer-null-*.vl`)
- **A-infer-empty Usage-driven element inference for empty `[]`** — an un-annotated empty array literal now infers its element type from DOWNSTREAM usage, exactly like a generic resolves its type arg from arguments: `const xs = []; xs.push(1)` infers `xs: i32[]` (and `xs.push("a")` → `string[]`), as does passing `xs` to a `T[]` param, an annotated assignment (`const ys: i32[] = xs`), a `T[]`-returning tail, or an index-set (`xs[i] = v`). The constraint is threaded back to the binding's shared element hole (`constrainElementHole` for `push`; the existing index-signature unify in `ensureType` for param/return/assignment), then the hole is collapsed in place so codegen sees a normal typed array. The "cannot infer — annotate" diagnostic (the robustness floor) is now deferred to scope-close, so it fires ONLY for a genuinely-unconstrained empty (no resolving use). (`tests/cases/arrays/infer-empty-*.vl`, `error-empty-uninferred.vl`)
- **A-infer-map-set Usage-driven key/value inference for `Map()` / `Set()`** — the `Map()`/`Set()` analogue of `A-infer-empty`. An un-annotated `const m = Map()` now infers its key/value type from a downstream `m.set(k, v)` (`const m = Map(); m.set("a", 1)` infers `m: {[string]: i32}`), and `const s = Set(); s.add("x")` infers `s: Set<string>`. The `Map()`/`Set()` constructor binds a bare `Infer<Unknown, mapCtor>` hole (the same object the binding holds); the first `.set`/`.add` MATERIALISES that hole IN PLACE (`constrainMapHole`) into the concrete string-keyed index-signature object — `{[string]: V}` for a Map, `{[string]: boolean}` for a Set — byte-for-byte identical to what an annotated `const m: {[string]: i32} = Map()` produces, so `.get`/`.has`/`.length`/`.keys()`/`.values()`/iteration/codegen all work unchanged. Every later `.set`/`.add` then type-checks STRICTLY against the now-pinned key/value, so a conflicting `m.set("b", "x")` after `m.set("a", 1)` (resp. `s.add(1)` after `s.add("x")`) is a clean type error, never a crash. Map/Set keys must be `string` (the only hash key the rep supports today); an inferred i32 key is rejected with the same message the annotated `{[i32]: …}` path uses. The "cannot infer — annotate" floor stays deferred to scope-close, firing ONLY for a genuinely-unconstrained `Map()`/`Set()` (no `.set`/`.add`). (`tests/cases/maps/infer-from-set.vl`, `maps/error-infer-conflict.vl`, `maps/error-uninferred.vl`, `sets/infer-from-add.vl`, `sets/error-infer-conflict.vl`)
- **A-definite-assign Definite assignment for uninitialized locals** — an uninitialized `let x` / `let x: T` now starts UNassigned and NON-null (NOT implicitly nullable); a READ where `x` is not provably written on every preceding path is a `` `x` is used before it is assigned `` error. This closes a silent-`0` soundness gap (`let x: i32; return x` used to compile and fabricate a `0`). The check reuses the parser's single-pass flow machinery — the same `is`-narrowing fork/join that scopes through `if`/`else`/`while` and lets a diverging branch drop out: a per-`Binding` "unassigned" bit is forked at each branch, joined at the merge (assigned only if written on every surviving path — a `return`/`break`/`continue` branch contributes nothing, reusing `divergesStatement`), and reset to the pre-loop state after a `while`/`for`/`for-in` body (zero-iteration). The declaration itself is fine; only the reads are gated, and the binding's type is unchanged (`let x: i32` stays `i32`, not `i32 | null`). Initialized `let x = …` and every `const` are assigned from the start and untouched. (`tests/cases/variables/definite-assign-*.vl`)
- **A11 Recursive structural types** — `type Tree = { value, left: Tree | null, … }` constructs/traverses/compiles (cycle-safe traversals + self-referential WasmGC rec-group). Mutual recursion across *separate* `type` decls (`type A = { b: B | null }` ↔ `type B = { a: A | null }`) now resolves: a top-level type-name pre-pass hoists every `type` name before any body is parsed, so a forward/mutual reference stays a lazy `Alias` leaf and both aliases tie into one rec-group. (`tests/cases/types/mutual-recursive-type.vl`) Recursion through an **array element type** (`type List = { value: i32, rest: List[] }`) now compiles too — the element struct, its growable-list `{backing,len,cap}` wrapper, and the backing array are discovered as one strongly-connected component and built in a single WasmGC rec group (forward-referencing each other), instead of unboundedly re-entering type lowering and overflowing the stack. (`tests/cases/types/recursive-array-element.vl`) Recursion through a **map value type** (`type Tree = { value: i32, children: { [string]: Tree } }`) now compiles as well — the map's hash struct and its `vals` backing array join the same rec group, so the value-type back-edge is threaded via forward refs instead of overflowing through `mapType`→`mapArrayType`→`refHeapType`. (`tests/cases/types/recursive-map-value.vl`)
- **A12 Soundness corpus (started)** — must-error/must-not-error corpus under `tests/cases/soundness/`; strict by default; known-unsound corners `xfail`-marked.
- **A13 Operator-constraint inference (core)** — fully-inferred structural functions monomorphize per call shape (i32 & f64 from two call sites).
- **A14 Named/opaque type crash fix** — bodyless-`type Point` infinite-recursion crash fixed (cycle-guarded `getConcreteType`; now errors cleanly). → `DECISIONS.md`
- **A15 Structural equality** — `==`/`!=` by value; functions by reference; arrays + nested structs recurse via `valueEq`/`isEquatable`. → `DECISIONS.md`
- **A15 fix: structural `==`/`!=` on a value-union operand** — `v == 7` where `v: "x" | 7` no longer crashes codegen (`Have only handled i32 with ==, got Union`); it lowers structurally by reusing the boxed-union `{tag, value}` rep and the `is`-literal machinery — tag-select the matching arm, then `valueEq` the unboxed payload against the other operand (a cross-type arm is structurally unequal, e.g. the `"x"` arm vs an `i32` → `false`). `union == union` lands too (same arm + payload-equal, short-circuiting each unbox on its tag). Reuses `unionInfo`/`findVariant`/`unboxPayload`/`valueEq`; no checker change. (`tests/cases/operators/union-eq.vl`)
- **A15 fix: symmetric `==`/`!=` type-checking for a value-union operand** — `7 == v` (where `v: "x" | 7`) now type-checks and runs just like `v == 7` did; equality is symmetric, but the checker previously gated the structural value-union branch on the *left* operand being the union (`leftType.type === "Union" | "Nullable"`), so the reversed form fell through to the numeric `==` path and reported `expected i32, got "x" | 7`. The branch now fires when *either* operand is a value-union; the comparability test (`validateType` either direction) is already order-independent, and the reversed form additionally tests the union against the *un-softened* left operand so a bare literal (`7`, softened to `i32` for arithmetic) still matches one of the union's arms. A plain mismatch with no union (`"a" == 7`) still errors as before. Codegen already handled both orders (PR #200) — no `toWasm.ts` change. (`tests/cases/operators/union-eq-reversed.vl`, `eq-no-union-mismatch.vl`)
- **A16 Literal-union types (type-level)** — annotations constrain (`"a"|"b"` rejects `"c"`); `==`/`!=` discriminate + narrow; covering `if/else if` chain exhaustive. `flattenType` dedup uses an O(n) value-key set when every variant is a literal (the common case; distinct literals never subsume), replacing the O(n²) pairwise `validateType` scan — a left-folded N-member literal union now type-checks in O(n²) not O(n³) (160 members 485ms→3.8ms). Shared front-end logic (carries to `typecheck.vl`). (`tests/cases/types/literal-union-dedup.vl`)
- **A7b `boolean`-where-`i32`-expected coercion** — a `boolean`-typed VALUE coerces to `i32` (true→1, false→0) at an assignment boundary (binding/argument/return); no-op in codegen (shared i32 rep). One-directional (`i32` ⊄ `boolean`); excludes a bare boolean literal and membership/narrowing queries (`x is boolean` on `string | i32` still rejected).
- **A17 Forward / mutual-reference return-type inference** — demand-driven (lazy + memoized, cycle-detected) return-type inference; a function calling a later-defined or mutually-recursive function no longer infers `any`; only a genuinely base-case-less inferred cycle still needs an explicit annotation. (#105; detail: `docs/selfhost-gaps.md` §A17)

## Codegen, memory & runtime (Track B)

- **B0 Numeric literals, i32/f64 arithmetic, if/while, direct calls, start fn, memory builtins.**
- **B1 Allocation = WasmGC** (binaryen 116→130 for GC API). → `DECISIONS.md`
- **B2 Numeric codegen (core)** — i64 & f32 arithmetic + float `/` & comparisons; i64/f32 type mappings; range-aware integer-literal defaults.
- **B2 Value-level bitwise/shift operators** — `& | ^ ~ << >> >>>`, integer-only, i32/i64 → native wasm instructions (#99; H4.2 self-host gap).
- **B3 First-class functions / indirect calls + per-shape monomorphization** — fat-pointer closure `{ tableIndex, env }`; each call site gets a fresh signature keyed by wasm param signature.
- **B4 Closures (first WasmGC codegen)** — nested decls, capture analysis, env struct, escaping closures.
- **B5 Objects (core)** — literals, read/write, nested, f64 fields, args/returns, reassignment, captured-in-closures, excess-property width subtyping, function-valued fields + member-call; method-shorthand `{ add(a,b){…} }`.
- **B6 Growable `T[]` (core)** — `{backing,len,cap}` WasmGC struct monomorphized per element type; 2× growth; `[...]` seed; `a[i]`/`a[i]=v` trap on OOB; `.length`/`.get(i)`/`push`/`pop`/`clear`/`+`; for-in + structural equality updated. (`compiler/builtins/lists.ts`; → `docs/collections-design.md`) `.capacity` removed (leaky growth detail; `cap` stays internal). → `DECISIONS.md`
- **B6b Build-loop fusion** — a loop building a fresh local list by one push per iteration — `for i in A to B [step S] { a.push(e) }` or the counter-`while` `while i <cmp> N { a.push(e); i = i ± C }` — lowers to a pre-sized indexed fill, ~3–4× faster than per-element push (elides the frontier bounds-check the engine can't); unified recognizer → one fill core, guard-gated, falls back to push otherwise. (`tests/cases/lists/build-fusion-*`; → `DECISIONS.md`)
- **B6a `Map` + `Set` (core)** — ordered open-addressing hash maps (Python-dict shape) over WasmGC; string keys; `Map[k]: V|null`; `.size`/`.get`/`.has`/`.set`/`.delete`/`.keys()`/`.values()`; `Set` `.add`/`.has`/`.delete`; deterministic insertion-order iteration; tombstone-aware probing + compaction. (`compiler/builtins/maps.ts`) A map whose VALUE type is the enclosing recursive type (`{ [string]: Tree }`) now compiles — the hash struct + its `vals` array thread into the type's WasmGC rec group (mirrors A11 array-element recursion). (`tests/cases/types/recursive-map-value.vl`)
- **B6a perf: per-entry stored hash** — the open-addressing map now stores a parallel `hashes` i32 array (Python-dict model): a probe hashes the key once and gates the expensive (string) `keyEq` behind a stored-hash `i32.eq`, so the full key compare runs only on a genuine hash match; a resize re-places from the stored hash instead of re-hashing every key. `set`/grow/compact maintain the array; appended as map-struct field 6 at both `toWasm.ts` sites (standalone `mapType` + the recursive-map-value rec-group path), so existing field indices are unchanged.
- **B7 Strings (core)** — WasmGC i32-array of code points; literal, `.length`/`s[i]`, `+`, `==`/`!=`, `print`.
- **B8 Loops (core)** — `for…in` over arrays, direction-aware `step`, single-line block bodies, empty-range warning.
- **B9 `break` / `continue` in codegen** (incl. labelled `break outer`).
- **B10 Unary / prefix / postfix ops** — `-`, `++`/`--`, `!`. → `DECISIONS.md` for `!`-not-`not`.
- **B11 `while true` return analysis** — non-escaping `while true` types as `Never`; no spurious `| null` on functions tail'd by one.
- **B13 Well-known-symbol dispatch (operators + index)** — operator overloading via `"+"` field or free `self` function; `"[]"`/`"[]="` index traps dispatched statically. → `DECISIONS.md`
- **B14 Methods via explicit `self` + UFCS (core)** — free `self`-first function callable as `o.f(args)`; field-then-self-fn resolution; non-`self` functions not instance-reachable. → `DECISIONS.md`
- **B15 Lambdas / declaration-vs-value (typed)** — `FunctionDeclaration` in value position lowers to its closure value (let-bound, capturing, higher-order, inline object fields). → `DECISIONS.md`
- **B17 Diagnostics + lint (started)** — `severity`, `@warning` directive, empty-range warning, stable `code`s; lint pass (`compiler/lint.ts`): unused-variable, unreachable-code (`unnecessary` tag = VS Code grey-out).
- **B17 Export-aware top-level unused** — exported top-level bindings exempt from unused-variable lint; landed with `export` keyword (H0 phase 1).
- **B17 fix: unused-import diagnostic + remove fix** — an unused imported name now squiggles the imported NAME (`a`, or the `as`-alias `y` in `{ x as y }`), not the `import` keyword (`parseImport` threads each specifier's local-identifier token span to `declareBinding`), and is reported under a dedicated `unused-import` code with the message "Unused import `a` (remove it)". Imports get NO `_`-prefix suggestion (prefixing would require aliasing, not a bare `_`); the only quick-fix removes the specifier — deleting the whole `import` line when it was the sole specifier, else dropping just that specifier and one adjacent comma (first/middle/last handled with no dangling comma; an `{ x as y }` alias clause is removed whole). Import bindings carry an `isImport` flag (`Binding.isImport`, set in `parseImport`) that the lint branches on.
- **B17 lints: dead/constant branch + degenerate `for` step** — two more lint rules (`compiler/lint.ts`). **Constant condition** (`code: constant-condition`, `warning`, `unnecessary` tag = grey-out) flags a branch whose literal-boolean condition makes it unreachable: an `if false { … }` body, the `else` (and later arms) of an `if true { … }`, and a `while false { … }` body. Literal-only — no general constant folding; recurses into nested bodies. **Degenerate `for` step** (`code: for-step-zero`, `error`) flags a range `for` written with a literal `step 0` — it never advances the loop variable (an infinite loop), the case the parser's empty-range warning deliberately excludes (an empty-range check can't fire when the step is zero); points at the `step 0` expression. (`tests/cases/lint/dead-branch-if-false.vl`, `dead-branch-if-true-else.vl`, `dead-branch-while-false.vl`, `for-step-zero.vl`)
- **B17 lint: unused-function** — a non-exported top-level `function` that is never referenced anywhere in its file is dead code in a whole-program compile; flagged with a `warning` (`code: unused-function`, `source: vital`, `unnecessary` tag = grey-out) — `Unused function \`f\` (prefix with \`_\` to suppress, remove it, or \`export\` it)`. EXPORTED functions are NEVER flagged (public API — VL is multi-file, so an `export function` may be imported and called by another module). A `_`-prefixed name is the "intentionally unused" convention and downgrades to a `hint` (greyed, not a warning), mirroring the unused-variable rule. Reuses that rule's reference model (the symbol table's non-decl occurrences) and additionally consults an AST-level call/value-reference set so FORWARD/mutually-recursive calls (resolved against a hoisted signature before the real binding is declared) and UFCS/operator-method calls (`x.m(…)`, `a + b`, which the parser desugars to a named `FunctionCall`) count as uses — a function reachable from any call site is never falsely flagged. The LSP offers the `_`-prefix quick-fix (no multi-line removal). (`tests/cases/lint/unused-function.vl`, `exported-function-no-warn.vl`, `called-function-no-warn.vl`, `mutual-recursion-no-warn.vl`, `tests/lint_unused_function_test.ts`)
- **B17 fix: `run`/`build` suppress info/hint advisories** — `vl run` and `vl build` now apply a `warning` display floor (via the existing `meetsThreshold` helper): errors (still blocking) and warnings (dead code, empty intersections) print, but info/hint-level style lints (e.g. prefer-`const`) are suppressed — those are the job of `vl check` and the editor, not noise on every run. `vl check` is unchanged and still shows the full set.
- **B19 `return` / early returns** — early, from loops, fall-through; bare `return` yields null.
- **B-debug Name section + source maps + trap-to-source** — wasm name section + Source Map v3 (binaryen debug locations, survives `optimize()`); trap → precise VL `file:L:C` error message. (#76)
- **B6 Nullable-list narrow-then-use codegen fix** — a `T[] | null` narrowed to non-null (`if xs != null { … }`) then read into a list op (`xs[i]`, `xs[i]=v`, `xs + …`, `for…in`, `map`/`filter`) no longer crashes binaryen's `LocalSubtyping` (`isSubType` assertion): narrowing is type-level only, so the value keeps its nullable `(ref null $list)` slot, and stashing it in the op's non-null `(ref $list)` local was a subtype violation. Each such read now `ref.as_non_null`s the list (the narrowing guarantees non-null). Reproduces non-recursively with `i32[] | null`; found by the A11 array lane. (`tests/cases/types/nullable-list-index.vl`)

## CLI (Track C)

- **C1 Headless `compile(source) → { ast, wasm, diagnostics }`** (`compiler/compile.ts`), shared by LSP, test runner, and CLI.
- **C2 `vl run`** — compile + run a file / `-e` snippet / stdin (`deno task run`).
- **C3 `vl build <file> [-o out.wasm] [--wat]`** — emit wasm bytes (+ optional `.wat`).
- **C4 `vl check <file>`** — diagnostics only, non-zero exit on errors; `--codegen` opt-in for full binaryen pipeline.
- **C5 Distribution via `deno compile`** — native `vl` binary (binaryen embedded); `deno task compile`/`smoke`; 11 smoke checks pass. → `DECISIONS.md`

## LSP / editor (Track D)

- **D0 Diagnostics on change.**
- **D1 Hover types** — `onHover` resolves cursor via D2 symbol table + `receiver.member` (object fields, array/list/string members via `typeFromExpression`/`listMemberType`/field); rendered via `stringifyType`.
- **D2 Go-to-definition / find-references** — symbol/binding table (`compiler/symbols.ts`); `textDocument/definition` + `textDocument/references`; locals, params, function decls, type aliases, single-document. → `DECISIONS.md`
- **D3 Autocomplete (core)** — scope-aware identifier completion + structural member completion (`lsp/src/typeFeatures.ts`); tested (`tests/lsp_completion_test.ts`).
- **D4 Formatter (core)** — AST-driven source formatter (`compiler/format.ts`, `vl fmt`, LSP `textDocument/formatting`); idempotent, round-trip-AST-equivalent, comment-preserving, 80-col reflow.
- **D5 Semantic tokens** — `textDocument/semanticTokens/full`: identifiers via D2 table, literals/keywords/operators via lexer, comments by source scan, `receiver.member` names (→ `property`/`method`).
- **D6 Inlay hints** — inline `: <type>` at unannotated `let`/`const`/params and omitted returns; annotated positions and unresolved holes suppressed.
- **D6 fix: an inferred array displays as `T[]`, not `{i32: T}`** — an array is internally an `{[i32]: T}` index-signature object; an array LITERAL (`[1, 2]`) and an `A-infer-empty` empty `[]` were keyed by the integer type `i32` and so fell through to the generic object render `{i32: i32}` in hover / inlay / error messages, while a `T[]` annotation already printed `i32[]`. Array construction now CANONICALIZES on a single `number`-keyed index signature (`number` = the i32-width array-index alias) across all three paths — the `T[]` annotation suffix (`parseArrayType`), the array literal (`ArrayLiteral`), and the intrinsic `listOf` (`.push`/`.map`/`.values()` results) — so a literal, an annotation, and an inferred-empty `[]; .push(1)` yield the SAME type shape and all render `i32[]` (`["a"]` → `string[]`, `[1.0]` → `f64[]`, `[[1]]` → `i32[][]`). `arrayElementType` / `isListType` / `getConcreteType` / `ensureType` treat the `number` key as the i32-width index (resolving to `i32`), so element inference, indexing, `.push`/`.length`, assignability, and codegen are unchanged; an explicit `{[i32]: V}` index-signature annotation keeps its `i32` key (still a native array, rendered `{i32: V}`), and a `string`-keyed map (`{[string]: V}`) stays a distinct map. (`tests/cases/arrays/render-i32-array.vl`, `tests/lsp_inlay_hint_test.ts`)
- **D3 Keyword + snippet completions** — 26 keywords + 10 snippet skeletons with tab-stops; after-dot completions suppress keywords/snippets; trigger-character tuning. (#143)
- **D4 Formatter: collapse short if/if-else** — a single-conditional `if { stmt }` or `if { a } else { b }` that fits 80 cols folds to one line; comments/multi-statement bodies stay block. (#138)
- **D4 Formatter: trailing comment on `type` alias** — a trailing comment on a `type X = …` line now stays on that line instead of being displaced. (#146)
- **D4 Formatter: verbatim-fallback for fn with commented expression body** — a function whose body is a single expression spanning multiple lines with an own-line comment is reproduced verbatim, preventing the comment from being displaced after the closing brace.
- **D4 Formatter: trailing comment on bare-body loop header** — a trailing comment on a `while`, `for`, or `for-in` header with a bare (brace-less) body (e.g. `while r < 10 // cond`) now stays on the header line instead of being displaced outside the enclosing function.
- **D4 Formatter: trailing comment on block closing brace** — a trailing comment written on the same source line as a closing `}` (e.g. `} // done`, `} // end of f`) now stays on that line for `if`, `while`, `for`, `for-in`, function, and free-standing blocks instead of being displaced to an own line before the next statement; the `if`-block case was also not idempotent (second pass re-moved the comment).
- **D4 Formatter: trailing comment on verbatim-fallback function last line** — a trailing comment on the last source line of an operator-named (`function +(self,b){…}`) or method-shorthand-body function (reproduced verbatim because the AST discards the surface syntax) now stays attached to that line instead of being displaced as an own-line comment before the next statement.
- **D7 Doc-comment cross-references (single-file)** — `` [`Name`] `` / `[Name]` rustdoc-style intra-doc links in `///` comments resolve via D2's symbol table and render as clickable markdown links in hover and completion `documentation`; unresolved names left verbatim; cross-import resolution deferred (needs H0 phase 3 module graph).
- **Module-aware diagnostics (H0 phase 3)** — the open file is analyzed as the entry module through a workspace `ModuleReader` (open buffers + disk, `lsp/src/moduleGraph.ts`): `import`ed names resolve to their real types instead of flagging "undeclared", and genuine import errors (bad path / not-exported / cycle) surface on the import line; hover/completion seed the same imported-name types; no-import files unchanged.
- **Cross-file navigation (H0 phase 3)** — go-to-definition and doc-comment `` [`Name`] `` xrefs on an imported name jump to the EXPORTING sibling module's declaration (`lsp/src/moduleGraph.ts` resolves an imported local name → exporting module + the export's decl span via the symbol table); find-references gathers a symbol's occurrences across the current file + other OPEN documents (graph-seeded so importer uses are recorded); single-file go-to-def/refs/doc-xref unchanged; refs over unopened on-disk siblings deferred.
- **Find-references: unopened on-disk sibling modules (H0 phase 3)** — find-references now crawls `.vl` files on disk that are not open in the editor: project root is detected from the LSP workspace folder or by walking up to the nearest `deno.json`/`package.json`/`.git` ancestor (max 6 levels); `.git`, `node_modules`, `dist`, `.claude`, `reference` dirs are skipped; at most 500 files per request; open-buffer text wins over disk; on-disk results are deduplicated against already-found open-doc refs by URI.
- **D8 (partial) Type-alias name preservation** — hover/inlay render the alias name (`Point`/`I32`) not its expanded body; `Type` node carries the source alias name (`aliasSymbol`-style), `stringifyType` `maxDepth=0` default preserves all names. (Verbosity step-expansion still ahead — needs proposed LSP 3.18.)
- **D — Project-wide unused-export hints (two signals)** — a debounced workspace pass (triggered on document save and after a 3-second idle) builds a global use-map over up to 500 `.vl` files and publishes `hint`/`unnecessary`-tagged diagnostics for two export conditions: (1) **fully dead** (`code: "unused-export"`) — exported symbol with zero references anywhere (not imported by any other module AND not used within its own file) — greys the export **name**; (2) **redundant export** (`code: "redundant-export"`) — exported symbol used only within its own module (e.g. a recursive function or a locally-consumed value) but never imported by any other module — greys the **`export` keyword** itself with message `` `export` is redundant — `foo` is only used within this module, never imported ``; the `export` keyword span is captured in `Binding.exportKeywordSpan` (stamped in `compiler/parser.ts` alongside `binding.exported = true`); the use-map tracks cross-module imports and same-file local uses separately (`ExportRefCounts { cross, local }`); struct fields are out of scope (structural typing makes field-level analysis fuzzy); merged with regular lint/type diagnostics; tests in `tests/lsp_unused_export_test.ts`.

## Browser playground (Track E)

- **E1 Browser bundle** — esbuild + `esbuild-deno-loader`; binaryen@130 runs client-side ESM; `deno task playground`.
- **E2 Playground UI (core)** — `<textarea>` + Run + diagnostics/log/WAT panes + sample picker (`playground/`).
- **E2 Playground redesign** — branded light/dark theme pair (system-default with revert-to-system toggle, lock-step Monaco theme), results-as-tabs (Output/WAT/Diagnostics + size/count badges), multi-file projects (per-file diagnostics, whole-program `compileProgram` run/WAT), opt-in auto-run (always-on debounced analysis), full-width status bar, real Share + Format wiring.
- **E4 Shareable links** — source encoded in URL hash (#94).
- **Playground↔LSP parity test** — `tests/playground_lsp_parity_test.ts` enumerates the language features `lsp/src/server.ts` exposes (handlers self-validated against the live source) and asserts, per feature, that `playground/src/lspAdapter.ts` exports the pure binding AND `playground/src/main.ts` wires the matching Monaco provider — so an LSP feature can't be added without wiring the playground (the gap that left "Auto Fix" silently missing). Three current gaps are documented as asserted known-TODOs: code actions (`registerCodeActionProvider` unwired in `main.ts` though the `codeActions` adapter binding exists), find-references, and completion.

## Infrastructure (Track F)

- **F1 Test harness** — `deno task test` over the `.vl` corpus with `// @directive` expectations.
- **F3 Retired `ts-interpreter/` → `reference/`.**
- **F8 Dropped binaryen patch + `patch-package`** — LSP server is ESM; binaryen's TLA is legal there.
- **F9 Perf baseline** — `deno task perf`; compile-time (front/codegen split) + wasm size over corpus; best-of-N. Finding: literal-union compilation is ~cubic in member count (200 → ~2 s). (→ `docs/perf-findings.md`)
- **F9c Memoize `structSig` in `toWasm.ts`** — the structural-signature walk was uncached and dominated IR-build on the self-host module (~6 s of ~7.7 s total; 268k calls). Caching by type-node identity (empty `nameStack` only) cut selfhost-suite wall time ~107 s → ~30 s with byte-identical wasm. Post-fix: binaryen `optimize()` is only ~0.8 s on this module, not the bottleneck. (#107; detail: `docs/perf-findings.md`)
- **F10 Bare `deno check` passes** — lsp sub-project excluded; playground + lsp-test type errors fixed so top-level `deno check` exits clean. (#140)
- **F11 CI skips heavy suite for docs-only changes** — an in-job `git diff` gate (not a workflow-level `paths` filter) `if`-guards the heavy steps, so docs-only PRs go green fast while the `ci` check still reports. (#142)
- **F12 GitHub Pages deploy** — `.github/workflows/pages.yml` builds the playground (`deno task playground:build`) and deploys the static bundle to `https://voces.github.io/vl/` on every master push that touches `playground/`, `compiler/`, or build-config files.
- **F — Deleted stale `samples/` sketches** — the 7 `samples/*.vl` early design sketches (manual malloc / linear-memory, pre-WasmGC) are removed and dropped from the test corpus (formatter walk, `smoke-binary.ts`, repo-layout docs). The one idea not already tracked — expression `step` on a counter range (`for i = 1 to 5 step i * 2`) — was captured under B8; the rest were obsolete or already covered.

## Parser (Track G — complete)

- **G Hand-written parser** — replaced antlr4 with hand-written TS lexer + recursive-descent/Pratt parser emitting the typed AST directly; antlr4/Gradle/generated dirs gone. → `DECISIONS.md`
- **G AST source spans** — `NodeSpans` + `spanOf` publicly exposed via `toAST`/`compile`; unblocks D4/D6/D7.
- **G Comment-carrying tokens** — comments retained with spans (flat `comments` list + per-token `leading`/`trailing` trivia) without entering the grammar stream; unblocks AST-driven formatter.
- **G8 Soft-newline statement boundaries** — newlines are never force-required; statements abut on one line. Fixed `return <expr>` followed by a same-line statement (`return 1 print(9)`): the trailing dead statement was mistaken for the block's implicit return value and spuriously checked against an annotated return type (`expected i32, got null`) — now `return 1` carries the value and the trailing statement is left for the next position (flagged by the unreachable-code lint). → `DECISIONS.md` (G8)

## Self-hosting & modules (Track H)

- **H0 Module system phase 1** — relative-path named `import { a, b as c } from "./util"` + `export` modifier on `function`/`let`/`const`/`type`; whole-program resolver walks import graph, detects cycles, type-checks across modules, merges into ONE wasm module with per-module name mangling. `compileProgram`/`checkProgram`. (#96; → `DECISIONS.md`, `docs/modules-design.md`)
- **H-M1 Distribute via `deno compile`** — same as C5.
- **H1 Parser self-hostable** — same as Track G above.
- **H2 gap-1 Push non-nullable struct array** — `Tok[]` works; backing slot nullable-widened to `(ref null $T)`; reads `ref.as_non_null` to surface non-null type. Unblocks AST-node lists.
- **H2 gap-2 Module-level mutable `let` through a function** — scalar top-level bindings lower to shared wasm `global` (`global.get`/`global.set`). (`tests/cases/globals/`)
- **H2 gap-3 String escapes** — `\n`/`\t`/`\xXX`/`\uXXXX`/… decoded in lexer.
- **H2 gap-4 `toString` prototype-chain leak fix** — scope lookup uses `Object.hasOwn` everywhere (closes `constructor`/`valueOf`/etc. hazards).
- **H2 gap-5 `fromCodePoint(code): string` builtin** — code-point-named; replaces JS-ism `fromCharCode`.
- **H2 gap-6 Ref/string module-globals through a function** — extends gap-2's scalar-only support to ref/string cells.
- **H2 gap-7 One-char string literal `.length`** — string literals soften to nominal `string`; no longer mis-types as a char.
- **H2 char literals + `toString`/stringify** — both landed as part of H2 gap work.
- **H3-gap3 `checkProgram` in value position** — resolved by #89 (void/statement-position value drop); a discarded value is dropped in statement position, so `checkProgram(...)` as a bare statement (or `let r = ...`) no longer hits "Expected numeric type" in codegen. (detail: `docs/selfhost-gaps.md` §3)
- **H4.2 Value-level bitwise/shift operators** — shipped in #99 (see B2 above).
- **H4.3 Unsigned right-shift** (`>>>`) — resolved by #99 (`>>>` is now a native operator); `ulebToArr` in `wasmEmit.vl` uses `v >>> 7` for all i32 values including those with bit 31 set. (detail: `docs/selfhost-gaps.md` §H4.3)
- **H4.4 Signed `%` sign fix** — resolved via H4.2 (#99): `& 0x7f` / `& 0xff` bitwise masks replace the arithmetic correction branches; naturally unsigned, no special-casing needed. (detail: `docs/selfhost-gaps.md` §H4.4)
- **H-pipeline VL-in-VL front end end-to-end** — `lexer.vl → parser.vl → typecheck.vl` chain driving source text through a wired pipeline; proves the front end self-hosts. (`tests/selfhost_pipeline_test.ts` + `tests/selfhost/pipeline_harness.vl`)
- **H-emitProgram structs** — `emitProgram` parses + emits WasmGC struct types, `struct.new`, and `struct.get`; construct + field-read proven by real `WebAssembly.instantiate`. (#137)
- **H-emitProgram arrays** — `emitProgram` parses + emits WasmGC array ops: literal (`array.new_fixed`), index read/set, `.length`; 8 new live-instantiation cases. (#145)
- **H-emitProgram strings** — `emitProgram` lowers string literals + `.length`/`s[i]` to the array-i32 code-point representation (a VL string is CURRENTLY an `array i32` of code points, reusing the arrays slice's `(array (mut i32))` machinery); string-typed locals/params/returns carry the array ref valtype; 7 live-instantiation cases. Concat (`+`), value-equality (`==`/`!=`), and `.slice` land in G6; the UTF-8 `array i8` storage migration (B7) is deferred.
- **H-emitProgram growable arrays / push** — `emitProgram` now lowers `i32[]` to the growable `{ backing, len, cap }` wrapper struct (mirroring `toWasm.ts`'s list rep): `[…]` literals build it, `.push(x)` grows the backing in place (2× / floor 4) when full via `array.new_default` + `array.copy` + `struct.set` then writes + bumps `len`, `.length` reads the `len` field, and `a[i]`/`a[i]=v` go through the backing. `string` stays the raw fixed `(array (mut i32))` rep (immutable, no push). 6 new live-instantiation cases (grow past initial capacity, `while`-loop build-then-sum, `[]`+push+`.length`, push→set→read, list across a function boundary).
- **H-exports Host-callable wasm exports** — entry-module `export function`s become host-callable wasm exports via a thin scalar no-env wrapper; non-entry exports remain DCE-able. (#141; → `DECISIONS.md`)
- **H-emitProgram unions + is** — `emitProgram` now lowers discriminated `type N = A | B | …` struct unions with `is`-narrowing (G1, the bootstrap keystone), mirroring `toWasm.ts`'s boxed tagged-struct rep (`docs/unions.md`): a union value is a `{ tag: i32, value: anyref }` box wrapping the variant payload struct; construction is `struct.new $box (i32.const tagOf(V)) (struct.new $V …)` with global tags assigned by sorted variant structural signature; `n is V` is the box-tag compare (`struct.get $box 0` then `i32.eq`); a narrowed field read `n.f` (inside `if n is V`) recovers + `ref.cast`s the payload then `struct.get`s the field. The type section lifts the one-struct cap (multiple variant struct heap types + the union box). `compiler/parser.vl`/`compiler/ast.vl` gained the union-alias decl + `x is T` guard (the self-host parser parsed neither before). 7 new live-instantiation cases (construct+narrow+read, false-`is` other branch, 2-variant dispatch, 3-variant dispatch, multi-field variant, union local→helper, union local discriminated in place). Deferred: arrays-of-unions (`Node[]`, the G7-ref follow-up), unions mixing scalars + structs, and `!is`/negated guards.
- **H-emitProgram ref-element lists / arrays-of-structs-and-unions (G7-ref)** — `emitProgram` now lowers growable lists whose ELEMENT is a reference — `Tok[]` (a struct ref) and `Node[]` (the union box ref), the self-host parser's `P.nodes: Node[]` arena + token stream shapes. The `{ backing, len, cap }` wrapper is reused, but the backing array slot is `(ref null $elem)` (`0x5e 0x63 <elem> 0x01` — nullable-WIDENED so `array.new_default` defaults spare slots to null; verified against `toWasm.ts`'s `arraySlotWasmType`/`arrayReadCast`); an index READ `array.get`s the slot then `ref.as_non_null` (`0xd4`) recovers the non-null element (reads in `[0,len)` are always populated). `.push`/index-set reuse the i32-list grow/store sequence over the ref list/backing type indices + a ref scratch triple. For a union-element list `ns[i]` yields the union box, usable with `is`-narrowing (`let n0 = ns[i]; if n0 is A { n0.av }`). One ref-element list per program (struct OR union element), alongside the existing i32 list. 6 new live-instantiation cases (struct push+index-a-field+length, `while`-loop build-then-sum, `T[]` literal, struct index-set, union push+index+`is`-narrow, a `Node[]`-shaped mini arena narrowing each element). No `parser.vl`/`ast.vl` change needed (`S[]`/`N[]` already parse to a `TypeRef` with the `[]` suffix).
- **H-emitProgram logical `&&`/`||`/`!` + value-typed `if` (G4)** — `emitProgram` now lowers the short-circuit logical operators, introducing the first VALUE-TYPED `if` (the existing statement `if` stays VOID, blocktype `0x40`). `&&`/`||` are EXPRESSIONS that yield an i32, so they emit an `if` with an i32 RESULT-TYPE blocktype (`0x04 0x7f`) where both arms leave one i32: `a && b` ≡ `if (a) { b } else { 0 }` (then = `b`, `else 0x05`, else = `i32.const 0`, `end 0x0b`); `a || b` ≡ `if (a) { 1 } else { b }` — mirroring `toWasm.ts`'s `m.if(left, right, i32.const 0)` / `m.if(left, i32.const 1, right)` so the RHS is genuinely SKIPPED when short-circuited. `!a` (logical not) is `i32.eqz` (`0x45`) — a `boolean` rides in an i32, so eqz maps 0→1 / non-zero→0, exactly `a == 0`. 8 new live-instantiation cases (`&&` + `||` truth tables, `!a`, `&&`/`||` short-circuit OBSERVED via a global a skipped helper would have written — confirmed untouched, then written when run, on one shared instance, a char-class predicate `(x > 0) && (x < 10)` driving a branch, and a combined `!(a && b) || c`). The value-typed `if` is reusable for any future value-position `if`. No `parser.vl`/`ast.vl` change needed (`&&`/`||` already parse via the precedence table, `!` via `parseUnary`).
- **H-emitProgram boolean params/locals/returns + BoolLit/CharLit (G3)** — `emitProgram` now accepts `boolean` as a valid scalar wherever i32 is (boolean param in `checkParams`, boolean local in `checkLocalI32`, boolean return via `emitExpr`), treating it as the SAME i32 wasm valtype (`0x7f`) since a `boolean` already rides in an i32 (0/1) — no separate boolean valtype is introduced (`vtKindOfType`/`vtKindOfParam` already fall through to i32). `BoolLit` lowers to `i32.const 1` (true) / `i32.const 0` (false) from the already-decoded `boolVal`; `CharLit` lowers to `i32.const <code point>`, decoding `charText` the SAME way string literals decode (`decodeStr` strips the quotes + resolves escapes, yielding exactly one code point). Comparisons/branches on booleans already worked via i32. This unblocks the ~40+ self-host predicates that return `boolean`. 7 new live-instantiation cases (boolean param returned true→1/false→0, `if true`/`if false` BoolLit conditions, a function returning `boolean` (a comparison) used in a branch, a boolean local driving an `if`, `'a'`→97, and a char local + arithmetic `'A' + 1`→66). f64 stays out of scope. No `parser.vl`/`ast.vl` change needed (`boolean`, `true`/`false`, and char literals already parse).
- **H-emitProgram struct field write + module globals (G2)** — `emitProgram` now lowers struct field WRITES (`s.field = v` → `struct.set $0 <fieldidx>`, `0xfb 0x05`, struct-ref-then-value operand order, reusing the `emitPush` field-store shape) and module-level mutable GLOBALS: top-level `let`/`const` lower to a wasm global section (id 6, `<valtype> 0x01 <constexpr-init> 0x0b`) collected by `collectFns` in declaration order, with bare-identifier reads/writes routed to `global.get`/`global.set` (`0x23`/`0x24`) when the name resolves to a global rather than a local. Globals init via constant expressions only — i32 (`i32.const`), string (`array.new_fixed`), struct (`struct.new`), and `i32[]` (`struct.new` of `array.new_fixed`) literals — so no start function is needed (the `__program__` start-fn fallback for non-constexpr initializers is deferred). A bare top-level call statement (helper invoked for its global side effect) now emits the call + `drop`. 9 new live-instantiation cases (field write-then-read, both fields, read-modify-write, field mutated in a `while` loop, i32 global read+written, a counter bumped by a helper across calls, two globals one seeded non-zero, a struct global field-mutated, an array global indexed + `.length`).
- **H-emitProgram string `+`/`==`/slice (G6)** — `emitProgram` now lowers the three string operators the self-host sources lean on (diagnostics build messages with `+`, the lexer keyword tables compare with `==`, lexeme extraction uses `gSrc.slice(start, end)`). A string is the SAME `(array (mut i32))` of code points, so all three lower INLINE (no helper functions, unlike `toWasm.ts`'s `__string_eq__`/`__string_slice__`): `a + b` allocates a new array of `len(a)+len(b)` (`array.new_default`, `0xfb 0x07`) and `array.copy`s (`0xfb 0x11`) a into `[0,lenA)` and b into `[lenA, lenA+lenB)`; `a == b`/`a != b` are ELEMENT-WISE VALUE-equality (a value-typed `block`/`loop`: `false` on a length mismatch, else a per-code-point compare loop, all-equal → 1; `!=` adds `i32.eqz`) — a correctness FIX, since the prior i32 `==` path would have emitted `i32.eq` = WRONG ref-identity for strings; `s.slice(start, end)` clamps each bound JS-style (negative-from-end then `[0,len]`, via `select` `0x1b`) and `array.copy`s the half-open range into a fresh array. Only STRING-typed operands take these paths (via the existing `exprString` tracking) — i32/other-type `+`/`==`/`!=` are unchanged. A shared per-function scratch frame (3 string refs + 4 i32 temps, reserved after the push triples when `fnHasStrOp`) stashes operands so a NESTED string op completes before any scratch write; `exprString` now also recognizes string concat + `.slice` as string-producing (so `let s = a + b` / `let s = x.slice(…)` get the string ref valtype). 14 new live-instantiation cases (concat `.length` + index checks, concat of two locals, `==` same/different-content/different-length, value-equality of two BUILT-UP strings (not ref identity), `!=`, `.slice` compared with `==` + length/index + out-of-range clamp, keyword-table `if word == "let"` dispatch hit + miss, concat built in a `while` loop). `.indexOf`/`.includes`/`.charCodeAt`/`fromCodePoint` and the UTF-8 `array i8` storage migration (B7) stay deferred. No `parser.vl`/`ast.vl` change needed (`+`/`==`/`!=` parse via the precedence table, `.slice(…)` as a member-call).
- **H-emitProgram string-keyed maps via parallel lists (G8)** — `emitProgram` now lowers `{[string]: i32}` maps — the shape the self-host checker (`typecheck.vl`) uses for its scope chain (`Map()`, `m[k] = v`, `m.has(k)`, `m[k] ?? -1`). DELIBERATELY the SIMPLEST correct rep, NOT `toWasm.ts`'s open-addressing hash map: a map is two PARALLEL growable lists + LINEAR SCAN — a 2-field struct `{ keys: (ref $keysList), vals: (ref $i32List) }` reusing the G6/G7 `{backing,len,cap}` wrapper twice (a string-ref keys list over `(array (mut (ref null $strArr)))`, and the existing i32 vals list), with NO hashing/probing/new wasm primitives. `Map()` builds two empty lists boxed into the struct; `m[k]=v` scans `keys` via element-wise string `==` (the just-merged G6 compare) and overwrites `vals[i]` on a hit else `keys.push(k); vals.push(v)`; `m[k] ?? d` is a FUSED scan-then-`vals[i]`-or-`d`; `m.has(k)`/`k in m` scans → 1/0. Absent-key semantics match the checker's usage (it reads every scope map through `?? -1`, guarded by `.has`). A per-function 9-slot map scratch frame (map/key/cur refs, scan + char cursors, found index, two grow-backing refs) is reserved after the string-op frame when a function touches a map; scans are O(n) but the checker's scopes are small so linear is correct and fast. `compiler/parser.vl` gained the `{ [string]: T }` index-signature TYPE annotation (a synthetic `"{[K]:V}"` `TypeRef` name, mirroring how `i32[]` carries its suffix) and the `??` operator (precedence table); `ast.vl` needed no new node (`??` is a plain `BinExpr`, `Map()`/`m[k]`/`m.has` already parse). 6 new live-instantiation cases (set+get, two keys, overwrite-in-place, missing-key `??` default, `.has` present/absent, a scope-chain bind-then-lookup pattern). Iteration/`.keys()`/`.values()`/`delete`/`.pop` are deferred (the checker uses none). With G8 the full annotated self-host front end is now emitProgram-compilable.
- **H-emitProgram maps → real open-addressing hash map (G8)** — `emitProgram`'s map lowering is now a REAL ordered open-addressing hash map (FNV-1a + linear probing + load-factor-1/2 resize), mirroring the host's `compiler/builtins/maps.ts` and SUPERSEDING the parallel-lists/linear-scan placeholder above (which stays in history as a fallback). The map struct grows to 5 fields `{ keys, vals, index, count, size }`: `keys`/`vals` remain the insertion-ordered entry lists (so order is preserved and the G6/G7 grow machinery is reused for append), `index` is a raw i32 array of hash slots → entry-position + 1 (0 = empty, initial cap 8), and `count`/`size` track appended/live entries. `Map()` allocates the lists + an 8-slot zero index; every op HASHES the key (FNV-1a over its code points → non-negative i32, `emitMapHashOf`) into `index` and LINEAR-PROBES (wrapping, `emitMapProbe`) to a free-or-matching slot, comparing keys with the G6 element-wise `==` — no O(n) scan. `m[k]=v` overwrites `vals[entry-1]` on a hit else appends an ordered entry, links `index[slot]=count+1`, bumps count/size, and RESIZES (`emitMapResize`) when `size*2 ≥ cap`: a fresh `2×` index is rebuilt from the ordered entries (hash + inner-probe each, keys/vals untouched). `m[k] ?? d` / `m.has(k)` read the probe result. A per-function 12-slot scratch frame (map/key/cur refs, probe slot/cap/entry, two grow backings, char cursor, hash accumulator, resize index backing + inner slot) replaces the old 9-slot frame. 2 new resize-proving cases (ten keys forcing TWO rehashes with every key looked up; resize interleaved with overwrites + a missing-key probe), atop the 6 original G8 behavior tests — all through real `WebAssembly.instantiate`. DELETE/tombstones (the host's `live` array + compaction) are DEFERRED: the checker's op set has no delete, and the struct shape stays forward-compatible for adding a `live` array later. No `parser.vl`/`ast.vl` change.
- **H-emitProgram front-end attempt: `/`/`%`, implicit trailing return, multiline type/union parse (120 green)** — the slices that let the real `ast.vl`/`parser.vl` source flow through the pipeline. `binOpcode` maps `/` → `i32.div_s` (signed) and `%` → `i32.rem_s` (the two integer ops `ast.vl`'s `i32ToStr` needs). A function whose LAST statement is a bare expression (no `return` keyword) now returns that value — the idiom every `ast.vl` constructor uses (`addNode(n)` / `out` as the final line). The self-host parser's `parseTypeDecl` now skips NEWLINE tokens inside a `{ … }` struct-field list and around `A | B | C` union variants (a `skipNewlines()` helper), so a multiline `type` written with a trailing comma parses (every `ast.vl` `type` is multiline). The unsupported-operator guard test repointed to `a ?? 2` outside a map get (still fails loudly). (`tests/selfhost_emit_program_test.ts`)
- **H-emitProgram multiple struct types + string fields (arena keystone, 132 green)** — `emitProgram`'s single-struct path generalized into a flat multi-struct table (`sNames`/`sFieldNames`/`sFieldStart`/`sFieldCount`/`sFieldTypes`/`sHeapIdx`, mirroring the union-variant table): each `type X = { … }` interns its own WasmGC struct heap type, and object-literal construction / field reads / field writes resolve the struct index (by literal field-set / annotation / param / local / return / global). A per-field wasm type is recorded so a STRING struct field lowers to a non-null `(ref $aTypeIdx)` and round-trips; the type section is wrapped in one WasmGC rec group (`0x4e`) so a struct can forward-reference the raw array type that follows it (V8 rejects an out-of-group forward type-index). `exprString` recognizes a string-typed struct-field read so the local infers the string ref type. String fields extend to UNION VARIANTS too (`uFieldTypes`), unblocking `ast.vl`'s Node variants that carry string payloads (`NumLit.numText`, …). (`tests/selfhost_emit_program_test.ts`)
- **H-emitProgram array struct/variant fields + `.push` (139 green)** — a struct or union-variant field whose type is an ARRAY now lowers to a ref to the matching growable-list wrapper: `i32[]` → `(ref $lTypeIdx)`, `T[]`/`N[]` → `(ref $rlTypeIdx)`, reusing the existing list machinery via the generic `struct.new`/`struct.get`/`struct.set` paths (`fieldTypeCode` classifies a field's `TypeRef`, `pushFieldStorage` emits the storagetype, `memFieldTypeCode` + `Member` branches type a field read as a list so `.length`/indexing/assignment work). A narrowed union-variant array-field read recovers the list ref through the box — the `Node`-variant shape (`Call.callArgs: Node[]`, `Block.blkStmts: i32[]`). `.push` onto a struct-field array runs the same inline grow/append. (`tests/selfhost_emit_program_test.ts`)
- **H-emitProgram `string[]` struct/variant fields + `.push` — arena fully expressible (145 green)** — a `string[]` struct or union-variant field is now a string-ref list (the same `{ backing, len, cap }` wrapper the map's keys list uses, over a `(ref null $aTypeIdx)` string backing — no new heap type; shares `mkArrIdx`/`mkListIdx`): `fieldTypeCode` classifies it as a distinct code, `collectA` forces the string-ref-list + string-backing types when a `string[]` annotation appears (instead of rejecting), construction builds a string-list literal (incl. empty `[]` via a threaded `pendingListKind`), reads recover a non-null string element + the wrapper `len`, and field writes / a `string[]` LOCAL store the list ref. `.push` onto a `string[]` field (`b.names.push("x")`) runs the inline grow/append over a dedicated string-push scratch quad, with the push-kind detector now a 3-way i32/ref/str split so an i32 push can't mis-claim a string-list receiver. With this the self-host arena's full field vocabulary is emittable. (`tests/selfhost_emit_program_test.ts`)
- **H-emitProgram struct-typed params in mutually-recursive groups (151 green)** — closes the keystone gap: passing a struct VALUE into a forward-referenced / mutually-recursive callee whose parameter is a struct type miscompiled when two structs shared a field-name set — a struct-literal call argument was constructed from a field-name guess (the first matching struct) instead of the callee parameter's DECLARED struct type, so the module failed to validate (the `parseExpr(p)`/`parseStmt(p)` recursive-descent shape, where every parser-state struct looks alike). A `pendingStructIdx` hint (mirroring `pendingListKind`) is threaded from `emitCall` — for each struct-typed parameter position it sets the callee's struct index (via `vtKindOfParam`/`paramStructIdxOf` over the callee `FuncDecl`), and `emitObj` consults it to OVERRIDE the field-name match, consuming it so nested literals fall back to inference. The functype param type-indices were already per-param-correct; only the call-site literal construction was wrong. (`tests/selfhost_emit_program_test.ts`)
- **H-fixpoint self-hosting fixpoint — M_self emits the goldens byte-for-byte** — closes the loop from VALIDITY (#241: the emitter self-compiles to an instantiable module) to CORRECTNESS. Drives the genuine lexer→parser→`emitProgram` pipeline over the emitter's OWN source plus a tiny exported entry point to produce `M_self` (the module the self-host emitter emits from itself), then INSTANTIATES `M_self` and runs it AS an emitter over the 14 byte-exact golden sources, asserting each output is BYTE-IDENTICAL to the host-pinned `tests/golden/*.wasm` — i.e. `f_self(P) == f_host(P)` for 14 real programs. The self-emitted module has no `print` import and `emitProgram` lowers only functions + module globals, so it is driven through `export function`s (`runEmit`/`rbyteLen`/`rbyteAt`) and its WasmGC byte buffer read back one byte at a time. The golden source set is single-sourced into `tests/selfhost/goldens.ts` so the byte-exact pin and the fixpoint drive the IDENTICAL set. No `compiler/*.vl` change needed — the emitter was already a correct fixed point. (`tests/selfhost_emit_fixpoint_test.ts`, #242)
- **H-fixpoint full-source fixpoint (gcc stage2==stage3) — M_self re-emits the WHOLE compiler byte-identically** — the bootstrap capstone, on the single largest real input there is: the compiler itself. `M_self` (the self-emitted emitter, ~1.2 MB) re-emits the entire compiler source `X` (lexer+ast+parser+wasmEmit, ~402 K chars → 132157 wasm bytes) BYTE-FOR-BYTE identically to the host-compiled emitter (`self_bytes(X) == host_bytes(X)`, both instantiate), exercising the large-magnitude LEB128 / section-size encodings the toy goldens never reach. GATED behind `SELFHOST_FULL_FIXPOINT=1` (two ~50–64 s host compiles of ~0.8 M / ~1.2 M-char sources embedding `X` as `array.new_fixed` runs); the always-run goldens fixpoint guards the emitter every commit. (`tests/selfhost_emit_fullfixpoint_test.ts`, #242)
- **H-check self-hosted `vl check` substrate — type-check GATES emit (Phase 1)** — wires the self-host TYPECHECKER (`typecheck.vl`) into the back-end path: `lexer → parser → checkProgram → (gate) → emitProgram`, all in one VL-compiled module. A clean program type-checks then emits a valid, instantiable module; an ill-typed one is REJECTED by `checkProgram` and never reaches `emitProgram`. Pure wiring — the self-host emitter takes only the parser arena and re-derives all types itself (`collectS`/`collectU`/`buildFnMap` + its own narrowing stack), so `checkProgram` is a DIAGNOSTICS GATE with no data handoff to emit; the five modules concatenate with ZERO symbol collisions beyond the usual lexer renames. `checkProgram`'s return rides in expression position (`i32ToStr(checkProgram(root))`, the PR#5 codegen gap), then `T.diags` drives the gate. 7 cases: 4 must-pass (check clean → emit → instantiate → run: `main`/`fib(10)`/locals/call-chain) + 3 must-reject (undeclared identifier, wrong arg count, `i32 = "hi"` assign mismatch — each blocked before emit). Proven on the subset `typecheck.vl` covers today (primitives, arithmetic/logical/compare ops, calls, member access, `let`/`func`/`if`/`return`/`block`, structural assignability, scope/shadowing); growing it to the full emit vocabulary (structs/unions/arrays/strings/maps/`is`/loops → eventually the compiler's own source) is the Phase 2 coverage-closure track. (`tests/selfhost_check_emit_test.ts`)
- **H-check Phase 2.1: STRUCTS in `typecheck.vl`** — the first coverage-closure step grows the self-host checker to type-check declared struct types. A new `userTypes: {[string]: i32}` registry on the `Checker` maps a `type Name = { … }` to its arena `TyObj`, filled by a Program **pass-0** (before function hoisting, so a struct name resolves in param/return/`let` annotations and in later structs' fields — reading `TypeDecl`/`FieldDef`/`TypeRef` INLINE, the mono-visitor constraint); `nameToTy` consults it after the primitives. An `ObjLit` (`{ x: e, … }`) now types as a structural `TyObj` of its field value types, which the existing `assignable`/`objFieldType` machinery matches to a declared struct by STRUCTURE (field order-independent). Struct-typed params/returns/locals, object-literal construction (annotation- / return-typed), member reads (`p.x`), and field WRITES (`p.x = v`, via the existing `=` path over a `Member` LHS) now type-check; field-type mismatches, missing/unknown fields, and unknown type names are rejected. Wired through the check→emit gate: 4 new cases — 2 must-pass (the `g_struct` coverage program → 45; structs + module globals + field write `g_globals` → 42, both emit + instantiate + run) + 2 must-reject (`{x:"hi"}` to `{x:i32}`; read of an absent field — gate-blocked). Forward references to a LATER-declared struct report "unknown type" (deferred — unused by the immediate set). (`compiler/typecheck.vl`, `tests/selfhost_check_emit_test.ts`)
- **H-check Phase 2.2: UNIONS + `is`-narrowing in `typecheck.vl`** — the bootstrap-keystone coverage step (both the self-host AST `Node` and the checker's own `Ty` are discriminated unions). A `type N = A | B` (`UnionDecl`, variant NAMES) registers in the same Program **pass-0** as structs — each variant name resolves (its struct registered earlier in source order) into a `TyUnion` of member type-arena indices. `x is T` (`IsExpr`) always types as `bool` and checks `T` names a known type; the actual **flow narrowing** happens in `if x is T { … }`: when the condition is an `is` over a simple IDENT, a pushed scope shadows `x` with the variant type `T` for the THEN branch only (reading the `IsExpr` + `Ident` INLINE, mono-visitor), so a narrowed field read (`if n is A { n.av }`) type-checks while an un-narrowed union member access stays an error. Union CONSTRUCTION needs no new code — an object literal assignable to ANY variant already satisfies the union-target branch of `assignable`. Wired through the gate: 4 new cases — 2 must-pass (`g_union` → 7: narrow-and-read + `{av:x}` construction typed by a `Node` return; `g_multiunion` → 30: two coexisting unions, each narrowed) + 2 must-reject (`is A` then read B's field → "no field"; a union naming an undeclared variant → "unknown type"). Negative narrowing (the ELSE branch / `!(x is T)`) and narrowing a non-ident operand are deferred. (`compiler/typecheck.vl`, `tests/selfhost_check_emit_test.ts`)
- **H-check Phase 2.3: ARRAYS in `typecheck.vl`** — grows the self-host checker to type-check growable arrays. A new `TyArray { aElem }` arena variant; `nameToTy` resolves a `T[]` annotation by stripping the parser's literal `[]` suffix and wrapping the element type (nests for `T[][]`); `assignable`/`tyToStr` gain array cases (an empty `[]` has element `-1`, assignable to any array — its concrete element pinned by the binding annotation). `checkNode` gains: array literals (`[e, …]` → `T[]` of the first element's type, rest checked assignable; empty `[]` → hole), index `a[i]` (i32 index, yields the element; string index → i32 code point), `.length` on arrays/strings → i32, and the `xs.push(e)` builtin method (receiver-is-array special case in the call path, element-type-checked — reads the Member callee inline, mono-visitor). Wired through the gate: 4 new cases — 2 must-pass (`i32[]` push/index-write/index-read/`.length` → 142; `Tok[]` push object literals + index a field → 32, both emit + instantiate + run) + 2 must-reject (`.push` of the wrong element type; indexing a non-array). (`compiler/typecheck.vl`, `tests/selfhost_check_emit_test.ts`)
- **H-check CORPUS-driven conformance (the durable harness)** — pivots self-host validation off bespoke snippets onto the REAL, implementation-agnostic corpus (`tests/cases/**/*.vl` + `@directive`s), whose own header promises it "will validate a future self-hosted compiler unchanged." `tests/selfhost_corpus_test.ts` drives a WHITELIST of corpus files through the VL front end (`lexer.vl → parser.vl → typecheck.vl`) and asserts VL's VERDICT matches the spec: an `@error` file must be REJECTED (≥1 parse/type diagnostic), any other ACCEPTED (0 diagnostics) — the directive is the oracle, so no TS run and no fragile message-text comparison (VL's diagnostic WORDING differs from TS's; text+position parity needs source-span threading, a later milestone). Seeded at **17 files** (8 `@error` type errors the checker raises + 9 clean programs it checks cleanly), discovered by a sweep over candidate dirs; the count is the conformance ledger and grows as coverage lands. Enabler: `typecheck.vl` now knows the `print` builtin (a `print(x)` Call special-case → void, args still checked) so printing programs don't false-error. SCOPE today is typecheck-verdict only — emit/`@run` log cross-check awaits `print` EMISSION in `wasmEmit.vl`; `@error-at L:C` awaits span threading. The bespoke snippet tests become transitional, retiring as the corpus whitelist absorbs their coverage. (`compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.4: LOOPS in `typecheck.vl` + corpus ledger 17 → 50** — `checkNode` now type-checks `while` (condition must be `bool`, body checked) and accepts `break`/`continue` (loop-nesting not tracked yet — a misplaced one is a parser concern). With loops checkable, a wider corpus sweep grew the conformance whitelist from 17 to **50 files**: 31 clean programs VL parses + checks with zero diagnostics (arith, `while`-loops, globals/struct-field mutation, mutual recursion + forward references incl. struct params, definite-assignment, object/array equality, empty-array usage inference) + 19 genuine front-end rejections (type errors the checker raises: assign/return/arg-count/arg-type/condition/boolean-coercion mismatches; lexer errors: malformed hex/separators; parser errors: illegal commas). The reject set is curated to rejections VL makes for a SPEC-ALIGNED reason — advanced-feature `@error` files (negation/intersection/recursive-types/null-inference/lambdas) are excluded as parse-subset artifacts. (`compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.5: STRING methods + FULL-CORPUS sweep — ledger 57 → 156** — `checkNode` now type-checks the string methods the compiler's own sources lean on: `.slice(i32, i32) → string`, `.indexOf(string) → i32`, `.includes(string) → bool`, `.charCodeAt(i32) → i32` (string-receiver cases in the call path, args checked; `.length` stays a Member, `s[i]` an Index), plus the `fromCodePoint(i32) → string` builtin (Ident special-case alongside `print`). The self-host checker now models every type the emitter does (i32/f64/bool/string/struct/union/array; maps are the last core-vocabulary gap). With that, a **full-corpus sweep** (all 422 single-file cases driven through `lexer.vl → parser.vl → typecheck.vl` in per-file crash isolation) grew the conformance whitelist from 57 to **156 files** — **64 ACCEPTED** (clean programs VL parses + checks with zero diagnostics) + **92 REJECTED** (invalid programs VL refuses: type errors the checker raises, lexer errors, parser syntax errors). Each entry is VL behaving correctly per the directive (verdict conformance; some advanced `@error` files are refused because the construct is outside VL's subset — a correct refusal, and the test self-corrects if a file later flips). The 266 remaining disagreements are the work left: clean files VL can't yet PARSE (`for`-loops, lambdas, generics, `if/then/else` expressions) and `@error` files VL doesn't yet CATCH (redeclaration, const-reassign). (`compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.6: FOR-loops (parser + ast + checker) — ledger 156 → 179** — the single biggest corpus blocker was `for`: 82 valid files used it and the self-host parser had ZERO support. Added two AST nodes — `ForRange` (`for v in A to B [step S] { … }`, a numeric range) and `ForIn` (`for v in arr { … }`, array iteration) — with `mkForRange`/`mkForIn` (`ast.vl`); `parseFor` in `parser.vl` (dispatched from `parseStmt` on the `FOR` keyword; `in`/`to`/`step` are SOFT keywords matched by text, the range-vs-in form decided by whether a soft `to` follows the first expression); and `checkNode` cases in `typecheck.vl` (range bounds + step must be i32, loop var is i32 in the body scope; for-in binds the var to the array's element type and rejects a non-array iterable). Purely additive — no existing self-host test uses `for`, and the compiler's own source is `while`-only, so the golden/fixpoint bytes are unchanged. A re-sweep grew the ledger from 156 to **179** (+23 valid `for` programs now conform; the `for` `@error` files — non-i32 bound, for-in over a non-array — are correctly rejected by the new checks). (`compiler/ast.vl`, `compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.7: inline UNION type annotations — ledger 179 → 182** — the largest remaining parser blocker was `|` in type position (`x: A | B`, `T | null`): the self-host parser only did `type N = A | B` decls, not inline annotations. `parseTypeName` now parses a `|`-chain of type atoms into a synthetic name `"A|B|null"` (mirroring the `i32[]`/`{[K]:V}` synthetic names; `null` is accepted as a member; safe because `parseTypeName` is only reached in type position, where `|` is a union separator not the bitwise-OR operator, and `parseTypeDecl` parses its own variants). `nameToTy` splits the synthetic name on `|` (checked BEFORE the `[]`/map shape so a member's suffix isn't misread) and builds a `TyUnion` (or `TyNullable` when a `null` member is present), reusing the existing union/nullable `assignable`. Net +3 (re-sweep: +6 valid union-using programs conform; −3 `@error` union files that now PARSE but whose error VL doesn't yet catch are honestly dropped). COMPLEMENT (negative) narrowing — refining `x` to its type-minus-`T` after a diverging `if x is T { … return }` — was implemented and REVERTED: plain `is T` complement made VL MISS real errors in soundness files that need richer flow narrowing (`!(x is T)`, `== null`, value-kind unions), a net verdict regression, so it is deliberately deferred until done properly. (`compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H3 codegen-bug pay-down: the workaround-forcing host bugs are FIXED — pinned + de-contorted** — the four historical host miscompiles that forced the self-host `.vl` sources into workaround style were re-tested and are ALL resolved by intervening host fixes: (1) two functions `is`-narrowing the SAME union, mutually recursive (the "Finding 2" mono-visitor constraint); (2) a per-variant helper taking the NARROWED variant struct and recursing back; (3) a struct-typed value passed into a mutually-recursive function group ("Unhandled AST → WASM Never"); (4) `.pop()` discarded at statement position on a struct-field array + a discarded/`let`-bound call return at module scale (PR #4/#5 gaps). Each is pinned by a new corpus case (`types/union-two-visitors.vl`, `types/union-narrowed-helper-recursion.vl`, `functions/struct-param-mutual-recursion-global.vl`, `lists/struct-field-pop-statement.vl`, `statements/discarded-call-return.vl`). The `typecheck.vl` mega-visitor is then DE-CONTORTED as the module-scale proof: the `Call`/`FuncDecl`/`Program` arms move out of `checkNode` into natural per-kind helpers (`checkCallNode(n: Call, ix)`, `checkFuncDeclNode(n: FuncDecl)`, `checkProgramNode(n: Program)`) that take the narrowed variant and recurse back — four functions now narrow the `Node` union in the largest concatenated module and everything passes (corpus 182/182, check→emit, pipeline). `popScope` drops the `_dropped` binding; `checkProgram` drops the `_root` indirection (its own body now exercises the discarded-call shape). Stale gap notes in `ast.vl`/`parser.vl`/`typecheck.vl` updated — `P`/`T` as globals is now a documented STYLE choice, not a forced workaround. This unblocks writing all future self-host code in natural, idiomatic shape (the Phase A step-1 of the self-hosting plan). (`compiler/typecheck.vl`, `compiler/ast.vl`, `compiler/parser.vl`, `tests/cases/…`)
- **H-check Phase 2.8: bare-expression bodies + RETURN-TYPE INFERENCE — ledger 182 → 219** — the "inference everywhere" idiom lands in the self-host front end. `parser.vl`: a function with no `{` after its signature (`function dbl(x: i32) x * 2`) parses its single expression into a synthetic one-statement Block (the same shape as a braced trailing-expression body). `typecheck.vl`: an UN-ANNOTATED return type is INFERRED — the function hoists with a `-2` PENDING sentinel; checking its body accumulates witnesses (each `return e` and the trailing expression, via `noteInferredRet` under the pending `curRet`; first concrete type wins, an in-cycle call contributes only "err"); the result is PATCHED into the hoisted `TyFunc` IN PLACE so every caller sees it. A forward call to a not-yet-inferred callee triggers DEMAND inference (`checkCallNode` checks the callee's FuncDecl via the new `fnDeclIx` name→decl map, with an `inferStack` in-progress guard; pass-2 then skips the already-inferred body so diagnostics aren't duplicated). A base-case-less cycle (`ping(){return pong()}` ↔ `pong(){return ping()}`) has no concrete witness and reports "cannot infer a return type — annotate" (matching the host's A17 contract); a self-recursive function WITH a base case infers fine (the in-cycle call types as the err hole, the base case provides the witness). Re-sweep: **+37 files** (the `functions/*` bucket — bare bodies, nested fns, forward/mutual recursion with inference — plus inference-dependent strays). `++`/`--` was considered and deliberately SKIPPED: a parse-time desugar would bake wrong post-increment value semantics into the AST (debt); proper inc/dec nodes come with emit support. (`compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.9: MAPS + SETS in `typecheck.vl` — ledger 219 → 229** — the last core-vocabulary gap closes: the self-host checker now models string-keyed maps and sets. A `TyMap { mKey, mVal, mSet }` arena variant — `mSet` distinguishes the SET flavor (a set is internally a boolean-valued map but exposes ONLY `.add`/`.has`/`.delete`/`.length`/`.values()`; the Map surface — `.set`/`.get`/`.keys` — must not leak, per the B6a/C2.2 regression cases). `nameToTy` resolves the parser's synthetic `{[K]:V}` index-signature name (splitting at the first `]:` so a nested-map VALUE parses; non-string keys resolve to "unknown type" — the host's rep). `Map()`/`Set()` construct HOLE maps (key/value `-2`… `-1`) pinned by the binding annotation or the FIRST `.set`/`.add` (usage inference, mutating the TyMap in place — a later conflicting use is a clean type error, the `error-infer-conflict` cases). A `Set()` initializer keeps its set flavor under a map-shaped annotation. Index `m[k]` checks the key and yields the NULLABLE value (`V | null`, defaulted with the new `??` nullish-coalescing case: a nullable LHS strips to its inner type, the default checked against it); `.length` → i32; map `.keys()`/`.values()` → `K[]`/`V[]`; set `.values()` → its ELEMENTS (`K[]`). Re-sweep: +13 accepts (the `maps/`/`sets/` behavior files: basics, delete/churn, iteration-order, infer-from-set, many-keys) and recovers 4 rejects (infer-conflict ×2, set `.get`/`.set` leak). The remaining map disagreements need the scope-close "cannot infer" floor (deferred). (`compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.10: inline OBJECT types + `null` + NULL-NARROWING + `?.` — ledger 229 → 245** — three thematically-one pieces of the soundness slice. (1) INLINE object type annotations `{ x: i32 }` (anonymous structural types, e.g. a `{ x: i32 } | null` return): `parseTypeAtom`'s `{` branch now forks on the next token — `[` stays the map `{[K]:V}`, an IDENT parses a field list into the synthetic name `"{x:i32,y:string}"`; `nameToTy` splits it back (TOP-LEVEL commas, each field at its first top-level `:`) into a structural `TyObj`, and the union `|`-split / map split became DEPTH-AWARE so a field's own union/map type doesn't shred the outer name. (2) The `null` LITERAL: a `NullLit` AST node, a `parsePrimary` case, a `TY_NULL` interned prim, and `assignable`'s nullable-target rule accepts it. (3) NULL-NARROWING over a simple identifier: `if x != null { … }` refines a nullable `x` to its inner type in the THEN branch; `if x == null { … } else { … }` refines the ELSE; and a diverging then (`if x == null { return … }` — return/break/continue last) makes `x` non-null for the REST of the block (the guard-clause idiom). Conservative null-stripping only — the union-complement lesson held: REJECTS stayed flat through the change (no newly-missed errors). Plus `?.` OPTIONAL CHAINING: an additive `OptMember` node (`o?.field` — no `Member` shape churn), a `parsePostfix` case over the lexer's existing `QUESTION_DOT`, and a checker arm that reads the field off a nullable receiver's INNER type and re-wraps the result nullable (`({x:i32}|null)?.x` → `i32|null`, defaulted with `??`). Re-sweep: +17 accepts (soundness/types null-guard files, map-of-struct `?.` files), −1 reject. (`compiler/ast.vl`, `compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H-check Phase 2.11: FUNCTION GENERICS `<T>` — ledger 245 → 251** — function type parameters with genuine CALL-SITE inference. `FuncDecl` gains `fnTyParams: string[]` (`parseFunc` collects `<T, U>` after the name — declaration position only, no `<`-comparison ambiguity). A new `TyVar { tvName }` arena variant: OPAQUE inside the generic body (assignable both ways, numeric-permissive — like the err hole, so `id<T>(x: T): T x` checks), but at a CALL SITE each TyVar param (or `T[]`) BINDS its type-param to the argument's type — first use wins, later uses must agree — and the RETURN substitutes through the binding (`resolveAnnot` maps a param/return annotation through the fn's type-param list before `nameToTy`). So `id("s")` IS `string`, and the correlation `@error` files (`let n: i32 = id("s")`, `first(["s"])`) remain genuinely REJECTED — rejects stayed flat through the change. Re-sweep: +6 accepts (`generics/{identity,first,last,swap,trailing-comma,generic-array-passthrough}`-class files). Generic `type` ALIASES (`type Box<T>`) stay out of scope (their `@error` arity files keep failing at parse, correctly). (`compiler/ast.vl`, `compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
- **H nullable struct refs end-to-end + checker flow-narrowing collectors — runtime oracle 113 → 119** — `S | null` (a declared name or an inline `{x:i32}` shape) lands as emitter kind 9, `(ref null $S)`: params/returns/locals/globals, `ref.is_null` for `== null`/`!= null`/`is null`, seeded `ref.null $S` for a bare `null` value (return/argument context), and `ref.as_non_null` on guarded member reads (the checker proves every access guarded, so the cast never traps on a checked program); `retStructIndex` resolves the nullable BASE as the one member-layout chokepoint, and kind-9 cells skip the non-const global read recast (their null IS the value). The checker's narrowing rewrites onto two condition COLLECTORS (`collectThenNarrows`/`collectElseNarrows`): `&&`/`||` chains (both facts), De Morgan via `!(…)`, `is null`, and `&&`/`||` RHS checked UNDER the left side's facts (`p != null && p.x > 0`); guard clauses apply the false-branch facts to the rest of the block. (`compiler/typecheck.vl`, `compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-front parser/lexer sugar tail + for-loop `continue` fix — runtime oracle 102 → 113** — compound assignment (`+=`/`-=`/`*=`/`/=` desugar to `a = a + (b)`), prefix/postfix `++`/`--` (desugar to `x = x + 1`; postfix-in-VALUE-position old-value semantics deferred), `if cond then <stmt>` single-statement sugar, trailing commas + multi-line forms in array literals and call args, bitwise `^`/`<<` (precedence ladder renumbered to the host's `| < ^ < &` order), and hex/binary/octal literals with `_` digit separators (legal only BETWEEN digits — doubled/trailing separators still reject). The `continue` fix: a for-range/for-in body with a DIRECT `continue` wraps in an INNER block so the branch lands on the increment (branching to the loop header skipped it — an infinite loop); bodies without one keep the golden-pinned shape. The self-host checker gains `const` reassignment rejection (`constScopes`, a parallel per-scope const-name chain; `x++` on a const now errors like the host). (`compiler/lexer.vl`, `compiler/parser.vl`, `compiler/typecheck.vl`, `compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit `toString` builtin + checker array methods — runtime oracle 90 → 102** — `toString(x)` lands end-to-end: the self-host CHECKER types it (i32/boolean → string) and the EMITTER lowers it inline over the string-op scratch frame — digits build in NEGATIVE space (a positive negates, so i32 MIN needs no overflowing flip; each digit is `48 - (v % 10)` of the negative remainder), a boolean picks "true"/"false" via typed `select` (`0x1c` — an if/else `local.set` of a NON-defaultable ref local does not count as initialization past the `end`). The checker gains array `.pop()` / `.get(i)` (both `T | null`) and `.clear()`. Start-fn scratch detection now descends TOP-LEVEL `if` chains (extracted `startScanBlock`/`startScanIf`). Anonymous shapes get synthetic `#anonN` names (the empty string is `rlInternName`'s "not a ref element" sentinel, so a ref list of anon-shape elements never interned — an OOB trap); the `fbValtype` family guards table OVERRUNS too. (`compiler/typecheck.vl`, `compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit `for…in` + map surface + uninitialized locals — runtime oracle 82 → 90** — `for v in <iter>` lowers over an i32 list, a `string[]` list, or a map's ordered `.keys()`/`.values()` (three pre-order cursor slots: element var, hidden i32 cursor, hidden list temp — the iterable evaluates ONCE; every scratch/locals walker recurses into `fiBody`); the start fn collects locals BEFORE scratch detection (the detectors classify through the declared-locals registry — `print(k)` over a for-in string element needs `k`'s slot). Map surface: `.length` (the live `size` field), the `.set(k, v)` statement, GLOBAL map receivers (`globalIsMap`), un-annotated `Map()` (type collection + `globalKind` kind 6), `.has` printing true/false, and a LOUD failure for ref-valued sets. Uninitialized `let x: i32` (A-definite-assign) emits as a zero-init slot. String-element array literals force the string-list types (`collectA` + `globalKind` kind 7); `names[i]` classifies as a string (`exprString` Index case). (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit anonymous struct shapes + trap pay-down — runtime oracle 71 → 82, emitter traps 12 → 0** — `collectAnonShapes` interns every un-declared object-literal field-name set as an unnamed struct (appended AFTER declared structs, so declared indices — and goldens — are unchanged), unlocking the no-`type` `objects/*` family; `fRetBool` joins the inferred-return tables (un-annotated `isEven`/`isOdd` print true/false); `globalKind` classifies NON-literal global initializers (a concat, `fromCodePoint(..)`, a cross-global ref) via the expression classifiers, fixing the string-global family; a tail-ASSIGNMENT return infers as its RHS. Every remaining unsupported shape now fails LOUDLY instead of trapping: unresolved-slot guards in `fbValtype`/`fbValtypeNullable`/`fbRefNullForKind`/`sFieldIndex`, the start-fn (`fnIx -1`) LetDecl arena read, and a final `emitFailed` check in `emitProgram` that catches a deep helper's swallowed failure. (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit return-type INFERENCE + print(string) — runtime oracle 60 → 71** — two pieces that compose. (1) Un-annotated non-void functions infer their RETURN KIND from the body's return expression (`fnRetExprOf`: the tail value or the first valued `return`, walking if/while/for): `computeRetInference` classifies it with the expression classifiers (`exprString`/`exprStruct`/`exprArray`/`exprRefArray`/`exprUnion`) and SETS the SAME `fRet*` tables the annotation path fills — so the functype result valtype AND every call-site classification follow from one place; iterated to a fixed point like voidness (`function outer() { return inner() }` needs inner's inferred kind, and `exprString`'s Call case reads `fRetStr`). (2) `print(string)` streams each code point through `__print_char__` (import 2) then `__print_str_flush__` (import 3) — an inline loop over the string-op scratch frame (A = the ref, I0 = cursor, I1 = `array.len`); the strop detection walkers treat `print(<string>)` as a string op so the frame reserves. Together: `function get() { return strGlobal }` + `print(get())` — the `globals/string-*` family — runs byte-correct. (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit VOID functions — runtime oracle 58 → 60** — `emitProgram` classifies genuinely-void functions (`function f() { print(x) }`: no return annotation, no valued `return`, no tail value) and emits them as `()->()`: a new `fRetVoid` table computed as a MONOTONE FIXED POINT (`computeVoidFns` — `stmtIsTailValue`'s Call case consults `fRetVoid`, so a tail call to a void callee makes the CALLER void too; the void set only grows, bounded by the function count). A void functype emits 0 results; a void body skips the fallthrough-`unreachable` padding (empty-stack fallthrough is valid — this was the RUNTIME-trap source); a statement call to a void function skips the result `drop` (alongside `print`). Un-annotated functions RETURNING refs (a string global) still default their functype to i32 — emitter-side return-type inference is the next slice, with the 11 emitter `unreachable` traps (unguarded paths in `objects/*` methods, map delete). (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit assignment-as-expression — runtime oracle 54 → 58** — assignment yields its RIGHT-HAND SIDE (confirmed deliberate; → `DECISIONS.md`): `emitExpr`'s `=` case lowers an Ident-target assignment as STORE-then-RE-READ (`emitAssign`, then the target re-reads through the normal Ident path with its non-const-ref re-cast logic), so `b = (a = 5)`, `while (a = a - 1) > 0`, and nested-assign conditions all emit; `stmtIsTailValue` admits a trailing Ident-target assignment, so `function bump() { count = count + 1 }` RETURNS the new count via the trailing-expression rule (fixing the "expected 1 elements on the stack" family). The self-host checker's `=` now yields the RHS type (host parity). Index/Member-target assignments stay statements in value position (later slice). ROADMAP B17 gains the user-flagged lints: assignment-of-a-LITERAL in condition position (`if x = true` with a boolean `x` slips past the mandatory-bool check — almost certainly a mistyped `==`) and per-line/per-file diagnostic suppression (`// vl-ignore <code>`). Remaining run-failures: 10 (genuinely-void functions — trailing `print` — the `()->()` functype slice), 2 log-diffs, 11 emitter traps. (`compiler/wasmEmit.vl`, `compiler/typecheck.vl`, `DECISIONS.md`, `ROADMAP.md`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit start-fn scratch fix + bool prints — runtime oracle 28 → 54** — two fixes nearly double the runtime oracle. (1) The start function's scratch DETECTION used only the `.pop` walker for the push flags; `fnHasPush` uses BOTH (`blockHasPushKind` OR `blockHasMemberPopKind` — a Member-receiver pop reuses the push frame). The top-level-statement scan now mirrors it exactly (block walkers for `for`/`while` bodies, `stmtIsPushKind` + the expr walkers for bare statements, plus ref-push SLOT recording via `refListSlotOfExpr`), so a top-level `out.push(i)` (the `lists/build-fusion` family) reserves its quad — fixing ~24 "invalid local index" modules. (2) `print(x)` now calls `__print_bool__` (import 1, renders true/false) when the arg is BOOLEAN-classified — `exprIsBool`: bool literals, comparisons, logical connectives, `!`, `is` guards, parens, and calls to a `boolean`-annotated function; identifiers stay unclassified (annotation threading later). Remaining run-failures: 14 (void functions — `function f() { … }` with no return value — the next emit feature), 2 log-diffs (inferred-bool returns; f64), 11 emitter traps. (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-emit FOR-RANGE lowering + start-fn locals — runtime oracle 24 → 28** — `emitProgram` lowers `for v in A to B [step K] { … }` (INCLUSIVE range) to the same block/loop shape as `while`: the loop var and a synthetic bound temp (`v#b` — the `to` expression evaluates ONCE) own the next two cursor slots (`collectLocals` allocates them in the same pre-order, mirrored by the emit-side cursor); the step must be a CONSTANT integer literal (optionally negated) and its SIGN picks the exit comparison (`i32.le_s` ascending / `i32.ge_s` descending, inverted + `br_if`); the increment is `v += K` before the back-branch, and `break`/`continue` ride the existing loop-frame stacks. The six scratch/locals WALKERS (`collectLocals`, `blockHasMemberPopKind`/`PushKind`/`StrOp`/`MapOp`, `collectRefPushSlots`/`MemberPopRefSlots`) recurse into `frBody` (+ the bound exprs), so pushes/string-ops/maps inside `for` bodies reserve their frames. TOP-LEVEL `for`s work too: the START FUNCTION now has DECLARED LOCALS (`collectStartLocals` walks `startStmts`; `fbBeginFunc`/`fbEmitLocalsVec` take the count), and `declaredIndexOf` resolves module-scope (`fnIx -1`) names through the scope stack (index == slot — no params; a global-section constexpr init still sees an empty stack, so nothing changes there). `ForIn` stays unsupported (fails loudly). Goldens 14/14 byte-identical; runtime oracle 24 → **28** (`loops/for-sum`, `for-step`, `for-range-bound-named-step`, `arrays/basics`); 12 more programs emit (70 emit gaps remain), 8 of them currently failing at run (moved buckets — never passing before; to investigate). (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`, `tests/selfhost_wasm_emit_test.ts`)
- **H-emit PRINT + top-level statements + the RUNTIME corpus oracle (tier 2) — 24 `@run` files run byte-correct** — the `docs/selfhost-print-emit-plan.md` implementation. `wasmEmit.vl` gains (1) an IMPORT SECTION (id 2): when any `print` call exists (an arena pre-pass sets `gImports = 4`), the host's `"imports"`-module print family (`__print_i32__`/`__print_bool__`/`__print_char__` `(i32)->()`, `__print_str_flush__` `()->()`) imports at wasm function indices 0..3 — every LOCAL function's index shifts by `gImports` at the WRITE points (the one `fbCall` site, the export entries, `startFnIdx`), `fnIndices` stays the arena position; two functypes append after the user/start functypes; a print-FREE program emits no import section, so the goldens stay byte-identical BY CONSTRUCTION (14/14 held, and the gated FULL FIXPOINT 2/2 — stage2==stage3 holds with the print machinery in). (2) `print(i32)` lowers to `call 0`; the void import means a bare print statement skips the result `drop`. (3) TOP-LEVEL STATEMENTS: `collectFns` now collects non-decl statements into `startStmts` instead of failing, and the synthetic START function runs them in source order after the non-const global inits (the host's `__program__` model) — a pure-statement program (most `@run` files) emits with 0 user functions. `print(string)`/`print(bool-as-true/false)` and scratch-needing top-level statements are the next slices. NEW `tests/selfhost_corpus_run_test.ts` (tier 2): each whitelisted `@run` file is lexed/parsed/CHECKED/EMITTED by the VL pipeline, the module instantiates with the host import family, top-level statements run via the wasm start function, and the captured logs must EQUAL the file's ordered `@log` directives — **24/304 pass** (arith, calls/forward-refs/mutual recursion, while-loops, soft keywords, discarded-call). Buckets of the rest: 84 emit gaps, 77 checker false-rejects, 76 parse gaps, 28 scratch-needing statements, 11 emitter traps (bugs to pin), 4 bool-print log diffs. (`compiler/wasmEmit.vl`, `tests/selfhost_corpus_run_test.ts`)
- **H-check Phase 2.12: LAMBDAS + function values + `.map`/`.filter` — ledger 251 → 255** — first-class functions land in the self-host front end (checker-side; emit lowering is a later slice). `parsePrimary` gains a `FUNCTION` case: a function EXPRESSION (`function(n: i32) n * 2`) parses via `parseFuncExpr` — like `parseFunc` but the name is OPTIONAL — into an anonymous `FuncDecl` (name `""`), with the same generics/bare-body shape. `checkFuncDeclNode` now RETURNS the function's own `TyFunc` (instead of void), so a `FuncDecl` in expression position evaluates to its type — `const f = inc` binds a function value (the Ident already resolved to the hoisted TyFunc), lambdas type as arguments, and anonymous functions skip the name-binding bookkeeping. Array `.map(fn)`/`.filter(pred)` type-check: the callback is a 1-arg function over the element type; `map` yields an array of the callback's RETURN type (`xs.map(function(n: i32) n > 2)` is `bool[]`), `filter` yields the receiver array type and requires a bool predicate. Re-sweep: +4 accepts (`arrays/map-filter`, `functions/function-equality`-class), rejects flat. Un-annotated function-typed PARAMS (`function apply(fn, a, b) fn(a, b)`) need param inference — deferred. (`compiler/parser.vl`, `compiler/typecheck.vl`, `tests/selfhost_corpus_test.ts`)
