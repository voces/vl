// @emit-error call to unknown function
// The zero-argument half of the same split: `__memory_size__` is declared and
// unlowered, so a program cannot ask how big linear memory is. With
// `__memory_grow__` equally unlowered, the one page the memory section hardcodes
// is the entire budget a program can ever address.
print(__memory_size__())
