// @run
// @log 7
// Order-independent module scope: a function body resolves a top-level `const`
// declared LATER in the file (a function runs at call time, after module init), the
// same way it already resolves a later-declared function. No annotation needed.
function seven(): i32 { base + 2 }
const base = 5
print(seven())
