// @check
// @error `x` is used before it is assigned
// Straight-line top-level code reading a global before its `let` runs is a
// use-before-initialization (TDZ) error — the name is hoisted/visible, not assigned.
print(x)
let x = 9
