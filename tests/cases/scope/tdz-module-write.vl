// @check
// @error `n` is written before its declaration
// A top-level write above a module binding's declaration would be overwritten by its
// initializer, so it is refused like the early read beside it (D2654).
n = 99
let n = 3 + 4
print(n)
