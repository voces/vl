// @check
// @error `b` is used before it is assigned
// Order-independence does NOT defeat initialization order: a global initializer
// that reads a LATER global hits the temporal dead zone — `b` is visible (hoisted)
// but not yet assigned when `a` initializes.
let a = b + 1
let b = 2
print(a)
