// @error the element type of this argument is still open here — fill or annotate it before passing it to `call`
// CONTRACT (must-error): an open `_ | null` element at any depth of an argument refuses a call
// to a hole-parameter function (D2506). Here the element sits
// under six nested lists, past the depth a bounded walk
// once stopped at, where the instance stored into it as invalid wasm.
function call(xs) { const r = xs[0][0][0][0][0][0]; r.push(null); r.length }
function main() {
  const a = []
  a.push(null)
  print(call([[[[[[a]]]]]]))
  a.push("s")
  print(a.length)
}
main()
