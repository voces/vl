// @trap
// The reference half of D2652: a string global read before its initializer ran held a null
// cell and died with an anonymous `null reference` (D2653). It now names the global and the
// reading function before the abort.
function rs(): string { ls }
print(rs())
const ls = "s" + "t"
