// @trap
// The write is inside a lambda that top-level code hands to a function before the
// declaration; the string the lambda stored was lost to the initializer (D2654).
function run(h: () => void) { h() }
run(() => {
  s = "early"
})
let s = "s" + "t"
print(s)
