// @trap
// A module-level loop before the declaration reaches the read through two function hops;
// the boolean cell read `false` (D2652).
function seven() { 7 }
function r1(): boolean { g }
function r2() { r1() }
for i in 0 until 2 { print(r2()) }
const g = seven() > 3
