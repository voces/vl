// @trap
// The read is inside a lambda that top-level code hands to a function before the
// declaration. The f64 cell read `0` (D2652).
function seven() { 7 }
function run(h: () => f64) { h() }
print(run((): f64 => g))
const g = seven() * 1.5
