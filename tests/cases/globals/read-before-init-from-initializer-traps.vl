// @trap
// An earlier global's initializer calls a function that reads a later global; the i64 cell
// read `0` and `a` was built from it (D2652).
function seven() { 7 }
function r(): i64 { g }
const a = r() + 1
const g = seven() as i64 * 3
print(a)
