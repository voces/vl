// @run
// @log 0
// @warning Unused variable
// A NULLABLE array of NULLABLE structs carrying a union field, as a module GLOBAL
// (`({a: boolean, f: f32 | string, z: string} | null)[] | null`). The outer `| null`
// makes the cell a `nulreflist` (kind 18) whose wrapper heap type is the ref-list
// slot's — NOT the i32-list wrapper the classifier fell through to before; the `[null]`
// element is a non-const init, so the cell zero-inits + start-fn stores through the same
// wrapper. Declaring the cell was `global.set`-type-mismatch INVALID-WASM; now it holds.
const g: ({a: boolean, f: f32 | string, z: string} | null)[] | null = [null]
function go() { print(0) }
go()
