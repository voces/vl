// @trap
// A nullable global's zero default is `null`, a value of its type, so an early read went
// unnoticed: `r() ?? "none"` printed `none` before the initializer ran (D2652).
function mk(): string | null { "x" }
function r(): string | null { g }
print(r() ?? "none")
const g = mk()
