// @trap
// A module global written before its initializer ran is a runtime error (owner ruling
// 2026-09-26). `setN` stored `99`, then the initializer stored `7` over it and the program
// printed `7` with nothing reported (D2654).
function setN() { n = 99 }
setN()
let n = 3 + 4
print(n)
