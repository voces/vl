// @run
// @log 0
// @log 3
// A module-level mutable binding written from inside a function: the function
// must mutate the shared global, not a private copy (bootstrap: compiler state).
let count = 0
function bump() {
  count = count + 1
}
print(count)
bump()
bump()
bump()
print(count)
