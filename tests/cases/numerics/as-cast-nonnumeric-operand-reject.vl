// @check
// @error `as` supports numeric conversions only
// The OPERAND of `as` must also be numeric: a `string` value cannot be cast to a
// numeric scalar with `as` (there is no numeric conversion from a string).
const s = "hi"
const n = s as i32
print(n)
