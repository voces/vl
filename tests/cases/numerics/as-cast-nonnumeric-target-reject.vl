// @check
// @error `as` supports numeric conversions only
// `as` is a NUMERIC-only conversion for now (B2): a non-numeric TARGET type is a
// type error, not a coercion. (`string` is a real type but not a numeric scalar.)
const a: i32 = 5
const s = a as string
print(s)
