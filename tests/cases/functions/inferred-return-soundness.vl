// @check
// @error-at 11:17 expected i32, got f64
// Inferred-return soundness (ROADMAP A12/A10): `apply(addf, …)` returns f64 (the
// return inference hole unifies against `addf`), so storing that result via the
// i32-typed `__store_i32__` must be rejected. Each call site instantiates a
// fresh copy of `apply`'s signature and *collapses* the resolved holes to
// concrete types, so the check is strict — no longer masked by `ensureType`'s
// always-widening `Infer` case. The error points at the offending argument.
function addf(a: f64, b: f64) a + b
function apply(fn, a, b) fn(a, b)
__store_i32__(0, 0)
__store_i32__(4, apply(addf, 1, 2))
__log__(0, 8)
