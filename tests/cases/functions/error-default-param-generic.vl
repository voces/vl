// @check
// @error its type names a type parameter of `id`
// A defaulted parameter's annotation may not name one of the function's own TYPE
// PARAMETERS: a call that omits the argument would drop one of that type
// parameter's binding sites, and the call-site inference that pins `T` would be
// deciding a slot the caller never wrote.
function id<T>(x: T, y: T = 5): T { x }
print(id(1))
