// @check
// @error `?` already means `= null`
// `p?: T` IS `p: T | null = null`, so spelling both a `?` and a `= e` says the
// same thing twice and disagrees about the value. Write the union out when the
// default is not `null`.
function f(a: i32, b?: i32 = 3): i32 { a }
print(f(1))
