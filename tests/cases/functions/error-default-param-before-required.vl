// @check
// @error defaults must be the LAST parameters
// Defaults are TRAILING-ONLY. A required parameter after a defaulted one would
// mean omitting the earlier argument has to shift the later ones, and positional
// omission stops being decidable.
function f(a: i32 = 1, b: i32): i32 { a + b }
print(f(1, 2))
