// @check
// @error unexpected ","
// @error unmatched parameter
// A lone comma in an otherwise-empty call argument list (`f(,)`) is still a
// syntax error: a trailing comma is only allowed AFTER at least one argument.
// The stray comma is parsed as a (bad) argument, so an arity error follows.
function f() 5
let x = f(,)
