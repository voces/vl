// @trap :4:25
// A generic instantiated at `T = never` through a function argument runs like its `void`
// twin, and the call it makes does trap (D1972).
function fail(): never { __trap__("boom") }
function call<T>(f: () => T) {
  f()
  print("returned")
}
call(fail)
