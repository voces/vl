// @check
// @error unmatched parameter
// A named argument whose name matches no parameter is a type error — the
// checker rejects it (and codegen agrees, since both bind args by name).
function f(a: i32, b: i32) a + b
f(c: 1, a: 2)
