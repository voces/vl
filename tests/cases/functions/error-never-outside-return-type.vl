// @check
// @error `never` has no values, so it is legal only as a function's declared return type
// @error `never` has no values, so it is legal only as a function's declared return type
// @error `never` for field 'a' has no values, so it is legal only as a function's declared return type
// `never` names no values, so it is spelled only as a declared return type: a field, a
// parameter and a binding annotation all refuse it. D1972.
type T = { a: never }
function p(x: never): i32 { 1 }
const v: never = __trap__()
print(1)
