// @check
// @error `x` would hold a `never` value, and none exists
// @error field 'b' would hold a `never` value, and none exists
// @error a list element would hold a `never` value, and none exists
// A call of type `never` cannot be stored: an un-annotated binding, an object field and a list
// element would each take a type no value has. An annotated binding, an argument and a
// `return` accept one, since nothing ever arrives (never-call-ends-path.vl). D1972.
function g(c: boolean): i32 {
  if c {
    const x = __trap__()
    const o = { b: __trap__() }
    const xs = [__trap__()]
  }
  1
}
print(g(false))
