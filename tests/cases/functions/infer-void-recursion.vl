// @run
// @log 6
// A recursive PROCEDURE (no value return position) infers `void` and is NOT mistaken
// for an uninferable cycle — no annotation, no "cannot infer" error.
let total = 0
function sumTo(n: i32) {
  if n > 0 {
    total = total + n
    sumTo(n - 1)
  }
}
sumTo(3)
print(total)
