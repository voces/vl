// @run
// @log 2
// Binding mutability is distinct from data mutability: a `const` forbids
// rebinding the *name*, but mutating the data behind it (a field write) stays
// legal. `o = …` would error; `o.x = …` does not.
const o = { x: 1 }
o.x = 2
print(o.x)
