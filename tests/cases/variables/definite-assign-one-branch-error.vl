// @check
// @error `x` is used before it is assigned
// Path join: assigning on only ONE branch leaves the else (fall-through) path
// unassigned, so the read after the `if` is not definitely assigned — error.
function f(c: boolean): i32 {
  let x: i32
  if c { x = 1 }
  return x
}
print(f(true))
