// @check
// @error Map() needs a map type annotation
// DIAGNOSTIC (B6a, BUG 3): `Map()` takes its concrete type from context (the
// binding annotation). With no annotation the type can't be pinned, so the
// compiler reports a clear, actionable error instead of dying with a generic
// "Unhandled Unknown type". (The type checker catches this inside a type-inferred
// block; at module top level — which isn't body-inferred — codegen surfaces the
// identical message, so the diagnostic is the same wherever the decl appears.)
let m = Map()
