// @check
// @error An object literal isn't a map value
// DIAGNOSTIC (B6a, BUG 3): an object literal coerced to a map type is a category
// error — a `Map<K,V>` is a distinct hash type, not a plain object struct. It
// used to hard-fail in codegen (a binaryen struct subtype mismatch). The type
// checker now rejects it cleanly: construct a map with `Map()`.
let o: {[string]: i32} = {}
