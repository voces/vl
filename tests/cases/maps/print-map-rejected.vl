// @check
// @error print of {[string]: i32} is type-valid but not yet supported by codegen
// CONTRACT (must-error): the same admission for a MAP. `m.length` and `m.get(k)` are
// unaffected — the first is an i32 and the second is a boxed `i32 | null` the union arm
// already rejects loudly.
const m: {[string]: i32} = Map()
print(m)
