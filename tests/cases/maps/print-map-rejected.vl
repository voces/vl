// @check
// @error print expects one scalar or string value (i32 | i64 | f32 | f64 | boolean | string), got {[string]: i32} — print the elements or fields individually, e.g. `print(xs[0])`
// CONTRACT (must-error): the same DOMAIN rejection for a MAP. `m.length` and `m.get(k)` are
// unaffected — the first is an i32 and the second is a boxed `i32 | null` the union arm
// already rejects loudly.
const m: {[string]: i32} = Map()
print(m)
