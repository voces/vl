// @run
// @log 109854365458523
// @log NULL
// @log OTHER
// @hint Intentionally-unused parameter `_q0`
// @hint Intentionally-unused parameter `_q1`
// @hint Intentionally-unused parameter `_q2`
// The original fuzz multiobs keep case (seed 301346386 case_00047), adapted: the outer
// struct literal `{a: string|null, f: () => i64, z: (i32,i32,i32) => {[string]: {a: f64,
// f: f64, z: string}} | boolean}` coexists with the map's VALUE struct `{a,f,z}` of the
// SAME field-name set. The map value struct must resolve to its own `{f64,f64,string}`
// row (field-TYPE-guarded), not the first same-fieldset row (the outer struct).
function go() {
  const v: {a: string | null, f: () => i64, z: (i32, i32, i32) => {[string]: {a: f64, f: f64, z: string}} | boolean} = {
    a: null,
    f: () => (109854365458523),
    z: (_q0, _q1, _q2) => (false),
  }
  print(v.f())
  const t200 = v.a
  if t200 != null { print(t200) } else { print("NULL") }
  const t220 = v.z(1, 1, 1)
  if t220 is {[string]: {a: f64, f: f64, z: string}} {
    const t221 = t220["k"] ?? { a: 2.5, f: 14.5, z: "dx" }
    print(t221.f)
  } else {
    print("OTHER")
  }
}

go()
