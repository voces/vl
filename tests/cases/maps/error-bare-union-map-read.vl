// @emit-error a bare `m[k]` read of a union- or list-valued map
// A bare `m[k]` over a LIST-valued map is `V | null` with no nullable local rep —
// a loud EMIT-stage reject pointing at the `m[k] ?? <default>` spelling.
const m: {[string]: i32[]} = Map()
function go() {
  const v = m["x"]
  print(1)
}
go()
