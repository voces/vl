// @check
// @error A boolean-keyed Map isn't supported yet
// DIAGNOSTIC (B6a): the map rep hashes exactly two key types — `string`
// (`__str_hash__`) and `i32` (the integer mix). Any other key annotation is
// reported directly, naming the key and the supported set, rather than
// degrading to a bare "unknown type '{[boolean]:i32}'". An emitter/runtime
// capability rejection, not a type error — the unsupported-lowering channel.
let m: {[boolean]: i32} = Map()
