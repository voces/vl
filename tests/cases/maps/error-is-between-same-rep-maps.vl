// @check
// @error `is` cannot tell {[string]: i32} from {[string]: boolean} yet
// D2493: two maps whose keys hash alike and whose values each rep as one i32 cell lower to one
// map struct and one box tag, so `is {[string]: i32}` answered true for the boolean map. The
// test that has to tell them apart is refused; the union itself is not
// (`union-of-same-rep-maps-without-a-test.vl`).
function f(u: {[string]: i32} | {[string]: boolean}) {
  if u is {[string]: i32} { print("int map") } else { print("bool map") }
}
const n: {[string]: boolean} = Map()
n.set("b", true)
f(n)
