// @run
// @log 1
// ROBUSTNESS FLOOR (positive): the ANNOTATED counterpart of the un-inferable
// `Map()` must still compile and run — the annotation pins `{[string]: i32}`, so
// no "cannot infer" diagnostic fires.
const m: {[string]: i32} = Map()
m.set("a", 1)
print(m["a"] ?? -1)
