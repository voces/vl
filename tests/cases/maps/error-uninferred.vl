// @check
// @error cannot infer a type for `m`
// ROBUSTNESS FLOOR: a `Map()` with no annotation AND no downstream usage that
// pins its key/value type has a genuinely unknown type. Usage-driven inference
// (`infer-from-set.vl`) resolves the hole from a later `m.set(k, v)` — but here
// there is NO such use, so the hole stays open. It must then report a clean,
// source-located "cannot infer … annotate" diagnostic at scope-close — never a
// thrown `TypeError`, never the opaque `Unhandled AST -> WASM "Unknown" type`
// crash. (See `annotated-empty-ok.vl` for the annotated form that compiles fine.)
const m = Map()
print(1)
