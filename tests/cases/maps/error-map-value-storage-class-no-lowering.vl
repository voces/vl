// @check
// @error-at 10:9 {[string]: i32} is not {[string]: i32 | null}: a container never widens implicitly
// The MAP arm of the no-implicit-container-widening rule, over a map's KEY and VALUE, that
// `arrays/error-elem-storage-class-no-lowering.vl` pins over an array's element. A
// `{[string]: i32}` stores its values unboxed and a `{[string]: i32 | null}` boxed, so only a
// copy could relate them, and a copy would stop sharing writes with the original.
//
function widenVal(_m: {[string]: i32 | null}) { print(1) }
const mi: {[string]: i32} = Map()
mi["a"] = 1
widenVal(mi)
