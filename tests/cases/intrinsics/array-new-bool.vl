// @run
// @log 11
// @log 22
// A boolean fill: the elements carry the truth value (consumed via `if` —
// boolean lists share the i32 backing in both compilers).
const bs = __array_new__(3, true)
if bs[2] {
  print(11)
}
bs[1] = false
if bs[1] {
  print(99)
} else {
  print(22)
}
