// @run
// @log 01234
// ADVERSARIAL: the appended expression reads the accumulator (`s.length`). Buffered
// fusion would see a stale length → must fall back to per-append concat.
let s = ""
for i in 0 to 4 {
  s = s + toString(s.length)
}
print(s)            // 01234
