// @run
// @log 5
// @log xxxxx
// String accumulation (positive): `let s = ""` built by `s = s + e` in a loop.
// However lowered (per-append concat or buffered fill), the result must be identical.
let s = ""
for i in 0 to 4 {
  s = s + "x"
}
print(s.length)   // 5
print(s)          // xxxxx
