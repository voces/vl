// @check
// @error-at 11:25 no method '.padLeft' on string
// D1230's CONTROL for the string receiver: the SAME call that gets a missing-import
// suggestion in `tests/cases/modules/ufcs-not-imported-transitive-std/entry.vl`
// keeps the plain sentence here, because this program has no module graph and so
// no candidate to name.
//
// The pair is what makes the suggestion falsifiable. Without it, an enrichment
// that fired on every unresolved string method would pass the positive fixture and
// this file would be the only thing to notice — which is exactly the widening the
// `ufcs-not-imported` code must not do.
print("ab".padLeft(5, "."))
