// @run
// @log xxxx
// @log 10
// ADVERSARIAL: the accumulator is read mid-loop (`total + s.length`) → fall back.
let s = ""
let total = 0
for i in 0 to 3 {
  s = s + "x"
  total = total + s.length
}
print(s)            // xxxx
print(total)        // 1+2+3+4 = 10
