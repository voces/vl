// @run
// @log b
// ADVERSARIAL: a non-append assignment to the accumulator (`s = "b"`) means it is
// not purely accumulated → must fall back.
let s = ""
for i in 0 to 2 {
  s = s + "a"
  s = "b"
}
print(s)            // b
