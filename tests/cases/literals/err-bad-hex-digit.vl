// @error hex literal has no digits after `0x`
// @error undeclared G
// `0xG` has no valid hex digit, so the literal is empty (lex error) and the
// stray `G` lexes as an identifier (a second, cascading error).
let a = 0xG
print(a)
