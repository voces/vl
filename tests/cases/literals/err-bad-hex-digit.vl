// @error invalid hex digit 'G'
// `0xG` is rejected at the lexer: `G` is not a hex digit. The whole `0xG` is
// consumed as ONE (error) token, so there is no cascading "undeclared G" — a
// cleaner single diagnostic than the TS host's two (which `cases_test` skips).
let a = 0xG
print(a)
