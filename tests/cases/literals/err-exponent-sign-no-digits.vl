// @error float exponent has no digits after `e`
// The sign is optional and the DIGITS are not: `1e+` and `1e-` are the same error
// as a bare `1e`. So is a separator where the first digit belongs (`1e_5`) — `_`
// separates digits, it never starts a run.
let a = 1e+
print(a)
