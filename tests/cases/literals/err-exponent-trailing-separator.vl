// @error `_` may only separate digits in a numeric literal
// The exponent's digits take the same separator rule as the mantissa's: legal
// BETWEEN digits (`1_0e1_0` runs), never trailing.
let a = 1e5_
print(a)
