// @check
// @error refers to itself
// The bodyless `type Foo` (grammar alt `TYPE ID`) aliases the name to itself.
// Using it as a type must report cleanly (opaque/recursive aliases unsupported —
// ROADMAP A14), not send `getConcreteType` into infinite recursion / stack
// overflow. The `= { … }` form is the way to declare a structural type today.
type Foo
let x: Foo = 0
