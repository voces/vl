// @error-at 7:9 unknown type 'Array<i32>'; did you mean 'i32[]'?
// @error-at 8:9 unknown type 'Record<string,i32>'; did you mean '{[string]: i32}' or 'Map<string, i32>'?
// @error-at 9:9 unknown type 'Record<string,Array<f64>>'; did you mean '{[string]: f64[]}' or 'Map<string, f64[]>'?
// @error-at 11:6 undeclared identifier 'len' — a length is a member in VL; did you mean 'xs.length'?
// Another language's collection spellings are refused with the VL spelling named. A program
// that declares its own `Array`, `Record` or `len` never sees these
// (foreign-collection-names-declared.vl).
const a: Array<i32> = [1]
const r: Record<string, i32> = Map()
const q: Record<string, Array<f64>> = Map()
const xs = [1, 2]
print(len(xs))
print(a.length + r.length + q.length)
