// @run
// @log ok
// THE OWNER'S OWN JSON VALUE TREE, pinned at the one thing it can already do: declare.
//
// Both container arms, both self-referential, beside a `null` arm. `is` against any arm is
// still refused (D982/D985) and that is tracked by the capability probes — this file pins
// only that the DECLARATION keeps compiling, which it stopped doing for one merge.
type Json = null | boolean | f64 | string | Json[] | {[string]: Json}

print("ok")
