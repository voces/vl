// @check
// @error unknown type 'bool'
// The `bool` spelling was removed in favour of the single canonical `boolean` (both used to be accepted,
// an aliasing wart). `bool` is now an ordinary unknown type name — rejected by the checker, not silently
// normalized. Use `boolean`.
const b: bool = true
print(b)
