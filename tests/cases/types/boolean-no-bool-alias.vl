// @check
// @error unknown type 'bool'; did you mean 'boolean'?
// The `bool` spelling was removed in favour of the single canonical `boolean` (both used to be accepted,
// an aliasing wart). `bool` is now an ordinary unknown type name — rejected by the checker, not silently
// normalized. Use `boolean`.
//
// D1590 — THE REFUSAL STANDS AND THE MESSAGE NAMES THE SPELLING. `bool` is the first entry of the
// newcomer spelling map, so the diagnostic ends `; did you mean 'boolean'?`. That is a suffix, not a
// rewrite: the `unknown type 'bool'` prefix this file pinned before is still what every consumer
// matches on, and `bool` is no more a type than it was.
const b: bool = true
print(b)
