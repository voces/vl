// @check
// @error-at 5:26 0 | 1
// Negation in an annotation (A4): `(0 | 1 | 2) & !2` is "one of 0/1/2 but not 2"
// = `0 | 1`. Assigning the excluded value `2` is a type error — the surface `!T`
// reaches the existing `subtractType` algebra (via `intersectType`).
let a: (0 | 1 | 2) & !2 = 2
