// @run
// @log ok
// A REFUTATION PIN, and it exists because this program silently stopped running once.
//
// A self-referential MAP arm beside a `null` arm. The declaration alone is the whole
// witness — no `is`, no binding, no value of type `T`. It RUNS today and must keep running.
//
// It broke when the transparency gate in `nulAliasMemberFaithful` was narrowed to register
// self-referential unions (D982/D985): registering a self-referential map arm walks
// `registerInlineUnion` -> `registerMapValUnion` -> `registerInlineUnion` by NAME with no
// depth bound and exhausts the wasm stack (D984). The `null` arm had been masking that by
// keeping the alias transparent, so the declaration was never registered at all.
//
// Eleven gates stayed green through the regression because nothing covered this spelling.
// That is what this file is for. When D984's recursion is bounded, the map clause in that
// gate can be lifted — and this program must still print `ok`.
type T = string | null | {[string]: T}

print("ok")
