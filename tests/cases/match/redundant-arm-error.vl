// @check
// @error redundant match arm
// A pattern already covered by an earlier arm is a dead arm — a compile error.
type K = "a" | "b"
function go() {
  const k: K = "a"
  match k {
    "a" => { print(1) }
    "a" => { print(2) }
    "b" => { print(3) }
  }
}
go()
