// @check
// @error enum 'Color' has no member 'Purple'
// An enum member access is checked: a non-member is a type error, not a silent 0.
enum Color { Red, Green, Blue }

function go() {
  print(Color.Purple)
}

go()
