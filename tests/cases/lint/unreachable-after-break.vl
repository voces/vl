// @check
// @warning Unreachable code
// A statement after `break` in a loop body is unreachable. `continue` behaves the
// same; both unconditionally leave the current iteration's straight-line block.
function f(n: i32) {
  let s = 0
  for i in 0 to n {
    break
    s = s + i
  }
  return s
}
