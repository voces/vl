// @run
// @log 3
// @log ok!
// @hint redundant type annotation: `p` is inferred as `{x: i32, y: i32}`
// NEGATIVE: imports used ONLY outside value-`Ident` positions must not flag —
// `Point` appears only as a TYPE annotation, `shout` only as a UFCS member-call
// property (`"ok".shout()` dispatches to the imported free `self`-function by
// name). Both ride the unused-import rule's side channels, not the binding scan.
import { Point, shout } from "./import-use-helper"

const p: Point = { x: 1, y: 2 }
print(p.x + p.y)
print("ok".shout())
