// @check
// @warning Unused variable `y`
// A local `let` inside a function body that is never read warns (B17 lint pass).
// `x` is read (returned); `y` is dead.
function f(n: i32): i32 {
  const x = n + 1
  let y = n + 2
  return x
}
