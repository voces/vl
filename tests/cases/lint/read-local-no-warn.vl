// @check
// A local that IS read produces no warning (the must-not-warn baseline: this
// file declares no diagnostics, so the harness asserts zero). Guards against the
// unused-variable lint over-firing on a used binding.
function g(n: i32): i32 {
  const total = n + 1
  return total
}
