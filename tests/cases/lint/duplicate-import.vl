// @run
// @log 4
// @warning Duplicate import `used` from "./import-use-helper"
// duplicate-import: the SAME specifier written twice — same source text, same
// imported name, same local binding. The second one binds nothing new (D1120
// rules that legal and it RUNS), so it is a warning, not an error, anchored at
// the SECOND occurrence's name token. Exactly one diagnostic: the first
// specifier is the one that binds and is not flagged.
import { used } from "./import-use-helper"
import { used } from "./import-use-helper"

print(used(2))
