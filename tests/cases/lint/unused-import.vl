// @run
// @log 4
// @warning Unused import `never`
// An imported name never referenced in this module flags `unused-import`
// (binding-keyed: the ImportDecl local `never` has no resolving occurrence).
// `used` is a value use — not flagged.
import { never, used } from "./import-use-helper"

print(used(2))
