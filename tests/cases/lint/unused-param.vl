// @check
// @warning Unused parameter `b`
// A function parameter that is never read warns. `a` is returned (used); `b` is
// dead. Prefix with `_` to suppress (see underscore-suppressed.vl).
function add1(a: i32, b: i32) {
  return a + 1
}
