// @check
// A leading-underscore name is the "intentionally unused" convention, so even
// though `_scratch` is never read it does NOT warn (no diagnostics declared =>
// zero asserted). Same suppression applies to an unused parameter `_unused`.
function h(n: i32, _unused: i32): i32 {
  let _scratch = n + 1
  return n
}
