// @check
// @error expected {i32: any}, got 5
// for…in requires an array (an i32-indexed collection); a non-array iterable is
// a clean type error, not a crash.
let s = 0
for x in 5 {
  s = s + x
}
