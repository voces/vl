// @check
// @error operator '&' is integer-only, got f64 and i32
// Bitwise operators are integer-only: a float operand has no `&` operator
// method, so `1.0 & 2` is a type error.
const x = 1.0 & 2
