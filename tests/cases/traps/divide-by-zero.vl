// @trap division by zero
// @trap :7:6
// A runtime divide-by-zero traps. The mapped message names the VL source
// location (`:7:6`, the `a / b` expression on line 7) and the reason.
const a = 10
const b = 0
print(a / b)
