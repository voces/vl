// @trap array index out of bounds
// A list WRITE at or past `len` traps like the read does — without the
// `i u< len` guard the store would land in the backing's `len <= i < cap`
// slack, silently invisible to `.length`/iteration.
const xs: i32[] = []
xs.push(1)
xs[3] = 7
print(xs.length)
