// @trap array index out of bounds
// A list read beyond even the backing capacity traps (len=1, cap=4, i=9) —
// the same `array.get` trap the in-slack read produces, so the two
// out-of-bounds flavors are indistinguishable to the program.
const xs: i32[] = []
xs.push(42)
print(xs[9])
