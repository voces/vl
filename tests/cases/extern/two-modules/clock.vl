// A second module declaring the SAME extern as the entry, with the same signature. An
// extern's name is its wasm import name, so both declarations describe one import — this
// module needs no `import` from the entry to call it.
extern function nowMillis(): i64

export function tick(): i64 {
  return nowMillis()
}
