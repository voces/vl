// @check
// @error an `extern function` has no body — the host provides it, so a body here would never run
// CONTRACT (must-error): the declaration form carries no body. A body would be dead code by
// construction, since every call to the name lowers to the host import.
extern function nowMillis(): i64 {
  return 0
}

print(nowMillis())
