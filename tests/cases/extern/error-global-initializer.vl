// @check
// @error `extern let rax` has no initializer — the host or the linked unit that exports it provides its value
// CONTRACT (must-error): an imported global's value belongs to whoever exports it. An
// initializer here would either be ignored or race the provider's, so it is refused.
extern let rax: i64 = 0

print(rax)
