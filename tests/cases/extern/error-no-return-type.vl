// @check
// @error extern `g` has no return type — an extern has no body to infer one from; write `: void` for an extern that returns nothing
// CONTRACT (must-error): an extern states its result, `: void` included. A `function` with no
// annotation infers one from its body; an extern has no body, so the missing annotation is
// refused rather than guessed (D1997).
extern function g(a: i32)

g(1)
