// @check
// @error extern `sink` has no return type — an extern has no body to infer one from; write `: void` for an extern that returns nothing
// CONTRACT (must-error): a later declaration of a link name that names no return type is told to
// write one, as a first declaration would be, rather than that it disagrees with `: void`.
extern function sink(a: i32): void
extern function sink(a: i32)

sink(1)
