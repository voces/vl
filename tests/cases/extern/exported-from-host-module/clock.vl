// One importer, using one of the host module's externs by name. No re-declaration: the
// `import` is the whole of what this module needs to call it.
import { nowMillis } from "./host"

export function elapsed(since: i64) {
  return nowMillis() - since
}
