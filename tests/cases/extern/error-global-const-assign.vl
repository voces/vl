// @check
// @error cannot reassign `const` width
// CONTRACT (must-error): `extern const` imports an immutable global, and a write to it is the
// ordinary `const` refusal — the engine would reject the `global.set` anyway.
extern const width: i32

width = 4
print(width)
