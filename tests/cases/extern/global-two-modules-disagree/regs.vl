// The same global at a different type from the entry's. One name is one import, so the two
// cannot both be satisfied and the checker says so at the declaration that disagrees.
extern let rax: i32

export function readA(): i32 { return rax }
