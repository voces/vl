// @check
// @error `extern const width` needs a type — an imported global's type is the whole contract, so write it: `extern const width: i64`
// CONTRACT (must-error): with no initializer there is nothing to infer from, and the type is
// what the import entry declares, so the author states it.
extern const width

print(width)
