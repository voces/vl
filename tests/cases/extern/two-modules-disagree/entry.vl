// @check
// @error extern `nowMillis` is declared `()->i64` here and `()->i32` elsewhere — one name is one host import, so every declaration of it must agree
// CONTRACT (must-error): the flat link namespace's one obligation. Two modules may declare
// one extern — that is the supported shape and costs nothing — but a DISAGREEMENT has no
// import section that satisfies both, so it is refused at the declaration that disagrees.
import { tick } from "./clock"

extern function nowMillis(): i64

print(nowMillis() >= 0)
print(tick())
