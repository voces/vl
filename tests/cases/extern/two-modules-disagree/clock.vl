// The same name at a DIFFERENT signature from the entry's. One name is one import, so the
// two cannot both be satisfied and the checker says so at the second declaration.
extern function nowMillis(): i32

export function tick(): i32 {
  return nowMillis()
}
