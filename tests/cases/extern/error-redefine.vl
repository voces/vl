// @check
// @error 'nowMillis' is declared `extern` — every call to it lowers to a host import, so this definition would never run
// CONTRACT (must-error): the same reservation the fs floor has. `emitCall` routes a call to
// the import before it looks up any declaration, so a function of that name would be emitted
// and never reached — refused at the definition, where the author can see it.
extern function nowMillis(): i64

function nowMillis(): i64 {
  return 0
}

print(nowMillis())
