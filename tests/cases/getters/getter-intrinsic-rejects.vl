// @check
// @error-at 25:29 the getter `userSqrt` on V calls `sqrt`, which has no step bound: it loops (`while` at line 22), and a getter must not loop
// @error-at 27:2 the getter `storeV128` on R calls `__store_v128__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 31:2 the getter `storeI64` on V calls `__store_i64__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 34:26 the getter `grows` on V calls `__memory_grow__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 35:26 the getter `sizes` on V calls `__memory_size__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 37:2 the getter `copies` on V calls `__memory_copy__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 41:2 the getter `fills` on V calls `__memory_fill__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 45:2 the getter `logs` on V calls `__log__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// @error-at 48:24 the getter `arr` on V calls `__array_new__`, and a getter may call only other getters, the single-instruction numeric, load and SIMD intrinsics, and functions that write, allocate and print nothing within the step budget
// The intrinsics a getter body may NOT call (D2064). The admitted set is read off the emitter's
// own classifiers — numeric opcodes, linear-memory loads, SIMD value operations, the heap window
// and `__trap__` — so everything else stays out: every store width (including the SIMD store,
// which shares the SIMD list), memory growth, size, copy and fill, the log hook, and an
// allocating array helper. A program function spelled like a numeric intrinsic is that function,
// and is judged by its own summary: this one loops.
type V = new { a: i32, f: f64 }
type R = new v128

function sqrt(x: f64) {
  let y = x
  while y > 2.0 { y = y / 2.0 }
  return y
}

get userSqrt(self: V): f64 { sqrt(self.f) }
get storeV128(self: R): i32 {
  __store_v128__(0, self as! v128)
  1
}
get storeI64(self: V): i32 {
  __store_i64__(0, 1 as i64)
  self.a
}
get grows(self: V): i32 { __memory_grow__(self.a) }
get sizes(self: V): i32 { __memory_size__() + self.a }
get copies(self: V): i32 {
  __memory_copy__(0, 8, 8)
  self.a
}
get fills(self: V): i32 {
  __memory_fill__(0, 0, 8)
  self.a
}
get logs(self: V): i32 {
  __log__(0, 0)
  self.a
}
get arr(self: V): i32 { __array_new__(self.a, 0).length }
