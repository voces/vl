// @check
// @error-at 8:4 a getter declares its result type
// @error-at 9:4 a getter takes exactly one parameter, its annotated receiver
// @error-at 12:2 a getter is a top-level declaration
// A getter declares exactly one parameter, its annotated `self`, and a result type, and it is
// a top-level declaration.
type V = new { a: i32 }

get noResult(self: V) { self.a }
get twoParams(self: V, k: i32): i32 { self.a + k }

function f(v: V): i32 {
  get inner(self: V): i32 { self.a }
  return v.a
}
