// Regression: the RETURN-position spelling of the null-hole list-element twin
// (`nested-null-hole-list-elem-twin.vl`): `return [{f: {f: null}}]` against a declared
// `{f: {f: i64 | null}}[]` return type mis-keyed the element through the INNER
// same-fieldset row — INVALID-WASM at the literal build (fuzz sweep7 declared leg,
// seed 129301940 case_00202). Same fix pair: `recordNullHoleObjAdopt` destination
// re-stamp + the ambiguity arm's canonical-render retry.
// @run
// @log NULL
// @log 0
type T0 = {f: {f: i64 | null}}
function makeIt(): {f: {f: i64 | null}}[] { return [{ f: { f: null } }] }
function go() {
  const v: {f: {f: i64 | null}}[] = makeIt()
  const t0 = v[0].f.f
  if (t0 != null) { print(t0) } else { print("NULL") }
  print(0)
}
go()
