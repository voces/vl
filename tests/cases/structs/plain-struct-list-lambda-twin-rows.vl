// Regression: a lambda returning a PLAIN-STRUCT-element list (`{a: i64, f: f64,
// z: {a: string, f: boolean, z: string}}[]`) in a program whose OTHER shapes put
// same-fieldset {a,f,z} TWIN rows in scope. Un-pinned (the spaced record matched no
// pin arm; `structElemListRetName` declined the nested-struct field), the element
// literal fieldset-matched the WRONG row and the list built as a union-box-element
// array — INVALID-WASM (fuzz sweep3 multiobs leg, seed 369283890 case_00290). The
// field gate now admits nested plain structs and the canon record pins the lambda.
// @run
// @log 0
// @hint Intentionally-unused variable `_g`
type K0 = "6" | "hhfn" | "vjlt"
const _g: {a: {a: boolean, f: {a: boolean, f: K0, z: i32} | i32, z: i64}[], f: () => {a: i64, f: f64, z: {a: string, f: boolean, z: string}}[], z: string | i32} = {
  a: [
    {
      a: true,
      f: {
        a: true,
        f: "hhfn",
        z: 11,
      },
      z: 6000000000,
    },
    {
      a: true,
      f: {
        a: true,
        f: "6",
        z: 11,
      },
      z: 6000000000,
    },
  ],
  f: () => ([{ a: 894143218309458147, f: 5.0, z: { a: "6624", f: true, z: "0" } }]),
  z: "42668",
}
function go() { print(0) }
go()
