// @check
// @error-at 4:0 is only valid inside a function
// PL-028 (plumb, D2262): a VALUED `return` at module scope refuses the same way as a bare
// one — the position is illegal regardless of whether it carries a value.
return 1
