// @check
// @error-at 7:3 return needs a value of type i32
// THE OTHER SIDE of `bare-return-void-early-exit.vl`, and the reason that file's emitter
// arm needs no arity guess of its own: a bare `return` in a VALUE-returning function is
// refused by the CHECKER, by name of the type it owes. So an empty result type is the
// whole population the emitter's bare-return lowering can see.
function g(): i32 {
  return
}
print(g())
