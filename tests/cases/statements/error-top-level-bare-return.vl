// @check
// @error-at 4:0 is only valid inside a function
// PL-028 (plumb, D2262): a bare `return` standing directly at module scope, with no
// enclosing control-flow at all — the plainest program the message forbids.
return
