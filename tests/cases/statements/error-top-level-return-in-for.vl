// @check
// @error-at 4:18 is only valid inside a function
// PL-028 (plumb, D2262): the same refusal, reached through a top-level `for` instead of
// an `if` — the checker's test is "no enclosing function", not "no enclosing `if`".
for i in 0 to 3 { return }
