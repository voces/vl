// @check
// @error-at 17:2 an empty block does nothing; remove it
// @error-at 19:17 an empty block does nothing; remove it
// @error-at 19:29 an empty block does nothing; remove it
// @error-at 21:2 an empty block does nothing; remove it
// @error-at 25:2 an empty block does nothing; remove it
// @error-at 28:2 an empty block does nothing; remove it
// @error-at 31:0 an empty block does nothing; remove it
// @error-at 34:2 a `{}` here is an empty block; to produce an empty record write `({})`
// @error-at 37:14 a `{}` here is an empty block; to produce an empty record write `({})`
// @error-at 40:2 a `{}` here is an empty block; to produce an empty record write `({})`
// A bare `{}` standing as a statement inside a block is an empty block, refused (D2223). Where
// it is a block's last statement and a record value is expected there, the message says to
// write `({})`; elsewhere it says to remove it. One diagnostic each and nothing after it.
type Cfg = { out: string | null }
let c = true
if c {
  {}
}
const v = if c { {} } else { {} }
function f() {
  {}
  print(1)
}
const g = () => {
  {}
}
while c {
  {}
  c = false
}
{}
function tail(k: i32): Cfg | null {
  if k == 0 { return null }
  {}
}
function arm(k: i32): Cfg | null {
  if k == 0 { {} } else { null }
}
const lam = (): Cfg => {
  {}
}
f()
g()
print(v)
print(tail(1) != null)
print(arm(0) != null)
print(lam().out ?? "none")
