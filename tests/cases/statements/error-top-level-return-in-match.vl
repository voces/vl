// @check
// @error-at 10:4 is only valid inside a function
// PL-028 (plumb, D2262): the same refusal, reached through a top-level `match` arm instead
// of an `if` — the checker's test is "no enclosing function", not "no enclosing `if`".
const n = 3
match n {
  0 => {
    print("zero")
  }
  _ => {
    return
  }
}
