// @check
// @error-at 5:14 is only valid inside a function
// PL-028 (plumb, D2262): the same refusal, reached through a top-level `while` instead of
// an `if` — the checker's test is "no enclosing function", not "no enclosing `if`".
const n = 3
while n > 0 { return }
