// @check
// @error-at 7:27 is only valid inside a function
// PL-028 (plumb): `vl check` accepted a bare `return` reachable only through a top-level
// `if`, and the emitter refused it instead (D2262) — "the checker owed this diagnosis" is
// the standing rule this closes. The checker now refuses at the statement itself, whatever
// control-flow construct stands between it and the module's outermost scope.
const n = 3
if n > 0 { print("early"); return }
print("late")
