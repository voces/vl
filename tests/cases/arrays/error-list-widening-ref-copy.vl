// @check
// @error-at 20:4 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant. A store through Shape[] could put in an element that Circle[] does not allow. Declare the destination `readonly Shape[]` for a read-only view, or copy it with `.map((x): Shape => x)`
// @error-at 23:21 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 27:33 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 30:18 Circle[] is not Shape[] (reached through {xs: Circle[]} into Holder): a container never widens implicitly, and only a readonly list is covariant
// @error-at 32:26 Circle[] is not Shape[] (reached through Circle[][] into Shape[][]): a container never widens implicitly, and only a readonly list is covariant
// @error-at 35:11 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 37:19 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 40:27 Circle[] is not Shape[]: a container never widens implicitly, and only a readonly list is covariant
// @hint redundant type annotation: `cs` is inferred as `{r: i32}[]` — remove it to lean on inference
// The retired read-only ref copy (D791): a `Circle[]` into a mutable `Shape[]` would need a
// hidden copy, and a `Shape` stored through it could not be a `Circle`. Only a `readonly`
// list is covariant, so every mutable destination refuses, at each delivery position, and
// names both fixes. `list-widening-readonly-destination-runs.vl` is the readonly twin.
type Circle = { r: i32 }
type Sq = { s: i32 }
type Shape = Circle | Sq
const cs: Circle[] = [{ r: 1 }]

function arg(xs: Shape[]) { print(xs.length) }
arg(cs)

function bind() {
  const b: Shape[] = cs
  print(b.length)
}

function ret(): Shape[] { return cs }

type Holder = { xs: Shape[] }
const h: Holder = { xs: cs }

const nested: Shape[][] = [cs]

const outer: Shape[][] = []
outer.push(cs)

const g: Shape[] = cs

let ga: Shape[] = []
function reassign() { ga = cs }
