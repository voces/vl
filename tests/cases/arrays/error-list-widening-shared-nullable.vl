// @check
// @error-at 18:4 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant. A store through (C | null)[] could put in an element that C[] does not allow. Declare the destination `readonly (C | null)[]` for a read-only view, or copy it with `.map((x): C | null => x)`
// @error-at 21:26 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 25:38 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 28:18 C[] is not (C | null)[] (reached through {xs: C[]} into Holder): a container never widens implicitly, and only a readonly list is covariant
// @error-at 30:31 C[] is not (C | null)[] (reached through C[][] into (C | null)[][]): a container never widens implicitly, and only a readonly list is covariant
// @error-at 33:11 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 35:24 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 38:27 C[] is not (C | null)[]: a container never widens implicitly, and only a readonly list is covariant
// @hint redundant type annotation: `cs` is inferred as `{n: i32}[]` — remove it to lean on inference
// D2161's close: a list whose element keeps its storage is SHARED by a wider handle, so a
// `null` stored through `(C | null)[]` would reach the `C[]`. Only a `readonly` list is
// covariant, so every mutable wider destination refuses, at each delivery position, and names
// both fixes. `list-widening-readonly-destination-runs.vl` is the readonly twin that runs.
type C = { n: i32 }
const cs: C[] = [{ n: 1 }]

function arg(xs: (C | null)[]) { print(xs.length) }
arg(cs)

function bind() {
  const b: (C | null)[] = cs
  print(b.length)
}

function ret(): (C | null)[] { return cs }

type Holder = { xs: (C | null)[] }
const h: Holder = { xs: cs }

const nested: (C | null)[][] = [cs]

const outer: (C | null)[][] = []
outer.push(cs)

const g: (C | null)[] = cs

let ga: (C | null)[] = []
function reassign() { ga = cs }
