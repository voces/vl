// @check
// @error cannot infer a type for `xs`
// ROBUSTNESS FLOOR: an empty `[]` with no annotation AND no downstream usage that
// pins the element type has a genuinely unknown element type. Usage-driven
// inference (`infer-empty-*.vl`) resolves the hole from a later `xs.push(v)` /
// `xs[i] = v` / a `T[]` param / a `T[]`-returning tail — but here there is NO such
// use, so the hole stays open. It must then report a clean, source-located
// "cannot infer … annotate" diagnostic at scope-close — never a thrown
// `TypeError`, never the opaque `Unhandled AST -> WASM "Unknown" type` crash.
const xs = []
print(1)
