// @check
// @error cannot infer a type for `xs`
// ROBUSTNESS FLOOR: an empty `[]` with no annotation and no resolving context
// has an unknown element type. This used to CRASH the compiler — `containsInfer`
// dereferenced the undefined element of the array (an empty union softened to
// `undefined`). It must instead report a clean, source-located "cannot infer …
// annotate" diagnostic BEFORE codegen — never a thrown `TypeError`, never the
// opaque `Unhandled AST -> WASM "Unknown" type`. (Usage-based inference of the
// element type from `xs.push(1)` is a separate roadmap item; here we only make
// the FAILURE clean.)
const xs = []
xs.push(1)
