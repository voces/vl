// @run
// @log 1
// @log 1
// ROBUSTNESS FLOOR (positive): the ANNOTATED counterpart of the un-inferable
// empty `[]` must still compile and run — the annotation pins the element type,
// so the inference hole is resolved and no "cannot infer" diagnostic fires.
const xs: i32[] = []
xs.push(1)
print(xs.length)
print(xs.get(0) ?? -1)
