// @check
// @error-at 8:15 i32[] is not readonly f64[] here: this list is written (line 10), and a written container never widens implicitly
// @error-at 14:9 i32[] is not readonly f64[] here: this list is written (line 12), and a written container never widens implicitly
// D2411: an assignment yields its right side, so `const v = (c = x)` is one more handle on
// `x`. A write through that handle makes `x` a written list, which never widens implicitly
// into `readonly f64[]`, exactly as a write through `const v = x` does.
let c: readonly f64[] = [1.5]
const x = [5, 2]
const v = (c = x)
v[0] = 9
print(x[0])
function pass(p: i32[]) { p[1] = 3 }
let d: readonly f64[] = [1.5]
const y = [5, 2]
pass(d = y)
