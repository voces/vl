// @check
// @error-at 19:4 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant. Its elements would be stored differently, so it would be a copy, and a write through either list would not reach the other. Declare the destination `readonly f64[]` for a read-only view, or copy it with `.map((x) => x as f64)`
// @error-at 22:19 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 26:31 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 29:18 i32[] is not f64[] (reached through {xs: i32[]} into Holder): a container never widens implicitly, and only a readonly list is covariant
// @error-at 31:24 i32[] is not f64[] (reached through i32[][] into f64[][]): a container never widens implicitly, and only a readonly list is covariant
// @error-at 34:11 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 36:17 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 39:27 i32[] is not f64[]: a container never widens implicitly, and only a readonly list is covariant
// @error-at 41:18 i32[] is not i64[]: a container never widens implicitly, and only a readonly list is covariant
// @hint redundant type annotation: `ns` is inferred as `i32[]` — remove it to lean on inference
// The retired read-only converting copy (D965): an `i32[]` is stored differently from an
// `f64[]`, so a mutable `f64[]` destination would need a hidden copy that a write through
// either list never reaches. Only a `readonly` list is covariant, so every mutable
// destination refuses, at each delivery position, and names both fixes.
// `list-widening-readonly-destination-runs.vl` is the readonly twin that runs.
const ns: i32[] = [1, 2]

function arg(xs: f64[]) { print(xs.length) }
arg(ns)

function bind() {
  const b: f64[] = ns
  print(b.length)
}

function ret(): f64[] { return ns }

type Holder = { xs: f64[] }
const h: Holder = { xs: ns }

const nested: f64[][] = [ns]

const outer: f64[][] = []
outer.push(ns)

const g: f64[] = ns

let ga: f64[] = []
function reassign() { ga = ns }

const ls: i64[] = ns
