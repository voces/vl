// @run
// @log 13131313131
// @log 712583
// @log 1.5
// @log 2
// @log 13131313131
// An UN-ANNOTATED numeric array literal whose WIDE element is not first: ANY
// i64-magnitude / float element makes the literal an i64 / f64 list, exactly as when
// the wide element leads ([13131313131, 712583] / [1.5, 2] already classified so).
// First-element-only classification (`arrLitIsI64` / `arrLitIsF64` / `anonFieldCode`'s
// list arm) claimed the i32 list and emitted `i64.const`/`f64.const` into the i32
// array — INVALID-WASM (fuzz frontier seed 70441494 d4 --branching, the
// `{f: [712583, 13131313131]}` closure-result lambda).
function go() {
  const a = [712583, 13131313131]
  print(a[1])
  print(a[0])
  const b = [2, 1.5]
  print(b[1])
  print(b[0])
  // The struct-FIELD literal takes the same all-element leaf split.
  const s = { f: [712583, 13131313131] }
  print(s.f[1])
}
go()
