// @trap cannot be called in this context
// The 64-bit twin of wait32-unshared-traps.vl: `memory.atomic.wait64` on a memory that
// is not `shared` traps, here with an infinite (negative) timeout that would otherwise
// block forever.
print(__atomic_wait64__(16, 0 as i64, -1 as i64))
