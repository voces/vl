// @check
// @error argument 2: expected i64, got f64
// An i64 row takes its value at i64: the checker refuses an f64 operand rather than
// converting it, as it does for `__store_i64__`.
const x = 1.5
__atomic_store_i64__(0, 1)
print(__atomic_rmw_add_i64__(0, x))
