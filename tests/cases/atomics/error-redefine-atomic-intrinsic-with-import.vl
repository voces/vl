// @check
// @error '__atomic_fence__' is a compiler intrinsic and cannot be redefined
// The import face of error-redefine-atomic-intrinsic.vl: an import renames every top-level
// declaration before the checker runs, and the reservation still holds for an atomic name.
import { hypotF64 } from "std:math"

function __atomic_fence__() { 0 }
__atomic_fence__()
print(hypotF64(3.0, 4.0))
