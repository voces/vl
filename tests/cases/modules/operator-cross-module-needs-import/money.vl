export type Money = new { cents: i32 }
export function mkMoney(c: i32): Money {
  const m: Money = { cents: c }; return m
}
function "+"(self: Money, other: Money): Money {
  const r: Money = { cents: self.cents + other.cents }
  return r
}
