// @check
// @error-at 6:4 the getter `extra` on V must be declared in the module that declares `V`
// The orphan rule: a getter belongs to its receiver type, so only the module that declares
// the type may declare one — as for operators (#3003) and type-bound methods (#3005).
import { V, mk } from "./lib"

get extra(self: V): i32 { 1 }

print(mk(4).extra)
