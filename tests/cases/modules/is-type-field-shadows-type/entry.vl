// @run
// @log 2
// D-PARSETY P2: the `is` check type's spelling is recorded on the `IsExpr` node
// and the merge renames the TREE. The union member leg alone is not enough —
// with the members un-mangled and this one still scanned as characters, the
// check type is `{w$m1: string}` and the emitter rejects it with
// "`is` names a type that is not a union variant".
import { mk, tell } from "./lib"

print(tell(mk()))
