// The second module is the whole control. Its only job is to exist: with two modules the
// merge mangles every TOP-LEVEL name per module, so the entry's `const n` becomes `n$m1`
// while the block-local `n` keeps its source spelling and the D1597 collision cannot form.
export const K = 5
