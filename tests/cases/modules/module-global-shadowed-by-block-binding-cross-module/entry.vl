// @run
// @log hi
// @log 9
// @log 5
// D1597's CONTROL, and it runs today and must keep running. This is
// `tests/cases/scope/module-global-read-claimed-by-block-binding.vl` with one import
// added — the block still binds `n`, the module global is still `n`, and the read after
// the block still names `n`. The single-file spelling refuses
// `emitProgram: field access receiver is not a struct`; this one prints the right values.
//
// The difference is the module merge, which mangles top-level names per module. A fix for
// D1597 must not reach this file: the mangling is not the fix, it is the reason the family
// was invisible to every cross-module fixture in the tree.
import { K } from "./table"

if true {
  const n = "hi"
  print(n)
}

const n = { base: 9 }
print(n.base)
print(K)
