// HOP 1 — republishes the leaf's names AND calls them itself (`compiler/typecheck.vl`).
export { nameElem, nameEndsArr } from "./leaf"

export function midDepth(name: string) {
  let d = 0
  let cur = name
  while nameEndsArr(cur) {
    d = d + 1
    cur = nameElem(cur)
  }
  d
}
