// @check
// @error-at 7:28 'loadU8' is not imported — a free `loadU8(self: …)` accepting Buf is exported by "std:buffer"; a UFCS call resolves only names in scope, so import `loadU8` from there — add `loadU8` to the existing `import { … } from "std:buffer"`
// D1230 — the entry already imports `Buf` from "std:buffer", so the suggested
// edit extends that import rather than proposing a second, colliding one.
// Sibling to `ufcs-not-imported-transitive-std` (no import of the module yet).
import { Buf } from "std:buffer"

function f(b: Buf): i32 { b.loadU8(0) }
