// Neither export carries a type annotation — `add`'s return type is inferred
// from `a + b` and `SEED` is an inferred const. Cross-module resolution uses the
// inferred types; the module boundary adds no annotation requirement.
export function add(a: i32, b: i32) {
  return a + b
}

export const SEED = 41
