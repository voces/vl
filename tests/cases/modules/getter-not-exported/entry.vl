// @check
// @error-at 7:8 the getter `thrice` on V is not exported by the module that declares it
// A getter reaches another module only when it is declared `export get` — the D1191
// boundary type-bound methods keep. Inside "./lib" the un-exported getter reads normally.
import { mk } from "./lib"

const v = mk(4)
print(v.thrice)
print(v.twice)
