// The DEPENDENCY that uses `b`'s `area`. It never reaches the entry's scope — its
// only role is to register a UFCS call site for the plain name `area` first, which
// is what the dependency-first rewrite walk made decisive.
import { area, sq } from "./b"

export function nine(): i32 { return sq(3).area() }
