// @run
// @log 10
// @warning Duplicate import `area` from "./a" (remove one)
// The NARROW half of D1120's duplicate-import rule, pinned. Importing one name twice
// from the SAME module binds it to ONE declaration: redundant, not ambiguous, and it
// runs today. The rule tests the resolved merged target (`modMergedTargetOf`), not the
// specifier text, so this stays legal while two DIFFERENT declarations do not — and a
// re-export chain that lands on the same declaration is legal for the same reason
// (`../duplicate-import-reexport-chain/`).
//
// Redundant is not silent: the `duplicate-import` LINT says so (warning, owner ruling
// 2026-09-02). The @warning above is DOCUMENTATION here — this harness adjudicates the
// lint tier on SINGLE-FILE cases only, so the graded pin for the warning is
// `tests/cases/lint/duplicate-import.vl` and the graded pin for its message text over a
// module GRAPH is `tests/vl_check_module_lint_test.ts`.
import { area, box } from "./a"
import { area } from "./a"

print(box(5).area())
