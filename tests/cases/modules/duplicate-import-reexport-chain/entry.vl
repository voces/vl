// @run
// @log 10
// ONE declaration reached through TWO specifiers: `area` direct from "./a" and again
// from "./b", which re-exports it from "./a". Neither an error nor a warning, and it
// RUNS — the owner's ruling of 2026-09-02, pinned rather than asserted.
//
// The two rules divide this case by construction. D1120's ERROR tests the resolved
// merged target (`modMergedTargetOf`), and both specifiers resolve to `a.vl`'s `area`,
// so there is nothing ambiguous to refuse. The `duplicate-import` LINT tests the
// SPECIFIER TEXT, and `"./a"` is not `"./b"`, so there is no repeated specifier to
// report. An error here was ruled acceptable in principle but not worth analysis that
// is not free; keying the lint on the text is what makes leaving it legal free.
import { area, box } from "./a"
import { area } from "./b"

print(box(5).area())
