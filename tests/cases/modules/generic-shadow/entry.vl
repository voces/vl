// @run
// @log 41
// Imports a type whose name (`T`) collides with the exporter's generic type
// parameters — the rename must distinguish the binding from the parameter.
import { T, pickQ } from "./lib"

let t: T = { q: 41 }
print(pickQ(t))
