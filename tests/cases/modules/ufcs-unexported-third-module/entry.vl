// @error no field 'hidden' on Box
// D1191, one module over: the leak does not need the ENTRY to spell the call.
// `mid.vl` is the file that borrows `./lib`'s un-exported `hidden`, and the entry
// only calls `mid.twice(21)` — so the refusal lands in `mid`, where the out-of-scope
// name is written. Before the fix this whole graph compiled and printed 42.
import { twice } from "./mid"

print(twice(21))
