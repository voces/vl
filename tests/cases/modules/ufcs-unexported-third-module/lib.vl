// The declaring module: `hidden` is not exported, and lib's own UFCS call is
// what banked the merge's old name-keyed row.
export type Box = { v: i32 }

function hidden(self: Box): i32 { return self.v * 2 }

export function mk(v: i32): Box {
  const b: Box = { v: v }
  b.hidden()
  return b
}
