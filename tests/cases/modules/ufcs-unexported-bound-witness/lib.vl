// `tag` is un-exported and used via UFCS inside its own module — the bank.
export type Box = { v: i32 }

function tag(self: Box): string { return if self.v > 0 { "H" } else { "L" } }

export function mk(v: i32): Box {
  const b: Box = { v: v }
  b.tag()
  return b
}
