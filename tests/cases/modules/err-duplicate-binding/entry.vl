// @error Duplicate binding "helper"
// A top-level declaration colliding with an import local must be diagnosed —
// the merge rename would otherwise rebind every reference to the IMPORT's
// target and silently orphan the local declaration.
import { helper } from "./util"

function helper() {
  return 1
}

print(helper())
