// A dependency of D2268's witness: a plain exported function, and an export spelled as an
// intrinsic, whose declaration is refused here as well as the entry's import of it.
export function bump(x: i32) { return x + 1 }

export function __memory_size__() { return 77 }
