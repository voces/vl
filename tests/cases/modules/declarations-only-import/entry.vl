// @run
// @log 7
// THE CONTROL FOR D1080, and the reason it is a gap rather than a design rule: the same
// declarations-only file, IMPORTED, compiles and runs — and did so while naming it on the
// command line was refused.
import { Point } from "./decls"

const p: Point = { x: 3, y: 4 }
print(p.x + p.y)
