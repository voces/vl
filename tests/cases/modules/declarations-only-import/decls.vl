// A declarations-only module. As an IMPORT it always worked; as the ENTRY it was refused
// until D1080 — see ../declarations-only-entry/entry.vl, which is this file named on the
// command line.
export type Point = { x: i32, y: i32 }
export type Id = i32
