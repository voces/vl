// The SECOND of two modules exporting the same two names. Its rows land LAST in
// the merge's rename map, so a TYPE-position `Tag` resolves here while the value
// `tag` resolves to `first.vl` — the asymmetry `entry.vl` pins.
export type Tag = string

export function tag(): i32 {
  2
}
