// The FIRST of two modules exporting the same two names. `entry.vl` imports
// both under the same local names, so this module's rows land FIRST in the
// merge's rename map and its `tag` is the one a value reference resolves to.
export type Tag = i32

export function tag(): i32 {
  1
}
