// @check
// @error-at 8:10 'skipTags' is not a method — its first parameter is named `c`, not `self`, and a UFCS call `x.skipTags(…)` dispatches only to a `self` first parameter; rename it to `self`, or call `skipTags(…)` directly
// D1654's other face: the declaration the gate has to find is in the DEPENDENCY, not in
// this file, so the answer comes from the merged program rather than from either module
// alone. Its sibling `ufcs-not-method-self-name` keeps the declaration here.
import { Cursor, skipTags } from "./dep"

const cur: Cursor = { pos: 3 }
print(cur.skipTags())
