// @error Cannot resolve import
// A well-formed `std:` specifier naming a module that does not exist: the
// resolver returns the key verbatim, the reader finds nothing, and the
// EXISTING unresolvable-import diagnostic fires (no new diagnostic for std).
import { x } from "std:nope"

print(1)
