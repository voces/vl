const x = 7

export type Move = { x: i32, y: i32 }
export type Attack = { target: string }
export type Cmd = Move | Attack

export function tell(c: Cmd): i32 {
  match c {
    Move{x} => x
    Attack{target} => target.length
  }
}

export function global(): i32 { x }
