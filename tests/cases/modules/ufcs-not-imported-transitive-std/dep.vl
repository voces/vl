// A dependency that pulls `std:fmt` into the graph. The ENTRY never names
// `std:fmt`, which is the whole shape this fixture is for (D1260).
import { toString } from "std:fmt"

export function tag(n: i32): string { return "#" + toString(n) }
