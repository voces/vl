// @run
// @log 3
// @log 2
// ADVERSARIAL: a `break` cuts the loop short, so the push count is less than the
// range size. Body is not a bare push → fusion must not fire.
const a: i32[] = []
for i in 0 to 5 {
  a.push(i)
  if i == 2 {
    break
  }
}
print(a.length)   // 3
print(a[2])       // 2
