// @run
// @log 4
// @log 0
// @log 3
// ADVERSARIAL: the pushed expression reads `a` → must not fuse.
const a: i32[] = []
let i = 0
while i < 4 {
  a.push(a.length)
  i = i + 1
}
print(a.length)   // 4
print(a[0])       // 0
print(a[3])       // 3
