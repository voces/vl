// @run
// @log 4
// @log 7
// @log 2
// A non-empty seed must be preserved as the prefix: `[7]` then three pushed values.
const a = [7]
for i in 0 to 2 {
  a.push(i)
}
print(a.length)    // 4
print(a[0])        // 7 — seed prefix kept
print(a[3])        // 2 — last pushed
