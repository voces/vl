// @error `readonly` describes a list
// CONTRACT (must-error): the view is a LIST view. There is no read-only scalar, map or
// struct in this ruling, so a marker with no `[]` under it is refused where it is written
// rather than resolving to the bare type and losing the author's intent silently.
//
// A VERDICT pin: the program must not parse, so there is no run to compare against.
const x: readonly i32 = 5
print(x)
