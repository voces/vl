// @emit-error a nullable-string list element has no rep
// The `(string | null)[]` sibling of the nullable-boolean niche-element list reject —
// same no-rep hole, same loud EMIT-stage rejection (rep-fuzz wave 4).
function go() {
  const xs: (string | null)[] = ["x", null]
  print(xs.length)
}
go()
