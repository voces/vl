// @run
// @log 3
// @log 2
// ADVERSARIAL: a break cuts the loop short → body is not [push, increment] → fall
// back.
const a: i32[] = []
let i = 0
while i < 10 {
  a.push(i)
  if i == 2 {
    break
  }
  i = i + 1
}
print(a.length)   // 3
print(a[2])       // 2
