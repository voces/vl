// @run
// @log 3
// @log 4
// @log 2.5
// Non-i32 (f64) elements take the same fused path with the correct backing type.
const a: f64[] = []
for i in 0 to 2 {
  a.push(2.5)
}
print(a.length)        // 3
print(a.capacity)      // 4
print(a.get(0) ?? 0.0) // 2.5
