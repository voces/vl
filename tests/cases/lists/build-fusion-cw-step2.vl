// @run
// @log 5
// @log 0
// @log 8
// @log 10
// Step 2: count and the per-iteration value follow the actual stride.
const a: i32[] = []
let i = 0
while i < 10 {
  a.push(i)
  i = i + 2
}
print(a.length)   // 5 — 0,2,4,6,8
print(a[0])       // 0
print(a[4])       // 8
print(i)          // 10
