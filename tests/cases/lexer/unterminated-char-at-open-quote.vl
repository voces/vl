// @error-at 3:10 Unterminated char literal
// A missing closing quote terminates the literal at the raw newline and is
// reported at the OPENING quote — the char still decodes for recovery.
const c = 'a
print(c)
