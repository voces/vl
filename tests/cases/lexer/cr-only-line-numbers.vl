// @error-at 6:8 Unexpected character '@'
// @error-at 6:8 expected an expression
// CR-only (classic Mac) line endings advance the line counter like `\n`,
// so the diagnostic below lands on the RIGHT line.
let a = 1let b = 2let c = @