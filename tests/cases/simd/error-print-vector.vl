// @check
// @error print expects one scalar or string value (i32 | i64 | f32 | f64 | boolean | string), got F32x4
// `print` has no rendering for a 128-bit vector; store it to a Buf and print its lanes.
import { splatF32 } from "std:simd"

print(splatF32(1.0))
