// @check
// @error {pos: F32x4, m: f64} holds a 128-bit vector inside an array, struct field, map, nullable or union
// A struct field of vector type has no lowering yet (see error-vector-in-array.vl).
import { splatF32 } from "std:simd"

const b = { pos: splatF32(6.0), m: 1.0 }
print(b.m)
