// @check
// @error '__add_i32x4__' is a compiler intrinsic and cannot be redefined
// The import face of error-redefine-simd-intrinsic.vl (D2263). An import renames every
// top-level declaration before the checker runs, so the reservation is screened by spelling
// first; without that screen this definition, arity and types matching, silently won the call.
import { hypotF64 } from "std:math"

function __add_i32x4__(a: v128, _b: v128) { return a }
const v = __splat_i32x4__(3)
print(__extract_lane_i32x4__(__add_i32x4__(v, v), 0))
print(hypotF64(3.0, 4.0))
