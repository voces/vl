// @check
// @error expected i32
// The return is correlated to the argument: naming `T` in both the param and the
// return makes them the same hole, so calling `id` with a string yields a string
// return — assigning that to an `i32` must be rejected.
function id<T>(x: T): T x
let n: i32 = id("s")
