// @run
// @log 7
// The BOUNDARY of the reject above: a recursive generic alias that is never applied
// mints no instance, so the checker's declared-type registry holds no cyclic entry and
// the guard has nothing to report. Declaring one still compiles.
type L<T> = { head: T, tail: L<T> | null }

const n = 7
print(n)
