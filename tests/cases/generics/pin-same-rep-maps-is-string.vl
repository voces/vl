// @run
// @log 1
// An `is string` over a pinned pair of same-rep maps need not tell the maps apart, and runs.
type IM = {[i32]: i32}
type BM = {[i32]: boolean}
const mb: BM = Map()
mb.set(1, true)
const v: IM | BM = mb
function k<T>(u: T) {
  if u is string { return 0 }
  1
}
print(k(v))
