// @run
// @log 3
// @log z
// `last<T>(xs: T[]): T` returns `xs[xs.length - 1]` — element inference plus
// `.length` arithmetic inside a generic body, monomorphized at i32 and string.
function last<T>(xs: T[]): T xs[xs.length - 1]
print(last([1, 2, 3]))
print(last(["x", "y", "z"]))
