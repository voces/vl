// @run
// @log q
// The RETURN-position twin of `type-arg-list-field.vl`: a generic APPLICATION as
// the element of an inline-object RESULT annotation's list field. Same mechanism,
// a different producer of the annotation text — the application is interior to the
// shape spelling, so `collectGenAliasShapes`' bare-TypeRef peel never reaches it.
// Failed on master with "ref-list field element type is not interned"; interned by
// `internShapeDeep`'s `gaeEnsure` leaf arm (D-GAELEAF).
type Pair<A, B> = { a: A, b: B }

function f(): { v: Pair<i32, string>[] } {
  return { v: [{ a: 2, b: "q" }] }
}

print(f().v[0].b)
