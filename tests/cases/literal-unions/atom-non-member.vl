// @check
// @error cannot assign string to 'k' of type "a" | "b"
// A NAMED string-literal union enforces membership at assignment: a non-member string is a type error.
type K = "a" | "b"
function go() {
  const k: K = "z"
  if k == "a" { print(1) }
}
go()
