// @check
// @error-at 12:8 no field 'toString' on {foo: i32}
// THE MIGRATION NOTE IS SCOPED, and this is the boundary it must not cross.
// A `.toString` on a STRUCT that has no such field is an ordinary missing-field
// mistake: `std:fmt`'s `toString` takes `i32 | i64 | boolean | f64`, so the
// import the note recommends would not make this program work, and appending it
// here would send the reader to a fix that fails.
//
// So the note rides only the NON-OBJECT arm of the member sentence
// (`typecheck.memberGapMsg`), which is exactly the set of receivers std's
// `toString` serves. Its positive twin is `moved-to-std-hint.vl`.
const o = { foo: 1 }
print(o.toString())
