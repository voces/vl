// @run
// @log 42
// @log false
// @log -7
// `toString` resolves inside both arms of an `if`/`else` (nested scopes), for
// i32 and boolean.
if 5 > 3 {
  print(toString(42))
  print(toString(2 == 3))
} else {
  print(toString(0))
}
const n = -7
if n < 0 {
  print(toString(n))
}
