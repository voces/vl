// @run
// @log n=5
// @log -3 items
// `toString` composes with string `+` concatenation — the diagnostics idiom the
// self-hosted compiler needs ("label " + toString(n)).
print("n=" + toString(5))
print(toString(-3) + " items")
