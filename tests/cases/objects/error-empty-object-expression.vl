// @check
// @error-at 15:26 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 18:10 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 22:14 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 23:18 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 24:11 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 26:25 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 27:20 an empty object has no fields to hold; write its fields, or use `null` for no value
// @error-at 28:27 an empty object has no fields to hold; write its fields, or use `null` for no value
// A bare `{}` in expression position is refused (D2223, an owner ruling): VL has
// no empty object, so a `{}` value has no fields to hold. One diagnostic per literal, and
// no follow-on error: the literal types as the error type, so the annotated binding, the
// argument, the return, the field and the element below each report only the `{}` itself.
type P = { a: i32 }
function take(p: P) { p.a }
function mk(): P { return {} }
function pick(n: i32): P | null {
  match n {
    0 => ({})
    _ => null
  }
}
const _bare = {}
const _typed: P = {}
print(take({}))
print(mk().a)
const _nested = { inner: {} }
const _list: P[] = [{}]
const _either = pick(1) ?? {}
print(_typed.a)
