// @run
// @log 3
// @log 7
// @log 14
// Objects compile to WasmGC structs: literal -> struct.new, `p.x` -> struct.get,
// `p.x = …` -> struct.set. Fields are interned by shape (sorted field order).
// Also exercises the parser precedence fix (`p.x + p.y` is not `(p.x + p).y`).
const p = { y: 4, x: 3 }
print(p.x)
print(p.x + p.y)
p.x = 10
print(p.x + p.y)
