// @run
// @log 1
// @hint Intentionally-unused variable `_v`
// A LOCAL binding whose inline-shape type has a union field carrying a CLOSURE
// member (`{f: (() => i32) | string}`). Now that a function type is a value-union
// REF atom (kind 11), the shape interns as a box and lowers cleanly — previously a
// loud EMIT-stage reject (the last edge of the union-closure-member fix; the global
// spelling already worked). Regression guard that the local spelling stays lowerable.
function go() {
  const _v: {f: (() => i32) | string} = {f: "x"}
  print(1)
}
go()
