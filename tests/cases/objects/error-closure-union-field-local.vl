// @emit-error binding's inline-shape type has an unsupported field
// A LOCAL binding whose inline-shape type has a union field carrying a CLOSURE member
// (`{f: (() => i32) | string}`): the value-union interner has no closure atom kind, so
// the shape cannot intern as a box — a loud EMIT-stage reject (the documented remaining
// edge of the union-closure-member GLOBAL fix; the global spelling works).
function go() {
  const v: {f: (() => i32) | string} = {f: "x"}
  print(1)
}
go()
