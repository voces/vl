// @check
// @error operator `+` never dispatches: `self` has NO ANNOTATION, so its type is inferred and whether it fires is decided at each site — and every `+` site in this program took the language's own lowering. Annotate `self` with the object type the operator is for, or remove the declaration
// GRADUATED from `silent-class-inventory` D521 — D491's landing with the ANNOTATION
// DELETED, which is the population #2012 deliberately narrowed away from.
//
// An un-annotated parameter hoists as `unannotParamTy(...)` -> `mkTyVar(...)`, so
// `tyHasHole` is TRUE for it exactly as it is for `self: T`: this declaration and the
// generic one next door are the same shape to every type test in the family, and D491's
// first cut claimed both. It was narrowed to `parType >= 0` because the wider gate costs
// 20 running cells — `named/d425c001`…`d425c039` odd, whose recorded expectation is the
// BUILT-IN's answer, so the standing corpus scored them as correct programs.
//
// The price was re-measured on `042624e1` before this landed and it is REAL: exactly
// those 20, nothing else moved, nothing went silent. It is OVERRIDDEN rather than
// disputed, and the override is executable — `scripts/silent-sweep/d521/unannotgrid.py
// --price <seed>` re-runs every lost cell beside its own do-nothing control and asserts
// that it ran, that the declaration contributed nothing, and that the value it printed
// CONTRADICTS its own source. Each of the 20 prints the built-in's answer where its body
// says 99 (or the opposite boolean), so they were producing wrong values and turning them
// loud is strictly better. Four of them are BLIND — `>` / `>=` at `1, 2` are natively
// `false` and their bodies also return `false` — and the check decides those on a
// distinguishing twin it RUNS rather than folding them into the pass count.
//
// THE SPELLING IS NOT REFUSED, THE DEAD DECLARATION IS. `operator-self-method.vl` next
// door is written in exactly this spelling and keeps dispatching: measured on an
// instrumented compiler, it banks identically (`bankUn=1`) and is spared entirely by the
// SITE bank (`siteTake=3`). The rule is D491's unchanged — no site dispatched to the name
// while at least one took the built-in — and only its `parType >= 0` narrowing is gone.
function "+"(self, other) {
  return 99
}

const a: i32 = 7
const b: i32 = 1
print(a + b)
