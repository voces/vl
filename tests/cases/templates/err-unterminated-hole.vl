// @error Unterminated string literal
// @error-at 5:10 Unterminated string literal
// The input ran out INSIDE a hole. No part ever scanned to end of input here —
// the hole's tokens did — so the report comes from the still-open literal stack
// at EOF, and it still anchors at the opening quote.
const s = "abc\{1
