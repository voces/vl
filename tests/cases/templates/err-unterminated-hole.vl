// @error Unterminated template literal
// @error-at 6
// The input ran out INSIDE a hole. No part ever scanned to end of input here —
// the hole's tokens did — so the report comes from the still-open template
// stack at EOF, and it still anchors at the opening backtick.
const s = `abc\{1
