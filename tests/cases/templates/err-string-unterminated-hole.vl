// @error-at 7:10 Unterminated string literal
// The input ran out INSIDE a hole in a PLAIN STRING. No part ever scanned to end
// of input here — the hole's tokens did — so the report comes from the still-open
// run at EOF, and it anchors at the opening QUOTE.
//
// It says "string", not "template": the run records its own delimiter, so the
// message names the literal the author actually opened.
const s = "abc\{1
