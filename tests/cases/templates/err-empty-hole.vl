// @error an interpolation hole `\{}` needs an expression
// @error-at 5:9 an interpolation hole `\{}` needs an expression
// `\{}` — the delimiters with nothing between them. Reported as itself rather
// than left to "expected an expression but found TEMPLATE_TAIL", which names a
// token the author never typed.
print(`\{}`)
