// @run
// @log 985
// @log OTHER
// @log 18983
// A closure value returning a closure value returning a struct that mixes a closure-UNION
// field (`a: (() => i64) | f64`), a scalar field (`f: i32`), and a closure-ARRAY field
// (`z: (() => i32)[]`): `() => (() => {a: (() => i64) | f64, f: i32, z: (() => i32)[]})`.
// The double value call `v()()` reaches the struct; its fields are read/narrowed/called.
// Regression pin for a fuzz draw (sweep YKq9Jw multiobs p0r, seed 181173557 case_00201)
// that was ALREADY fixed on current source by the closure-result / mixed-field-struct arc
// — the sweep had run on a stale (~#1007-era) compiler seed. Entombed to hold the fix.
function go() {
  const v: () => (() => {a: (() => i64) | f64, f: i32, z: (() => i32)[]}) = () => (() => ({ a: 9.25, f: 985, z: [() => (18983), () => (12121), () => (12121)] }))
  print(v()().f)
  const t200 = v()().a
  if (t200 is () => i64) { print(t200()) } else { print("OTHER") }
  print(v()().z[0]())
}

go()
