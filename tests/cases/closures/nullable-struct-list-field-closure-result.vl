// The NAMED-function-value twin of error-nullable-struct-list-field-lambda-literal:
// a closure value whose result is a struct with a NULLABLE STRUCT-ELEMENT LIST field
// (`{f: {f: string}[] | null}`, code-28 slot) lowers fully through the annotated
// named-fn spelling — the sound path the lambda pin routes toward.
// @run
// @log 28
// @hint Intentionally-unused parameter `_q0`
// @hint Intentionally-unused parameter `_q1`
// @hint Intentionally-unused parameter `_q2`
function mk(_q0: i32, _q1: i32, _q2: i32): {f: {f: string}[] | null} {
  return { f: [{ f: "28" }] }
}
function go() {
  const v: (i32, i32, i32) => {f: {f: string}[] | null} = mk
  const t = v(1, 1, 1)
  const l = t.f
  if l != null { print(l[0].f) } else { print("NULL") }
}
go()
