// @run
// @log 32.5

// D2316 matrix — sibling function-scope blocks, closure return types i32 then f64.

function f() {
  let out = ""
  {
    const g = () => 3
    out = "\{g()}"
  }
  {
    const g = () => 2.5
    out = out + "\{g()}"
  }
  out
}
print(f())
