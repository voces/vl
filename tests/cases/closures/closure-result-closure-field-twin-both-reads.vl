// Regression companion: the same closure-result twin family (`{a: () => {f: i64}, f:
// {f: string[]}}`) but READING BOTH sides — the nested `{f: string[]}` field AND the
// closure sibling's `{f: i64}` result. Proves the disambiguated rows are BOTH correct:
// the string[] field lowers as its own struct and the i64 closure result keeps the
// `{f: i64}` twin. See closure-result-closure-field-nested-struct-twin.vl for the root
// cause (fuzz branching leg 2KEodO, seed 359696188).
// @run
// @log zdmy
// @log 3965894781
// @hint Intentionally-unused parameter `_q0`
// @hint Intentionally-unused parameter `_q1`
// @hint Intentionally-unused parameter `_q2`
function go() {
  const v: (i32, i32, i32) => {a: () => {f: i64}, f: {f: string[]}} = (_q0, _q1, _q2) => ({ a: () => ({ f: 3965894781 }), f: { f: ["pf", "zdmy"] } })
  const r = v(1, 1, 1)
  print(r.f.f[1])
  print(r.a().f)
}
go()
