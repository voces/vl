// D2387/D2388 member grid: a value-union local a closure assigns, read back under `is`
// for each member kind, both with and without `null`, annotated and inferred from a call.
// Each line is the narrowed read interpolated, bound at the member type, passed at it, and
// the other member's test. The binding at the member type is a delivery position, so its
// annotation is kept.
// @run
// @hint redundant type annotation: `w` is inferred as
// @log 7|7|7|not string
// @log 7|7|7|not string
// @log 7|7|7|not string
// @log 7|7|7|not string
// @log 2.5|2.5|2.5|not boolean
// @log 2.5|2.5|2.5|not boolean
// @log 2.5|2.5|2.5|not boolean
// @log 2.5|2.5|2.5|not boolean
// @log 5000000000|5000000000|5000000000|not string
// @log 5000000000|5000000000|5000000000|not string
// @log 5000000000|5000000000|5000000000|not string
// @log 5000000000|5000000000|5000000000|not string
// @log false|false|false|not string
// @log false|false|false|not string
// @log false|false|false|not string
// @log false|false|false|not string
// @log s|s|s|not f64
// @log s|s|s|not f64
// @log s|s|s|not f64
// @log s|s|s|not f64
// @log 2|2|2|not string
// @log 2|2|2|not string
// @log 2|2|2|not string
// @log 2|2|2|not string
// @log 2|2|2|not boolean
// @log 2|2|2|not boolean
// @log 2|2|2|not boolean
// @log 2|2|2|not boolean
// @log 1|1|1|not f64
// @log 1|1|1|not f64
// @log 1|1|1|not f64
// @log 1|1|1|not f64
// @log 2|2|2|not i32
// @log 2|2|2|not i32
// @log 2|2|2|not i32
// @log 2|2|2|not i32

function showI32(x: i32) { "\{x}" }
function showF64(x: f64) { "\{x}" }
function showI64(x: i64) { "\{x}" }
function showBoolean(x: boolean) { "\{x}" }
function showString(x: string) { "\{x}" }
function showI32list(x: i32[]) { "\{x.length}" }
function showF64list(x: f64[]) { "\{x.length}" }
function showI64list(x: i64[]) { "\{x.length}" }
function showStringlist(x: string[]) { "\{x.length}" }

function gI32A() {
  let v: i32 | string = "a"
  const k = () => {
    v = 7
  }
  k()
  const a = if v is i32 { "\{v}" } else { "no" }
  const b = if v is i32 { const w: i32 = v; "\{w}" } else { "no" }
  const c = if v is i32 { showI32(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI32U(): i32 | string { "a" }
function gI32U() {
  let v = mkgI32U()
  const k = () => {
    v = 7
  }
  k()
  const a = if v is i32 { "\{v}" } else { "no" }
  const b = if v is i32 { const w: i32 = v; "\{w}" } else { "no" }
  const c = if v is i32 { showI32(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI32NA() {
  let v: i32 | string | null = "a"
  const k = () => {
    v = 7
  }
  k()
  const a = if v is i32 { "\{v}" } else { "no" }
  const b = if v is i32 { const w: i32 = v; "\{w}" } else { "no" }
  const c = if v is i32 { showI32(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI32NU(): i32 | string | null { "a" }
function gI32NU() {
  let v = mkgI32NU()
  const k = () => {
    v = 7
  }
  k()
  const a = if v is i32 { "\{v}" } else { "no" }
  const b = if v is i32 { const w: i32 = v; "\{w}" } else { "no" }
  const c = if v is i32 { showI32(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gF64A() {
  let v: f64 | boolean = true
  const k = () => {
    v = 2.5
  }
  k()
  const a = if v is f64 { "\{v}" } else { "no" }
  const b = if v is f64 { const w: f64 = v; "\{w}" } else { "no" }
  const c = if v is f64 { showF64(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgF64U(): f64 | boolean { true }
function gF64U() {
  let v = mkgF64U()
  const k = () => {
    v = 2.5
  }
  k()
  const a = if v is f64 { "\{v}" } else { "no" }
  const b = if v is f64 { const w: f64 = v; "\{w}" } else { "no" }
  const c = if v is f64 { showF64(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gF64NA() {
  let v: f64 | boolean | null = true
  const k = () => {
    v = 2.5
  }
  k()
  const a = if v is f64 { "\{v}" } else { "no" }
  const b = if v is f64 { const w: f64 = v; "\{w}" } else { "no" }
  const c = if v is f64 { showF64(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgF64NU(): f64 | boolean | null { true }
function gF64NU() {
  let v = mkgF64NU()
  const k = () => {
    v = 2.5
  }
  k()
  const a = if v is f64 { "\{v}" } else { "no" }
  const b = if v is f64 { const w: f64 = v; "\{w}" } else { "no" }
  const c = if v is f64 { showF64(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI64A() {
  let v: i64 | string = "a"
  const k = () => {
    v = 5000000000
  }
  k()
  const a = if v is i64 { "\{v}" } else { "no" }
  const b = if v is i64 { const w: i64 = v; "\{w}" } else { "no" }
  const c = if v is i64 { showI64(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI64U(): i64 | string { "a" }
function gI64U() {
  let v = mkgI64U()
  const k = () => {
    v = 5000000000
  }
  k()
  const a = if v is i64 { "\{v}" } else { "no" }
  const b = if v is i64 { const w: i64 = v; "\{w}" } else { "no" }
  const c = if v is i64 { showI64(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI64NA() {
  let v: i64 | string | null = "a"
  const k = () => {
    v = 5000000000
  }
  k()
  const a = if v is i64 { "\{v}" } else { "no" }
  const b = if v is i64 { const w: i64 = v; "\{w}" } else { "no" }
  const c = if v is i64 { showI64(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI64NU(): i64 | string | null { "a" }
function gI64NU() {
  let v = mkgI64NU()
  const k = () => {
    v = 5000000000
  }
  k()
  const a = if v is i64 { "\{v}" } else { "no" }
  const b = if v is i64 { const w: i64 = v; "\{w}" } else { "no" }
  const c = if v is i64 { showI64(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gBooleanA() {
  let v: boolean | string = "a"
  const k = () => {
    v = false
  }
  k()
  const a = if v is boolean { "\{v}" } else { "no" }
  const b = if v is boolean { const w: boolean = v; "\{w}" } else { "no" }
  const c = if v is boolean { showBoolean(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgBooleanU(): boolean | string { "a" }
function gBooleanU() {
  let v = mkgBooleanU()
  const k = () => {
    v = false
  }
  k()
  const a = if v is boolean { "\{v}" } else { "no" }
  const b = if v is boolean { const w: boolean = v; "\{w}" } else { "no" }
  const c = if v is boolean { showBoolean(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gBooleanNA() {
  let v: boolean | string | null = "a"
  const k = () => {
    v = false
  }
  k()
  const a = if v is boolean { "\{v}" } else { "no" }
  const b = if v is boolean { const w: boolean = v; "\{w}" } else { "no" }
  const c = if v is boolean { showBoolean(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgBooleanNU(): boolean | string | null { "a" }
function gBooleanNU() {
  let v = mkgBooleanNU()
  const k = () => {
    v = false
  }
  k()
  const a = if v is boolean { "\{v}" } else { "no" }
  const b = if v is boolean { const w: boolean = v; "\{w}" } else { "no" }
  const c = if v is boolean { showBoolean(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gStringA() {
  let v: string | f64 = 1.5
  const k = () => {
    v = "s"
  }
  k()
  const a = if v is string { "\{v}" } else { "no" }
  const b = if v is string { const w: string = v; "\{w}" } else { "no" }
  const c = if v is string { showString(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgStringU(): string | f64 { 1.5 }
function gStringU() {
  let v = mkgStringU()
  const k = () => {
    v = "s"
  }
  k()
  const a = if v is string { "\{v}" } else { "no" }
  const b = if v is string { const w: string = v; "\{w}" } else { "no" }
  const c = if v is string { showString(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gStringNA() {
  let v: string | f64 | null = 1.5
  const k = () => {
    v = "s"
  }
  k()
  const a = if v is string { "\{v}" } else { "no" }
  const b = if v is string { const w: string = v; "\{w}" } else { "no" }
  const c = if v is string { showString(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgStringNU(): string | f64 | null { 1.5 }
function gStringNU() {
  let v = mkgStringNU()
  const k = () => {
    v = "s"
  }
  k()
  const a = if v is string { "\{v}" } else { "no" }
  const b = if v is string { const w: string = v; "\{w}" } else { "no" }
  const c = if v is string { showString(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI32listA() {
  let v: i32[] | string = "a"
  const k = () => {
    v = [1, 2]
  }
  k()
  const a = if v is i32[] { "\{v.length}" } else { "no" }
  const b = if v is i32[] { const w: i32[] = v; "\{w.length}" } else { "no" }
  const c = if v is i32[] { showI32list(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI32listU(): i32[] | string { "a" }
function gI32listU() {
  let v = mkgI32listU()
  const k = () => {
    v = [1, 2]
  }
  k()
  const a = if v is i32[] { "\{v.length}" } else { "no" }
  const b = if v is i32[] { const w: i32[] = v; "\{w.length}" } else { "no" }
  const c = if v is i32[] { showI32list(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI32listNA() {
  let v: i32[] | string | null = "a"
  const k = () => {
    v = [1, 2]
  }
  k()
  const a = if v is i32[] { "\{v.length}" } else { "no" }
  const b = if v is i32[] { const w: i32[] = v; "\{w.length}" } else { "no" }
  const c = if v is i32[] { showI32list(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI32listNU(): i32[] | string | null { "a" }
function gI32listNU() {
  let v = mkgI32listNU()
  const k = () => {
    v = [1, 2]
  }
  k()
  const a = if v is i32[] { "\{v.length}" } else { "no" }
  const b = if v is i32[] { const w: i32[] = v; "\{w.length}" } else { "no" }
  const c = if v is i32[] { showI32list(v) } else { "no" }
  const d = if v is string { "string" } else { "not string" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gF64listA() {
  let v: f64[] | boolean = true
  const k = () => {
    v = [1.5, 2.5]
  }
  k()
  const a = if v is f64[] { "\{v.length}" } else { "no" }
  const b = if v is f64[] { const w: f64[] = v; "\{w.length}" } else { "no" }
  const c = if v is f64[] { showF64list(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgF64listU(): f64[] | boolean { true }
function gF64listU() {
  let v = mkgF64listU()
  const k = () => {
    v = [1.5, 2.5]
  }
  k()
  const a = if v is f64[] { "\{v.length}" } else { "no" }
  const b = if v is f64[] { const w: f64[] = v; "\{w.length}" } else { "no" }
  const c = if v is f64[] { showF64list(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gF64listNA() {
  let v: f64[] | boolean | null = true
  const k = () => {
    v = [1.5, 2.5]
  }
  k()
  const a = if v is f64[] { "\{v.length}" } else { "no" }
  const b = if v is f64[] { const w: f64[] = v; "\{w.length}" } else { "no" }
  const c = if v is f64[] { showF64list(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgF64listNU(): f64[] | boolean | null { true }
function gF64listNU() {
  let v = mkgF64listNU()
  const k = () => {
    v = [1.5, 2.5]
  }
  k()
  const a = if v is f64[] { "\{v.length}" } else { "no" }
  const b = if v is f64[] { const w: f64[] = v; "\{w.length}" } else { "no" }
  const c = if v is f64[] { showF64list(v) } else { "no" }
  const d = if v is boolean { "boolean" } else { "not boolean" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI64listA() {
  let v: i64[] | f64 = 1.5
  const k = () => {
    v = [5000000000]
  }
  k()
  const a = if v is i64[] { "\{v.length}" } else { "no" }
  const b = if v is i64[] { const w: i64[] = v; "\{w.length}" } else { "no" }
  const c = if v is i64[] { showI64list(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI64listU(): i64[] | f64 { 1.5 }
function gI64listU() {
  let v = mkgI64listU()
  const k = () => {
    v = [5000000000]
  }
  k()
  const a = if v is i64[] { "\{v.length}" } else { "no" }
  const b = if v is i64[] { const w: i64[] = v; "\{w.length}" } else { "no" }
  const c = if v is i64[] { showI64list(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gI64listNA() {
  let v: i64[] | f64 | null = 1.5
  const k = () => {
    v = [5000000000]
  }
  k()
  const a = if v is i64[] { "\{v.length}" } else { "no" }
  const b = if v is i64[] { const w: i64[] = v; "\{w.length}" } else { "no" }
  const c = if v is i64[] { showI64list(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgI64listNU(): i64[] | f64 | null { 1.5 }
function gI64listNU() {
  let v = mkgI64listNU()
  const k = () => {
    v = [5000000000]
  }
  k()
  const a = if v is i64[] { "\{v.length}" } else { "no" }
  const b = if v is i64[] { const w: i64[] = v; "\{w.length}" } else { "no" }
  const c = if v is i64[] { showI64list(v) } else { "no" }
  const d = if v is f64 { "f64" } else { "not f64" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gStringlistA() {
  let v: string[] | i32 = 3
  const k = () => {
    v = ["p", "q"]
  }
  k()
  const a = if v is string[] { "\{v.length}" } else { "no" }
  const b = if v is string[] { const w: string[] = v; "\{w.length}" } else { "no" }
  const c = if v is string[] { showStringlist(v) } else { "no" }
  const d = if v is i32 { "i32" } else { "not i32" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgStringlistU(): string[] | i32 { 3 }
function gStringlistU() {
  let v = mkgStringlistU()
  const k = () => {
    v = ["p", "q"]
  }
  k()
  const a = if v is string[] { "\{v.length}" } else { "no" }
  const b = if v is string[] { const w: string[] = v; "\{w.length}" } else { "no" }
  const c = if v is string[] { showStringlist(v) } else { "no" }
  const d = if v is i32 { "i32" } else { "not i32" }
  "\{a}|\{b}|\{c}|\{d}"
}
function gStringlistNA() {
  let v: string[] | i32 | null = 3
  const k = () => {
    v = ["p", "q"]
  }
  k()
  const a = if v is string[] { "\{v.length}" } else { "no" }
  const b = if v is string[] { const w: string[] = v; "\{w.length}" } else { "no" }
  const c = if v is string[] { showStringlist(v) } else { "no" }
  const d = if v is i32 { "i32" } else { "not i32" }
  "\{a}|\{b}|\{c}|\{d}"
}
function mkgStringlistNU(): string[] | i32 | null { 3 }
function gStringlistNU() {
  let v = mkgStringlistNU()
  const k = () => {
    v = ["p", "q"]
  }
  k()
  const a = if v is string[] { "\{v.length}" } else { "no" }
  const b = if v is string[] { const w: string[] = v; "\{w.length}" } else { "no" }
  const c = if v is string[] { showStringlist(v) } else { "no" }
  const d = if v is i32 { "i32" } else { "not i32" }
  "\{a}|\{b}|\{c}|\{d}"
}

print(gI32A())
print(gI32U())
print(gI32NA())
print(gI32NU())
print(gF64A())
print(gF64U())
print(gF64NA())
print(gF64NU())
print(gI64A())
print(gI64U())
print(gI64NA())
print(gI64NU())
print(gBooleanA())
print(gBooleanU())
print(gBooleanNA())
print(gBooleanNU())
print(gStringA())
print(gStringU())
print(gStringNA())
print(gStringNU())
print(gI32listA())
print(gI32listU())
print(gI32listNA())
print(gI32listNU())
print(gF64listA())
print(gF64listU())
print(gF64listNA())
print(gF64listNU())
print(gI64listA())
print(gI64listU())
print(gI64listNA())
print(gI64listNU())
print(gStringlistA())
print(gStringlistU())
print(gStringlistNA())
print(gStringlistNU())
