// @emit-error ref valtype with no interned shape
// FLOOR (repOf scope): a closure returning an array of structs whose element struct has a
// NESTED struct field carrying composite fields (`() => {f: {g: (i64|string|{q:i64})[],
// b: i64}}[]`) — the DEEP form of the tFpE3l draw (declared leg, seed 18472788 p0c, whose
// inner field-struct mixes a nullable-valued map + value-union-element array + map-array).
// `structElemFieldsPlain` admits the nested composite so the lambda IS pinned, but the body
// literal's INNER-struct resolution mis-keys the nested-composite shape (the same-fieldset /
// nested-literal twin-resolution family — the emit_collect interning-walk rewrite scope), so
// emission floors LOUDLY at the un-resolved struct valtype. The named-fn value spelling of
// the same shape lowers today; full support awaits the twin-matcher / interning rewrite. This
// pins the boundary: a loud reject, never invalid wasm (master emitted INVALID here).
const v: () => {f: {g: (i64 | string | {q: i64})[], b: i64}}[] = () => ([{ f: { g: ["other"], b: 1 } }, { f: { g: [13131313131], b: 2 } }])
function go() {
  const t = v()
  print(1)
}
go()
