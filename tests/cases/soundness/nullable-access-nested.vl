// @check
// @error member access '.x' on non-object {x: i32}?
// CONTRACT (must-error): the nullable hides one level deep — `o.v` is
// `{ x: i32 } | null`, so `o.v.x` is unsound without first guarding `o.v`.
let o: { v: { x: i32 } | null } = { v: { x: 9 } }
print(o.v.x)
