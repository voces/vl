// @check
// @error cannot infer a nullable return type for 'f'
// A NICHE nullable (`boolean | null` / `string | null`) cannot be lowered from inference
// yet: the A20 inferred-return classifier carries the value-union box seed, not the niche
// seed. So an un-annotated function whose returns join to a niche nullable must be
// annotated (`: string | null`), which DOES lower. The box-able `i32 | null` case infers
// fine un-annotated (missing-return-inferred-nullable.vl); only the niche reps floor here.
function f(n: i32) {
  if n > 0 { return "hi" }
}
const _r = f(1)
