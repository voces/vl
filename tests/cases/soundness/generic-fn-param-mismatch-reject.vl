// @check
// @error argument 1: expected (i32, i32) -> string, got (any, any) -> any
// SOUNDNESS. A generic function value passed to a CONCRETE function-typed parameter is validated by
// the same instantiation check that governs an annotated binding (`genericFnAssignable`): `add` at
// (i32, i32) returns i32, so it cannot satisfy a `(i32, i32) => string` callback parameter. Without
// the check the hole return would match structurally and the emitter would materialize an
// i32-returning instance behind a string-returning closure — invalid wasm. Rejected at the argument.
function add(a, b) a + b
function apply2(f: (i32, i32) => string) f(3, 4)

print(apply2(add)) // REJECTED: add at (i32, i32) returns i32, not string
