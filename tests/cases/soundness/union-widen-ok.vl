// @ok
// CONTRACT (must-not-error, type-level): assigning a narrower type into a wider
// union is sound (every `string` is a `string | i32`), so it type-checks. Pure
// type-level check — not run.
let w: string | i32 = "a"
w = 5
let n: i32 = 7
let w2: string | i32 = n
