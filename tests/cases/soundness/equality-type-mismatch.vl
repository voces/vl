// @check
// @error expected i32, got "x"
// CONTRACT (must-error): `==`/`!=` require comparable operand types. Comparing an
// `i32` against a string literal is a type error, not a runtime always-false — VL
// does not have JS-style cross-type equality.
let a: i32 = 1
print(a == "x")
