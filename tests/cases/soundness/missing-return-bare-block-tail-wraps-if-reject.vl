// @check
// @error not all code paths return a value in 'k'
// CONTRACT (must-error): the value positions INSIDE a bare block statement are statements
// too, all the way down. A fix that only refused a block whose own last statement is a bare
// value would pass this one — the block here ends in an `if`/`else` whose arms each yield a
// value, which is total in a tail position and discarded in this one. It trapped identically
// before D1902, and it is the cell that separates the rule from the special case.
function k(): i32 {
  {
    if true { 5 } else { 6 }
  }
}
print(k())
