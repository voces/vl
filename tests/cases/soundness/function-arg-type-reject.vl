// @check
// @error argument 1: expected i32, got string
// CONTRACT (must-error): passing a value of the wrong type to a function
// parameter is rejected — even when the parameter name appears elsewhere with a
// compatible type, the check is per-call. Here `f` expects an `i32` but receives
// a string literal at the call site, which is a type mismatch.
function f(n: i32): i32 { return n + 1 }
print(f("wrong"))   // UNSOUND: "wrong" is not an i32
