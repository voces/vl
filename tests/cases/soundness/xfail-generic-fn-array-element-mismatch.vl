// @check
// @error cannot assign (any, any) -> any[] to 'fs' of type (i32, i32) -> string[]
// SOUNDNESS. A generic function used as an array element is validated at the element type by the
// same instantiation check that governs every other boundary — but through `assignable`'s nested
// `TyArray` recursion (the array's element assignability), not the expression-level entry. `add` at
// (i32, i32) returns i32, so it cannot be an element of a `((i32,i32)=>string)[]`. Without the
// type-level check the hole element would match structurally and the emitter would materialize an
// i32-returning instance into a string-returning closure slot: invalid wasm.
function add(a, b) a + b

const fs: ((i32, i32) => string)[] = [add] // REJECTED: add at (i32, i32) returns i32, not string
print(1)
