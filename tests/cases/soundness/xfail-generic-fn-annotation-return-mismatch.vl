// @check
// @error cannot assign (any, any) -> any to 'c' of type (i32, i32) -> string
// SOUNDNESS. A generic function value is assignable to a concrete function type only when
// INSTANTIATING it at the target's param types yields the target's return. `add` at (i32, i32)
// returns i32, not string — so binding it to a `(i32, i32) => string` slot is rejected. The hole
// return `(any, any) -> any` is permissively `assignable` to any concrete return, so the structural
// check would accept it and the emitter would materialize an i32-returning instance behind a
// string-returning closure: invalid wasm. `genericFnAssignable` instantiates at the target params
// and checks the substituted return instead.
function add(a, b) a + b

const c: (i32, i32) => string = add // REJECTED: add at (i32, i32) returns i32, not string
print(1)
