// @check
// @error argument of type string flows into `g` expecting i32
// SOUNDNESS. The DERIVED-hole variant of hole-arg-concrete-param-reject: the
// generic body reads a field off its hole param (`o.x`, a derived `?fld.` hole)
// and passes THAT into a concrete i32 parameter. The call site pins `o` to
// `{x: string}`, the derived hole resolves to string, and the recorded demand
// rejects it.
function g(n: i32) { return n + 1 }
function f(o) { return g(o.x) }
print(f({ x: "s" })) // REJECTED: the pinned field is string, g expects i32
