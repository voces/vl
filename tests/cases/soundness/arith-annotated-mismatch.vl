// @check
// @error operator '+' is not defined for i32 and string
// CONTRACT (must-error): when BOTH operands have concrete annotated types,
// `i32 + string` is rejected. The un-annotated twin `arith-hole-operand-reject.vl`
// rejects the same program through the deferred-constraint path, so the two
// spellings agree — an annotation buys readability, never safety.
let a: i32 = 1
let b: string = "x"
let c = a + b
