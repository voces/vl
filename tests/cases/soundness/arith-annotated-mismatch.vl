// @check
// @error expected i32, got string
// CONTRACT (must-error): when BOTH operands have concrete annotated types,
// `i32 + string` is rejected. Contrast this with xfail-arith-hole-operand.vl,
// where the operands are unresolved inference holes and the same mismatch is
// NOT yet caught — the gap is specifically the hole-operand rule (ROADMAP A13),
// not arithmetic type-checking in general.
let a: i32 = 1
let b: string = "x"
let c = a + b
