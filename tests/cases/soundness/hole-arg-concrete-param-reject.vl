// @check
// @error argument 1: expected i32, got string
// SOUNDNESS. An un-annotated param is an inference HOLE; a hole flowing as an
// argument into a CONCRETE parameter can't be judged inside the generic body
// (a hole is permissively assignable both ways). Before this reject `f("hello")`
// compiled clean and produced invalid wasm at run.
//
// `x`'s ONLY usage requires `i32`, so the parameter is SOLVED to `i32` from that
// usage and the rejection is the ordinary call-site argument check — the same
// message, at the same position, the annotated `function f(x: i32)` produces. The
// deferred-constraint route (`noteArgCstr` → `validateArgCstrs`, "argument of type
// string flows into `g` expecting i32") remains the reject for a hole no usage
// solves — a hole flowing into ANOTHER hole, re-based one hop at a time
// (`hole-join-generic-arg-reject.vl` still lands there).
function g(n: i32) { return n + 1 }
function f(x) { return g(x) }
print(f("hello")) // REJECTED: string flows into g's i32 param
