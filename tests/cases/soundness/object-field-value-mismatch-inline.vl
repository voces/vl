// @check
// @error expected {value: i32}, got {value: "x"}
// CONTRACT (must-error): same field-value soundness check via an INLINE object
// annotation (no named alias), confirming the fix is general to object-literal-
// vs-annotation checks, not specific to alias application.
let b: { value: i32 } = { value: "x" }
print(b.value)
