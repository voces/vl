// @check
// @error-at 14:6 operator '+' is not defined for i32 | null and i32
// @error-at 17:30 operator '+' is not defined for i32 | null and i32 — 'b' is not narrowed inside the function on line 18: it may run before 'b' is narrowed, or after a write that may store null; re-test 'b' inside it
// @error-at 25:8 operator '+' is not defined for i32 | null and i32
// @error-at 33:8 operator '+' is not defined for i32 | null and i32
// @error-at 42:32 member access '.length' on non-object string | null — 's' is not narrowed inside the function on line 43: it may run before 's' is narrowed, or after a write that may store null; re-test 's' inside it
// The refusing half of D2529: a function body that may run after a later write reads the
// binding at its declared type, so arithmetic on it, or on what it returns, needs a re-test.
// Before the fix each of these was `vl check`-clean and emitted a module the engine rejects.
let a: i32 | null = null
a = 1
function readA() { return a }
a = null
const r = readA()
print(r + 1)
let b: i32 | null = null
b = 1
function addB(): i32 { return b + 1 }
print(addB())
function local() {
  let v: i32 | null = null
  v = 1
  const h = () => v
  v = null
  const s = h()
  print(s + 1)
}
local()
function localScalar() {
  let v: i32 | null = null
  v = 1
  const h = () => v
  v = 2
  print(h() + 1)
}
localScalar()
type V = { a: i32 }
let s: string | null = null
function "+"(self: V, o: V) { return lenS() + self.a + o.a }
const x = { a: 1 }
print(x + x)
s = "ab"
function lenS(): i32 { return s.length }
