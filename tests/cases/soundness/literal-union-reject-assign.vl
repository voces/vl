// @check
// @error cannot assign string to "a" | "b"
// CONTRACT (must-error): the closed-set rule also holds on assignment — a
// variable typed `"a" | "b"` cannot be re-bound to an out-of-union literal.
let s: "a" | "b" = "a"
s = "c"
