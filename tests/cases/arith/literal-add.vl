// @run
// @log 5
// Bare numeric literals should support arithmetic (defaulting to i32 when
// unconstrained), not just variables bound to a softened type. Printing the
// result verifies codegen + runtime, not only that it type-checks.
const x = 2 + 3
print(x)
