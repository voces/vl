// @check
// @error A f64-keyed Set isn't supported yet
// The SET half of the inferred-key gate (`maps/error-infer-unsupported-key.vl`): a
// bare `Set()` takes its key from the first `.add`, and the diagnostic names the
// constructor actually in play — `Set`, not `Map` — the same way the annotated
// `sets/error-unsupported-key.vl` does.
const s = Set()
s.add(1.5)
