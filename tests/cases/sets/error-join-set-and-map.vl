// @check
// @error a Set and a map do not join into one value yet
// The join twin of `error-union-with-same-rep-map.vl`: an `if` whose arms are a set and a map
// with one representation has no type to become (D2493).
const s: Set<string> = Set()
const m: {[string]: boolean} = Map()
const x = if s.length > 0 { s } else { m }
print(x.length)
