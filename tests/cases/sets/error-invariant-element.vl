// @check
// @error cannot assign Set<"a" | "b"> to 't' of type Set<string>
// A set is invariant in its element: a `Set<string>` alias of a `Set<"a" | "b">` could add
// a string the narrower set does not admit.
const s: Set<"a" | "b"> = Set()
const t: Set<string> = s
