// @check
// @error cannot assign Set<string> to 's' of type {[string]: boolean}
// `Set<T>` is its own type (owner ruling Q2): a set is not a boolean-valued map, so it does
// not satisfy a `{[K]: boolean}` destination. A map of booleans is built with `Map()`.
const s: {[string]: boolean} = Set()
