// @check
// @error a Set has no index
// A set has no index: membership is `s.has(x)` and insertion `s.add(x)`, so `s[x]` and
// `s[x] = true` are refused with the sentence that names both.
const s: Set<string> = Set()
s.add("a")
print(s["a"])
