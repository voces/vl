// @check
// @error cannot infer a type for `s`
// ROBUSTNESS FLOOR: a `Set()` with no annotation has an unknown element type and
// must report a clean, source-located "cannot infer … annotate" diagnostic
// before codegen — never the opaque `Unhandled AST -> WASM "Unknown" type`.
const s = Set()
