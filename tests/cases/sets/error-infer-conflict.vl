// @check
// @error expected string, got 1
// SOUNDNESS: once the FIRST `s.add("x")` materialises the inferred `Set()` as
// `Set<string>` (`{[string]: boolean}`), a later `s.add(1)` with a CONFLICTING
// element type is a clean type error (`i32` not assignable to the pinned
// `string`) — NOT a crash, mirroring the Map conflict case.
const s = Set()
s.add("x")
s.add(1)
