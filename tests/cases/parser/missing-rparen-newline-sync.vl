// @check
// @error-at 5:0 expected `)` but found `let`
// A missing `)` at end of line does not consume the next statement's tokens:
// the resync stops at the statement boundary and `let ok = 1` parses clean.
print(1
let ok = 1
print(ok)
