// @run
// @log true
// @log false
// The parenthesized control for coalesce-logical-mix-error.vl: explicit
// grouping makes `??` beside `||` legal on both sides.
const a: boolean | null = null
print((a ?? true) || false)
print(false || (a ?? false))
