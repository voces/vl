// @check
// @error-at 9:12 a `while` body requires braces: `while cond { … }`
// @error-at 11:19 a `while` body requires braces: `while cond { … }`
// A `while` body requires braces — one unbraced body, one diagnostic, in the
// plain and the LABELLED form alike (`name: while …` reaches the same
// `parseWhile`). Before the recovery the plain form cost three: the `expected
// `{``, an `expected an expression` from the assignment's `=` (the mis-parse
// had consumed `i` as the body's first token), then the phantom closer.
let i = 0
while i < 3 i = i + 1
let j = 0
outer: while j < 3 j = j + 1
print(i + j)
