// `vl test` meta-fixture: a test file that does not COMPILE is one failing entry
// in the report — with the compiler's own diagnostics — and the other files in the
// run are unaffected. The meta-test asserts the diagnostic's position (9:10), which
// also proves the protocol re-export `vl test` appends did not shift any line.
// Not `vl fmt`-normalized — see the note in pass.test.vl.
import { expect, it, toEqual } from "std:test"

it("never gets to run", () => {
  expect(noSuchName).toEqual(1)
})
