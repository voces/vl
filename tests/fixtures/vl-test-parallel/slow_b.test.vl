// See slow_a.test.vl — one slow test per file, four files, for the parallelism
// measurement.
import { expect, it, toEqual } from "std:test"

it("burns cpu (b)", () => {
  let acc = 0
  let i = 0
  while i < 400000000 {
    acc = acc + (i % 7)
    i = i + 1
  }
  expect(acc > 0).toEqual(true)
})
